---
phase: 07-guardrails-before-churn
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/check-dom-sanitizer.mjs
  - scripts/check-dom-sanitizer.test.ts
  - Makefile
  - src/components/binaryViewers/HwpxViewer.tsx
autonomous: true
requirements: [SEC-02]

estimate:
  tokens: 45000
  raw_tokens: 45000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "make verify fails the moment a new dangerouslySetInnerHTML value in src/ does not trace to a DOMPurify-backed helper, and passes on the six sinks that exist today, proven by a deliberate red-then-green break of the guard itself (ROADMAP criterion 3, SEC-02)."
    - "The guard is one plain-node ESM script in the scripts/check-*.mjs family using only node:fs/node:path: violations accumulate, then console.error plus process.exit(1) (D-07); no AST parser and no new lint framework."
    - "The collector skips *.test.* files and __tests__/ directories, so the sink literal asserted as source text in src/components/EditorPane.test.tsx is never flagged (Pitfall 2)."
    - "Tracing is judged by a pinned module allowlist of DOMPurify-helper exporters plus explicit (file, function) registrations; name-pattern matching is rejected as spoofable and local aliases are not followed (D-07, D-08)."
    - "EditorPane.tsx's sink passes via the registered (file, decoratePreviewHtml) pair plus the narrow dynamic-import provenance pattern for previewBaseHtml (assignment inside import(\"../lib/markdown\").then(...)) (Pitfall 3)."
    - "HwpxViewer sanitizes through an exported module-level helper named sanitizeHwpxPreviewHtml that performs the same DOMPurify.sanitize call with USE_PROFILES { html: true } as today's inline code, registered as a (file, function) pair (Pitfall 4, D-08)."
    - "The guard runs green on the six current sinks on day one without weakening any sink or DOMPurify configuration."
  prohibitions:
    - statement: "The guard must not be weakened to make sinks pass: no name-pattern tracing, no local-alias following, no warn-only mode. An untraced sink fails closed with exit 1 (D-07/D-08)."
    - statement: "The six existing sinks and the four DOMPurify helpers must not be weakened (no bypassing DOMPurify, no broadening sanitize profiles) to satisfy the guard."
  artifacts:
    - path: "scripts/check-dom-sanitizer.mjs"
      provides: "The SEC-02 static guard: src/ walker, sink scanner, allowlist + pair tracing rule, violation exit"
      exports: ["ALLOWED_HELPER_MODULES (pinned constant)", "REGISTERED_LOCAL_HELPERS (pinned constant)"]
    - path: "scripts/check-dom-sanitizer.test.ts"
      provides: "Vitest source-assertion coverage pinning the tracing policy so it cannot silently drift"
    - path: "Makefile"
      provides: "check-dom-sanitizer target adjacent to check-select-chrome plus a verify chain entry"
    - path: "src/components/binaryViewers/HwpxViewer.tsx"
      provides: "Exported DOMPurify-backed helper sanitizeHwpxPreviewHtml replacing the inline useEffect sanitize"
      exports: ["sanitizeHwpxPreviewHtml"]
  key_links:
    - from: "Makefile verify chain (Makefile:355)"
      to: "scripts/check-dom-sanitizer.mjs"
      via: "check-dom-sanitizer prerequisite listed after check-select-chrome, same cost class"
      pattern: "check-dom-sanitizer"
    - from: "src/components/binaryViewers/HwpxViewer.tsx useEffect (~:37)"
      to: "sanitizeHwpxPreviewHtml"
      via: "module-level call replacing the inline DOMPurify.sanitize"
      pattern: "sanitizeHwpxPreviewHtml"
---

<objective>
Land the SEC-02 sanitizer guard before the milestone's own refactor churn (Phases
8-10) can introduce an untraced `dangerouslySetInnerHTML` sink: one static check in
the `scripts/check-*.mjs` family that traces every sink in `src/` to a
DOMPurify-backed helper, wired into `make verify` and proven by a deliberate
red-then-green break. This is the phase's leading tracer: it creates the pipeline
the rest of the milestone will be gated by.

Purpose: Maru ingests content the user did not author (Telegram, KakaoTalk, Gmail,
Outlook, inbox drops) and renders it through `dangerouslySetInnerHTML`. Four
helpers (`renderMarkdown`, `renderScratchpadMarkdown`, `sanitizeHtml`, HwpxViewer's
inline sanitize) are the only sanctioned path today, enforced by code review only
(CONCERNS.md §2). SEC-02 makes that boundary a build gate.

Output: `scripts/check-dom-sanitizer.mjs` (green on the six existing sinks), the
HwpxViewer extraction it requires, the Makefile wiring with a verify chain entry,
and a vitest source-assertion test pinning the policy. Implements D-07 and D-08.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-guardrails-before-churn/07-CONTEXT.md
@.planning/phases/07-guardrails-before-churn/07-RESEARCH.md
@.planning/phases/07-guardrails-before-churn/07-PATTERNS.md
</context>

## Artifacts this phase produces

Newly created symbols and paths (plan 07-01 scope only; see 07-02 through 07-05
for their own symbols):

- New file `scripts/check-dom-sanitizer.mjs` — ESM plain-node guard; pinned
  constants `ALLOWED_HELPER_MODULES` (the D-07 module allowlist:
  `src/lib/markdown.ts`, `src/lib/scratchpad.ts`, `src/lib/diagram/richText.ts`)
  and `REGISTERED_LOCAL_HELPERS` (the D-08 (file, function) pairs:
  (`src/components/EditorPane.tsx`, `decoratePreviewHtml`) and
  (`src/components/binaryViewers/HwpxViewer.tsx`, `sanitizeHwpxPreviewHtml`))
- New file `scripts/check-dom-sanitizer.test.ts` — vitest source-assertion test
  (house style per `src/components/EditorPane.test.tsx`)
- New Make target `check-dom-sanitizer` (`.PHONY`, adjacent to
  `check-select-chrome` at Makefile:174-176) and its entry in the `verify`
  prerequisite list (Makefile:355)
- New exported function `sanitizeHwpxPreviewHtml(html: string): string` in
  `src/components/binaryViewers/HwpxViewer.tsx` (module level, mirroring
  `src/lib/diagram/richText.ts:20-26`); the existing `useEffect` at :37-40 calls
  it instead of inlining `DOMPurify.sanitize`

<flagged_assumptions>
The spec-less edge-coverage probe returned one row for SEC-02 with category
`unclassified` and status `unresolved`. It is surfaced here, not dropped: the
probe could not classify the requirement's edge shape mechanically. Resolution by
plan decision: the guard's behavior for a sink whose provenance matches no
allowlisted import, no registered (file, function) pair, and no recognized narrow
pattern is fail-closed — the sink is reported as a violation and the script exits
1. Task 2's deliberate red-then-green drill exercises exactly this path with a
temporary fixture sink, which is the strongest verification available for the
unclassified row.
</flagged_assumptions>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Sanitizer guard script green on the six existing sinks, plus the HwpxViewer helper extraction</name>
  <files>scripts/check-dom-sanitizer.mjs, src/components/binaryViewers/HwpxViewer.tsx</files>
  <read_first>
    - `scripts/check-select-chrome.mjs` (whole file, 77 lines) — the idiom anchor: ESM, node:fs/node:path only, recursive collector skipping node_modules, violations array, console.error + process.exit(1), success console.log
    - `src/components/binaryViewers/HwpxViewer.tsx` lines 1-45 and 85-99 — the inline `DOMPurify.sanitize(preview.html, { USE_PROFILES: { html: true } })` inside useEffect (:37-40) and the sink `dangerouslySetInnerHTML={{ __html: sanitizedHtml }}` at :95
    - `src/lib/diagram/richText.ts` lines 15-26 — the module-level exported-helper shape to mirror (`export function sanitizeHtml(...)` with a doc comment stating the sanitization contract)
    - `.planning/phases/07-guardrails-before-churn/07-PATTERNS.md` §SEC-02 — the verbatim sink inventory and provenance chains the guard must pass on day one, including EditorPane's dynamic-import pattern (EditorPane.tsx:328) and the `previewMarkup` memo
    - `src/components/EditorPane.test.tsx` lines 95-105 — the source-assertion test style and the reason test files must be excluded from the scan
    - `src/components/EditorPane.tsx` lines 320-335 and 415-440 — the `previewBaseHtml` assignment inside `import("../lib/markdown").then(({ renderMarkdown }) => ...)` and the `previewHtml`/`previewMarkup` memo chain feeding the sink at :1041
  </read_first>
  <behavior>
    - `node scripts/check-dom-sanitizer.mjs` exits 0 against today's tree, printing one success line naming the sink count scanned.
    - A file under src/ containing a sink whose `__html` value is a plain string concatenation or an identifier assigned from a non-allowlisted call is reported as a violation and the script exits 1.
    - `src/components/EditorPane.test.tsx` is not scanned (its source-text assertion of the sink literal is not flagged).
    - `sanitizeHwpxPreviewHtml` in HwpxViewer returns byte-equivalent output to the current inline sanitize for the same input (same DOMPurify call, same profile).
  </behavior>
  <action>
    First do the HwpxViewer extraction the tracing rule needs (Pitfall 4): at
    module level in `src/components/binaryViewers/HwpxViewer.tsx`, add an exported
    function named `sanitizeHwpxPreviewHtml(html: string): string` that performs
    exactly the `DOMPurify.sanitize` call the useEffect at :37-40 performs today —
    same options object `{ USE_PROFILES: { html: true } }`, no added or removed
    configuration — with a one-line doc comment stating it is the DOMPurify-backed
    helper for the Hwpx preview sink. Replace the inline call in the useEffect
    with a call to this function and keep the `sanitizedHtml` state field name
    unchanged. Exporting (not module-private) matches the existing helper modules
    and makes the guard's (file, function) registration verifiable from the script.

    Then create `scripts/check-dom-sanitizer.mjs` following check-select-chrome.mjs
    verbatim in structure: ESM, `node:fs`/`node:path` imports only, recursive
    collector over `../src` that skips `node_modules` and additionally skips any
    path containing `__tests__/` or ending in `.test.ts`, `.test.tsx`, or
    `.spec.ts`/`.spec.tsx` (Pitfall 2 — EditorPane.test.tsx asserts the sink as
    source text). For each collected `.ts`/`.tsx` file, find sink occurrences with
    a line-level regex for the `dangerouslySetInnerHTML` prop in its two shapes:
    an inline object `={{ __html: <expr> }}` and a bare identifier `={<ident>}`.

    Implement the tracing rule per D-07/D-08 as pinned constants at the top of the
    script: `ALLOWED_HELPER_MODULES` listing `src/lib/markdown.ts`,
    `src/lib/scratchpad.ts`, and `src/lib/diagram/richText.ts`;
    `REGISTERED_LOCAL_HELPERS` listing the pairs (`src/components/EditorPane.tsx`,
    `decoratePreviewHtml`) and (`src/components/binaryViewers/HwpxViewer.tsx`,
    `sanitizeHwpxPreviewHtml`). A sink passes when, within the same file: the
    `__html` expression directly calls a function imported (static top-level
    import) from an allowlisted module; or the expression is an identifier whose
    in-file definition is a memo/const whose value directly calls an allowlisted
    import; or the expression is an identifier whose in-file definition calls a
    function named in a registered pair for that exact file; or — EditorPane only —
    the file is EditorPane.tsx and the identifier chain bottoms out at
    `previewBaseHtml`, which the file assigns inside
    `import("../lib/markdown").then(({ renderMarkdown }) => ...)`. This
    dynamic-import assignment recognition is a narrow, explicitly file-scoped
    pattern: check that the file contains the assignment of `previewBaseHtml`
    inside a `.then(` callback whose destructured parameter includes
    `renderMarkdown` imported from the allowlisted `src/lib/markdown.ts`. Do NOT
    build general alias following, general call-graph resolution, or AST parsing —
    D-07 rejects all three alternatives.

    Everything else is a violation: collect `file:line` entries, then print them
    with `console.error` and `process.exit(1)`; on success print one line naming
    the guard and the count of sinks verified. The success/failure output prefix
    should read `check-dom-sanitizer:` to match the family convention
    (`select-chrome: ...`).

    The six sinks the green run must pass are exactly those in PATTERNS.md §SEC-02:
    EditorPane.tsx (identifier form, decoratePreviewHtml chain), DraftsPane.tsx:968
    and :1033 (`renderMarkdown` from `../../lib/markdown`), ScratchpadPane.tsx:1360
    (`renderScratchpadMarkdown` from `../../lib/scratchpad`),
    InlineDocumentEditor.tsx:228 (`renderMarkdown` from `../lib/markdown`), and
    HwpxViewer.tsx:95 (identifier form via `sanitizeHwpxPreviewHtml`). If a sink
    fails, fix the tracing rule or the registration — never weaken a sink or its
    DOMPurify profile to force a pass.
  </action>
  <verify>
    <automated>node scripts/check-dom-sanitizer.mjs</automated>
    <automated>pnpm typecheck</automated>
    <automated>pnpm lint</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/check-dom-sanitizer.mjs` exists, is ESM, and imports only from `node:fs` and `node:path` (plus `node:url` for the import-meta-url idiom if used).
    - The script contains the pinned constants `ALLOWED_HELPER_MODULES` (exactly the three modules listed in `<action>`) and `REGISTERED_LOCAL_HELPERS` (exactly the two pairs listed in `<action>`).
    - The collector skips `__tests__/` and `*.test.*` / `*.spec.*` files: `node scripts/check-dom-sanitizer.mjs` exits 0 and never mentions `EditorPane.test.tsx`.
    - `src/components/binaryViewers/HwpxViewer.tsx` exports `sanitizeHwpxPreviewHtml`; the useEffect no longer contains an inline `DOMPurify.sanitize` call; the options object `{ USE_PROFILES: { html: true } }` appears exactly once, inside `sanitizeHwpxPreviewHtml`.
    - `node scripts/check-dom-sanitizer.mjs` exits 0 against the unmodified other five sinks and prints a success line prefixed `check-dom-sanitizer:`.
    - A temporary probe file `src/__dom_sanitizer_probe__.tsx` containing a sink with `__html` set to a non-allowlisted expression (e.g. a plain identifier assigned from `untrustedInput`) makes the script exit 1 and name the probe file; delete the probe after observing the red run.
    - `pnpm typecheck` and `pnpm lint` exit 0.
  </acceptance_criteria>
  <done>
    The guard exists in the check-*.mjs idiom, passes the six current sinks without
    weakening any of them, fails closed on an untraced probe sink, and HwpxViewer
    sanitizes through its registered exported helper.
  </done>
</task>

<task type="auto">
  <name>Task 2: Deliberate red-then-green proof and Makefile wiring into make verify</name>
  <files>Makefile</files>
  <read_first>
    - `Makefile` lines 170-195 — the `check-select-chrome` and `check-type-tokens` target shape (`.PHONY`, one-line `##` description, `$(NODE) scripts/...` recipe)
    - `Makefile` lines 350-360 — the `verify` prerequisite chain where the new guard lands
    - `.planning/STATE.md` GATE-02 entry — the deliberate break-and-revert precedent: the red case must be proven before the green wiring is accepted
    - `scripts/check-dom-sanitizer.mjs` as written in Task 1
  </read_first>
  <action>
    Wire the guard the way the family is wired: add a `.PHONY` target
    `check-dom-sanitizer` immediately after the `check-select-chrome` target
    (adjacent placement per RESEARCH Open Question 2's recommendation — same cost
    class, same static-guard semantics), with a `##` description line in the family
    voice (static guard: every dangerouslySetInnerHTML sink in src/ must trace to a
    DOMPurify-backed helper) and a recipe of `$(NODE) scripts/check-dom-sanitizer.mjs`.
    Append `check-dom-sanitizer` to the `verify` prerequisite list at Makefile:355,
    directly after `check-select-chrome` and before `check-type-tokens`.

    Then run the deliberate red-then-green drill ROADMAP criterion 3 demands, in
    this order, and record the transcript in the plan SUMMARY: (1) create a
    temporary fixture `src/__dom_sanitizer_red_drill__.tsx` holding one component
    whose sink sets `__html` from a non-allowlisted expression; (2) run
    `make check-dom-sanitizer` and observe exit 1 with the fixture named in the
    violation output; (3) delete the fixture; (4) run `make check-dom-sanitizer`
    again and observe exit 0. The red run must happen before the green acceptance,
    and the fixture must not survive the task.
  </action>
  <verify>
    <automated>make check-dom-sanitizer</automated>
    <automated>test "$(make -n verify | grep -c check-dom-sanitizer)" = 1</automated>
  </verify>
  <acceptance_criteria>
    - `make check-dom-sanitizer` exits 0 on the clean tree.
    - `make -n verify` output includes `check-dom-sanitizer` exactly once as a prerequisite of `verify`, positioned after `check-select-chrome`.
    - The red-then-green drill transcript (fixture created, red exit 1 observed with the fixture named, fixture deleted, green exit 0 observed) is recorded in `07-01-SUMMARY.md`.
    - `src/__dom_sanitizer_red_drill__.tsx` does not exist in the tree after the task (verified by `test ! -f src/__dom_sanitizer_red_drill__.tsx`).
    - The Makefile diff touches only the new target block and the one verify-chain line.
  </acceptance_criteria>
  <done>
    `make verify` now fails the moment a new untraced sink appears in src/, the
    failure was demonstrated live before the wiring was accepted, and the guard
    sits in the verify chain beside its sibling static guards.
  </done>
</task>

<task type="auto">
  <name>Task 3: Vitest source-assertion regression test pinning the tracing policy</name>
  <files>scripts/check-dom-sanitizer.test.ts</files>
  <read_first>
    - `src/components/EditorPane.test.tsx` lines 95-105 — the house source-assertion style this test copies (reads a source file as text, asserts on its content)
    - `scripts/check-dom-sanitizer.mjs` as written in Task 1 — the policy constants and fail-closed exit the test pins
    - `package.json` scripts.test — confirms `vitest run src scripts`, so a test under `scripts/` is picked up by the default suite
  </read_first>
  <action>
    Create `scripts/check-dom-sanitizer.test.ts` in the source-assertion style: read
    `scripts/check-dom-sanitizer.mjs` as text with `readFileSync` and assert the
    load-bearing policy points so a future edit cannot silently weaken the guard:
    the collector excludes `__tests__` and `*.test.`/`.spec.` filename patterns;
    `ALLOWED_HELPER_MODULES` pins exactly the three helper modules and
    `REGISTERED_LOCAL_HELPERS` pins exactly the two (file, function) pairs; the
    script contains a `process.exit(1)` on violations and no `process.exit(0)`-on-
    violation path (fail-closed); the script does not contain an AST parser import
    (no `@babel`, `typescript`, `acorn`, `espree` style dependency references);
    and the EditorPane dynamic-import provenance pattern (`import("../lib/markdown")`
    paired with a `.then(` callback) is what the script recognizes. Keep the test
    hermetic — no subprocess, no temp fixtures; the drill in Task 2 is the
    behavioral proof, this test is the policy pin.
  </action>
  <verify>
    <automated>pnpm test -- check-dom-sanitizer</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm test -- check-dom-sanitizer` exits 0 with at least five assertions covering the bullet list in `<action>`.
    - The test reads the guard script from disk rather than duplicating its policy constants, so it fails if the script drifts.
    - `pnpm test` (full frontend suite) still exits 0 with the new file included.
  </acceptance_criteria>
  <done>
    The tracing policy is pinned by a test in the suite `make verify` already runs,
    so weakening the guard turns a verify gate red without needing a live sink.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Ingested third-party content (Telegram/KakaoTalk/Gmail/Outlook/inbox drops) -> webview DOM via dangerouslySetInnerHTML | Untrusted HTML reaches a React innerHTML sink; DOMPurify helpers are the only sanctioned path |
| Guard script -> build pipeline | A static check that fails open or is weakened silently stops protecting the sink boundary during Phases 8-10 churn |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-7-01 | Tampering (stored XSS) | Any new dangerouslySetInnerHTML sink in src/ | high | mitigate | Guard wired into `make verify` (this plan); every sink must trace to a DOMPurify-backed helper via module allowlist or registered pair; fail-closed exit 1; red-then-green drill proves the gate has teeth before the wiring commit |
| T-7-02 | Elevation of privilege (webview code exec) | Sink-provenance spoof — naming a function to look sanitized | high | mitigate | D-07 pins tracing to a module allowlist (name patterns rejected as spoofable); D-08 requires explicit (file, function) registration with no alias following; Task 3's source-assertion test pins both constants |
| T-7-03 | Tampering | The guard itself weakened to pass sinks (warn-only, broader allowlist) | medium | mitigate | Prohibition recorded in must_haves; Task 3 pins the policy in the vitest suite that `make verify` runs; the SEC-02/unclassified edge is resolved fail-closed and drill-proven |

No package installs this plan — T-7-SC (supply chain) is not applicable. ASVS L1:
V5 Output Encoding is the applicable category and the guard is the control.
</threat_model>

<verification>
- `node scripts/check-dom-sanitizer.mjs` green on the six existing sinks
- Deliberate fixture break observed red (exit 1, fixture named), then green after removal — transcript in the SUMMARY
- `make check-dom-sanitizer` green; `make -n verify` shows the guard in the chain
- `pnpm test -- check-dom-sanitizer` green; `pnpm typecheck`; `pnpm lint`
</verification>

<success_criteria>
- SEC-02 is a live gate: `make verify` fails on any new untraced sink in src/
- The six current sinks pass without weakening; the guard is in the check-*.mjs idiom
- The tracing model is exactly D-07/D-08: module allowlist plus explicit pairs, no aliases, no AST
</success_criteria>

<output>
Create `.planning/phases/07-guardrails-before-churn/07-01-SUMMARY.md` when done
</output>
