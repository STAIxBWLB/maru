---
phase: 10-bundle-and-build-hardening
plan: 03
type: execute
wave: 3
depends_on: ["10-02"]
files_modified:
  - src/lib/modePreload.ts
  - src/main.tsx
  - e2e/first-activation-styles.spec.ts
autonomous: true
requirements: [PERF-05]

estimate:
  tokens: 45000
  raw_tokens: 25000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "No mode shows unstyled content on its first activation, verified by `pnpm test:e2e` running `e2e/first-activation-styles.spec.ts` against the split build: each mode's first activation renders computed styles (background-color, font-family) from its mode CSS before and after the chunk arrives, under both `colorScheme: \"light\"` and `colorScheme: \"dark\"` (ROADMAP Phase 10 criterion 3)."
    - "Every registered mode chunk (JS and its CSS) is warmed during idle after app load and before first activation, by the D-03 idle preload built on the existing `scheduleStartupIdle` helper."
    - "Preload failure degrades gracefully: a rejected chunk load never throws unhandled and never blocks activation (the normal lazy load path still works)."
  artifacts:
    - path: "src/lib/modePreload.ts"
      provides: "scheduleModePreload(): idle-scheduled warm of every available mode's lazy chunk via the registry descriptors; returns the cancel handle from scheduleStartupIdle"
    - path: "src/main.tsx"
      provides: "One wiring line: scheduleModePreload() called at entry, after the existing startup marks"
    - path: "e2e/first-activation-styles.spec.ts"
      provides: "Playwright FOUC spec: computed-style assertion on first mode activation, both colorSchemes, exercising the .empty-state duplicated-selector home from 10-02"
  key_links:
    - from: "src/lib/modePreload.ts"
      to: "src/lib/modeRegistry.tsx"
      via: "getRegisteredModeIds() + getModeDescriptor(): iterates descriptors, skips unavailable modes, calls descriptor.load()"
      pattern: "getRegisteredModeIds"
    - from: "src/lib/modePreload.ts"
      to: "src/lib/startupProfile.ts"
      via: "scheduleStartupIdle(work, timeout): requestIdleCallback with setTimeout fallback and cancel handle — reused, not hand-rolled (D-03)"
      pattern: "scheduleStartupIdle"
    - from: "src/main.tsx"
      to: "src/lib/modePreload.ts"
      via: "scheduleModePreload() invoked at entry — same wiring site as the existing font-CSS void imports"
      pattern: "scheduleModePreload"
  prohibitions:
    - statement: "No hover/focus-triggered preload on top of idle preload (D-03 locked idle-only; hover adds a first-hover lag path)."
    - statement: "modePreload.ts does not import component modules — src/lib must not import components (project constraint); it references only registry descriptors, so adapter code stays behind the lazy seam."
    - statement: "No new scheduling primitive: reuse scheduleStartupIdle (requestIdleCallback + timeout fallback + cancel handle); do not hand-roll a second idle scheduler."
  flagged_assumptions:
    - "Criterion 3 (FOUC) verification is scoped to the browser-mode Playwright suite (`pnpm test:e2e`, runs in `make release-preflight`, NOT in `make verify`); the native/webview confirmation remains the human-run macOS `make release-preflight` gate per the existing verification culture. The planner assumes this satisfies the 'packaged build rather than dev server' bar for the styling criterion together with the release-preflight native suite."
    - "The e2e spec assumes every mode is reachable by direct navigation or in-app activation in the Playwright environment; modes gated behind flags (diagram, e2e) are exercised via the same enablement hooks the existing e2e suite uses, or skipped with an explicit note in the SUMMARY if their enablement requires a packaged build."
---

<objective>
Close Phase 10's last success criterion: no mode shows unstyled content on first
activation after the CSS split. Two pieces: the D-03 idle preload that warms every
mode chunk (JS + CSS) during idle after load, built on the existing
`scheduleStartupIdle` helper and the mode registry descriptors; and the Playwright
FOUC spec that verifies first-activation styling under both color schemes against
the split build from 10-02.

Purpose: The CSS split moves each mode's styles out of the entry stylesheet, so a
mode's first activation now waits on its chunk pair. The idle preload makes that
wait invisible in practice; the e2e spec proves it stays invisible under both
color schemes — the dark fallback is the first-paint path where a FOUC would
actually show.

Output: `src/lib/modePreload.ts`, the main.tsx wiring line, and
`e2e/first-activation-styles.spec.ts` green in `pnpm test:e2e`. Implements D-03;
verifies ROADMAP criterion 3.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-bundle-and-build-hardening/10-CONTEXT.md
@.planning/phases/10-bundle-and-build-hardening/10-RESEARCH.md
@.planning/phases/10-bundle-and-build-hardening/10-PATTERNS.md
@.planning/phases/10-bundle-and-build-hardening/10-UI-SPEC.md
@.planning/phases/10-bundle-and-build-hardening/10-02-SUMMARY.md
</context>

## Artifacts this phase produces

Newly created symbols and paths (plan 10-03 scope only):

- New file `src/lib/modePreload.ts` — exports `scheduleModePreload(): () => void`;
  iterates `getRegisteredModeIds()`, resolves each descriptor via
  `getModeDescriptor(id)`, skips `!isAvailable()` modes, and warms each
  `descriptor.load()` with a `.catch(() => {})` (failure is non-fatal); the whole
  loop is scheduled through `scheduleStartupIdle(work, 2000)` and its cancel
  handle is returned
- New wiring in `src/main.tsx` — one line after the existing imports/marks
- New file `e2e/first-activation-styles.spec.ts` — the FOUC spec (two projects'
  worth of colorScheme coverage via Playwright's test.use or per-test
  colorScheme option)

<tasks>

<task type="tracer">
  <name>Task 1: modePreload.ts idle preload of every mode chunk, wired into main.tsx</name>
  <files>src/lib/modePreload.ts, src/main.tsx</files>
  <read_first>
    - `src/lib/startupProfile.ts:88-100` — `scheduleStartupIdle(work, timeout = 1500)`: requestIdleCallback with `{ timeout }`, cancelIdleCallback handle, setTimeout fallback; reuse, do not hand-roll
    - `src/lib/modeRegistry.tsx` — `getRegisteredModeIds()` (the public accessor; `registeredModeIds` is module-private), `getModeDescriptor(mode)`, `ModeDescriptor.load()` returning `Promise<{ default: ComponentType<ModeAdapterProps> }>`, `isAvailable()`
    - `src/main.tsx` (19 lines) — the wiring site: existing `void import(...)` font-CSS precedent and `markStartup("app:entry")`
    - `.planning/phases/10-bundle-and-build-hardening/10-CONTEXT.md` D-03 — idle preload locked decision; no hover trigger
    - Project CLAUDE.md — `src/lib` must not import components; the preload module references registry descriptors only
  </read_first>
  <action>
    Create `src/lib/modePreload.ts`: export `scheduleModePreload(): () => void`.
    The body returns `scheduleStartupIdle(() => { for (const id of
    getRegisteredModeIds()) { const descriptor = getModeDescriptor(id); if
    (!descriptor || !descriptor.isAvailable()) continue; void
    descriptor.load().catch(() => {}); } }, 2000);` — the timeout is 2000ms
    (idle preload has no UI deadline; the existing helper's default 1500 is
    reserved for startup-critical work). The `.catch(() => {})` keeps a rejected
    chunk load from surfacing as an unhandled rejection: preload failure
    degrades to the normal lazy load path. The returned cancel handle
    (scheduleStartupIdle's return value) is passed through for future callers;
    main.tsx does not need to cancel.

    Wire into `src/main.tsx`: one import line for `scheduleModePreload` and one
    call after `markStartup("app:entry")` — before the render call, so the idle
    window starts as early as possible. Keep the diff to exactly these two line
    changes; the existing font-CSS void imports and the render tree are
    untouched.

    The module imports only `./modeRegistry` and `./startupProfile` — no
    component imports (src/lib boundary), no new scheduling code (D-03 reuse
    rule).
  </action>
  <verify>
    <automated>pnpm exec vitest run src/lib --reporter=dot</automated>
    <fails_when>non-zero exit, or any src/lib vitest suite failing with the new module present</fails_when>
    <automated>pnpm typecheck && pnpm lint</automated>
    <fails_when>non-zero exit, or any ESLint warning (lint runs --max-warnings 0)</fails_when>
    <automated>test "$(grep -c "scheduleModePreload" src/main.tsx)" -ge 2</automated>
    <fails_when>the grep count is below 2, meaning the import line or the call line is missing from src/main.tsx</fails_when>
  </verify>
  <acceptance_criteria>
    - `src/lib/modePreload.ts` exists and exports `scheduleModePreload`; it imports only `./modeRegistry` and `./startupProfile`.
    - The preload loop skips unavailable modes and swallows chunk-load failures (`.catch`); the whole body runs inside `scheduleStartupIdle(..., 2000)`.
    - `src/main.tsx` calls `scheduleModePreload()` once; no other main.tsx line changes.
    - `pnpm exec vitest run src/lib` and `pnpm typecheck` and `pnpm lint` exit 0.
  </acceptance_criteria>
  <done>
    Idle preload is wired: every available mode's lazy chunk (JS + its CSS) is
    scheduled to warm during idle after entry, with graceful failure.
  </done>
</task>

<task type="auto">
  <name>Task 2: Playwright FOUC spec for first-activation styling under both color schemes</name>
  <files>e2e/first-activation-styles.spec.ts</files>
  <read_first>
    - `e2e/startup.spec.ts` (27 lines, whole) — the exact analog: localStorage clear beforeEach, `?startupProfile=1` navigation, role-based assertions, the startup-profile marks evaluation pattern
    - `playwright.config.ts` — testDir ./e2e, chromium project, webServer `pnpm exec vite` on 5307, trace retain-on-failure
    - `.planning/phases/10-bundle-and-build-hardening/10-UI-SPEC.md` §UI Considerations — the covered FOUC row and the both-colorSchemes note; the cascade-order backstop row naming the `.empty-state` duplicate
    - `src/lib/modeRegistry.tsx` — the 18 mode ids and which are flag-gated (`isDiagramEnabled`, `isE2EFlowEnabled`) so the spec can enable or skip them explicitly
    - `src/styles.css` `.mode-loading` (:4024-4035 census) — the Suspense fallback the entry stylesheet must keep rendering during the chunk window
  </read_first>
  <action>
    Create `e2e/first-activation-styles.spec.ts` following startup.spec.ts's
    structure: localStorage clear in beforeEach; navigation to `/`; activation of
    each non-flag-gated mode via the same in-app affordance the existing e2e
    suite uses to switch modes (activity rail buttons; enable diagram/e2e modes
    via their existing enablement hooks only if the browser-mode suite already
    exercises them, otherwise skip with an explicit note recorded in the plan
    SUMMARY).

    Per mode, on FIRST activation (before any prior visit in that page context):
    1. Assert the activation did not produce unstyled content: immediately after
    the mode's surface mounts, poll `getComputedStyle` on the mode's root
    surface element until stable (Playwright's `expect.poll`), and assert the
    computed background-color is not the document default (transparent /
    initial) and the computed font-family matches the Pretendard stack token
    from styles.css — i.e. the mode's CSS (and the entry stylesheet) is applied.
    2. Assert the `.mode-loading` fallback path still exists in the entry
    stylesheet: while the chunk loads (or by asserting the class rule exists in
    `document.styleSheets`), the fallback style is resolvable.

    Run the whole spec under both color schemes: `test.use({ colorScheme: "light" })`
    for one describe block and `test.use({ colorScheme: "dark" })` for a second
    (the dark fallback is the first-paint path where a FOUC would actually show).
    Include one assertion exercising the `.empty-state` home resolved in 10-02
    (open a mode whose empty state renders — e.g. drafts or gap with no data —
    and assert its computed styles match the resolved single home).

    Keep the spec hermetic: no artificial sleeps; rely on Playwright
    auto-waiting and `expect.poll`; if the idle preload makes the chunk window
    too fast to observe the fallback, that is a pass condition (the FOUC window
    is closed), not a flake to fix — the computed-style assertions are the
    acceptance, not fallback observation.
  </action>
  <verify>
    <automated>pnpm test:e2e -- first-activation-styles</automated>
    <fails_when>non-zero exit, or "0 passed" in the Playwright summary line, or any computed-style assertion failing</fails_when>
    <automated>pnpm lint</automated>
    <fails_when>non-zero exit, or any ESLint warning in the new spec (lint covers e2e/ with --max-warnings 0)</fails_when>
  </verify>
  <acceptance_criteria>
    - `e2e/first-activation-styles.spec.ts` exists and follows the startup.spec.ts structure (localStorage clear, `/` navigation).
    - The spec covers every non-flag-gated mode's first activation with computed-style assertions; flag-gated modes are exercised via existing enablement hooks or skipped with a recorded note.
    - The spec runs under both `colorScheme: "light"` and `colorScheme: "dark"`.
    - The `.empty-state` single-home assertion from 10-02's resolution is covered by at least one mode.
    - `pnpm test:e2e -- first-activation-styles` exits 0; `pnpm lint` exits 0.
  </acceptance_criteria>
  <done>
    First-activation styling is proven by the browser-mode e2e suite under both
    color schemes, closing ROADMAP Phase 10 criterion 3 against the split build.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Preload scheduling -> app entry | A preload that throws or blocks the entry path turns a graceful degradation into a startup failure |
| e2e spec -> CI/release-preflight | A FOUC spec that cannot fail (asserts nothing observable) provides false confidence in the styling criterion |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-10-07 | Denial of Service (startup failure) | modePreload.ts throwing or blocking entry | low | mitigate | Preload failures are swallowed (`.catch(() => {})`); the schedule is idle-time (non-blocking); vitest+typecheck+lint gate the module |
| T-10-08 | Repudiation (false confidence) | FOUC spec asserting nothing observable | medium | mitigate | The spec asserts concrete computed styles (background-color, font-family) per mode under both colorSchemes; `pnpm test:e2e` fails on any assertion miss |

No package installs in this plan — T-10-SC (supply chain) is not applicable.
ASVS L1: no new trust boundary; the controls above are failure-containment and
assertion-strength gates, grep-verifiable at the cited files.
</threat_model>

<verification>
- `pnpm exec vitest run src/lib` green with modePreload.ts present
- `pnpm test:e2e -- first-activation-styles` green: every non-flag-gated mode's first activation styled, both colorSchemes
- `pnpm typecheck`, `pnpm lint` green
- main.tsx diff is exactly the import + call lines
</verification>

<success_criteria>
- ROADMAP Phase 10 criterion 3: no mode shows unstyled content on its first activation, verified by the browser-mode e2e suite against the split build, both color schemes
- D-03: idle preload warms every available mode chunk during idle after load, built on the existing scheduleStartupIdle helper, degrading gracefully on failure
</success_criteria>

<output>
Create `.planning/phases/10-bundle-and-build-hardening/10-03-SUMMARY.md` when done
</output>
