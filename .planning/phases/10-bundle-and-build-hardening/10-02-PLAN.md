---
phase: 10-bundle-and-build-hardening
plan: 02
type: execute
wave: 2
depends_on: ["10-01"]
files_modified:
  - src/styles.css
  - src/components/drafts/DraftsPane.tsx
  - src/components/drafts/drafts.css
  - src/components/gap/GapPane.tsx
  - src/components/gap/gap.css
  - src/components/agents/AgentsPane.tsx
  - src/components/agents/agents.css
  - src/components/meetings/MeetingsPane.tsx
  - src/components/meetings/meetings-pane.css
  - src/components/today/TodayPane.tsx
  - src/components/today/today.css
  - src/components/tasks/TasksPane.tsx
  - src/components/tasks/tasks.css
  - src/components/calendar/UnifiedCalendarPane.tsx
  - src/components/calendar/calendar.css
  - scripts/check-bundle-budget.mjs
  - scripts/check-mode-css-ownership.mjs
  - scripts/check-mode-css-ownership.test.ts
  - package.json
autonomous: true
requirements: [PERF-05]

estimate:
  tokens: 90000
  raw_tokens: 45000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "The initial-CSS budget check passes at the original 70 KiB gzip threshold, NOT a raised one, proven by `node scripts/check-bundle-budget.mjs` after the split (D-06: pass is the bar; no headroom target)."
    - "Every mode's section CSS ships inside that mode's own lazy chunk, proven by the per-file ownership assertion (check-mode-css-ownership.mjs) chained into `pnpm build:frontend`."
    - "No mode shows unstyled content on its first activation — the D-03 idle preload (10-03) warms every mode chunk, and the `.mode-loading` entry fallback covers the residual window — verified by the Playwright FOUC spec in 10-03 against the split build."
    - "The stale ~12% headroom comment is gone from check-bundle-budget.mjs and the budget numbers are byte-identical to today (320*1024 / 70*1024)."
  artifacts:
    - path: "src/components/<mode>/*.css (14 new files)"
      provides: "Every mode section's rules moved out of src/styles.css, colocated per the UI-SPEC placement contract, each headed by a `/*! maru:mode:<id> */` legal comment"
    - path: "scripts/check-mode-css-ownership.mjs"
      provides: "Per-file ownership assertion: dist/assets/*.css raw marker scan, red list vs full inventory, missing-marker and marker-class mismatch detection, missing --binary mode is a violation; chained into build:frontend"
    - path: "scripts/check-mode-css-ownership.test.ts"
      provides: "Vitest source-assertion pin on the guard policy"
    - path: "scripts/check-bundle-budget.mjs"
      provides: "Fresh-build reality comment; budget thresholds byte-identical (320*1024 / 70*1024); 18-mode inventory assertion + known-lazy-mode CSS chunks cannot sit in the entry chunk"
  key_links:
    - from: "mode pane component (e.g. src/components/drafts/DraftsPane.tsx)"
      to: "src/components/drafts/drafts.css"
      via: "CSS import at the very top of the component module (graph.css/diagram.css precedent)"
      pattern: "import \"./drafts.css\""
    - from: "src/components/<mode>/<mode>.css"
      to: "dist/assets/<OwningModeAdapter>-*.css"
      via: "Vite cssCodeSplit default: per-file legal-comment markers survive esbuild minify and locate the mode's section in the emitted chunk"
      pattern: "/\\*!\\s*maru:mode:([a-z0-9-]+)"
    - from: "package.json build:frontend"
      to: "scripts/check-mode-css-ownership.mjs"
      via: "chained after check-csp-blob.mjs (10-01's chain), same produced-artifact scan family"
      pattern: "check-mode-css-ownership"
  prohibitions:
    - statement: "No raw-px font-size rules are moved out of src/styles.css (check-type-tokens greps only src/styles.css; moving a raw-px rule into a per-mode file to escape the gate is a forbidden guard-weakening change)."
    - statement: "src/foundations.css is not split, not moved, not edited."
    - statement: "No :root block, no head-of-file prefers-color-scheme first-paint fallback, no .mode-loading / .editor-loading (styles.css:4024-4035), and no .sr-only (:294) rule leaves the entry stylesheet; shared utilities a lazy file references stay entry-defined (graph.css:529-530 documents the .sr-only precedent)."
    - statement: "No duplicated selector is left with two homes: every duplicated selector (`.empty-state` at styles.css:3608 vs :24063) must have exactly one owning section; the other block is removed or the selector stays entry-side (D-02 boundary case)."
    - statement: "No budget number is raised (D-06; PERF-05 explicitly forbids raising)."
  flagged_assumptions:
    - "PERF-05 (specless edge probe returned an unresolved row; surfaced, not dropped): the planner assumes Vite 7.3.2's default cssCodeSplit places each per-mode CSS file in the lazy chunk of the mode adapter that (transitively) imports it, and that legal comments (`/*! ... */`) survive esbuild CSS minify in a Vite 7.3.2 production build. Both legs are session-verified at the mechanism level (research + esbuild 0.27.7 transformSync probe in this planning session); the first production build in Task 1 falsifies either leg cheaply if wrong."
    - "Section-header line numbers in styles.css (:4024, :15479, etc.) are planning-session census values and will drift as the split progresses; the executor re-censuses with grep on the live tree rather than trusting them."
    - "The pkm boundary case is resolution 2 (entries stay in styles.css): PkmModeAdapter renders commands.renderPrimarySurface() with no pane component of its own, so the split is not applicable to its entry entries. The executor may instead exercise resolution 1 (a pkm.css import from PkmModeAdapter) if the census shows a pkm-specific section header block worth moving; either is D-02-conformant."
---

<objective>
Land PERF-05: move every mode's CSS out of the 26,458-line `src/styles.css`
monolith into per-mode files riding each mode's lazy chunk, restoring the
initial-CSS budget headroom spent since v0.4.46. Ownership is assigned
mechanically by the existing section-header/prefix convention (D-02), verified by
a produced-artifact ownership assertion, and every claim is proven against the
production bundle rather than dev-server behavior.

Purpose: The entry stylesheet carries every mode's rules for a surface the user
opens one mode at a time; the budget headroom spent since v0.4.46 is that cost.
After the split, the initial CSS passes its original 70 KiB budget with real
headroom, and no mode shows unstyled content on first activation (the D-03 idle
preload in plan 10-03 warms chunks before the first click).

Output: 14 new per-mode CSS files, `scripts/check-mode-css-ownership.mjs`
(ownership assertion chained into `build:frontend`), the check-bundle-budget.mjs
comment/inventory refresh, and the pane-component import wiring. Implements D-01
(full split) and D-02 (mechanical ownership).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-bundle-and-build-hardening/10-CONTEXT.md
@.planning/phases/10-bundle-and-build-hardening/10-RESEARCH.md
@.planning/phases/10-bundle-and-build-hardening/10-PATTERNS.md
@.planning/phases/10-bundle-and-build-hardening/10-UI-SPEC.md
</context>

## Artifacts this phase produces

Newly created symbols and paths (plan 10-02 scope only; 10-03 declares its own):

- New files `src/components/{drafts/gap/agents/today/tasks/meetings/calendar}/*.css`
  (14 total) — the moved mode sections; each headed by a `/*! maru:mode:<id> */`
  legal comment (verified: `/*!` comments survive esbuild 0.27.7 CSS minify, and a
  raw-text marker scan before comment stripping locates each section in the
  emitted chunk)
- New file `scripts/check-mode-css-ownership.mjs` — produced-artifact ownership
  assertion (raw marker scan + red-list check), chained into `build:frontend`
- New file `scripts/check-mode-css-ownership.test.ts` — vitest source-assertion
  pin on the guard policy
- Changed file `src/styles.css` — mode sections removed; stays the entry
  stylesheet carrying the :root token blocks, the head-of-file first-paint
  fallback, shell chrome, `.mode-loading`/`.editor-loading` (:4024-4035), and
  `.sr-only` (:294)
- Changed imports: six pane components gain a top-of-module `import "./<mode>.css"`
  line (graph.css/diagram.css precedent)
- Changed file `scripts/check-bundle-budget.mjs` — stale ~12% comment corrected;
  budget thresholds byte-identical
- Changed script `build:frontend` in package.json — ownership assertion chained
  after the csp-blob dist scan

<flagged_assumptions>
The spec-less edge-coverage probe returned PERF-05 as an unresolved row. Surfaced
here, not dropped: the planner assumes (a) Vite 7.3.2's default cssCodeSplit
ships each per-mode CSS file in the lazy chunk of the adapter that imports it and
(b) `/*! ... */` legal comments survive esbuild CSS minify so the ownership
assertion can locate mode sections in the emitted chunk. Both legs verified at
the mechanism level this session; Task 1's first production build falsifies
either leg cheaply if wrong. The pkm boundary case: PkmModeAdapter renders
`commands.renderPrimarySurface()` with no pane component of its own, so its
entry entries stay in styles.css (resolution 2); the executor may instead
exercise resolution 1 (a pkm.css import from PkmModeAdapter) if the census shows
a pkm-specific section worth moving. Section-header line numbers in this plan
are planning-session census values and will drift as the split progresses; the
executor re-censuses with grep on the live tree.
</flagged_assumptions>

<tasks>

<task type="tracer">
  <name>Task 1: Move the first three mode sections to colocated CSS files and prove the chunk placement mechanism</name>
  <files>src/styles.css, src/components/drafts/DraftsPane.tsx, src/components/drafts/drafts.css, src/components/gap/GapPane.tsx, src/components/gap/gap.css, src/components/agents/AgentsPane.tsx, src/components/agents/agents.css</files>
  <read_first>
    - `src/components/graph/GraphView.tsx:18` and `src/components/graph/graph.css` head — the colocated-CSS precedent: import at the top of the owning component module
    - `src/styles.css` — re-census the live tree with grep for section-header comments (`=== Drafts pane ===`, `=== Gap analysis pane ===`, `=== Agents mode ===`) and each section's full extent; line numbers in this plan are planning-session values that drift
    - `.planning/phases/10-bundle-and-build-hardening/10-UI-SPEC.md` §Styling source of truth — the entry-stylesheet invariants that never move
    - `scripts/check-select-chrome.mjs` — the stripComments idiom the ownership guard reuses; its all-`src/**/*.css` walk is why moved rules must not reintroduce background shorthands
    - `src/styles.css` `.empty-state` blocks (:3608 and :24063 in the planning-session census) — the duplicated-selector boundary case
  </read_first>
  <reversibility rating="costly">D-01 locks the full per-mode split: once rules are dispersed across per-mode files, re-consolidating means re-deriving ownership for every selector again (10-CONTEXT.md records the rating; flagged only, not gated — REVERSIBILITY_GATES suppresses checkpoint insertion, the record stays).</reversibility>
  <action>
    Re-census styles.css with grep on the live tree (do not trust planning-session
    line numbers). For the Drafts, Gap, and Agents sections — the smallest
    self-prefixed sections, chosen to prove the mechanism before the larger moves
    — do the following per mode:

    1. Create `src/components/<dir>/<mode>.css` (drafts.css, gap.css, agents.css)
       containing exactly the rules of that mode's styles.css section, copied
       byte-identically (same selectors, same properties, same values, same
       order). Prepend one line: `/*! maru:mode:<id> */` (legal comment,
       verified to survive esbuild CSS minify).
    2. Delete the moved rules from `src/styles.css` (and their section header
       comment).
    3. Add `import "./<mode>.css";` as the first import line of the mode's
       top-level pane component (DraftsPane.tsx, GapPane.tsx, AgentsPane.tsx),
       matching the graph.css/diagram.css precedent. Do NOT import from the
       adapter file or anywhere in src/lib.

    Cross-check every moved selector against the D-02 boundary rules: a selector
    that other modes or entry chrome also uses (shared utilities, `.sr-only`,
    loading fallbacks) stays in styles.css — move only the mode's own prefixed
    rules. For any rule whose selector appears in multiple sections (including
    the `.empty-state` duplicate), apply the one-home rule and record the
    disposition in the plan SUMMARY.

    Then run `pnpm build:frontend` and verify the mechanism against the produced
    artifacts: `dist/assets/` gains `DraftsModeAdapter-*.css`, `GapModeAdapter-*.css`,
    and `AgentsModeAdapter-*.css` chunks, each containing its `/*! maru:mode:<id> */`
    marker, and the entry `index-*.css` contains none of the three markers. This
    is the tracer proof: Vite's default cssCodeSplit riding the mode's lazy
    import. If a marker lands in the entry chunk instead, the CSS import is
    reachable from the entry graph — find the transitive static path and fix the
    import placement; do not proceed to Task 2 until the mechanism is proven.
  </action>
  <!-- planner-discipline-allow: maru:mode: -->
  <verify>
    <automated>pnpm build:frontend</automated>
    <fails_when>non-zero exit, or the "initial CSS" budget line is absent from the output</fails_when>
    <automated>node -e "const {readdirSync,readFileSync}=require('node:fs');const dir='dist/assets';const files=readdirSync(dir);const markers=[...readFileSync(files.find(f=>/^index-.*\.css$/.test(f)),'utf8').matchAll(/\/\*!\s*maru:mode:([a-z0-9-]+)/g)].map(m=>m[1]);const bad=markers.filter(m=>['drafts','gap','agents'].includes(m));const pairs=['drafts','gap','agents'].map(m=>{const f=files.find(f=>new RegExp('^'+m[0].toUpperCase()+'.*ModeAdapter-.*\\.css$').test(f));return [m, f && readFileSync('dist/assets/'+f,'utf8').includes('/*! maru:mode:'+m)];});if(bad.length>0||pairs.some(([,ok])=>!ok))process.exit(1);"</automated>
    <fails_when>non-zero exit, meaning a moved marker sits in the entry chunk or a mode chunk is missing its marker</fails_when>
    <automated>pnpm typecheck && pnpm lint</automated>
    <fails_when>non-zero exit, or any ESLint warning (lint runs --max-warnings 0)</fails_when>
  </verify>
  <acceptance_criteria>
    - drafts.css, gap.css, agents.css exist under their component directories, each headed by its `/*! maru:mode:<id> */` legal comment.
    - The three sections' rules are gone from src/styles.css; the styles.css diff removes only those sections' rules and header comments.
    - Each of the three pane components imports its CSS as its first import line.
    - `pnpm build:frontend` exits 0: the three mode adapters emit `*-ModeAdapter-*.css` chunks carrying their markers, and the entry chunk carries none of the three markers.
    - No moved rule changed any property value (byte-identical copy except the marker line).
    - Duplicated-selector dispositions are recorded in the plan SUMMARY.
    - `pnpm typecheck` and `pnpm lint` exit 0.
  </acceptance_criteria>
  <done>
    The first three modes' CSS ships in their own lazy chunks with the entry chunk
    free of their markers, proving the split mechanism end-to-end against the
    production bundle.
  </done>
</task>

<task type="auto">
  <name>Task 2: Move the remaining mode sections and correct the check-bundle-budget comment</name>
  <files>src/styles.css, src/components/meetings/MeetingsPane.tsx, src/components/meetings/meetings-pane.css, src/components/today/TodayPane.tsx, src/components/today/today.css, src/components/tasks/TasksPane.tsx, src/components/tasks/tasks.css, src/components/calendar/UnifiedCalendarPane.tsx, src/components/calendar/calendar.css, scripts/check-bundle-budget.mjs</files>
  <read_first>
    - `src/styles.css` — re-census the live tree: remaining mode-prefixed section headers (`Meetings`, `Today`, `Tasks`, calendar block) and the D-02 boundary cases (pkm entries, "System mode" (:15479 census), Editor pane (:3651 census), Outline (:6644 census), tool panel (:12693 census), "Workspace pairing" (:15434 census), Outbox (:21684 census), the `.empty-state` duplicate)
    - `.planning/phases/10-bundle-and-build-hardening/10-CONTEXT.md` D-02 — mechanical prefix/section ownership; only boundary cases get manual review
    - `scripts/check-bundle-budget.mjs:27-28` — the stale ~12% headroom comment to correct
    - `src/lib/modeRegistry.tsx` — the 18 mode ids (`getRegisteredModeIds()`) for the inventory assertion
  </read_first>
  <action>
    Continue the same per-mode procedure for the remaining movable mode sections:
    Meetings, Today, Tasks, and the calendar block (Unified Calendar is used by
    both tasks and meetings — its CSS home is a shared lazy chunk imported by both
    TasksPane and MeetingsPane, or entry-side if the boundary review concludes it
    is genuinely cross-mode; record the disposition).

    Apply the D-02 boundary-case review to the census blocks that are NOT cleanly
    mode-scoped and record each disposition in the SUMMARY: pkm entries (stay —
    no pane component), "System mode" / Editor pane / Outline / tool panel /
    "Workspace pairing" (stay unless a clean single-mode home exists), Outbox
    (SharedOutboxPane has no dedicated adapter; stay unless cleanly single-mode),
    and the `.empty-state` duplicate (one home per duplicated selector; the other
    block is removed or the selector stays entry-side). Shared utilities and
    loading fallbacks stay entry-side per the prohibitions.

    Then correct the stale comment in `scripts/check-bundle-budget.mjs:27-28`:
    replace the "~12% headroom" sentence with the fresh-build reality after the
    split (the measured initial-CSS number and the fact that only the 70 KiB gate
    is enforced, per D-06). The thresholds `320 * 1024` and `70 * 1024` are
    byte-identical to today. Optionally (planner-assumed, flagged in
    must_haves.assumptions): extend the guard with the 18-mode inventory
    assertion using `getRegisteredModeIds()` semantics — for each mode id, the
    adapter's lazy CSS chunk must exist and the entry chunk must not contain that
    mode's marker; mark this clearly as an optional hardening the executor may
    skip if the census reveals modes without distinct sections (pkm).
  </action>
  <verify>
    <automated>pnpm build:frontend</automated>
    <fails_when>non-zero exit, or the "initial CSS" line missing, or the initial CSS budget exceeds 70 KiB gzip</fails_when>
    <automated>grep -c "maru:mode:" dist/assets/index-*.css; test "$(grep -c 'maru:mode:' dist/assets/index-*.css)" = 0</automated>
    <fails_when>the entry chunk contains any maru:mode marker, meaning a moved section still ships in the entry stylesheet</fails_when>
    <automated>node scripts/check-bundle-budget.mjs</automated>
    <fails_when>non-zero exit, or the "initial JS"/"initial CSS" success lines absent</fails_when>
    <automated>pnpm test</automated>
    <fails_when>non-zero exit, or any vitest suite failing after the styles.css section removals</fails_when>
  </verify>
  <acceptance_criteria>
    - Meetings, Today, Tasks, and the calendar block are moved (or entry-side with a recorded disposition for genuinely cross-mode selectors).
    - Every D-02 boundary case listed in `<action>` has a recorded disposition in the plan SUMMARY.
    - dist/assets/index-*.css contains zero `maru:mode:` markers.
    - `node scripts/check-bundle-budget.mjs` passes with thresholds unchanged (320*1024 / 70*1024) and the stale ~12% comment replaced with fresh-build reality.
    - `pnpm test` exits 0.
    - No moved rule changed any property value.
  </acceptance_criteria>
  <done>
    Every cleanly mode-scoped section ships in its mode's lazy chunk; boundary
    cases have recorded dispositions; the budget gate comment reflects reality at
    unchanged thresholds.
  </done>
</task>

<task type="auto">
  <name>Task 3: check-mode-css-ownership guard chained into build:frontend, with policy pin test</name>
  <files>scripts/check-mode-css-ownership.mjs, scripts/check-mode-css-ownership.test.ts, package.json</files>
  <read_first>
    - `scripts/check-native-e2e-isolation.mjs` — the two-half produced-artifact guard idiom (violations array, one stderr block, exit 1, single success line)
    - `scripts/check-bundle-budget.mjs` — the dist/assets readdir idiom and the missing-dist remediation message
    - `src/lib/modeRegistry.tsx` — the 18-mode inventory (`getRegisteredModeIds()`); the guard hardcodes the same list with an in-sync comment (source files are not imported by node scripts)
    - `package.json` scripts — the exact build:frontend chain 10-01 left; the new guard chains after check-csp-blob.mjs
    - `scripts/check-dom-sanitizer.test.ts` — the source-assertion test style
  </read_first>
  <action>
    Create `scripts/check-mode-css-ownership.mjs` in the check-*.mjs idiom: ESM,
    node builtins only. No-arg mode: readdir `dist/assets/` (missing dir: exit 1
    with the family remediation message). Collect every CSS chunk; for each, scan
    the RAW text (before any comment stripping — the markers live in comments, so
    a raw scan cannot false-positive) for `/*! maru:mode:<id> */` markers with
    `/\*!\s*maru:mode:([a-z0-9-]+)\s*\*\//g`. Assertions, each violation pushed
    with a named message: (1) RED LIST — the entry chunk (`index-*.css`) contains
    zero markers; (2) FULL INVENTORY — every mode id in the hardcoded 18-mode
    list (with the "keep in sync with modeRegistry.getRegisteredModeIds()" header
    comment) appears as a marker in exactly one produced CSS chunk (pkm
    anticipated as the possible exception per the flagged assumption — the guard
    carries an explicit, documented exception list if the census kept pkm
    entry-side; an empty exception list otherwise); (3) NO CROSS-CHUNK DUPLICATES
    — no marker appears in two different chunks; (4) MISSING MARKER — every
    per-mode CSS file under `src/components/**/*.css` whose `maru:mode:` marker
    appears in NO produced dist chunk is a violation (a per-mode file no emitted
    chunk carries was never imported by any component — a silent split defeat);
    (5) ORPHANED MARKER — a marker in a produced chunk whose id is not in the
    18-mode list is a violation. Violations: one stderr block, exit 1; success:
    one `mode-css-ownership:` line naming the chunk count and marker count.

    Add the same guard to `package.json`: a `check:mode-css-ownership` script and
    the `&& node scripts/check-mode-css-ownership.mjs` tail on `build:frontend`,
    after the check-csp-blob.mjs tail 10-01 added.

    Then create `scripts/check-mode-css-ownership.test.ts` in the
    source-assertion style: read the guard from disk, assert the raw-marker-scan
    regex, the red-list entry-chunk assertion, the no-cross-chunk-duplicates
    assertion, the exit-1-on-violations path with no exit-0-on-violation path,
    and that the hardcoded mode list carries the in-sync comment. Hermetic, no
    subprocess.
  </action>
  <verify>
    <automated>pnpm build:frontend</automated>
    <fails_when>non-zero exit, or the mode-css-ownership success line absent from the chained output</fails_when>
    <automated>pnpm test -- check-mode-css-ownership</automated>
    <fails_when>non-zero exit, or "0 passed" in the vitest summary line</fails_when>
    <automated>pnpm typecheck && pnpm lint</automated>
    <fails_when>non-zero exit, or any ESLint warning (lint runs --max-warnings 0)</fails_when>
  </verify>
  <acceptance_criteria>
    - `scripts/check-mode-css-ownership.mjs` exists, is ESM, node builtins only, and prints a `mode-css-ownership:` success line against the split bundle.
    - The red-list assertion fails the guard when a fixture marker is injected into the entry chunk (temporary fixture test, deleted after observing the red run).
    - The chained `pnpm build:frontend` runs check-bundle-budget.mjs, check-native-e2e-isolation.mjs, check-csp-blob.mjs, then check-mode-css-ownership.mjs, in that order.
    - `pnpm test -- check-mode-css-ownership` exits 0 with the policy assertions green.
    - `pnpm typecheck` and `pnpm lint` exit 0.
  </acceptance_criteria>
  <done>
    The ownership assertion is a standing build gate: any future CSS placement
    that lands a mode's styles in the entry chunk (or loses a mode's marker)
    turns `pnpm build:frontend` red without any manual check.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Guard script -> build pipeline | A guard that fails open (missing marker silently accepted, budget raised) stops protecting the entry-CSS boundary |
| Moved CSS -> produced bundle (esbuild minify, Vite chunking) | The ownership marker must survive minification and chunk assignment for the produced-artifact assertion to be sound |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-10-04 | Tampering (guard weakening) | check-type-tokens scope (src/styles.css only) | medium | mitigate | Prohibition: no raw-px rule moves out of styles.css; the guard's scope is not escaped by the split; reviewers check the moved rules carry var(--type-*) tokens |
| T-10-05 | Tampering (guard weakening) | check-mode-css-ownership.mjs itself | medium | mitigate | Policy pin test (Task 3) in the suite `pnpm test` already runs; violations exit 1 fail-closed; the red-list drill proves the guard has teeth |
| T-10-06 | Denial of Service (styling regression) | Cross-file selector dependencies (.sr-only, .mode-loading, duplicated selectors) | medium | mitigate | D-02 boundary review with recorded dispositions; shared utilities stay entry-side; the e2e FOUC/parity spec in 10-03 catches a flip on first activation |

No package installs in this plan — T-10-SC (supply chain) is not applicable.
ASVS L1: no new trust boundary is introduced; the threats above are guard-tampering
and styling-regression controls, grep-verifiable at the cited files.
</threat_model>

<verification>
- `pnpm build:frontend` green: budget gate + csp-blob dist scan + mode-css-ownership all chained and passing
- dist/assets/index-*.css contains zero `maru:mode:` markers; every movable mode's marker appears in exactly one lazy chunk
- `node scripts/check-bundle-budget.mjs` passes at unchanged thresholds; stale comment corrected
- `pnpm test`, `pnpm typecheck`, `pnpm lint` green
- D-02 boundary-case dispositions recorded in 10-02-SUMMARY.md
</verification>

<success_criteria>
- PERF-05: per-mode CSS ships in the mode's lazy chunk; the initial-CSS budget check passes at the original 70 KiB threshold, not a raised one
- The ownership assertion is a standing gate on `pnpm build:frontend`
- No duplicated selector keeps two homes; shared utilities and first-paint rules stay entry-side
</success_criteria>

<output>
Create `.planning/phases/10-bundle-and-build-hardening/10-02-SUMMARY.md` when done
</output>
