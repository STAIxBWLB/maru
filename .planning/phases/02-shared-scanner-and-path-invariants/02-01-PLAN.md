---
phase: 02-shared-scanner-and-path-invariants
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src-tauri/src/paths.rs
  - src-tauri/src/lib.rs
  - src-tauri/src/workspace_files.rs
  - src-tauri/src/content_search.rs
  - src-tauri/src/maru_dir.rs
autonomous: true
requirements: [SCAN-01, SCAN-02, SCAN-03]

estimate:
  tokens: 55000
  raw_tokens: 55000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "src-tauri/src/paths.rs is the single module holding GENERATED_DIRS, ensure_within, and require_absolute together (D-01, D-02)"
    - "GENERATED_DIRS is the flat 14-entry union (node_modules, target, dist, build, .next, .turbo, .cache, .git, .venv, .context, .omc, .omx, .pnpm-store, __pycache__) with no core+extras split (D-03)"
    - "workspace_files.rs and content_search.rs both consume the shared constant and compile and test green against the 14-entry union"
    - "ensure_within returns Ok when child equals parent or descends from it, and Err on a lexical escape, with no canonicalize() anywhere in the containment path (D-07)"
    - "Generated directories can no longer be allowlisted back into content search results — rg_visibility reports exclude_git: true even for an allowlisted nested .git path (SCAN-02's real delta)"
    - "maru_dir.rs's rule_path and its second call site resolve containment through crate::paths::ensure_within, and no private copy remains in maru_dir.rs"
  prohibitions:
    - "GENERATED_DIRS never contains .maru — evidence_binder.rs keeps excluding it module-locally"
    - "ScanFilter::is_excluded_path keeps its generated_dirs parameter — inbox.rs passes an empty slice at three call sites and must keep doing so"
    - "ScanFilter::from_options gains no rejection of user-allowlisted generated dirs — the scope reduction stays silent, never a user-facing settings error"
  artifacts:
    - path: "src-tauri/src/paths.rs"
      provides: "Shared path-invariants module: prune constant, containment helper, absolute-base guard"
      contains: "pub const GENERATED_DIRS"
      exports: ["GENERATED_DIRS", "ensure_within", "require_absolute", "lexical_normalize"]
    - path: "src-tauri/src/lib.rs"
      provides: "Module registry entry for paths"
      contains: "mod paths;"
    - path: "src-tauri/src/workspace_files.rs"
      provides: "Scanner consuming the shared constant (former const owner)"
      contains: "use crate::paths::GENERATED_DIRS;"
    - path: "src-tauri/src/content_search.rs"
      provides: "Sixth consumer repointed to the shared constant, rg visibility reconciled"
      contains: "use crate::paths::GENERATED_DIRS;"
    - path: "src-tauri/src/maru_dir.rs"
      provides: "First caller of the promoted canonical ensure_within"
      contains: "use crate::paths::ensure_within;"
  key_links:
    - from: "src-tauri/src/workspace_files.rs"
      to: "src-tauri/src/paths.rs"
      via: "import of the shared prune constant consumed by filter_entry call sites"
      pattern: "use crate::paths::GENERATED_DIRS"
    - from: "src-tauri/src/maru_dir.rs"
      to: "src-tauri/src/paths.rs"
      via: "both former private-call sites resolve to the promoted helper"
      pattern: "paths::ensure_within"
    - from: "src-tauri/src/paths.rs"
      to: "src-tauri/src/vault.rs"
      via: "lexical_normalize import and re-export — containment stays lexical (D-07)"
      pattern: "crate::vault::lexical_normalize"
---

<objective>
Create `src-tauri/src/paths.rs` as the single home for the phase's three
invariants (D-01, D-02), register it, and prove the shared-constant path
end-to-end by rewiring the constant's current owner (`workspace_files.rs`)
plus the sixth consumer (`content_search.rs`) that the move force-repoints.
Then reconcile content search's rg visibility with the union and switch
`maru_dir.rs`'s two call sites to the promoted `ensure_within`.

Purpose: This is the tracer plan for Phase 2 — it proves the
new-module → registry → consumer-rewire path through the Phase 1 gate set
before the remaining four scanners are rewired in 02-02.
Output: paths.rs with tests, four rewired/registered modules, gates green.
</objective>

<execution_context>
@$HOME/.kimi-code/gsd-core/workflows/execute-plan.md
@$HOME/.kimi-code/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-shared-scanner-and-path-invariants/02-CONTEXT.md
@.planning/phases/02-shared-scanner-and-path-invariants/02-RESEARCH.md
@.planning/phases/02-shared-scanner-and-path-invariants/02-PATTERNS.md
</context>

<tasks>

<task type="tracer">
  <name>End-to-end "shared paths module": paths.rs created with all three invariants, registered in lib.rs, feeding workspace_files.rs and content_search.rs</name>
  <reversibility rating="costly">D-02/D-03: once six consumers import paths.rs, moving the module or splitting the union re-touches every call site and every scanner's coverage expectations.</reversibility>
  <precondition>Phase 1 gate set is live: the Makefile verify prerequisite list includes fmt-check, clippy, and lint (confirm with grep before starting). src-tauri/src/lib.rs carries concurrent hwped-session edits — the mod-declaration insertion must merge alongside them, never revert them.</precondition>
  <files>src-tauri/src/paths.rs, src-tauri/src/lib.rs, src-tauri/src/workspace_files.rs, src-tauri/src/content_search.rs</files>
  <read_first>
    - .planning/phases/02-shared-scanner-and-path-invariants/02-PATTERNS.md (paths.rs, lib.rs, workspace_files.rs, content_search.rs sections)
    - .planning/phases/02-shared-scanner-and-path-invariants/02-RESEARCH.md (Pattern 1, Pattern 2, Pitfalls 1 and 2)
    - src-tauri/src/workspace_files.rs (lines 1-40 for the const and imports; 610-710 for the filter_entry consumption)
    - src-tauri/src/content_search.rs (lines 1-20 for the import at :7)
    - src-tauri/src/lib.rs (lines 1-82, the flat alphabetical mod list)
    - src-tauri/src/maru_dir.rs (lines 185-200, the ensure_within source to move verbatim)
    - src-tauri/src/vault.rs (lines 590-595 for the lexical-containment rationale comment; 614-634 for lexical_normalize)
  </read_first>
  <action>
    First capture the baseline: run `cd src-tauri && cargo test --lib 2>&1 | tail -5` and record the summary line; the SUMMARY must report baseline vs final test counts.

    Create src-tauri/src/paths.rs per D-01/D-02 with a module-level doc comment that (a) states this module is the one place to reach for path invariants, (b) carries the lexical-containment rationale paraphrased from vault.rs:590-595 (no canonicalize — user symlinks inside a workspace stay valid; `..` traversal is still blocked by lexical_normalize), and (c) includes a usage example of ensure_within fenced as `rust,ignore` with a one-line note that the module is crate-private so the example is illustrative, not a compiled doctest (D-06).

    Contents, in order:
    1. `pub const GENERATED_DIRS: &[&str]` with exactly the 14-entry D-03 union in this order: node_modules, target, dist, build, .next, .turbo, .cache, .git, .venv, .context, .omc, .omx, .pnpm-store, __pycache__. Doc comment: "Directories produced by tooling, never authored content. Every scanner prunes these; adding one is a one-line edit here (SCAN-01)." plus a second doc line stating `.maru` is deliberately absent — it is Maru state, not a generated dir.
    2. `use crate::vault::lexical_normalize;` plus `pub use crate::vault::lexical_normalize;` re-export so new authors get both helpers from one module (D-02's "one place to reach for"; planner discretion per RESEARCH Pattern 2 — decision: include the re-export).
    3. `pub fn ensure_within(parent: &Path, child: &Path) -> Result<(), String>` moved verbatim from maru_dir.rs:190-196: normalize child via lexical_normalize, Err if the result does not start_with parent. Keep the error message byte-identical: "Path escapes the .maru directory" (Pitfall 4 — planner discretion resolved to verbatim, safest; noted as a flagged assumption below). No canonicalize anywhere (D-07).
    4. `pub fn require_absolute(path: PathBuf) -> Result<PathBuf, String>`: Ok(path) when path.is_absolute(), else Err(format!("Home root must be absolute, got: {}", path.display())) (D-02/D-08 — Err, never assert!/panic/debug_assert!).
    5. `#[cfg(test)] mod tests` at file bottom (repo convention) with: ensure_within accepts a descendant, accepts child == parent (starts_with is reflexive), rejects a `..` escape, rejects an unrelated absolute path; require_absolute accepts an absolute path and rejects a relative one. Use std::path::Path fixtures only — no TempDir needed here.

    Register the module in src-tauri/src/lib.rs: grep the mod list first to confirm the current neighbors (concurrent hwped session edits this file), then insert `mod paths;` alphabetically between `mod ops_catalog;` (was :48) and `mod outlook_mso;` (was :49). Touch nothing else in lib.rs.

    Rewire the const owner, src-tauri/src/workspace_files.rs: delete the local pub(crate) prune-list constant at lines 21-29 and add `use crate::paths::GENERATED_DIRS;` to the crate-import group (line 17 area, alphabetical within the group). The filter_entry consumption sites (:625, :697) and everything else stay untouched.

    Repoint the sixth consumer, src-tauri/src/content_search.rs (Pitfall 1 — compile-forced by the move): split the import at :7 into two lines — `use crate::paths::GENERATED_DIRS;` and `use crate::workspace_files::is_binary_file;` (rustfmt-ordered in the crate group). The three use sites (:203, :359-362, :467) stay exactly as they are; the rg glob loop now iterates the 14-entry union, which is the intended SCAN-02 behavior. The rg_visibility reconciliation is Task 2, not this task.
  </action>
  <verify>
    <automated>cd src-tauri && cargo test --lib paths:: workspace_files:: content_search:: && cargo clippy -- -D warnings && cargo fmt --check</automated>
  </verify>
  <acceptance_criteria>
    - `cd src-tauri && cargo test --lib paths::` exits 0; the new tests named in the action all pass
    - `grep -c 'mod paths;' src-tauri/src/lib.rs` prints 1, and the line sits between the ops_catalog and outlook_mso declarations
    - `grep -c 'use crate::paths::GENERATED_DIRS;' src-tauri/src/workspace_files.rs` prints 1; `grep -v '^\s*//' src-tauri/src/workspace_files.rs | grep -c 'const GENERATED_DIRS'` prints 0 (local constant fully removed)
    - `grep -c 'use crate::paths::GENERATED_DIRS;' src-tauri/src/content_search.rs` prints 1; `grep 'use crate::workspace_files::' src-tauri/src/content_search.rs` shows only is_binary_file
    - `for d in node_modules target dist build .next .turbo .cache .git .venv .context .omc .omx .pnpm-store __pycache__; do grep -q "\"$d\"" src-tauri/src/paths.rs || echo "MISSING $d"; done` prints nothing (all 14 present)
    - `grep -c '"\.maru"' src-tauri/src/paths.rs` prints 0 outside doc comments (verify with `grep -v '^\s*//' src-tauri/src/paths.rs | grep -c '"\.maru"'` == 0)
    - `cd src-tauri && cargo test --lib workspace_files:: content_search::` exits 0; final `cargo test --lib` summary equals the recorded baseline plus only the new paths::tests entries (record both in SUMMARY)
    - `cd src-tauri && cargo clippy -- -D warnings` and `cargo fmt --check` both exit 0
  </acceptance_criteria>
  <done>paths.rs exists with all three invariants and passing unit tests; lib.rs registers it alphabetically alongside the concurrent session's edits; workspace_files.rs and content_search.rs compile and test green against the 14-entry union; clippy and fmt gates clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Reconcile content_search rg_visibility with the union — generated dirs become un-allowlistable</name>
  <files>src-tauri/src/content_search.rs</files>
  <read_first>
    - src-tauri/src/content_search.rs (lines 260-280 rg_visibility; 355-375 the rg glob loop; 855-930 the allowlist and parity tests)
    - .planning/phases/02-shared-scanner-and-path-invariants/02-RESEARCH.md (Pitfall 2, Open Question 2)
    - .planning/phases/02-shared-scanner-and-path-invariants/02-PATTERNS.md (content_search.rs section)
  </read_first>
  <behavior>
    - Test update first (red): edit the git_allowed case in rg_hidden_and_git_traversal_follow_dot_folder_allowlist (:889-899) to expect exclude_git: true, run it against the not-yet-changed rg_visibility, observe the failure
    - After the rg_visibility change (green): the same test passes, and the full content_search suite — including the rg/fallback parity tests (:902-917), which really execute because rg 15.0.0 is installed locally — passes
  </behavior>
  <action>
    In rg_visibility (content_search.rs:266-271), make exclude_git reflect the new invariant that a generated dir can never be allowlisted back into search: `.git` is now in GENERATED_DIRS and the glob loop at :359-362 excludes it unconditionally, so set exclude_git to true unconditionally (RESEARCH Open Question 2's recommended shape; final micro-shape is executor's discretion but the observable semantic — allowlisting a generated dir changes nothing — is not). Keep the RgVisibility struct and the hidden field computation unchanged.

    Do NOT add any generated-dir rejection to ScanFilter::from_options (Pitfall 2: that converts a silent scope reduction into a user-facing settings error — forbidden).

    Update the test at :867-900 deliberately: flip the git_allowed expectation from exclude_git: false to true and extend the test name or add a sibling assertion making the new rule explicit (generated dirs are un-allowlistable). Prove red-then-green in the order given in behavior. This is the phase's one deliberately-updated pre-existing test — name it and the expectation change in the SUMMARY, per the Phase 1 evidence standard.
  </action>
  <verify>
    <automated>cd src-tauri && cargo test --lib content_search::</automated>
  </verify>
  <acceptance_criteria>
    - The red-then-green proof happened in the documented order (test edit failed before the rg_visibility edit; passes after) — both runs' outputs are summarized in SUMMARY
    - `cd src-tauri && cargo test --lib content_search::` exits 0, including rg_and_fallback_produce_identical_results (must not skip: rg is installed — if it skipped, say so explicitly in SUMMARY)
    - `grep -n 'exclude_git' src-tauri/src/content_search.rs` shows the updated computation and the updated test expectation
    - `grep -c 'from_options' src-tauri/src/content_search.rs` is unchanged in behavior: no rejection path for generated dirs was added (diff shows only rg_visibility + test edits)
    - `cd src-tauri && cargo clippy -- -D warnings && cargo fmt --check` exit 0
  </acceptance_criteria>
  <done>rg_visibility can no longer report a generated dir as searchable; the allowlist test asserts the new expectation; the parity tests pass locally with rg present.</done>
</task>

<task type="auto">
  <name>Promote ensure_within: maru_dir.rs drops its private copy and calls the canonical helper</name>
  <files>src-tauri/src/maru_dir.rs</files>
  <read_first>
    - src-tauri/src/maru_dir.rs (lines 185-200 the private copy; 890-900 and 1110-1120 the two call sites; 75-95 MARUIGNORE_DEFAULTS which must not change; line 24 the existing crate::vault::lexical_normalize import for style)
    - src-tauri/src/paths.rs (the promoted helper created in Task 1)
    - .planning/phases/02-shared-scanner-and-path-invariants/02-PATTERNS.md (maru_dir.rs section)
  </read_first>
  <action>
    Delete the private ensure_within function at maru_dir.rs:190-196 and add `use crate::paths::ensure_within;` to the crate-import group (same style as the existing crate::vault import at :24). The two call sites (:896 in rule_path, :1116) keep their exact call shape — only the resolution target changes. The error message stays byte-identical ("Path escapes the .maru directory"), so maru_dir's externally visible behavior is unchanged (SCAN-03, D-06).

    Do NOT touch: MARUIGNORE_DEFAULTS (:79-93, thirteen entries — D-05, user-facing file format), the sibling private copies at studio/mod.rs:287-293 and diagram/mod.rs:68-74 (D-06 — no caller conversion), and the terminal_hooks.rs private maru_home.
  </action>
  <verify>
    <automated>cd src-tauri && cargo test --lib maru_dir:: && cargo clippy -- -D warnings && cargo fmt --check</automated>
  </verify>
  <acceptance_criteria>
    - `grep -v '^\s*//' src-tauri/src/maru_dir.rs | grep -c 'fn ensure_within'` prints 0 (private copy gone)
    - `grep -c 'use crate::paths::ensure_within;' src-tauri/src/maru_dir.rs` prints 1
    - `grep -c 'Path escapes the .maru directory' src-tauri/src/maru_dir.rs` prints 0 and the same grep on src-tauri/src/paths.rs prints 1 (message moved, byte-identical, exactly one home)
    - `cd src-tauri && cargo test --lib maru_dir::` exits 0; the full `cargo test --lib` summary matches the Task 1 baseline plus only paths::tests (no maru_dir test count change)
    - `git diff --stat src-tauri/src/studio/mod.rs src-tauri/src/diagram/mod.rs` is empty (sibling copies untouched)
    - `sed -n '75,95p' src-tauri/src/maru_dir.rs` still shows the thirteen-entry MARUIGNORE_DEFAULTS (D-05)
  </acceptance_criteria>
  <done>maru_dir.rs resolves containment through the canonical helper with byte-identical behavior; no sibling copies or the maruignore defaults were touched.</done>
</task>

</tasks>

## Artifacts this plan produces

- `src-tauri/src/paths.rs` — NEW module (registered as `mod paths;` in lib.rs, alphabetical between ops_catalog and outlook_mso)
- `pub const GENERATED_DIRS: &[&str]` — the flat 14-entry union (D-03)
- `pub fn ensure_within(parent: &Path, child: &Path) -> Result<(), String>` — promoted canonical containment helper (D-06)
- `pub fn require_absolute(path: PathBuf) -> Result<PathBuf, String>` — the SCAN-04 guard predicate (consumed by plan 02-03)
- `pub use crate::vault::lexical_normalize;` re-export from paths.rs
- `paths::tests` — unit tests for ensure_within (descendant/equal/escape/absolute-outside) and require_absolute (absolute/relative)
- Rewired imports: `use crate::paths::GENERATED_DIRS;` in workspace_files.rs and content_search.rs; `use crate::paths::ensure_within;` in maru_dir.rs
- Updated test expectation: content_search.rs rg_hidden_and_git_traversal_follow_dot_folder_allowlist (exclude_git flips to true for allowlisted .git)

## Flagged assumptions (specless probe edges — not silently dropped)

- **SCAN-01 / adjacency:** Name-equality matching semantics are preserved verbatim. The union changes WHICH names are pruned, not HOW matching works; a file whose final component exactly equals a union entry is treated exactly as it was before the rewire (each scanner's existing component-matching behavior is untouched).
- **SCAN-01 / empty:** Authored into must_haves.truths and paths::tests — ensure_within returns Ok for child == parent (the reflexive boundary case) and Err for escapes.
- **SCAN-01 / ordering:** Scan result ordering is unchanged. Every rewire changes only the constant's origin and contents; no sort, collect, or emission order is modified anywhere in this plan.
- **SCAN-03 / unclassified:** The ensure_within error message is kept byte-identical ("Path escapes the .maru directory") rather than generalized — Claude's-discretion item resolved to the safest option per RESEARCH Pitfall 4. The message is a live user-visible string on command paths; changing it is out of scope for a behavior-preserving phase.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| untrusted workspace trees → scanner prune filters | directory names in user-controlled trees decide traversal scope |
| caller-supplied relative paths → ensure_within | containment decision for paths joined under a root |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-01 | Tampering/Elevation | ensure_within (paths.rs) | medium | mitigate | Promote the lexical containment helper as the single canonical example with unit tests covering descendant/equal/escape cases; existing per-module checks stay untouched (D-06) so no new bypass is introduced |
| T-02-02 | Information disclosure | scanner prune lists (workspace_files.rs, content_search.rs) | medium | mitigate | Single 14-entry union including .git and .venv; Task 2 makes generated dirs un-allowlistable in content search so .git internals can never be resurrected into results |
| T-02-03 | Tampering | symlink-following containment bypass | low | accept | Lexical containment is deliberate design (D-07; vault.rs:590-595): canonicalize() would falsely reject user symlinks inside workspaces; `..` traversal remains blocked by lexical_normalize |
</threat_model>

<verification>
Per-gate verification strategy (Phase 1 precedent, concurrent-checkout hazard):
`src-tauri/src/hwped.rs` and `src-tauri/src/lib.rs` are dirty from a concurrent
session, so a full `make verify` can fail on files this plan does not own.
Prove each owned gate individually: `cargo test --lib` (scoped per task, then
full), `cargo clippy -- -D warnings`, `cargo fmt --check`, plus `pnpm typecheck`
/ `make lint` / `make test-e2e` untouched-but-green if run. CI is the
authoritative composite. The SUMMARY must record: baseline vs final
`cargo test --lib` counts, the red-then-green proof for the content_search
expectation flip, and confirmation that lib.rs edits merged alongside the
concurrent session's changes.
</verification>

<success_criteria>
- paths.rs holds GENERATED_DIRS (14 entries), ensure_within, require_absolute together (D-01/D-02/D-03)
- workspace_files.rs and content_search.rs consume the shared constant; the content_search allowlist contradiction is reconciled with parity tests green locally
- maru_dir.rs calls the promoted ensure_within with byte-identical messages; studio/diagram copies and MARUIGNORE_DEFAULTS untouched
- All owned gates green; test-count delta is exactly the new paths::tests
</success_criteria>

<output>
Create `.planning/phases/02-shared-scanner-and-path-invariants/02-01-SUMMARY.md` when done
</output>
