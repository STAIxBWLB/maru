---
phase: 02-shared-scanner-and-path-invariants
plan: "03"
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - src-tauri/src/skill_host/fs.rs
  - Users/
autonomous: true
requirements: [SCAN-04, SCAN-05]

estimate:
  tokens: 28000
  raw_tokens: 28000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "maru_home(), install_root_base(), and env_root() each return Err when the resolved home base is not absolute (D-08, D-09)"
    - "The guard validates the final value on every return path of both functions, including the test-override branch (Pitfall 6)"
    - "No directory tree materializes in the process working directory when the guard fires — proven by test assertion"
    - "The stray Users/ tree no longer exists at the repo root, and the delete shipped in the same plan as the guard (D-10)"
    - "All 15 existing maru_home/env_root/install_root_base call sites compile unchanged — they already propagate Result"
  prohibitions:
    - "The guard fails via Err only — no assert!/panic (release crash) and no debug_assert! (release unguarded) anywhere in the guard path (D-08)"
    - "test_maru_home_override stays cfg(test)-gated — the guard is production code validating both branches, not test-only scaffolding (Pitfall 6)"
    - "env_root() and skills_root() get no separate guard — they derive from maru_home() and are covered transitively; duplicating the check re-fragments the invariant"
  artifacts:
    - path: "src-tauri/src/skill_host/fs.rs"
      provides: "Guarded home-root constructors and the SCAN-04 regression test"
      contains: "require_absolute"
  key_links:
    - from: "src-tauri/src/skill_host/fs.rs"
      to: "src-tauri/src/paths.rs"
      via: "maru_home() and install_root_base() wrap their final return values in crate::paths::require_absolute"
      pattern: "require_absolute"
---

<objective>
Land the SCAN-04 absolute-base guard inside maru_home() and
install_root_base() (validating the final value on every return path, per
D-08/D-09), prove it with a regression test that sets a relative
MARU_TEST_HOME and asserts loud failure plus no materialized tree, and
delete the stray Users/ tree at the repo root in the same plan (D-10).

Purpose: The guard stops the incident class that produced the stray tree;
shipping the delete without the guard would be cosmetic (locked D-10).
Output: guarded constructors, one regression test, Users/ gone, gates green.
</objective>

<execution_context>
@$HOME/.kimi-code/gsd-core/workflows/execute-plan.md
@$HOME/.kimi-code/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-shared-scanner-and-path-invariants/02-CONTEXT.md
@.planning/phases/02-shared-scanner-and-path-invariants/02-RESEARCH.md
@.planning/phases/02-shared-scanner-and-path-invariants/02-PATTERNS.md
@.planning/phases/02-shared-scanner-and-path-invariants/02-01-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>SCAN-04 guard: require_absolute inside maru_home()/install_root_base(), red-then-green regression test</name>
  <files>src-tauri/src/skill_host/fs.rs</files>
  <read_first>
    - src-tauri/src/skill_host/fs.rs (lines 13-60 the guard targets maru_home/install_root_base/env_root/skills_root; 193-246 the test_maru_home_override/test_maru_home_lock/BundleTestHome fixture; 249 onward the existing mod tests)
    - src-tauri/src/paths.rs (require_absolute, created in plan 02-01)
    - .planning/phases/02-shared-scanner-and-path-invariants/02-PATTERNS.md (skill_host/fs.rs section — guard shape and test idiom)
    - .planning/phases/02-shared-scanner-and-path-invariants/02-RESEARCH.md (Pattern 3, Pitfall 6)
    - .planning/phases/02-shared-scanner-and-path-invariants/02-01-SUMMARY.md (confirm require_absolute landed with the exact signature)
  </read_first>
  <behavior>
    - Red first: add the test maru_home_rejects_relative_test_home to the existing mod tests (:249), run it against the unguarded code — it must FAIL because a relative MARU_TEST_HOME override currently returns Ok
    - Test contract: with MARU_TEST_HOME set to "relative-home" under test_maru_home_lock(), maru_home(), env_root(), and install_root_base() all return Err, and no "relative-home" path exists in the cwd afterward
    - Green after: applying require_absolute to both functions' final return values makes the test pass; all pre-existing skill_host::fs tests still pass
  </behavior>
  <action>
    Step 1 (red): add the test to fs.rs's existing `mod tests` using the established fixture idiom — acquire test_maru_home_lock() (:201-207), save the previous MARU_TEST_HOME value, set_var to "relative-home" (edition 2021: set_var is safe, not unsafe), call maru_home(), env_root(), install_root_base() capturing the Results, restore the previous env value on all paths (BundleTestHome Drop pattern, :225-233), then assert: all three Results are Err, and !Path::new("relative-home").exists(). Run it and record the failure output for the SUMMARY.

    Step 2 (green): apply the guard per D-09 and Pitfall 6 — the FINAL value of maru_home() (:17-22) and install_root_base() (:35-40) is validated on every return path, including the test-override early-returns. Either restructure each function to a single exit wrapped in crate::paths::require_absolute(...), or wrap each Ok(...) return in it; both shapes are acceptable, single-exit preferred for readability. The guard error comes from require_absolute verbatim: "Home root must be absolute, got: {display}". env_root() (:28) and skills_root() (:24) derive from maru_home() and are covered transitively — do not add separate checks there (see must_haves.prohibitions). All 15 downstream call sites already propagate Result (? or let-else, e.g. store.rs:2887), so no call-site edits.

    Step 3: run the full skill_host::fs test module — new test green, every pre-existing test green, no count change beyond the one new test.
  </action>
  <verify>
    <automated>cd src-tauri && cargo test --lib skill_host::fs && cargo clippy -- -D warnings && cargo fmt --check</automated>
  </verify>
  <acceptance_criteria>
    - The red run's failure output (test failed pre-guard because the relative override returned Ok) is captured and quoted in the SUMMARY
    - `grep -c 'require_absolute' src-tauri/src/skill_host/fs.rs` is at least 2 (both constructors guarded)
    - `grep -n 'debug_assert\|assert!' src-tauri/src/skill_host/fs.rs` shows no new assertion macros in the guard path (diff-review: guard is Err-only, D-08)
    - `grep -c 'use crate::paths::require_absolute' src-tauri/src/skill_host/fs.rs` prints 1
    - `cd src-tauri && cargo test --lib skill_host::fs` exits 0, including maru_home_rejects_relative_test_home
    - `cd src-tauri && cargo test --lib` full-suite summary matches the 02-01 baseline (recorded in 02-01-SUMMARY.md, in this plan's context) plus exactly one new test from this task — plus, if same-wave plan 02-02 has already run first, the one vault union-proof test it adds; either ordering is valid, so the allowed delta over the 02-01 baseline is +1 (02-02 not yet run) or +2 (02-02 already run). State which ordering applied in the SUMMARY
    - `cd src-tauri && cargo clippy -- -D warnings && cargo fmt --check` exit 0
  </acceptance_criteria>
  <done>Both home-root constructors validate is_absolute on their final values via Err; the regression test proves loud failure with no cwd tree; all existing call sites compile unchanged.</done>
</task>

<task type="auto">
  <name>SCAN-05: delete the stray Users/ tree at the repo root (same plan as the guard, per D-10)</name>
  <precondition>Task 1 of this plan is complete — the guard and its regression test are committed, so the incident class cannot silently recur (D-10: delete without the guard is cosmetic).</precondition>
  <files>Users/</files>
  <read_first>
    - .gitignore (line 13 — the node_modules/ rule that hid this tree from git status)
    - .planning/phases/02-shared-scanner-and-path-invariants/02-RESEARCH.md (Runtime State Inventory — the tree's confirmed contents and untracked status)
  </read_first>
  <action>
    Safety check first, recorded in the SUMMARY: `git ls-files Users/` must return empty (the tree is fully untracked) and `find Users -maxdepth 3 -type d | head -20` should show the expected stray shape (Users/yj.lee/.maru/env/... including node_modules caches — regenerable tooling output, not authored content). If git ls-files returns ANY tracked path, STOP and surface a checkpoint instead of deleting.

    Then delete: `rm -rf Users/` from the repo root. No git ceremony — the tree is untracked (RESEARCH Runtime State Inventory). Verify with `test ! -e Users` and `git status --porcelain -- Users/` returning empty.
  </action>
  <verify>
    <automated>test ! -e Users && test -z "$(git status --porcelain -- Users/)" && test -z "$(git ls-files Users/)"</automated>
  </verify>
  <acceptance_criteria>
    - Pre-delete evidence in SUMMARY: `git ls-files Users/` output was empty; the find listing matched the expected stray shape
    - `test ! -e Users` exits 0 post-delete
    - `git status --porcelain -- Users/` prints nothing post-delete
    - `cd src-tauri && cargo test --lib 2>&1 | tail -3` still green after the delete (the tree was never a build input)
  </acceptance_criteria>
  <done>The stray tree is gone from the repo root, the deletion is verified by filesystem and git checks, and the SCAN-04 guard preventing recurrence landed in Task 1 of this same plan.</done>
</task>

</tasks>

## Artifacts this plan produces

- Guarded constructors in src-tauri/src/skill_host/fs.rs: `maru_home()` and `install_root_base()` wrap their final return values in `crate::paths::require_absolute` (consumes the helper created in plan 02-01)
- New regression test: `skill_host::fs::tests::maru_home_rejects_relative_test_home` (relative MARU_TEST_HOME → Err on maru_home/env_root/install_root_base; no "relative-home" tree in cwd)
- Deletion: the untracked `Users/` tree at the repo root (SCAN-05)

## Flagged assumptions (specless probe edges — not silently dropped)

- **SCAN-04 / unclassified (production branch not unit-testable):** Only the test-override branch can be forced relative in a unit test (via MARU_TEST_HOME). The production branch fed by dirs::home_dir() is guarded by the same code path but cannot be exercised as a failure case in tests (RESEARCH Assumption A2). The guard covers both branches regardless of which one historically produced the stray tree.
- **SCAN-05 / unclassified (deletion irreversibility):** The Users/ tree is fully untracked, so `rm -rf` is not recoverable via git. Accepted because the confirmed contents are regenerable tooling caches (node_modules under .maru/env, including .bin/tsserver, vite, playwright) — no authored content. The pre-delete find listing is recorded in the SUMMARY as evidence.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| environment (MARU_TEST_HOME, dirs::home_dir()) → home-root constructors | a non-absolute base crossing this boundary silently materializes directory trees in the process cwd |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-07 | Tampering | skill_host/fs.rs::maru_home / install_root_base | high | mitigate | Task 1 wraps both functions' final return values in crate::paths::require_absolute (Err per D-08, both branches per Pitfall 6) plus the maru_home_rejects_relative_test_home regression test; Task 2 removes the existing stray artifact in the same plan (D-10) |
| T-02-08 | Tampering | env-mutating guard test racing parallel tests | medium | mitigate | Test holds test_maru_home_lock() and restores the previous MARU_TEST_HOME value on all paths (existing fs.rs:199-246 fixture idiom), so the process-global env mutation cannot leak into sibling tests |
</threat_model>

<verification>
Per-gate verification strategy (Phase 1 precedent, concurrent-checkout
hazard): the shared checkout carries a concurrent hwped session's dirty Rust
files, so a full `make verify` can fail on files this plan does not own.
Prove owned gates individually: `cargo test --lib skill_host::fs` (red→green
pair recorded), full `cargo test --lib` (count delta = exactly one new
test), `cargo clippy -- -D warnings`, `cargo fmt --check`, and the SCAN-05
filesystem assertions. CI is the authoritative composite.
</verification>

<success_criteria>
- maru_home()/install_root_base() fail loudly via Err on a non-absolute base, on every return path (SCAN-04)
- The regression test proves no tree materializes in the cwd when the guard fires
- Users/ is gone from the repo root, deleted in the same plan as the guard (SCAN-05, D-10)
- Full lib suite green with only the one expected new test
</success_criteria>

<output>
Create `.planning/phases/02-shared-scanner-and-path-invariants/02-03-SUMMARY.md` when done
</output>
