---
phase: 02-shared-scanner-and-path-invariants
plan: "02"
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - src-tauri/src/vault.rs
  - src-tauri/src/secrets.rs
  - src-tauri/src/project_activity.rs
  - src-tauri/src/evidence_binder.rs
autonomous: true
requirements: [SCAN-01, SCAN-02]

estimate:
  tokens: 50000
  raw_tokens: 50000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "vault.rs, secrets.rs, project_activity.rs, and evidence_binder.rs all prune exactly the shared 14-entry GENERATED_DIRS union (D-04)"
    - "evidence_binder.rs still never surfaces .maru contents as evidence candidates — the module-local .maru check survives the rewire (Pitfall 3)"
    - "project_activity.rs keeps its broader dot-prefix rule alongside the union membership check"
    - "inbox.rs's three empty-slice is_excluded_path call sites and its .omc allowlist tests are untouched and green (Pitfall 5)"
    - "A vault scan over a fixture tree containing __pycache__/, .git/, and .venv/ returns none of their contents — proven by a test that was red before the rewire and green after (SCAN-02's real delta: __pycache__ is the genuinely new exclusion)"
  prohibitions:
    - "ScanFilter::is_excluded_path keeps its generated_dirs: &[&str] parameter — the exclusion predicate stays shared, the list stays injected per call site"
    - "GENERATED_DIRS never contains .maru — evidence_binder.rs excludes it module-locally with a comment explaining why"
    - "ops_catalog/scan.rs's is_excluded_dir stays untouched — it is a domain-specific BU-scan list (vault, _Archived Items, .obsidian), not one of the five in-scope scanners"
  artifacts:
    - path: "src-tauri/src/vault.rs"
      provides: "Scanner consuming the shared constant; ScanFilter predicate signature preserved"
      contains: "use crate::paths::GENERATED_DIRS;"
    - path: "src-tauri/src/secrets.rs"
      provides: "Scanner consuming the shared constant; should_prune transparent to the wider union"
      contains: "use crate::paths::GENERATED_DIRS;"
    - path: "src-tauri/src/project_activity.rs"
      provides: "Scanner consuming the shared constant; dot-prefix rule retained"
      contains: "use crate::paths::GENERATED_DIRS;"
    - path: "src-tauri/src/evidence_binder.rs"
      provides: "Scanner consuming the shared union plus module-local .maru exclusion"
      contains: "use crate::paths::GENERATED_DIRS;"
  key_links:
    - from: "src-tauri/src/vault.rs"
      to: "src-tauri/src/paths.rs"
      via: "ScanFilter::is_excluded_path still takes the list as an injected parameter; only the constant's origin changed"
      pattern: "generated_dirs: &\\[&str\\]"
    - from: "src-tauri/src/evidence_binder.rs"
      to: "src-tauri/src/paths.rs"
      via: "is_excluded_dir checks union membership OR the module-local .maru name"
      pattern: "\\.maru"
---

<objective>
Rewire the remaining four scanners (vault, secrets, project_activity,
evidence_binder) to the shared 14-entry GENERATED_DIRS union created in
02-01, preserving each module's non-union rules (vault's injected-list
predicate signature, secrets' domain prefixes, project_activity's dot-prefix
rule, evidence_binder's module-local .maru exclusion). Prove the SCAN-02
delta with a red-then-green union test in vault.rs.

Purpose: Completes SCAN-01's "all five scanners honor the one-line edit"
and SCAN-02's real behavioral delta for the walkdir-based scanners.
Output: four rewired scanners, one new union-proof test, gates green.
</objective>

<execution_context>
@$HOME/.kimi-code/gsd-core/workflows/execute-plan.md
@$HOME/.kimi-code/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-shared-scanner-and-path-invariants/02-CONTEXT.md
@.planning/phases/02-shared-scanner-and-path-invariants/02-RESEARCH.md
@.planning/phases/02-shared-scanner-and-path-invariants/02-PATTERNS.md
@.planning/phases/02-shared-scanner-and-path-invariants/02-01-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>SCAN-02 union-proof test in vault.rs (red first): scans exclude __pycache__, .git, .venv contents</name>
  <files>src-tauri/src/vault.rs</files>
  <read_first>
    - src-tauri/src/vault.rs (lines 1245-1310 for the existing test module conventions; 324-379 for the scan entry the tests exercise)
    - src-tauri/src/workspace_files.rs (lines 1231-1261 — TempDir + write_file + rel_path assertion idiom to mirror)
    - .planning/phases/02-shared-scanner-and-path-invariants/02-RESEARCH.md (Code Examples: union proof test; State of the Art — real deltas)
    - .planning/phases/02-shared-scanner-and-path-invariants/02-01-SUMMARY.md (what 02-01 landed: paths.rs exports)
  </read_first>
  <behavior>
    - Fixture tree: keep.md at root, __pycache__/mod.cpython-312.pyc, .git/objects/ab/cdef, .venv/lib/python3.12/site.py
    - Expected: the vault scan returns exactly ["keep.md"] by rel path — no fixture-file contents from any union directory
    - Red proof: run the new test BEFORE Task 2's rewire — it must FAIL because vault's current private 7-entry list lacks __pycache__ (non-dot, so the dot-segment rule does not cover it); record the failure output for the SUMMARY
  </behavior>
  <action>
    Add one test to vault.rs's existing `#[cfg(test)] mod tests` (style per workspace_files.rs:1231-1261: TempDir fixture, write_file helper creating parents, assert on collected rel paths). Use the same scan entry point vault's own tests already use (scan_vault or the helper they wrap — match the existing test module's convention). Name it to state the invariant, e.g. scan_excludes_generated_dirs_union_including_pycache. The assertion compares the collected rel paths to exactly ["keep.md"].

    This test is deliberately added before the rewire: it is the phase's red-then-green proof that __pycache__ exclusion is a real behavior change and not test theater. Do not "fix" the test to pass pre-rewire; the failure IS the evidence. Record the failing output in the SUMMARY, then leave the test in place for Task 2 to turn green.

    Verification framing note (RESEARCH State of the Art): for ScanFilter-based scans, .git and .venv were already excluded by the dot-segment rule (vault.rs:245-258). The genuinely new exclusions this test proves are __pycache__ (non-dot) and unconditional union membership. Write the test comment to say that, so a future reader does not re-derive the stale roadmap framing.
  </action>
  <verify>
    <automated>cd src-tauri && cargo test --lib vault::tests:: 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - The new test exists in vault.rs's test module and asserts the collected rel paths equal exactly ["keep.md"]
    - The red run's failure output (test failed pre-rewire because __pycache__ content was returned) is captured and quoted in the SUMMARY
    - `cargo test --lib vault::tests::` output shows every pre-existing vault test still passing (only the new one fails at this point)
  </acceptance_criteria>
  <done>Union-proof test committed in its red state with the failure documented; pre-existing vault tests unaffected.</done>
</task>

<task type="auto">
  <name>Rewire vault.rs and secrets.rs to the shared constant (turns the Task 1 test green)</name>
  <files>src-tauri/src/vault.rs, src-tauri/src/secrets.rs</files>
  <read_first>
    - src-tauri/src/vault.rs (lines 15-30 the import group and private const; 238-259 is_excluded_path whose signature must not change)
    - src-tauri/src/secrets.rs (lines 1-28 imports and the 12-entry list; 710-733 should_prune and its module-specific extra rules)
    - .planning/phases/02-shared-scanner-and-path-invariants/02-PATTERNS.md (vault.rs and secrets.rs sections)
  </read_first>
  <action>
    vault.rs: delete the private 7-entry prune-list constant at lines 22-30 and add `use crate::paths::GENERATED_DIRS;` to the crate-import group (lines 15-18 area, alphabetical). CRITICAL (Pitfall 5): ScanFilter::is_excluded_path (:238-259) keeps its `generated_dirs: &[&str]` parameter exactly as-is — the predicate stays shared, the list stays injected per call site. Only the constant's origin changes. Do not touch lexical_normalize (:614-634), the dot-segment rule (:245-258), or resolve_inside_vault.

    secrets.rs: delete the private 12-entry list at lines 12-25 and add the same import (the file already imports crate::vault::lexical_normalize at :1 — same group, alphabetical). should_prune (:725-733) iterates `&[&str]` and works unchanged against the wider union — the union adds .turbo and __pycache__ for this scanner, which is the intended D-04 behavior. Leave the module-specific extra rules at :715-723 (vault / .maru/secrets / .secrets prefix checks) untouched — they are secrets-domain exclusions, not generated dirs.

    After both rewires, run the Task 1 test: it must now pass (green). Run the full vault and secrets test modules and confirm nothing else moved.
  </action>
  <verify>
    <automated>cd src-tauri && cargo test --lib -- vault:: secrets:: && cargo clippy -- -D warnings && cargo fmt --check</automated>
  </verify>
  <acceptance_criteria>
    - `grep -v '^\s*//' src-tauri/src/vault.rs | grep -c 'const GENERATED_DIRS'` prints 0; `grep -c 'use crate::paths::GENERATED_DIRS;' src-tauri/src/vault.rs` prints 1
    - `grep -v '^\s*//' src-tauri/src/secrets.rs | grep -c 'const GENERATED_DIRS'` prints 0; `grep -c 'use crate::paths::GENERATED_DIRS;' src-tauri/src/secrets.rs` prints 1
    - `grep -n 'generated_dirs: &\[&str\]' src-tauri/src/vault.rs` still shows the is_excluded_path parameter (signature preserved)
    - `sed -n '710,724p' src-tauri/src/secrets.rs` still shows the vault / .maru/secrets / .secrets prefix rules
    - `cd src-tauri && cargo test --lib -- vault:: secrets::` exits 0 — including the Task 1 union-proof test now green (record red→green pair in SUMMARY)
    - `cd src-tauri && cargo clippy -- -D warnings && cargo fmt --check` exit 0
  </acceptance_criteria>
  <done>vault.rs and secrets.rs consume the shared union; the red test from Task 1 is green; predicate signature and secrets' extra rules preserved.</done>
</task>

<task type="auto">
  <name>Rewire project_activity.rs and evidence_binder.rs — union membership with each module's non-union rules intact</name>
  <files>src-tauri/src/project_activity.rs, src-tauri/src/evidence_binder.rs</files>
  <read_first>
    - src-tauri/src/project_activity.rs (lines 22-34 the PRUNED_DIRS constant; 334-344 is_pruned_dir with its Korean doc comment and dot-prefix rule)
    - src-tauri/src/evidence_binder.rs (lines 1333-1337 is_excluded_dir; 896-909 discover_sidecar_candidates walk site; 1148-1153 scoped_bases for context on what trees it scans)
    - .planning/phases/02-shared-scanner-and-path-invariants/02-PATTERNS.md (project_activity.rs and evidence_binder.rs sections)
    - .planning/phases/02-shared-scanner-and-path-invariants/02-RESEARCH.md (Pitfall 3)
  </read_first>
  <action>
    project_activity.rs: delete the `PRUNED_DIRS: [&str; 6]` constant at lines 27-34, add `use crate::paths::GENERATED_DIRS;`, and change is_pruned_dir (:336-344) so the membership check reads from GENERATED_DIRS while the broader dot-prefix rule (`name.starts_with('.') && name.len() > 1`, :343) and the Korean doc comment above the function stay byte-for-byte intact — the dot rule is a documented, broader policy, not part of the union.

    evidence_binder.rs: rewire is_excluded_dir (:1333-1337) from the 5-name matches! to a two-part check: union membership OR the name equals .maru, written as GENERATED_DIRS.contains(&name) || name == ".maru", with a one-line comment that .maru is Maru state, not a generated dir, and deliberately stays out of the shared constant (Pitfall 3 — dropping it would let discover_sidecar_candidates descend into nested .maru/ and surface Maru state files as evidence). Add `use crate::paths::GENERATED_DIRS;` to the import block. The union widening 4 → 14 entries for this scanner is the intended D-04 scan-scope reduction.

    Then run the full lib suite: the only test-count delta versus the 02-01 baseline may be the new vault union-proof test; name any other deliberately-updated test in the SUMMARY (none are expected).
  </action>
  <verify>
    <automated>cd src-tauri && cargo test --lib -- project_activity:: evidence_binder:: inbox:: && cargo test --lib 2>&1 | tail -5 && cargo clippy -- -D warnings && cargo fmt --check</automated>
  </verify>
  <acceptance_criteria>
    - `grep -v '^\s*//' src-tauri/src/project_activity.rs | grep -c 'PRUNED_DIRS'` prints 0; `grep -c 'use crate::paths::GENERATED_DIRS;' src-tauri/src/project_activity.rs` prints 1
    - `grep -n "starts_with('.')" src-tauri/src/project_activity.rs` still shows the dot-prefix rule inside is_pruned_dir
    - `grep -A3 'fn is_excluded_dir' src-tauri/src/evidence_binder.rs` shows the union check ORed with the module-local .maru name check
    - `grep -c 'use crate::paths::GENERATED_DIRS;' src-tauri/src/evidence_binder.rs` prints 1
    - `cd src-tauri && cargo test --lib inbox::` exits 0 — the .omc allowlist tests (:2761, :3349) are green, proving the &[] call sites survived (Pitfall 5)
    - `cd src-tauri && cargo test --lib` full-suite summary matches the 02-01 baseline plus only the vault union-proof test; any deviation is named in the SUMMARY
    - `cd src-tauri && cargo clippy -- -D warnings && cargo fmt --check` exit 0
    - `git diff --stat src-tauri/src/ops_catalog/scan.rs` is empty (domain-specific BU-scan list untouched)
  </acceptance_criteria>
  <done>All four remaining scanners consume the union with their module-specific rules intact; .maru stays excluded from evidence discovery; inbox is unaffected; full lib suite green with the single expected test-count delta.</done>
</task>

</tasks>

## Artifacts this plan produces

- Rewired imports: `use crate::paths::GENERATED_DIRS;` in vault.rs, secrets.rs, project_activity.rs, evidence_binder.rs
- Deleted private lists: vault.rs's 7-entry constant, secrets.rs's 12-entry constant, project_activity.rs's `PRUNED_DIRS: [&str; 6]`, evidence_binder.rs's 5-name matches!
- New test in vault.rs's test module: the SCAN-02 union-proof test (red before rewire, green after)
- evidence_binder.rs's is_excluded_dir: union check OR module-local .maru check, with explanatory comment

## Flagged assumptions (specless probe edges — not silently dropped)

- **SCAN-02 / unclassified (stale vault cache):** RESEARCH Open Question 1 resolved as ACCEPT self-healing. A vault cache written before this phase can surface now-pruned paths (e.g. a `__pycache__/*.md` entry) on first paint until the next scan_vault rewrites it (vault.rs:417). This is a bounded transient — dot-dirs were already excluded and __pycache__ rarely holds documents — so no read-time cache filter is added. If execution reveals visible staleness, surface it rather than silently adding the filter.
- **SCAN-02 / unclassified (real deltas, not stale framing):** For ScanFilter-based scans the dot-segment rule (vault.rs:245-258) already excluded .git/.venv by default; the genuinely new exclusions proven here are __pycache__ (non-dot) and unconditional union membership (generated dirs no longer allowlist-resurrectable). Verification asserts these deltas, not the roadmap's broader "walks into git object storage" framing.
- **SCAN-02 / unclassified (wild allowlists):** RESEARCH Assumption A3 — no user has a generated-dir name in their scan.includeDotFolders setting; if one does, that allowlist silently stops resurrecting it. Accepted per D-04's "intended behavior change" framing; no settings error is surfaced (see must_haves.prohibitions).

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| untrusted workspace/vault trees → scanner prune filters | directory names in user-controlled trees decide traversal scope and what reaches indexes/evidence discovery |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-04 | Information disclosure | evidence_binder.rs::is_excluded_dir | high | mitigate | Task 3 retains the module-local .maru exclusion ORed with the union (Pitfall 3); acceptance criteria grep-prove the .maru branch survives, so nested Maru state (incl. secrets material) can never surface as evidence candidates |
| T-02-05 | Information disclosure | vault.rs::ScanFilter::is_excluded_path | medium | mitigate | Task 2 preserves the generated_dirs injected-parameter signature; inbox.rs's three empty-slice call sites and .omc allowlist tests are run as a regression check, keeping inbox drop folders outside the union |
| T-02-06 | Information disclosure | secrets.rs::should_prune | low | accept | The union widens this scanner's prune set (adds .turbo, __pycache__); the secrets-domain prefix rules (.maru/secrets, .secrets) are untouched and remain the primary guard |
</threat_model>

<verification>
Per-gate verification strategy (Phase 1 precedent, concurrent-checkout
hazard): the shared checkout carries a concurrent hwped session's dirty Rust
files, so a full `make verify` can fail on files this plan does not own.
Prove owned gates individually: scoped `cargo test --lib` per task, then the
full lib suite (count delta = exactly one new vault test), plus
`cargo clippy -- -D warnings` and `cargo fmt --check`. CI is the
authoritative composite. The SUMMARY must record the red→green pair for the
union-proof test and the full-suite count comparison against the 02-01
baseline.
</verification>

<success_criteria>
- All five original scanners plus the content_search sixth consumer (02-01) honor the single 14-entry union — SCAN-01's one-line-edit claim is now true end to end
- The SCAN-02 delta is test-proven: __pycache__ exclusion went red→green; .git/.venv remain excluded
- evidence_binder keeps .maru out of evidence discovery; inbox keeps its empty-slice exception; ops_catalog/scan.rs untouched
- Full lib suite green with only the one expected new test
</success_criteria>

<output>
Create `.planning/phases/02-shared-scanner-and-path-invariants/02-02-SUMMARY.md` when done
</output>
