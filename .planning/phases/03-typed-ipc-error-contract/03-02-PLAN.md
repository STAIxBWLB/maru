---
phase: 03-typed-ipc-error-contract
plan: "02"
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - src-tauri/src/today_store.rs
  - src-tauri/src/today_calendar.rs
  - src-tauri/src/today_lifecycle.rs
  - src-tauri/src/today_ai.rs
  - src-tauri/src/document.rs
autonomous: true
requirements: [ERR-01, ERR-04]
estimate:
  tokens: 55000
  raw_tokens: 55000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "[ERR-01] All 7 branch-on commands and their 3 shared helpers return Result<_, IpcError>; every contract emit site constructs IpcError with one of the four constants and a prefix-free message"
    - "[edge:empty/ERR-04] Non-conflict error paths inside migrated commands keep their exact legacy text — From<String> carries the whole string into message with an empty code, and Display renders it unchanged"
    - "[edge:encoding/ERR-04] Every Result<T, String> signature outside the 10-item migration set (7 commands + 3 helpers) is byte-for-byte untouched, and no legacy error string is reworded"
    - "[edge:precision/ERR-04] Migrated conflict messages retain the full original suffix text (expected/found revision and hash values) verbatim — only the machine prefix moves from the string into the code field; nothing the old string carried is dropped"
  artifacts:
    - today_store.rs / today_calendar.rs / today_lifecycle.rs / today_ai.rs / document.rs migrated to IpcError on the contract paths
    - today_ai.rs today_apply_plan_result keeps Result<_, String> via .map_err(|e| e.to_string()) (ERR-04: display-only, no frontend branch)
  key_links:
    - "check_revision / load_context / assert_expected_revision are the only constructors of contract errors for their domains — all 7 commands inherit the typed error through them"
    - "today_apply_plan_result -> today_mutate plain-fn call adapts via map_err, Display preserving byte-identical text"
  prohibitions:
    - requirement_id: ERR-01
      category: values
      status: resolved
      verification: judgment
      statement: "MUST NOT alter the user-visible text of any existing error message — every legacy string riding From<String> stays verbatim; only the four contract prefixes move into the code field"
    - requirement_id: ERR-03
      category: values
      status: resolved
      verification: judgment
      statement: "MUST NOT expand the contract beyond the four D-04 codes — display-only commands keep Result<T, String>, and the Rust-internal prefix families (unknown_source:, install_target_exists:, terminal_kill_failed:) stay untyped (D-05)"
---

<objective>
Migrate the remaining three contract codes (`today_conflict`, `task_conflict`, `document_conflict`) to the `IpcError` struct proven by the 03-01 tracer — the Rust half of ERR-01 — while holding ERR-04: only the 7 commands and 3 shared helpers change signature, and every other error path keeps its exact legacy text.

Purpose: the shared guards (`check_revision`, `load_context`, `assert_expected_revision`) are the only constructors of contract errors for their domains; converting them plus the two inline `today_mutate` guards converts all 7 commands with minimal diff, because every other `?`/`map_err` String path rides the tracer's `From<String>` impl untouched.

Output: the full Rust emit side of the contract; `today_apply_plan_result` adapted with `map_err` (stays display-only); the five existing prefix-string test assertions migrated to `err.code`/`err.to_string()` form.

**Scope deviation (carried from 03-01, ratified at its checkpoint):** 7 commands migrate, not the "four" in CONTEXT specifics — D-04 locks four codes; the branch-on set measures 7 commands. `graph_link_apply` stays `Result<_, String>` (display-only consumer; migrating it would violate ERR-04).
</objective>

<execution_context>
@$HOME/.kimi-code/gsd-core/workflows/execute-plan.md
@$HOME/.kimi-code/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-typed-ipc-error-contract/03-CONTEXT.md
@.planning/phases/03-typed-ipc-error-contract/03-RESEARCH.md
@.planning/phases/03-typed-ipc-error-contract/03-PATTERNS.md
@.planning/phases/03-typed-ipc-error-contract/03-VALIDATION.md
@.planning/phases/03-typed-ipc-error-contract/03-01-SUMMARY.md
@src-tauri/src/ipc_error.rs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Migrate the today domain (today_store, today_calendar, today_lifecycle, today_ai) to IpcError</name>
  <files>src-tauri/src/today_store.rs, src-tauri/src/today_calendar.rs, src-tauri/src/today_lifecycle.rs, src-tauri/src/today_ai.rs</files>
  <read_first>
    - .planning/phases/03-typed-ipc-error-contract/03-PATTERNS.md (sections: today_store.rs, today_lifecycle.rs / today_calendar.rs / today_ai.rs — contains every analog excerpt with line anchors)
    - src-tauri/src/ipc_error.rs (the tracer's struct, constants, From<String> semantics)
    - src-tauri/src/today_store.rs (~:506-518 check_revision, :604-665 today_mutate incl. inline guard :617-626, :666-720 today_finalize_setup, :1055-1075 second inline guard, tests ~:780-800 and ~:1030-1045)
    - src-tauri/src/today_calendar.rs (~:413-430 today_calendar_publish)
    - src-tauri/src/today_lifecycle.rs (~:75-100 load_context hash guard, :368 task_transition, :448 task_trash, tests ~:568-575 and ~:785-792)
    - src-tauri/src/today_ai.rs (~:260-300 today_apply_plan_result and its plain-fn call to today_mutate at :293)
  </read_first>
  <behavior>
    - The four migrated prefix-string assertions (today_store.rs:788, :1038; today_lifecycle.rs:574, :789) are red against the old string form and green after migration in their new shape: assert the err.code constant and assert err.to_string() equals the code literal + ": " + the original suffix text with the test's fixture values.
    - After migration, no string literal beginning with the today_conflict or task_conflict code followed by a colon is constructed anywhere in these four files' non-test code (the codes live only in the constants and in Display output).
    - today_apply_plan_result still returns Result&lt;TodaySnapshot, String&gt; and its propagated conflict text is byte-identical to today (Display does the work).
  </behavior>
  <action>
    Per D-01/D-02, using the tracer's constants and IpcError shape (03-01):
    - today_store.rs: change `check_revision` (:507-518) to return `Result<(), IpcError>`, constructing `IpcError { code: TODAY_CONFLICT.to_string(), message: format!("expected revision {expected_revision}, found {}", snapshot.revision) }` — the message keeps the original suffix verbatim with the machine prefix removed (D-02). Apply the identical construction at today_mutate's two inline revision guards (:617-626 and :1055-1075). Flip the signatures of `today_mutate` (:604) and `today_finalize_setup` (:666) to `Result<_, IpcError>`. Every other error path in these functions (validate_logical_day, assert_maru_can_write, normalize_existing_dir, work_lock_for, the state-missing/state-corrupt/undo-unavailable strings, snapshot_revision, apply_mutation, persist_snapshot, mutation_event_details) rides `From<String>` via the existing `?`/map_err UNCHANGED — do not reword a single legacy string (prohibition: byte-identical text).
    - today_calendar.rs: flip `today_calendar_publish` (:414) to `Result<_, IpcError>`; its `check_revision(...)?` call (:427) then propagates IpcError with no further edit.
    - today_lifecycle.rs: change `load_context`'s return type to `Result<TransitionContext, IpcError>` and its hash-mismatch guard (:89-94) to construct `IpcError { code: TASK_CONFLICT.to_string(), message: format!("expected hash {expected_task_hash}, found {actual_hash}") }` (prefix removed, suffix verbatim). Flip `task_transition` (:368) and `task_trash` (:448) to `Result<_, IpcError>`.
    - today_ai.rs: at the plain-fn call to `today_mutate` (:293) append `.map_err(|e| e.to_string())` so `today_apply_plan_result` keeps `Result<TodaySnapshot, String>` (ERR-04 — it has no active frontend branch; Display makes the propagated text byte-identical). Do NOT migrate its signature (RESEARCH Pitfall 4).
    - Migrate the four existing test assertions (today_store.rs:788, :1038; today_lifecycle.rs:574, :789) to the new shape in behavior above. If cargo test surfaces any further prefix-string assertion in these modules, migrate it the same way — never by weakening the assertion.
    - Import the constants/struct from `crate::ipc_error` in each file.
    Run the migrated tests first to watch the old assertions go red, then land the migration (tdd="true" red→green by construction).
  </action>
  <verify>
    <automated>cd src-tauri && cargo test --lib today && cargo clippy --lib -- -D warnings</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n 'Result<.*, IpcError>' src-tauri/src/today_store.rs src-tauri/src/today_calendar.rs src-tauri/src/today_lifecycle.rs` shows the signatures of check_revision, today_mutate, today_finalize_setup, today_calendar_publish, load_context, task_transition, and task_trash
    - `grep -rn '"today_conflict: ' src-tauri/src/today_store.rs src-tauri/src/today_calendar.rs src-tauri/src/today_lifecycle.rs src-tauri/src/today_ai.rs | grep -v '#\[cfg(test)\]' | grep -v 'to_string()' | wc -l` returns 0 outside test modules
    - src-tauri/src/today_ai.rs contains `.map_err(|e| e.to_string())` at the today_mutate call site and today_apply_plan_result still returns Result<_, String>
    - `cd src-tauri && cargo test --lib today` exits 0 and `cargo clippy --lib -- -D warnings` exits 0
  </acceptance_criteria>
  <done>All five today-domain commands and both shared helpers return typed errors; the four migrated assertions pin code + display text; every legacy string in the domain is byte-identical; clippy stays clean with zero new suppressions.</done>
</task>

<task type="auto" tdd="true">
  <name>Migrate save_document (document.rs) to IpcError and record the ERR-04 signature count</name>
  <files>src-tauri/src/document.rs</files>
  <read_first>
    - .planning/phases/03-typed-ipc-error-contract/03-PATTERNS.md (section: document.rs — assert_expected_revision excerpt and the non-conflict read-error guidance)
    - src-tauri/src/ipc_error.rs (tracer struct + DOCUMENT_CONFLICT constant)
    - src-tauri/src/document.rs (:118-193 assert_expected_revision + save_document incl. all three conflict emit sites :126-128, :150-152, :185-188; tests ~:730-743 and ~:1220-1235)
  </read_first>
  <behavior>
    - The existing assertions at document.rs:738 and :1227 are red against the old string form and green after migration in the new shape: err.code equals DOCUMENT_CONFLICT and err.to_string() carries the code literal + ": " + the original suffix (including the "file is missing" variant).
    - A save with a read failure (non-conflict path) still surfaces text beginning "Cannot read document: " — the string rides From<String> verbatim with an empty code.
  </behavior>
  <action>
    Per D-01/D-02: change `assert_expected_revision` (:122-132) to return `Result<(), IpcError>`, constructing `IpcError { code: DOCUMENT_CONFLICT.to_string(), message: format!("expected revision {expected}, found {actual}") }`. Flip `save_document` (:134-193) to `Result<DocumentPayload, IpcError>`. The two inline missing-file conflict returns (:150-152 and :185-188) construct IpcError with DOCUMENT_CONFLICT and message `format!("expected revision {expected}, file is missing")` (prefix removed, suffix verbatim). The "Cannot read document: {err}" map_err paths and every other String error path (resolve_inside_vault, assert_document_owner, assert_maru_can_write, validate_managed_write, snapshot writing, write_atomic, read_document tail-call) ride `From<String>` UNCHANGED — do not reword them (prohibition: byte-identical text; PATTERNS explicitly sanctions non-contract paths keeping their strings via the conversion, and no non-contract code is added to the TS union).
    Migrate the test assertions at :738 and :1227 to the new shape in behavior above. Import from `crate::ipc_error`.
    Then run the pinned ERR-04 count command — `grep -roE "Result<.*, String>" src-tauri/src --include="*.rs" | wc -l` — and record the number in the task commit message / SUMMARY for plan 03-04's guard (planning-time baseline: 1,138, re-confirmed 2026-08-23 on the committed tree at 34f96ee, the merge that adopted the hwp-editor bridge - 19 of those matches live in hwped.rs, so the baseline is only valid for a tree that contains it; expected after both this plan's tasks: roughly 1,128 — the 7 commands + 3 helpers flip — but the recorded measurement is what 03-04 asserts against).
  </action>
  <verify>
    <automated>cd src-tauri && cargo test --lib document && cargo clippy --lib -- -D warnings && cd .. && grep -roE "Result<.*, String>" src-tauri/src --include="*.rs" | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - src-tauri/src/document.rs declares `save_document` returning `Result<DocumentPayload, IpcError>` and `assert_expected_revision` returning `Result<(), IpcError>`
    - `grep -n '"document_conflict: ' src-tauri/src/document.rs | grep -v 'to_string()' | wc -l` returns 0 outside test modules
    - The "Cannot read document: " map_err strings are unchanged (still plain format! strings riding the From conversion)
    - `cd src-tauri && cargo test --lib document` exits 0; the Result&lt;*, String&gt; count is printed and recorded in the SUMMARY
  </acceptance_criteria>
  <done>save_document and its guard return typed errors with all three conflict sites migrated; non-conflict read errors keep byte-identical text; the post-migration signature count is measured and recorded for the 03-04 ERR-04 guard.</done>
</task>

</tasks>

## Artifacts this phase produces

(Same phase-level list as 03-01; this plan lands the Rust-signature subset.)

- Rust, `src-tauri/src/ipc_error.rs` (from 03-01): `IpcError { code, message }`, `TODAY_CONFLICT` / `TASK_CONFLICT` / `DOCUMENT_CONFLICT` / `EVIDENCE_BINDER_REVISION_CONFLICT`, Display/Error/From<String> impls, `ipc_error::tests`
- This plan: `check_revision`, `load_context`, `assert_expected_revision` → `Result<_, IpcError>`; commands `today_mutate`, `today_finalize_setup`, `today_calendar_publish`, `task_transition`, `task_trash`, `save_document` → `Result<_, IpcError>`; `.map_err(|e| e.to_string())` adapter at `today_ai.rs` today_mutate call site
- No new files, no new dependencies, no new error codes beyond the four constants

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Rust guards → Tauri bridge | The four conflict codes now cross as structured data; legacy strings cross unchanged inside IpcError's message field |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-03 | Information Disclosure | From<String> conversion + Display (src-tauri/src/ipc_error.rs consumers) | low | mitigate | Legacy messages ride the conversion verbatim (no rewording, no added internal detail); Display reproduces exactly today's user-visible text — asserted by the migrated err.to_string() tests |
| T-03-04 | Tampering | today_ai.rs map_err adapter | low | mitigate | The adapter only re-stringifies via Display; it cannot synthesize or alter a code — today_apply_plan_result stays display-only per ERR-04 |
| T-03-SC | Tampering | cargo installs | low | accept | Zero packages installed; legitimacy audit vacuous |
</threat_model>

<verification>
- `cargo test --lib today` and `cargo test --lib document` green (migrated assertions pin code + display text)
- `cargo clippy --lib -- -D warnings` green, zero new suppression attributes
- ERR-04 count command output recorded in the SUMMARY
</verification>

<success_criteria>
All 7 branch-on commands emit typed `IpcError` rejections for their conflict paths; every other error in the touched files is textually identical to before; the measured `Result<T, String>` count reflects only the 10-signature migration delta.
</success_criteria>

<output>
Create `.planning/phases/03-typed-ipc-error-contract/03-02-SUMMARY.md` when done. Record: the measured Result&lt;*, String&gt; count (03-04 asserts against it), any prefix-string assertions beyond the five enumerated that needed migration, and any deviation from this plan.
</output>
