---
phase: 03-typed-ipc-error-contract
plan: "03"
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - src/lib/today.ts
  - src/lib/today.test.ts
  - src/components/today/TodayReview.tsx
  - src/components/today/TodayExecute.test.tsx
  - src/lib/api.ts
  - src/lib/diagram/reportInsert.ts
  - src/lib/diagram/reportInsert.test.ts
  - e2e/helpers/todayFixtures.ts
  - e2e/smoke.spec.ts
autonomous: true
requirements: [ERR-01, ERR-03]
estimate:
  tokens: 50000
  raw_tokens: 50000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "[ERR-03] All five frontend branch sites read err.code via instanceof IpcError; zero substring matchers on any of the four contract codes remain in src/"
    - "[edge:adjacency/ERR-03] Branch predicates use strict equality on err.code, so a code that is a prefix or superstring of another can never false-match — the old substring-matcher hazard is gone"
    - "[edge:empty/ERR-03] isTodayConflict, isTaskConflict, and reportInsert's isConflict return false (never throw) on null, undefined, empty-string, and plain-Error rejections"
    - "[edge:encoding/ERR-03] The smoke.spec conflict alert assertion passes unmodified — conflict alert text stays byte-identical because normalized messages keep the 'code: message' form"
    - "[edge:ordering/ERR-03] The contract contains exactly four codes — IPC_ERROR_CODES in types.ts and the Rust pin test enumerate the same four literals, and no non-contract code (e.g. evidence_binder_lock_poisoned) enters the TS union"
    - "[D-08] todayErrorCode and its test describe are deleted; no caller remains anywhere in src/ or e2e/"
  artifacts:
    - todayInvoke / saveDocument funnels wrapped in normalizeIpcError (today.ts, api.ts)
    - isTodayConflict / isTaskConflict / isConflict rewritten to typed code checks
    - e2e fixtures and component tests throw the raw wire shape (prefix-free message + separate `code` field, per D-02 — what migrated Rust actually sends) that the funnels normalize into IpcErrors
  key_links:
    - "todayInvoke wraps BOTH the e2e-override path and the real invoke path — fixtures thrown by the override are normalized identically to real rejections"
    - "normalizeIpcError (03-01) is the single conversion point; branch sites never see a raw {code,message} object"
  prohibitions:
    - requirement_id: ERR-01
      category: values
      status: resolved
      verification: judgment
      statement: "MUST NOT alter the user-visible text of any existing error message — fixture and mock updates preserve the exact NORMALIZED display text (fixtures throw the raw wire shape with prefix-free messages per D-02, and the funnel normalizer re-attaches the code prefix, so toasts/alerts render byte-identical text); only the machine-parse target changes"
    - requirement_id: ERR-03
      category: values
      status: resolved
      verification: judgment
      statement: "MUST NOT expand the contract beyond the four D-04 codes — display-only commands keep Result<T, String>, and the Rust-internal prefix families stay untyped (D-05)"
---

<objective>
Migrate the frontend half of the contract (ERR-03): wire `normalizeIpcError` into the two remaining invoke funnels, convert every branch site to typed `err.code` checks, retire the `code: message` prefix parser per D-08, and align the e2e mock layer and component tests with the normalized rejection shape.

Purpose: after this plan, no frontend recovery path parses message text — a renamed code breaks typecheck instead of silently never matching (ERR-02's TS half becomes live at all five sites).

Output: normalized funnels in `today.ts` and `api.ts`; five migrated branch sites; `todayErrorCode` deleted with its tests; fixtures/mocks throwing the normalized shape.

This plan is wave-parallel with 03-02 (Rust emit migration): zero file overlap, and each side's tests are independently green (TS tests mock the boundary; Rust tests never see TS). The contract is whole once both land — 03-04 proves it.
</objective>

<execution_context>
@$HOME/.kimi-code/gsd-core/workflows/execute-plan.md
@$HOME/.kimi-code/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-typed-ipc-error-contract/03-CONTEXT.md
@.planning/phases/03-typed-ipc-error-contract/03-RESEARCH.md
@.planning/phases/03-typed-ipc-error-contract/03-PATTERNS.md
@.planning/phases/03-typed-ipc-error-contract/03-VALIDATION.md
@.planning/phases/03-typed-ipc-error-contract/03-01-SUMMARY.md
@src/lib/ipcError.ts
@src/lib/types.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>today funnel normalization, typed conflict helpers, and todayErrorCode retirement (D-08)</name>
  <files>src/lib/today.ts, src/lib/today.test.ts, src/components/today/TodayReview.tsx, src/components/today/TodayExecute.test.tsx</files>
  <read_first>
    - .planning/phases/03-typed-ipc-error-contract/03-PATTERNS.md (sections: funnel wiring, retired-helper pattern, TS tests)
    - src/lib/ipcError.ts and src/lib/types.ts (tracer's IpcError class, IpcErrorCode union, normalizeIpcError)
    - src/lib/today.ts (:1-20 todayInvoke, :690-720 todayErrorCode + isTodayConflict + isTaskConflict)
    - src/lib/today.test.ts (:190-230 — the describes being replaced)
    - src/components/today/TodayReview.tsx (:10-20 imports, :166-189 saveReflection catch)
    - src/components/today/TodayExecute.test.tsx (:245-260 — the task_conflict mock rejection)
  </read_first>
  <behavior>
    - isTodayConflict returns true only for an IpcError whose code is the today_conflict literal; false for task_conflict-coded errors, plain strings (including legacy prefix strings), plain Errors, null, and undefined.
    - isTaskConflict mirrors that for the task_conflict literal.
    - TodayReview's saveReflection catch branches on the document_conflict code via instanceof IpcError, with no import of the retired parser.
    - TodayExecute.test's conflict case mocks taskTransition rejecting with a real IpcError instance and still asserts the conflict notice + reload.
  </behavior>
  <action>
    Per D-07/D-08, using the tracer's normalizeIpcError/IpcError:
    - src/lib/today.ts: wrap the ENTIRE body of `todayInvoke` (:12-16) in try/catch that rethrows `normalizeIpcError(err)` — wrapping the whole body (not just the invoke call) means e2e-override fixture throws and real invoke rejections are normalized identically (PATTERNS: the override resolves BEFORE invoke). Unmigrated today commands still reject with raw strings, which the normalizer passes through unchanged — safe.
    - Rewrite `isTodayConflict` (:709-711) to `err instanceof IpcError && err.code === "today_conflict"` and `isTaskConflict` (:715-717) to the task_conflict equivalent (RESEARCH Code Examples). Import IpcError from ./ipcError.
    - Delete `todayErrorCode` (:697-707) per D-08 — planning-time grep confirmed its only callers are the two helpers above and TodayReview.tsx:176, both migrated in this task (RESEARCH Open Question 2: delete it and its tests; the prefix convention it served is retired per D-02).
    <!-- planner-discipline-allow: todayErrorCode -->
    - src/components/today/TodayReview.tsx: change the catch branch at :176 to `if (err instanceof IpcError && err.code === "document_conflict")` and remove the retired parser from the import at :15 (readTaskEvents stays).
    - src/lib/today.test.ts: delete the describe block for the retired parser (:194-212, including the legacy-string cases — they tested the deleted function, not production behavior). Rewrite the conflict-helper describes (:214-228) to construct `new IpcError({ code: "today_conflict", message: "expected revision a, found b" })` / the task_conflict equivalent for the true cases, and for the false cases assert legacy plain strings, plain Errors, null, undefined, and wrong-code IpcErrors all return false (the edge:empty truth).
    - src/components/today/TodayExecute.test.tsx: change the mock rejection at :254 from the raw prefix string to `new IpcError({ code: "task_conflict", message: "expected hash a, found b" })` (import from the lib); the mock replaces the whole taskTransition function, so the rejection value reaches isTaskConflict directly with no funnel in between — it must already be the normalized shape.
    Write/rewrite the tests first and watch them fail against the un-migrated helpers where applicable (tdd="true").
  </action>
  <verify>
    <automated>pnpm vitest run src/lib/today.test.ts src/components/today/TodayExecute.test.tsx src/components/today/TodayReview.test.tsx && pnpm typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn "todayErrorCode" src/ e2e/ | wc -l` returns 0
    - src/lib/today.ts exports isTodayConflict and isTaskConflict implemented with `err instanceof IpcError && err.code ===` and contains `normalizeIpcError` in todayInvoke's catch path
    - src/components/today/TodayReview.tsx branches via `err.code ===` (grep -c 'todayErrorCode' src/components/today/TodayReview.tsx returns 0)
    - The three vitest files exit 0 and `pnpm typecheck` exits 0
  </acceptance_criteria>
  <done>The today funnel normalizes every rejection; both conflict helpers and the TodayReview branch read typed codes; the retired prefix parser and its tests are gone with zero remaining references; typecheck and the touched test files are green.</done>
</task>

<task type="auto" tdd="true">
  <name>save_document funnel, reportInsert branch, and e2e mock-layer alignment</name>
  <files>src/lib/api.ts, src/lib/diagram/reportInsert.ts, src/lib/diagram/reportInsert.test.ts, e2e/helpers/todayFixtures.ts, e2e/smoke.spec.ts</files>
  <read_first>
    - .planning/phases/03-typed-ipc-error-contract/03-PATTERNS.md (sections: funnel wiring, branch sites, e2e/helpers/todayFixtures.ts; Pitfall 1 and Pitfall 2 in 03-RESEARCH.md)
    - src/lib/api.ts (:1076-1101 saveDocument, incl. the !isTauri() override branch at :1082-1094)
    - src/lib/diagram/reportInsert.ts (:70-115, the isConflict helper at :92-94 and its caller)
    - src/lib/diagram/reportInsert.test.ts (:180-200 — the conflict mock + message assertion)
    - e2e/helpers/todayFixtures.ts (:730-800 — the today_mutate / today_finalize_setup conflict throws)
    - e2e/smoke.spec.ts (:1261-1286 — the save_document conflict mock and alert assertion)
    - src/lib/e2eInvoke.ts (the override seam the fixtures throw through)
  </read_first>
  <behavior>
    - reportInsert's conflict detection takes the rejection value (err: unknown) and returns true only for an IpcError with the document_conflict code; its caller passes the raw caught error, not a message string.
    - reportInsert.test's conflict case rejects with an IpcError instance and still asserts the outcome message contains the document_conflict text (byte-identical message form preserved).
    - The todayFixtures conflict throws use the RAW WIRE SHAPE — a prefix-free message plus a separate string .code property, exactly what migrated Rust sends over the bridge per D-02 — so the funnel's whole-body normalizer converts them into real IpcErrors exactly once (no doubled prefix); the conflict-recovery e2e paths keep exercising the branch.
    - smoke.spec's save_document mock still yields alert text containing the document_conflict literal (Pitfall 1 canary).
  </behavior>
  <action>
    Per D-07 and PATTERNS:
    - src/lib/api.ts: wrap the ENTIRE body of `saveDocument` (:1076-1101) in try/catch that rethrows `normalizeIpcError(err)` — the whole body, so the `!isTauri()` override branch (which is what Playwright exercises) and the real invoke branch normalize identically. Do not touch readDocument (read_document is not a migrated command).
    - src/lib/diagram/reportInsert.ts: change `isConflict` (:92-94) to accept `err: unknown` and return `err instanceof IpcError && err.code === "document_conflict"`; update its single caller inside insertDiagramIntoReport to pass the caught error object instead of a message string. Import IpcError from the lib.
    - src/lib/diagram/reportInsert.test.ts: change the conflict mock at :189 to reject with `new IpcError({ code: "document_conflict", message: "expected rev-1, found rev-2" })`; keep the existing assertion that the outcome message contains the document_conflict text (the normalized message form preserves it — byte-identical text prohibition).
    - e2e/helpers/todayFixtures.ts: change the conflict throws (~:751-755 and ~:790-794) to `throw Object.assign(new Error(`expected revision ${expected}, found ${snapRevision}`), { code: "today_conflict" })` — the RAW WIRE SHAPE (checker revision, supersedes the prefix-retaining fixture shape PATTERNS.md prescribed): prefix-free message plus a separate `code` field, exactly what migrated Rust sends per D-02, so todayInvoke's whole-body normalization attaches the prefix exactly once on the way to the branch sites (Pitfall 2). The fixtures throw the post-wire pre-normalization shape because the plans wrap the whole funnel body — overrides do NOT bypass normalization. The normalized display text ("today_conflict: expected revision …, found …") stays byte-identical to today's. Match the exact fixture variable names already in each throw.
    - e2e/smoke.spec.ts: change the save_document mock throw at :1266-1268 to `throw Object.assign(new Error("revision changed"), { code: "document_conflict" })` — raw wire shape (prefix-free message + separate code, per D-02); the normalizer produces "document_conflict: revision changed", so the :1281 alert assertion needs no change.
    Then run the e2e canary: `make test-e2e` (or the repo's e2e script targeting smoke.spec) and confirm the "keeps a Files draft intact when revision-checked save conflicts" test is green.
  </action>
  <verify>
    <automated>pnpm vitest run src/lib/diagram/reportInsert.test.ts && pnpm typecheck && pnpm playwright test e2e/smoke.spec.ts --grep "revision-checked save conflicts"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rnE '\.includes\("(today_conflict|task_conflict|document_conflict|evidence_binder_revision_conflict)"\)' src/ | wc -l` returns 0
    - src/lib/api.ts saveDocument rethrows `normalizeIpcError(...)` from a catch wrapping both its override and invoke branches
    - src/lib/diagram/reportInsert.ts isConflict accepts `err: unknown` and checks `err.code ===`
    - e2e/helpers/todayFixtures.ts conflict throws use Object.assign with a `code:` property and a prefix-free message (raw wire shape per D-02 — no code prefix inside the message string); e2e/smoke.spec.ts:1266-1268 mock carries `code: "document_conflict"` with the prefix-free message "revision changed"
    - reportInsert.test.ts and the smoke.spec conflict canary are green; `pnpm typecheck` exits 0
  </acceptance_criteria>
  <done>The save path normalizes rejections on both its branches; every substring matcher on a contract code is gone from src/; the e2e mock layer throws the raw wire shape (prefix-free message + code field, per D-02) which the funnels normalize exactly once, and the conflict canary passes with byte-identical alert text.</done>
</task>

</tasks>

## Artifacts this phase produces

(Same phase-level list as 03-01; this plan lands the frontend-migration subset.)

- From 03-01: `src/lib/types.ts` (`IPC_ERROR_CODES`, `IpcErrorCode`, `IpcErrorBody`), `src/lib/ipcError.ts` (`IpcError`, `isIpcErrorCode`, `normalizeIpcError`), `src/lib/ipcError.test.ts`
- This plan: normalized `todayInvoke` and `saveDocument` funnels; typed `isTodayConflict` / `isTaskConflict` / `isConflict`; raw-wire-shaped e2e fixture throws (prefix-free message + `code` field, per D-02)
- Deleted by this plan: `todayErrorCode` (src/lib/today.ts) and its describe block in src/lib/today.test.ts (D-08)
- No new dependencies, no CLI flags

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| invoke/e2e-override → funnel normalizer → branch sites | Rejection values (real bridge errors and mock fixture throws) are normalized at exactly one point per funnel before any branch reads them |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-01 | Tampering / Spoofing | normalizeIpcError consumers (todayInvoke, saveDocument) | medium | mitigate | Tracer's union-membership guard applies at both new funnel wrap points; a forged fixture or rejection with an unknown code degrades to plain Error and never satisfies an err.code branch (fail-safe) |
| T-03-05 | Information Disclosure | e2e fixture message strings | low | accept | Fixture raw messages drop the retired code prefix (D-02) and carry a separate .code property; the normalized user-visible text is byte-identical to today's and no new internal detail is exposed |
| T-03-SC | Tampering | npm installs | low | accept | Zero packages installed; legitimacy audit vacuous |
</threat_model>

<verification>
- `pnpm vitest run src/lib/today.test.ts src/components/today/TodayExecute.test.tsx src/components/today/TodayReview.test.tsx src/lib/diagram/reportInsert.test.ts` green
- `pnpm typecheck` green
- The contract-code substring-matcher grep over src/ returns zero
- smoke.spec conflict canary green via Playwright
</verification>

<success_criteria>
All five branch sites branch on typed codes; `todayErrorCode` is deleted with zero remaining references; the e2e conflict paths still exercise the recovery branches through the normalized shape; every user-visible error string is unchanged.
</success_criteria>

<output>
Create `.planning/phases/03-typed-ipc-error-contract/03-03-SUMMARY.md` when done. Record: the residual-matcher grep output (must be empty), the e2e canary result, and any deviation from this plan.
</output>
