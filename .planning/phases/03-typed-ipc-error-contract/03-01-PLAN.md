---
phase: 03-typed-ipc-error-contract
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src-tauri/src/ipc_error.rs
  - src-tauri/src/lib.rs
  - src-tauri/src/evidence_binder.rs
  - src/lib/types.ts
  - src/lib/ipcError.ts
  - src/lib/ipcError.test.ts
  - src/lib/evidenceBinder.ts
  - src/components/evidence/EvidenceBinderPane.tsx
autonomous: false
requirements: [ERR-01, ERR-02]
estimate:
  tokens: 70000
  raw_tokens: 70000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "[ERR-01] An evidence-binder revision conflict reaches the frontend as an IpcError whose .code equals the contract literal and whose .message is human-readable; EvidenceBinderPane reloads without parsing message text"
    - "[edge:empty/ERR-01] normalizeIpcError passes null, non-object, missing-field rejections, and already-normalized IpcError instances through unchanged (idempotent by identity — re-normalizing never doubles the 'code: ' prefix); only objects whose code and message are both strings are normalized"
    - "[edge:encoding/ERR-01] Normalized error text for contract codes is exactly the four characters 'code', colon, space, message — byte-identical to today's prefix strings, so toasts and the smoke.spec conflict assertion survive unmodified"
    - "[ERR-02] The four contract codes exist as Rust constants pinned by a unit test AND as a TypeScript const-array/union mirror in src/lib/types.ts; cargo test gates the Rust side, tsc gates the TS side"
    - "[D-09] A serde_json round-trip test asserts the exact wire JSON shape ({code, message}) the TypeScript caller receives"
  artifacts:
    - src-tauri/src/ipc_error.rs (IpcError struct, four code constants, Display/Error/From<String> impls, tests)
    - src/lib/ipcError.ts (IpcError class, isIpcErrorCode, normalizeIpcError)
    - src/lib/ipcError.test.ts
    - IPC_ERROR_CODES + IpcErrorCode + IpcErrorBody added to src/lib/types.ts
  key_links:
    - "evidence_binder_mutate (Rust) -> Tauri bridge -> mutateEvidenceBinder funnel -> normalizeIpcError -> EvidenceBinderPane branch on err.code"
  prohibitions:
    - requirement_id: ERR-01
      category: values
      status: resolved
      verification: judgment
      statement: "MUST NOT alter the user-visible text of any existing error message — every toast/alert renders the same characters as today (sole sanctioned exception: the binder conflict error, today a bare machine code, gains a human-readable message suffix)"
    - requirement_id: ERR-02
      category: safety
      status: resolved
      verification: judgment
      statement: "MUST NOT weaken the Phase 1 gate set to land this phase — zero new suppression attributes or lint-disable directives, zero gate removals; a red gate is fixed, never bypassed"
---

<objective>
Prove the typed IPC error contract end-to-end on ONE code — `evidence_binder_revision_conflict` — touching every layer the phase modifies: a new Rust `IpcError` struct with pinned code constants, one migrated command, the TypeScript union mirror, the facade normalizer, one funnel, one branch site. Every later plan expands this proven skeleton to the remaining three codes.

Purpose: ERR-01/ERR-02's mechanism (typed code readable at the branch site; rename fails the build on both sides) is cheapest to validate on the smallest emitter — one command, one emit site, one branch site — before repeating the pattern six more times. This tracer is production-quality and permanent, not a prototype.

Output: `src-tauri/src/ipc_error.rs`, `src/lib/ipcError.ts` + tests, the `IpcErrorCode` mirror in `src/lib/types.ts`, and the full evidence-binder conflict path migrated per D-01/D-02/D-07.

**Scope deviation (visible, per orchestrator note):** CONTEXT specifics says "the four migrated commands", but the measured branch-on set under D-04's criterion is **7 commands**: `today_mutate`, `today_finalize_setup`, `today_calendar_publish`, `task_transition`, `task_trash`, `save_document`, `evidence_binder_mutate`. D-04 locks four *codes*, not four commands. This plan set migrates the 7-command set; `graph_link_apply` stays OUT (its only consumer is display-only). Surfaced for confirmation at this plan's checkpoint.

**Assumption-delta advisory (noted, not re-litigated):** the "chosen struct" phrasing in ROADMAP notes is already resolved by D-01/D-03 — a single `IpcError { code, message }` struct with Pattern-2 facade normalization. No identity-model transition is in scope.

**Canon breadcrumb (prohibition probe):** error-message information disclosure is canon — covered by this plan's threat model (T-03-02); not minted as a prohibition.
</objective>

<execution_context>
@$HOME/.kimi-code/gsd-core/workflows/execute-plan.md
@$HOME/.kimi-code/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/03-typed-ipc-error-contract/03-CONTEXT.md
@.planning/phases/03-typed-ipc-error-contract/03-RESEARCH.md
@.planning/phases/03-typed-ipc-error-contract/03-PATTERNS.md
@.planning/phases/03-typed-ipc-error-contract/03-VALIDATION.md
@src/lib/types.ts
@src/lib/evidenceBinder.ts
@src/components/evidence/EvidenceBinderPane.tsx
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Tracer: typed error contract end-to-end on evidence_binder_revision_conflict</name>
  <reversibility rating="costly">D-01: once the 7 callers branch on the {code,message} object shape, reverting to Result&lt;T, String&gt; means touching every migrated call site again — flag only, no gate (decision already locked in CONTEXT.md).</reversibility>
  <files>src-tauri/src/ipc_error.rs (new), src-tauri/src/lib.rs, src-tauri/src/evidence_binder.rs, src/lib/types.ts, src/lib/ipcError.ts (new), src/lib/ipcError.test.ts (new), src/lib/evidenceBinder.ts, src/components/evidence/EvidenceBinderPane.tsx</files>
  <read_first>
    - .planning/phases/03-typed-ipc-error-contract/03-PATTERNS.md (sections: ipc_error.rs, evidence_binder.rs, ipcError.ts, branch sites — contains the analog code excerpts to copy)
    - .planning/phases/03-typed-ipc-error-contract/03-RESEARCH.md (Patterns 1-3; Pitfall 5)
    - src-tauri/src/evidence_binder.rs (emit site ~:248-278, existing test ~:1711-1723)
    - src-tauri/src/lib.rs (module registry, ~:60-80 — strictly-alphabetical convention, Phase 2 precedent)
    - src-tauri/src/hub_client/http.rs:18-35 (Display impl idiom)
    - src/lib/types.ts:1-60 (union idiom at :52-60 to copy for IPC_ERROR_CODES)
    - src/lib/today.ts:696-717 (the unknown-narrowing style normalizeIpcError follows)
    - src/lib/evidenceBinder.ts:127-146 (funnel + browser mock)
    - src/components/evidence/EvidenceBinderPane.tsx:150-185 (catch block)
  </read_first>
  <behavior>
    - Rust `ipc_error_codes_are_stable` pins all four constants to their literals; renaming one fails this test.
    - Rust `ipc_error_wire_shape_round_trips` (D-09): serializing an IpcError yields exactly the JSON object with keys "code" and "message" (both strings), and serde_json::from_value reads those keys back.
    - Rust Display: a contract-coded IpcError renders as the code, then ": ", then the message; an empty-code IpcError (legacy From&lt;String&gt; path) renders its message alone — both byte-identical to today's user-visible strings.
    - Migrated binder conflict test: err.code equals the EVIDENCE_BINDER_REVISION_CONFLICT constant and err.to_string() starts with the code followed by ": ".
    - TS: normalizeIpcError on {code: "evidence_binder_revision_conflict", message: "m"} returns an IpcError that is instanceof Error, has .code equal to the literal, and .message equal to the literal + ": " + "m".
    - TS: a body whose code is not one of the four (including the empty string) normalizes to a plain Error with byte-identical display text and is NOT an IpcError.
    - TS: null, raw strings, plain Errors, and objects lacking string code/message pass through normalizeIpcError by identity.
    - TS: normalizeIpcError is idempotent — an already-normalized IpcError instance is returned by identity (same object, unchanged), so re-normalizing never produces a doubled "code: code: message" prefix (checker revision: the browser-mock path in this plan and the e2e-fixture paths in 03-03 route pre-thrown errors through the same normalizer).
  </behavior>
  <action>
    ERR-04 BASELINE — do this FIRST, before editing any file. Run the pinned count command `grep -roE "Result<.*, String>" src-tauri/src --include="*.rs" | wc -l` and record the result in 03-01-SUMMARY.md as the phase's pre-migration baseline B. This plan flips `evidence_binder_mutate`, so the count stops being pristine the moment the tracer lands, and 03-04 runs far too late to establish one. The planning-time figure is 1,138 (re-confirmed on the committed tree at 34f96ee), but the measured B is what 03-02 and 03-04 assert against — do not substitute the planning-time constant if they differ.
    RUST — create src-tauri/src/ipc_error.rs per RESEARCH Pattern 1 and PATTERNS, with these exact decisions (Claude's-discretion items, decided here):
    - `#[derive(Debug, Clone, serde::Serialize)] pub struct IpcError { pub code: String, pub message: String }` — both fields are single lowercase words, so no serde rename attribute (per PATTERNS). `code` is an owned String, NOT &'static str: `impl From<String> for IpcError` must exist so that every legacy `?`/map_err String path inside migrated commands converts without edits, and From puts the WHOLE original string into `message` with `code` set to the empty string (this keeps display-only text byte-identical — the behavior-preserving rule Phase 2 applied to ensure_within).
    - `impl std::fmt::Display for IpcError`: when `code` is empty, write only `message`; otherwise write code, ": ", message (hub_client/http.rs:25-35 idiom).
    - `impl std::error::Error for IpcError {}`.
    - Four constants: `pub const TODAY_CONFLICT: &str = "today_conflict";`, `pub const TASK_CONFLICT: &str = "task_conflict";`, `pub const DOCUMENT_CONFLICT: &str = "document_conflict";`, `pub const EVIDENCE_BINDER_REVISION_CONFLICT: &str = "evidence_binder_revision_conflict";` (D-03, D-04).
    - `#[cfg(test)] mod tests` with `ipc_error_codes_are_stable` (assert_eq each constant to its literal — the Rust rename gate, D-03) and `ipc_error_wire_shape_round_trips` (D-09: `serde_json::to_value` of an IpcError equals `serde_json::json!({"code": ..., "message": ...})` exactly, then `serde_json::from_value` reads both keys back — the tasks.rs:1225-1234 idiom). Add a Display test covering both the contract-coded and empty-code forms.
    - Register `mod ipc_error;` in src-tauri/src/lib.rs inside the existing module registry, keeping the strictly-alphabetical ordering convention (Phase 2 decision: true alphabetical, rustfmt-verified).
    RUST — migrate the evidence emit site (per D-01/D-02): in src-tauri/src/evidence_binder.rs, change `evidence_binder_mutate`'s return type to `Result<EvidenceBinderResponse, IpcError>`; at the revision-mismatch site (~:262, today a bare-code string with no message) return an IpcError with `code: EVIDENCE_BINDER_REVISION_CONFLICT.to_string()` and `message: format!("expected revision {}, found {}", req.expected_revision, actual_revision)` — the prefix moves to the code field (D-02) and the message becomes a real human string in the same expected/found shape the other three codes use. The lock-poison map_err string and every other String error path in the function ride `From<String>` UNCHANGED — do not reword them (prohibition: byte-identical legacy text). Migrate the existing test at ~:1722 from string equality to asserting `err.code` equals the constant and `err.to_string()` equals the constant's literal followed by ": expected revision …, found …" with the test fixture's actual values.
    TS — mirror + normalizer (D-03/D-07): in src/lib/types.ts, immediately after the existing union block (~:52-60, copy its formatting), add `export const IPC_ERROR_CODES = ["today_conflict", "task_conflict", "document_conflict", "evidence_binder_revision_conflict"] as const;`, `export type IpcErrorCode = (typeof IPC_ERROR_CODES)[number];`, and `export interface IpcErrorBody { code: string; message: string; }`.
    Create src/lib/ipcError.ts exporting: `isIpcErrorCode(code: string): code is IpcErrorCode` (membership test against IPC_ERROR_CODES); `class IpcError extends Error` with `readonly code: IpcErrorCode`, constructor taking `{ code: IpcErrorCode; message: string }`, calling `super(`${body.code}: ${body.message}`)` and setting `this.name = "IpcError"` (Pitfall 5: name managed so String(err) stays sane); and `normalizeIpcError(reason: unknown): unknown` whose FIRST statement is the idempotence guard `if (reason instanceof IpcError) return reason;` — an already-normalized IpcError passes through by identity, never re-wrapped, so the browser-mock path (which throws a real IpcError, below) and 03-03's e2e fixtures can never acquire a doubled `code: code:` prefix (checker revision). After the guard: (a) if reason is a non-null object whose `code` and `message` are both strings, returns `new IpcError(...)` when the code passes isIpcErrorCode, else a plain `new Error(...)` whose text is the code + ": " + message (or just message when code is empty); (b) returns every other rejection value unchanged. Follow the today.ts:698-706 unknown-narrowing style (RESEARCH Pattern 2).
    Create src/lib/ipcError.test.ts (Wave 0 gap, ERR-01) covering every bullet in behavior above, including: the empty-code body yields `Error(message)` with no prefix; a forged body with an unknown code never becomes an IpcError (T-03-01 fail-safe).
    FUNNEL — in src/lib/evidenceBinder.ts, wrap the ENTIRE body of `mutateEvidenceBinder` (:127-146) in try/catch that rethrows `normalizeIpcError(err)` — wrapping the whole body (not just the invoke call) covers both the browser-mock throw and the real invoke rejection. Update the browser mock at :138-139 to throw `new IpcError({ code: "evidence_binder_revision_conflict", message: `expected revision ${params.expectedRevision}, found ${current.revision}` })` so browser-dev behavior matches the migrated wire shape.
    BRANCH — in src/components/evidence/EvidenceBinderPane.tsx, replace the substring-matcher line at :174 with a typed check: `if (err instanceof IpcError && err.code === "evidence_binder_revision_conflict") void load();`, importing IpcError from the new lib module. The `err instanceof Error ? err.message : String(err)` toast line at :172 stays untouched (Pitfall 5: safe under the Error subclass).
    Write the new tests FIRST where feasible (Rust ipc_error tests, ipcError.test.ts), watch them fail, then implement (tdd="true"); the evidence_binder test migration is red-by-construction once the emit type changes.
  </action>
  <verify>
    <automated>cd src-tauri && cargo test --lib ipc_error && cargo test --lib evidence_binder && cd .. && pnpm vitest run src/lib/ipcError.test.ts src/lib/evidenceBinder.test.ts && pnpm typecheck</automated>
  </verify>
  <acceptance_criteria>
    - src-tauri/src/ipc_error.rs contains `pub struct IpcError` with fields `pub code: String` and `pub message: String`, the four `pub const ..._CONFLICT` declarations, `impl From<String> for IpcError`, and a `#[cfg(test)] mod tests` with `ipc_error_codes_are_stable` and `ipc_error_wire_shape_round_trips`
    - src-tauri/src/lib.rs contains `mod ipc_error;` in alphabetical position
    - src-tauri/src/evidence_binder.rs declares `evidence_binder_mutate` returning `Result<EvidenceBinderResponse, IpcError>` and its revision-mismatch path constructs IpcError with the EVIDENCE_BINDER_REVISION_CONFLICT constant
    - src/lib/types.ts contains `IPC_ERROR_CODES`, `IpcErrorCode`, and `IpcErrorBody`
    - src/lib/ipcError.ts exports `IpcError`, `isIpcErrorCode`, and `normalizeIpcError`; normalizeIpcError's first statement is the guard `if (reason instanceof IpcError) return reason;`, and ipcError.test.ts asserts re-normalizing an IpcError returns the same instance unchanged (no doubled `code: code:` prefix)
    - src/components/evidence/EvidenceBinderPane.tsx branches via `err.code ===` (grep -c '\.includes("evidence_binder_revision_conflict")' src/components/evidence/EvidenceBinderPane.tsx returns 0)
    - All four verify commands exit 0
  </acceptance_criteria>
  <done>The binder conflict path carries a typed code from the Rust guard through the bridge and funnel to the component branch; both test suites and typecheck are green; legacy error text is byte-identical everywhere except the binder conflict message, which gains its human suffix exactly as sanctioned.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Real-app smoke of the new wire shape + confirm the 7-command scope</name>
  <what-built>
    The first wire-format change of the milestone: `evidence_binder_mutate` now rejects with a serialized `{code, message}` object instead of a string, and the frontend normalizes it back into an Error with a typed `.code`. None of the Phase 1 gates can exercise the real WKWebView IPC path (the pre-disclosed `native-tauri-e2e-runner-missing` blind spot — REQUIREMENTS.md Phase 3 note), so a real-app smoke is the only proof the bridge round-trips.
    ALSO — scope deviation to confirm: CONTEXT specifics says "the four migrated commands", but the measured branch-on set (D-04's criterion, RESEARCH Pitfall 7) is 7 commands: today_mutate, today_finalize_setup, today_calendar_publish, task_transition, task_trash, save_document, evidence_binder_mutate. Plans 03-02/03-03 proceed with all 7; graph_link_apply stays display-only and OUT. Approving this checkpoint ratifies the 7-command set.
  </what-built>
  <how-to-verify>
    1. Run the real app: `pnpm tauri dev` (the CI e2e suite mocks IPC and cannot cover this).
    2. Open a document with an evidence binder; apply one accept/reject mutation.
    3. Expected: the mutation succeeds, the binder list updates, NO error toast appears, and the devtools console shows no IPC/serde errors.
    4. (Optional, conflict path) With the binder open, edit the document externally to bump its revision, then mutate again. Expected: the binder reloads (the recovery branch fires) and the error text shown reads "evidence_binder_revision_conflict: expected revision …, found …".
    5. Confirm you accept the 7-command migration scope (deviation above) — reply "approved" covers both.
  </how-to-verify>
  <resume-signal>Type "approved" (optionally noting the smoke result), or describe any failure / scope objection</resume-signal>
</task>

</tasks>

## Artifacts this phase produces

Symbols this phase creates (for plan-review-convergence source-grounding; these are NEW, not drift):

- Rust, new file `src-tauri/src/ipc_error.rs`:
  - `pub struct IpcError { pub code: String, pub message: String }` (Serialize, Debug, Clone)
  - `impl std::fmt::Display for IpcError`, `impl std::error::Error for IpcError`, `impl From<String> for IpcError`
  - `pub const TODAY_CONFLICT`, `pub const TASK_CONFLICT`, `pub const DOCUMENT_CONFLICT`, `pub const EVIDENCE_BINDER_REVISION_CONFLICT` (all `&str`)
  - test module `ipc_error::tests`: `ipc_error_codes_are_stable`, `ipc_error_wire_shape_round_trips`
  - `mod ipc_error;` registration in `src-tauri/src/lib.rs`
- TS, in `src/lib/types.ts`: `IPC_ERROR_CODES` (const array), `IpcErrorCode` (union type), `IpcErrorBody` (interface)
- TS, new file `src/lib/ipcError.ts`: `isIpcErrorCode`, `class IpcError extends Error` (field `code: IpcErrorCode`), `normalizeIpcError`
- TS, new file `src/lib/ipcError.test.ts`
- Modified Rust signatures (later plans): `check_revision`, `load_context`, `assert_expected_revision` return `Result<_, IpcError>`; commands `today_mutate`, `today_finalize_setup`, `today_calendar_publish`, `task_transition`, `task_trash`, `save_document`, `evidence_binder_mutate` return `Result<_, IpcError>`
- Deleted (later plan): `todayErrorCode` from `src/lib/today.ts` plus its test describe block (D-08)
- No new dependencies, no CLI flags, no codegen (D-03)

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Rust → Tauri bridge → frontend | Serialized `IpcError` rejection values cross as JSON and are consumed by `normalizeIpcError`; the rejection value is framework-supplied but shape-unverified until the normalizer guards it |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-01 | Tampering / Spoofing | normalizeIpcError (src/lib/ipcError.ts) | medium | mitigate | Type-guard `code`/`message` as strings and require union membership before constructing IpcError; forged or unknown codes degrade to a plain Error and never satisfy an `err.code ===` branch (fail-safe to generic display) — pinned by ipcError.test.ts cases |
| T-03-02 | Information Disclosure | IpcError wire shape | low | accept | Message content is byte-identical to today's prefix strings (canon error-disclosure surface unchanged); the code field exposes nothing not already present in the shipped binary and current wire strings |
| T-03-SC | Tampering | npm/cargo installs | low | accept | Zero packages installed this phase — RESEARCH.md Package Legitimacy Audit is vacuous; D-03 forbids codegen tooling |
</threat_model>

<verification>
- `cargo test --lib ipc_error` and `cargo test --lib evidence_binder` green (struct, pin test, D-09 round-trip, migrated emit test)
- `pnpm vitest run src/lib/ipcError.test.ts src/lib/evidenceBinder.test.ts` green
- `pnpm typecheck` green (union mirror compiles at the branch site)
- Real-app smoke approved at the checkpoint
</verification>

<success_criteria>
A revision conflict in the evidence binder reaches the component as a typed `IpcError` and triggers the reload branch without any message parsing; the four contract codes are pinned on both sides of the bridge; the user-visible text of every error is unchanged except the sanctioned binder-conflict suffix.
</success_criteria>

<output>
Create `.planning/phases/03-typed-ipc-error-contract/03-01-SUMMARY.md` when done. Record: **the pre-migration ERR-04 baseline B measured before any edit** (03-02 and 03-04 assert against it), the checkpoint outcome (including the 7-command scope ratification), the exact Display/normalization semantics chosen, and any deviation from this plan.
</output>
