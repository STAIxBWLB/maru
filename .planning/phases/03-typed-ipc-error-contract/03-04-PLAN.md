---
phase: 03-typed-ipc-error-contract
plan: "04"
type: execute
wave: 3
depends_on: ["03-02", "03-03"]
files_modified:
  - src-tauri/src/ipc_error.rs
  - src/lib/types.ts
autonomous: true
requirements: [ERR-02, ERR-03, ERR-04]
estimate:
  tokens: 35000
  raw_tokens: 35000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "[ERR-02] The rename drill is proven both directions: renaming a code constant fails cargo test on the Rust side, and renaming a union literal fails tsc on the TypeScript side — both observed red, then reverted to green"
    - "[edge:boundary/ERR-04] The pinned count command (grep -roE over src-tauri/src for Result<*, String> signatures) reports within [1118, 1138] — planning baseline 1,138 re-confirmed 2026-08-23 on the committed tree at 34f96ee, minus the 10-item migration set, never above baseline, measured with the identical command before and after"
    - "[ERR-03] The residual-matcher grep over src/ for substring matchers on the four contract codes returns zero (re-run as a phase-gate, not just at 03-03)"
    - "Full make verify is green with the migrated contract in place"
    - "D-06: the local, non-IPC matchers stay out of the contract and untouched — `clipboard.ts:30`'s \"clipboard is empty\" match and the `RibbonTable.tsx:84-86` diagram string matches are not migrated and not covered by the ERR-03 residual grep"
  artifacts:
    - Rename-drill evidence (red outputs + reverts) recorded in the SUMMARY
    - The final ERR-04 count recorded in the SUMMARY
  key_links:
    - "ERR-02's mechanism: Rust pin test (cargo test subset of make verify) x TS union (tsc -b subset of make verify) — the drill proves both gates actually fire"
  prohibitions:
    - requirement_id: ERR-02
      category: safety
      status: resolved
      verification: judgment
      statement: "MUST NOT weaken the Phase 1 gate set to land this phase — zero new suppression attributes or lint-disable directives, zero gate removals; a red gate is fixed, never bypassed"
    - requirement_id: ERR-03
      category: values
      status: resolved
      verification: judgment
      statement: "MUST NOT expand the contract beyond the four D-04 codes — display-only commands keep Result<T, String>, and the Rust-internal prefix families stay untyped (D-05)"
---

<objective>
Prove the phase's three remaining requirements with direct evidence: the ERR-02 rename drill (break one side, watch that side's gate go red, revert — the Phase 1 break-it-and-watch-it-fail method), the ERR-04 signature-count guard against the pinned command, and the ERR-03 residual-matcher grep as a phase gate, closing with a full green `make verify`.

Purpose: ERR-02 is a mechanism, not a state — only the drill demonstrates the gates actually fire. ERR-04's guard is a count with an explicit tolerance band, asserted against the number 03-02 recorded.

Output: drill evidence and the final count in the SUMMARY; full verify green.

**Flagged assumption (spec-less edge probe, no-silent-drop):** the probe's `unclassified` row for ERR-02 ("unclassified — review manually") cannot be auto-resolved and is carried here as a flagged assumption: it is treated as covered by the rename drill + dual pin tests, but a human should confirm at phase verification that no additional ERR-02 edge exists beyond rename-on-either-side.
</objective>

<execution_context>
@$HOME/.kimi-code/gsd-core/workflows/execute-plan.md
@$HOME/.kimi-code/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-typed-ipc-error-contract/03-CONTEXT.md
@.planning/phases/03-typed-ipc-error-contract/03-RESEARCH.md
@.planning/phases/03-typed-ipc-error-contract/03-VALIDATION.md
@.planning/phases/03-typed-ipc-error-contract/03-01-SUMMARY.md
@.planning/phases/03-typed-ipc-error-contract/03-02-SUMMARY.md
@.planning/phases/03-typed-ipc-error-contract/03-03-SUMMARY.md
@src-tauri/src/ipc_error.rs
@src/lib/types.ts
</context>

<tasks>

<task type="auto">
  <name>ERR-02 rename drill — prove both gates fire, then revert</name>
  <files>src-tauri/src/ipc_error.rs, src/lib/types.ts</files>
  <read_first>
    - .planning/phases/03-typed-ipc-error-contract/03-CONTEXT.md (D-03 mechanism; specifics: the rename drill is this phase's own ERR-02 proof)
    - src-tauri/src/ipc_error.rs (the four constants + ipc_error_codes_are_stable pin test)
    - src/lib/types.ts (IPC_ERROR_CODES array + IpcErrorCode union)
    - src/lib/today.ts (a comparison site, isTodayConflict, that the TS-side drill must break)
    - Makefile (:321 — the verify prerequisite list the drill's gates live in)
  </read_first>
  <action>
    Per D-03 and CONTEXT specifics (the Phase 1 D-13 break-it-and-watch-it-fail method). Do each half independently and capture the outputs:
    RUST HALF: (1) run `cd src-tauri && cargo test --lib ipc_error` and confirm GREEN as the control. (2) Edit ONLY the VALUE of `TODAY_CONFLICT` in src-tauri/src/ipc_error.rs — append `_v2` to the literal (the constant name and everything else stay). (3) Re-run the same command: it MUST go RED at `ipc_error_codes_are_stable` (the literal-pin assertion). If it stays green, the pin test is not load-bearing — stop and fix the test before continuing. (4) Revert the edit; re-run to GREEN.
    TS HALF: (5) run `pnpm typecheck` and confirm GREEN as the control. (6) Edit ONLY the `"today_conflict"` literal inside the `IPC_ERROR_CODES` array in src/lib/types.ts — append `_v2` (everything else stays, including the branch sites' comparison literals). (7) Re-run `pnpm typecheck`: it MUST go RED with TS2367-style no-overlap comparison errors at the `err.code === "today_conflict"` sites (isTodayConflict in src/lib/today.ts and any other comparison against that literal). If it stays green, the union is not load-bearing at the branch sites — stop and fix the typing before continuing. (8) Revert the edit; re-run to GREEN.
    Confirm both files are byte-identical to their pre-drill state (git diff empty for both) before committing anything in this task. Record each red output (the failing assertion name / the TS error codes and sites) in the SUMMARY as the ERR-02 evidence. Commit nothing from the broken intermediate states; the drill's only artifact is the recorded evidence.
  </action>
  <verify>
    <automated>cd src-tauri && cargo test --lib ipc_error && cd .. && pnpm typecheck && git diff --exit-code -- src-tauri/src/ipc_error.rs src/lib/types.ts</automated>
  </verify>
  <acceptance_criteria>
    - The SUMMARY records: the red cargo-test output naming `ipc_error_codes_are_stable`, and the red tsc output naming the comparison error(s) at the err.code sites
    - Both drill edits reverted — `git diff --exit-code -- src-tauri/src/ipc_error.rs src/lib/types.ts` exits 0 at task end
    - Final `cargo test --lib ipc_error` and `pnpm typecheck` both exit 0
  </acceptance_criteria>
  <done>Both gates are proven load-bearing — Rust rename fails cargo test, TS rename fails tsc — with red evidence recorded and both files reverted to green.</done>
</task>

<task type="auto">
  <name>ERR-04 count guard, ERR-03 residual grep, and full make verify</name>
  <files>(no files modified — guard task; reads only)</files>
  <precondition>Plans 03-02 and 03-03 have landed; 03-02's SUMMARY records the post-migration Result&lt;*, String&gt; count.</precondition>
  <read_first>
    - .planning/phases/03-typed-ipc-error-contract/03-02-SUMMARY.md (the recorded post-migration count)
    - .planning/phases/03-typed-ipc-error-contract/03-RESEARCH.md (Pitfall 6 — the count includes #[cfg(test)] lines; baseline and post-check must use the same command)
    - Makefile (:321 — verify target)
  </read_first>
  <action>
    - ERR-04 guard: re-run the pinned command — `grep -roE "Result<.*, String>" src-tauri/src --include="*.rs" | wc -l` — and assert the result is within [1118, 1138]: the planning baseline is 1,138 (re-confirmed 2026-08-23 with this exact command on the committed tree at 34f96ee; RESEARCH Pitfall 6 — CONCERNS.md's 1,118 is stale), the migration set is 10 signatures (7 commands + 3 helpers), and the count must never exceed the baseline. If the measured value differs from 03-02's recorded number, investigate before proceeding — the delta must be explainable by the migration set alone (**re-measure the baseline at the start of this plan, always** - 19 of the 1,138 matches live in hwped.rs and the hwp-editor track is still active, so drift is expected rather than exceptional. Record the new baseline, apply the same ≤20 delta rule, and update the band in this plan's automated check before asserting against it).
    - ERR-03 guard: re-run the residual-matcher grep — `grep -rnE '\.includes\("(today_conflict|task_conflict|document_conflict|evidence_binder_revision_conflict)"' src/` — and assert zero matches (the four codes are branched on only via err.code now). Also confirm `grep -rn "todayErrorCode" src/ e2e/` is empty (D-08 retirement held).
    - Full gate: run `make verify` and confirm green end-to-end (typecheck, lint, test-ts, test-rust, fmt-check, clippy, build-frontend — Makefile:321). The Phase 1 and 2 escape hatch for uncommitted hwped.rs/lib.rs no longer applies: that work is committed as of 34f96ee and `make verify` is green end-to-end on it, so a red gate here is this phase's to diagnose.
    - Record the final count and both grep results in the SUMMARY.
  </action>
  <verify>
    <automated>COUNT=$(grep -roE "Result<.*, String>" src-tauri/src --include="*.rs" | wc -l | tr -d ' ') && [ "$COUNT" -ge 1118 ] && [ "$COUNT" -le 1138 ] && ! grep -rnE '\.includes\("(today_conflict|task_conflict|document_conflict|evidence_binder_revision_conflict)"' src/ && make verify</automated>
  </verify>
  <acceptance_criteria>
    - The pinned count command prints a value in [1118, 1138] and the value is recorded in the SUMMARY
    - The residual-matcher grep over src/ prints nothing; the retired-parser grep over src/ and e2e/ prints nothing
    - `make verify` exits 0 (or the Phase 1 shared-checkout exception is documented per gate with each owned gate proven green individually)
  </acceptance_criteria>
  <done>The signature count sits inside the tolerance band against the pinned baseline, no substring matcher on a contract code survives in src/, and the full gate set is green with the migrated contract in place.</done>
</task>

</tasks>

## Artifacts this phase produces

(Same phase-level list as 03-01; this plan creates no new symbols — it proves the contract.)

- `src-tauri/src/ipc_error.rs`: `IpcError { code, message }`, four `*_CONFLICT` constants, Display/Error/From<String> impls, `ipc_error::tests` (`ipc_error_codes_are_stable`, `ipc_error_wire_shape_round_trips`)
- `src/lib/types.ts`: `IPC_ERROR_CODES`, `IpcErrorCode`, `IpcErrorBody`; `src/lib/ipcError.ts`: `IpcError`, `isIpcErrorCode`, `normalizeIpcError`; `src/lib/ipcError.test.ts`
- Migrated signatures: `check_revision`, `load_context`, `assert_expected_revision`, and the 7 commands → `Result<_, IpcError>`
- Deleted: `todayErrorCode` and its test describe (D-08)
- Drill edits to ipc_error.rs / types.ts are made and REVERTED inside the task — they produce no shipped artifact, only recorded evidence

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new — proof plan; the contract boundary was modeled in 03-01/03-02/03-03) | The drill touches and reverts constants only |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-06 | Tampering | rename-drill intermediate states | low | mitigate | Broken drill states are never committed; git-diff-empty check gates the task's end, so a half-reverted drill cannot leak into the tree |
| T-03-SC | Tampering | npm/cargo installs | low | accept | Zero packages installed; legitimacy audit vacuous |
</threat_model>

<verification>
- Rename drill: red evidence recorded both directions, both files reverted
- ERR-04 count within [1118, 1138] via the pinned command
- ERR-03 residual-matcher grep empty; retired-parser grep empty
- `make verify` green (or the documented shared-checkout exception)
</verification>

<success_criteria>
ERR-02 is proven by demonstration (both gates fire on a rename), ERR-04 by measurement (count inside the band), ERR-03 by absence (zero residual matchers) — and the full gate set passes with the typed contract live.
</success_criteria>

<output>
Create `.planning/phases/03-typed-ipc-error-contract/03-04-SUMMARY.md` when done. Record: the drill red outputs (both halves), the final count and grep results, the make verify outcome, and the flagged ERR-02 unclassified-edge assumption for the phase verifier.
</output>
