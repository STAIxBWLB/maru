---
phase: 08-main-thread-responsiveness
plan: "07"
type: execute
wave: 8
depends_on: ["08-29"]
files_modified: ["src-tauri/src/document.rs","src-tauri/src/file_manager.rs","src-tauri/src/vault_guard.rs","src-tauri/src/lib.rs","docs/performance/phase08-07.json"]
autonomous: true
requirements: ["PERF-01"]
estimate:
  tokens: 30000
  raw_tokens: 30000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "Guarded documents reaches no network, process or unbounded walk on the UI or shared async worker thread."
    - "All 11 owned commands preserve wire types, permission checks and synchronous internal callers."
    - "Concurrent operations preserve lifecycle ordering and current disk state; each retained/native UI exception has positive evidence."
  artifacts:
    - path: "src-tauri/src/document.rs"
      provides: "Guarded documents command isolation implementation or executable verification"
    - path: "src-tauri/src/file_manager.rs"
      provides: "Guarded documents command isolation implementation or executable verification"
    - path: "src-tauri/src/vault_guard.rs"
      provides: "Guarded documents command isolation implementation or executable verification"
    - path: "src-tauri/src/lib.rs"
      provides: "Guarded documents command isolation implementation or executable verification"
  key_links:
    - from: "src-tauri/src/document.rs"
      to: "src-tauri/src/file_manager.rs"
      via: "Existing registered command and explicit transaction/verification flow described in tasks"
  prohibitions:
    - statement: "No automatic sync retry, duplicate queue, new job center, or Phase 9 quit/terminal escalation."
      status: resolved
      verification: explicit
    - statement: "No live workspace, credentials, permission bypass, or native test hooks in production artifacts."
      status: resolved
      verification: explicit
---

<objective>
Guarded documents command isolation. Guarded documents reaches no network, process or unbounded walk on the UI or shared async worker thread. All 11 owned commands preserve wire types, permission checks and synchronous internal callers. Concurrent operations preserve lifecycle ordering and current disk state; each retained/native UI exception has positive evidence.
Purpose: satisfy PERF-01 within the existing command/UI contracts.
Output: tested source changes and exact owned-command evidence.
</objective>

<execution_context>
@.claude/gsd-core/workflows/execute-plan.md
@.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@README.md
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/08-main-thread-responsiveness/08-CONTEXT.md
@.planning/phases/08-main-thread-responsiveness/08-RESEARCH.md
@.planning/phases/08-main-thread-responsiveness/08-PATTERNS.md
@.planning/phases/08-main-thread-responsiveness/08-PLAN-MAP.md
</context>

## Artifacts this phase produces

- src-tauri/src/document.rs: pub mod ipc with async same-name wrappers for each converted owned command; original sync functions retained
- src-tauri/src/file_manager.rs: pub mod ipc with async same-name wrappers for each converted owned command; original sync functions retained
- src-tauri/src/vault_guard.rs: pub mod ipc with async same-name wrappers for each converted owned command; original sync functions retained
- docs/performance/phase08-07.json final command evidence
- Inline phase08_07 behavioral tests
- Owned-input <command>_blocking helpers only where State/borrow adaptation is required, retaining original synchronous API signatures
- Inline test module/name prefix `phase08_07` where tasks specify Rust tests; existing tests stay available.
- New helper identifiers are limited to the explicitly listed names, `<owned_command>_in_transaction` adapters and `<owned_command>_blocking` for each converted command in the table below; these are proposed symbols, not existing APIs.

Registration edits share lib.rs, so this plan depends on the immediately preceding tranche. This deliberately avoids parallel same-file conflicts while preserving independently green app/CLI builds. No extra dispatcher or build infrastructure is introduced to manufacture parallelism. Existing implementation helper modules are read-only unless explicitly listed; place shared admission in the owned mutation entry, before existing domain guards; no existing-guard exemption. Read src-tauri/src/atomic_file.rs before adapting any write; Plan 06 produces the shared path transaction helper.

## Exclusive command ownership

- `src-tauri/src/document.rs`: `read_document`, `save_document`, `update_frontmatter_field`, `create_document`, `move_document`, `duplicate_document`, `trash_document`, `create_version`.
- `src-tauri/src/file_manager.rs`: `reveal_in_file_manager`, `open_in_file_manager`.
- `src-tauri/src/vault_guard.rs`: `vault_validate_note`.

<tasks>
<task type="auto">
  <name>07-1: Isolate src/document.rs, src/file_manager.rs, src/vault_guard.rs</name>
  <reversibility rating="reversible">Internal scheduling and test changes preserve public command names and persisted data.</reversibility>
  <files>src-tauri/src/document.rs, src-tauri/src/file_manager.rs, src-tauri/src/vault_guard.rs, src-tauri/src/lib.rs</files>
  <read_first>src-tauri/src/document.rs; src-tauri/src/file_manager.rs; src-tauri/src/vault_guard.rs; src-tauri/src/lib.rs (new artifacts are created by this task if absent); README.md; docs/SSOT-TIERS.md; docs/BOUNDARIES.md; .planning/phases/08-main-thread-responsiveness/08-CONTEXT.md; .planning/phases/08-main-thread-responsiveness/08-PATTERNS.md; .planning/phases/08-main-thread-responsiveness/08-COMMAND-INVENTORY.md; src-tauri/src/hwped.rs; src-tauri/src/ipc_error.rs; src-tauri/src/lock_recovery.rs; src-tauri/src/atomic_file.rs; src-tauri/src/workspace_files.rs; src-tauri/src/frontmatter/ops.rs; .planning/phases/08-main-thread-responsiveness/08-PLAN-MAP.md</read_first>
  <action>Own and close these exact command rows: src-tauri/src/document.rs: read_document, save_document, update_frontmatter_field, create_document, move_document, duplicate_document, trash_document, create_version; src-tauri/src/file_manager.rs: reveal_in_file_manager, open_in_file_manager; src-tauri/src/vault_guard.rs: vault_validate_note. Read every body and all reached helpers including cfg/platform branches, traits/callbacks and command(async) bodies. For blocking synchronous functions, retain the original public synchronous function and its name for Rust/CLI callers; remove its command attribute and add a same-name async #[tauri::command] function inside this module's new pub mod ipc. That wrapper owns its inputs and awaits tauri::async_runtime::spawn_blocking calling super::the_original_name. Update only corresponding generate_handler paths in lib.rs to module::ipc::command; the wire name is unchanged. Existing genuinely async/isolated/UI entries keep their original registration path unless their body needs a finite blocking substep. Construct ProgressReporter inside worker; clone State's owned Arc/handle rather than carrying borrowed State or guards across await. Preserve Result<T,String> versus Result<T,IpcError>; inner typed errors pass through untouched, while JoinError becomes contextual display-only error of the same outer type. Do not use block_on or an executor/dispatch registry. Use Plan 06 shared hierarchical path-set admission across save/frontmatter/create/move/trash/version transactions, including source/destination and sidecar/rekey paths; it conflicts with workspace_files parent directory rename/trash. Acquire it before existing domain locks, and revalidate the selected existing parent and document revision after admission before write_atomic can create parents. Denied writes and revision conflicts remain typed; document body bytes remain unchanged for frontmatter-only writes. Add inline tests with prefix phase08_07 in modified modules: nonempty real fixture result, unchanged typed/legacy rejection, and at least one deterministic overlap case where a blocked worker still permits a same-runtime async yield. For every newly parallel write, inventory its transaction key and test same-target contention; keep authorization and revision validation inside the worker/transaction. Per D-04, annotate each user-started processing row's frontend call sites and existing operation completion ownership in this shard; Plan 25/26 implements the shared lifetime path, not a new job service. For a synchronous function that accepts State or another borrowed runtime object, extract an owned-input synchronous <command>_blocking helper for the actual work; both the unchanged public synchronous function and the async ipc wrapper call that helper. The wrapper clones the Arc/handle from State before awaiting. Do not fabricate a State or send a borrowed guard/reporter into spawn_blocking. Define and list exact owned argument types in the implementation summary. For every filesystem mutation, follow the Shared mutation admission contract in 08-PLAN-MAP.md using atomic_file::with_path_transactions from Plan 06, including ancestor/descendant and symlink-alias conflicts. Declare the complete source/destination/sidecar write set; acquire shared admission inside the blocking worker before existing domain guards, then revalidate lexical containment, required existing parents and revisions. Existing guards remain nested participants, never exemptions. Nested calls use the admitted PathTransactionLease via <owned_command>_in_transaction adapters without reacquiring guards. Hold admission through write/rekey/rollback; no old parent recreation. Record the shared entry and lock order for actual sync/IPC/background writers and add deterministic real-command cross-domain contention and error-release cases.</action>
  <acceptance_criteria>All commands named by this task compile with unchanged wire names and synchronous Rust call sites. Named phase08_07 tests run; blocking body and lock waits execute in a blocking worker; no lost update or permission bypass; shared hierarchical admission precedes domain locks, supported symlinks still work, and cross-domain contention/error-release cases pass. </acceptance_criteria>
  <verify>
    <automated>cargo test --manifest-path src-tauri/Cargo.toml --lib phase08_07</automated>
    <fails_when>Nonzero exit, zero matched tests, missing named cross-domain/lock-order/error-release case for a mutation, parent recreation, partial rekey, deadlock, or an expected case skipped.</fails_when>
    <automated>cargo check --manifest-path src-tauri/Cargo.toml -p maru-cli</automated>
    <fails_when>Nonzero exit.</fails_when>
  </verify>
  <done>All commands named by this task compile with unchanged wire names and synchronous Rust call sites. Named phase08_07 tests run; blocking body and lock waits execute in a blocking worker; no lost update or permission bypass; shared hierarchical admission precedes domain locks, supported symlinks still work, and cross-domain contention/error-release cases pass. </done>
</task>

<task type="auto">
  <name>07-2: Prove cross-command parent/child mutation exclusion</name>
  <reversibility rating="reversible">Internal scheduling and test changes preserve public command names and persisted data.</reversibility>
  <files>src-tauri/src/document.rs, docs/performance/phase08-07.json</files>
  <read_first>src-tauri/src/document.rs; docs/performance/phase08-07.json (new artifacts are created by this task if absent); README.md; docs/SSOT-TIERS.md; docs/BOUNDARIES.md; .planning/phases/08-main-thread-responsiveness/08-CONTEXT.md; .planning/phases/08-main-thread-responsiveness/08-PATTERNS.md; .planning/phases/08-main-thread-responsiveness/08-COMMAND-INVENTORY.md; src-tauri/src/hwped.rs; src-tauri/src/ipc_error.rs; src-tauri/src/lock_recovery.rs; src-tauri/src/atomic_file.rs; src-tauri/src/workspace_files.rs; src-tauri/src/frontmatter/ops.rs; .planning/phases/08-main-thread-responsiveness/08-PLAN-MAP.md</read_first>
  <action>Create phase08_07_parent_child_races in document.rs, using Plan 06 cfg(test) admission/pre-effect barriers to invoke real workspace_files::ipc::rename_workspace_entry and trash_workspace_entries against document::ipc::save_document and create_document. Cover rename/save, rename/create, trash/save and trash/create in both admission orderings. Child-first: hold after revision/parent validation before write, prove parent cannot enter effects, finish child then parent, and verify final content/rekey in the moved tree or Trash. Parent-first: hold before effects, start the child while its original parent exists, complete parent, then require the waiting child to fail post-admission validation without recreating A. Use nonempty fixtures and bounded waits with cleanup; do not substitute mocked locks or sleep-only helpers. Add frontmatter-versus-save contention through real wrappers using the same revision, denied-write/error release followed by a successful operation, and successful symlink alias cases. Assert no old tree recreation, lost update, partial sidecar/index rekey or deadlock. Extend the already converted module's inline tests to cover error/cleanup and concurrent operations through the actual wrapper. Use Plan 06 shared hierarchical path-set admission across save/frontmatter/create/move/trash/version transactions, including source/destination and sidecar/rekey paths; it conflicts with workspace_files parent directory rename/trash. Acquire it before existing domain locks, and revalidate the selected existing parent and document revision after admission before write_atomic can create parents. Denied writes and revision conflicts remain typed; document body bytes remain unchanged for frontmatter-only writes. Add inline tests with prefix phase08_07 in modified modules: nonempty real fixture result, unchanged typed/legacy rejection, and at least one deterministic overlap case where a blocked worker still permits a same-runtime async yield. For every newly parallel write, inventory its transaction key and test same-target contention; keep authorization and revision validation inside the worker/transaction. Write docs/performance/phase08-07.json with every owned row exactly once, final disposition and decisive helper/affinity/bound plus tests and current registration path. Zero AUDIT; retained/UI/ISOLATED rows require positive evidence too. Preserve other shards and the research inventory; final merge is Plan 28. Per D-04, annotate each user-started processing row's frontend call sites and existing operation completion ownership in this shard; Plan 25/26 implements the shared lifetime path, not a new job service. For a synchronous function that accepts State or another borrowed runtime object, extract an owned-input synchronous <command>_blocking helper for the actual work; both the unchanged public synchronous function and the async ipc wrapper call that helper. The wrapper clones the Arc/handle from State before awaiting. Do not fabricate a State or send a borrowed guard/reporter into spawn_blocking. Define and list exact owned argument types in the implementation summary. For every filesystem mutation, follow the Shared mutation admission contract in 08-PLAN-MAP.md using atomic_file::with_path_transactions from Plan 06, including ancestor/descendant and symlink-alias conflicts. Declare the complete source/destination/sidecar write set; acquire shared admission inside the blocking worker before existing domain guards, then revalidate lexical containment, required existing parents and revisions. Existing guards remain nested participants, never exemptions. Nested calls use the admitted PathTransactionLease via <owned_command>_in_transaction adapters without reacquiring guards. Hold admission through write/rekey/rollback; no old parent recreation. Record the shared entry and lock order for actual sync/IPC/background writers and add deterministic real-command cross-domain contention and error-release cases. Extend inline phase08_07 tests in document.rs with phase08_07_earlier_writer_document_races: actual store::ipc::skills_save_skill_file and store::skills_sync_source wrappers (from 01/03/29) and git::ipc::git_sync_pull_rebase (05/29) versus document::ipc::save_document and create_document produced by 07-1. Use real local Git/linked-source fixture files and Plan 06 scoped barriers; run every writer/document pair in both admission orders, lexical and symlink-alias paths, parent removal/recreation while waiting and injected failure release. No test-side manual admission around an unguarded function counts. A sync or pull that writes a checkout holds the whole checkout set; save/create waits, then revalidates revisions/parent identity. Assert exactly one conflict or complete serial result, no lost update/old tree recreation/partial sidecar, and independent registry list plus metadata-only removal still completes while network is held. Record these case IDs under crossDomainConsumers for integration 08-29 in this plan's own shard; 29 does not require these future cases to pass before 07. Existing 06/29 wrappers are read-only here.</action>
  <acceptance_criteria>All commands named by this task compile with unchanged wire names and synchronous Rust call sites. Named phase08_07 tests run; blocking body and lock waits execute in a blocking worker; no lost update or permission bypass; shared hierarchical admission precedes domain locks, supported symlinks still work, and cross-domain contention/error-release cases pass. All four real parent/child command pairs and every Skills-save/Skills-sync/Git-pull versus document-save/create pair pass in both admission orders and alias cases, including registry availability and error release; no old tree is recreated and rekeys remain complete. Owned shard passes exact coverage and zero AUDIT checks.</acceptance_criteria>
  <verify>
    <automated>cargo test --manifest-path src-tauri/Cargo.toml --lib phase08_07</automated>
    <fails_when>Nonzero exit, zero matched tests, missing named parent/child or earlier-writer/document pair/order/alias/registry-availability/error-release case, parent recreation, partial rekey, deadlock, or an expected case skipped.</fails_when>
    <automated>cargo check --manifest-path src-tauri/Cargo.toml -p maru-cli</automated>
    <fails_when>Nonzero exit.</fails_when>
    <automated>node scripts/check-command-isolation.mjs --plan 07</automated>
    <fails_when>Nonzero exit.</fails_when>
  </verify>
  <done>All commands named by this task compile with unchanged wire names and synchronous Rust call sites. Named phase08_07 tests run; blocking body and lock waits execute in a blocking worker; no lost update or permission bypass; shared hierarchical admission precedes domain locks, supported symlinks still work, and cross-domain contention/error-release cases pass. All four real parent/child command pairs and every Skills-save/Skills-sync/Git-pull versus document-save/create pair pass in both admission orders and alias cases, including registry availability and error release; no old tree is recreated and rekeys remain complete. Owned shard passes exact coverage and zero AUDIT checks.</done>
</task>
</tasks>

<threat_model>
OWASP ASVS L1; block on high. No new dependency or external integration.

| Trust boundary | Protected invariant |
|---|---|
| Webview IPC to worker | Owned inputs retain backend permission, containment and revision validation |
| Worker to filesystem/process | Scoped serialization preserves current disk state and fixed argv |
| Test fixture to native process | Only disposable roots and compile-time gated observation |

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-08-07-01 | Tampering | Owned mutation paths | high | mitigate | Task 07-1 preserves or adds scoped transaction validation and denied-write tests before external effects. |
| T-08-07-02 | Denial of service | IPC scheduling and lifecycle | high | mitigate | Finite blocking closure, no guard across await, cleanup on failure/unwind; named task tests plus native saturation gate. |
| T-08-07-03 | Information disclosure | Errors and evidence | medium | mitigate | Preserve redaction, record synthetic fixture paths only, and final feature-off binary/frontend guards. |
</threat_model>

<verification>
Run each task's exact commands from the repository root; compiled tests must match at least one named case. Test paths introduced here are produced before their first consuming verification. Plans with native commands are slow integration gates; other focused checks target under 60 seconds with cached compilation. Never report pending execution as passed.
</verification>

<success_criteria>
Every task acceptance condition holds, assigned inventory rows have a final justified disposition, and PERF-01 evidence is recorded without claims about unrelated commands. Descriptor-less prohibitions remain flagged-unverified until verifier supplies explicit evidence; never auto-dismiss them.
</success_criteria>

<output>
Create .planning/phases/08-main-thread-responsiveness/08-07-SUMMARY.md after execution, including actual checks, owned rows and threat dispositions.
</output>
