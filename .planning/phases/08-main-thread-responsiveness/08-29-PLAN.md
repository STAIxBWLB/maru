---
phase: 08-main-thread-responsiveness
plan: "29"
type: execute
wave: 7
depends_on: ["08-06"]
files_modified: ["src-tauri/src/skill_host/store.rs","src-tauri/src/git.rs","src-tauri/src/skill_host/env.rs","src-tauri/src/skill_host/dispatch.rs","src-tauri/src/dot_sync.rs","src-tauri/src/agent_host/event_store.rs","src-tauri/src/mission_state.rs","docs/performance/phase08-29-integration.json","scripts/check-command-isolation.mjs","scripts/check-command-isolation.test.mjs"]
autonomous: true
requirements: ["PERF-02","PERF-01"]
estimate:
  tokens: 45000
  raw_tokens: 45000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "Skills and Git writes conflict with real parent rename/trash in either admission order, including aliased checkouts."
    - "D-01..D-03: duplicate source admission remains nonblocking and metadata-only removal remains available during network work."
    - "Earlier env, dispatch and dot filesystem writers enter shared admission before domain locks and retain it through their actual mutation lifetime."
    - "Exactly 365 command owners remain; the integration overlay adds no command rows and Plan 07 owns later document-pair proof."
  artifacts:
    - path: "src-tauri/src/skill_host/store.rs"
      provides: "Source reservations plus shared checkout/commit admission and inline phase08_29_skills tests"
    - path: "src-tauri/src/git.rs"
      provides: "Complete checkout/Git metadata admission and inline phase08_29_git tests"
    - path: "src-tauri/src/skill_host/env.rs"
      provides: "Root/status/bootstrap lifetime admission and inline phase08_29_env tests"
    - path: "src-tauri/src/skill_host/dispatch.rs"
      provides: "Admitted event/mission callbacks and inline phase08_29_dispatch tests"
    - path: "src-tauri/src/agent_host/event_store.rs"
      provides: "Direct and borrowed-lease append entry adapters; final module owner 17"
    - path: "src-tauri/src/mission_state.rs"
      provides: "Direct and borrowed-lease mission mutation adapters; final module owner 16"
    - path: "src-tauri/src/dot_sync.rs"
      provides: "Local target-set admission before DOT_ACTION_LOCK and inline phase08_29_dot tests"
    - path: "docs/performance/phase08-29-integration.json"
      provides: "Earlier-writer integration evidence overlay referencing existing command keys"
  key_links:
    - from: "src-tauri/src/skill_host/store.rs"
      to: "src-tauri/src/atomic_file.rs"
      via: "Nonblocking source reservation then with_path_transactions before REGISTRY_LOCK"
    - from: "src-tauri/src/git.rs"
      to: "src-tauri/src/workspace_files.rs"
      via: "Shared complete checkout/alias set versus real rename/trash wrappers produced by 06"
    - from: "src-tauri/src/skill_host/dispatch.rs"
      to: "src-tauri/src/agent_host/event_store.rs"
      via: "Explicit borrowed PathTransactionLease passed through append adapters before append_lock_for"
    - from: "docs/performance/phase08-29-integration.json"
      to: "scripts/check-command-isolation.mjs"
      via: "--integration 29 validates overlay separately; --all requires Plan 07 document consumer evidence"
  prohibitions:
    - statement: "No automatic sync retry, duplicate queue, registry lock over network, or new job service."
      status: resolved
      verification: explicit
    - statement: "No live workspace or credentials; no cross-process exclusion guarantee."
      status: resolved
      verification: explicit
---

<objective>
Close cross-domain admission for earlier Skills, Git, environment, dispatch and dot writers before document conversion. Preserve D-01..D-05 while proving that existing real workspace rename/trash cannot overlap those mutations. Produce final integration evidence without changing command ownership or waiting for future document APIs.
Purpose: satisfy PERF-02/PERF-01 and close P08-02.
Output: shared admission in actual earlier entry points, deterministic races and one integration overlay.
</objective>

<execution_context>
@.claude/gsd-core/workflows/execute-plan.md
@.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@README.md
@.planning/phases/08-main-thread-responsiveness/08-CONTEXT.md
@.planning/phases/08-main-thread-responsiveness/08-RESEARCH.md
@.planning/phases/08-main-thread-responsiveness/08-PATTERNS.md
@.planning/phases/08-main-thread-responsiveness/08-PLAN-MAP.md
@.planning/phases/08-main-thread-responsiveness/08-06-PLAN.md
@.planning/phases/08-main-thread-responsiveness/08-07-PLAN.md
</context>

## Staged ownership and producer contract

No exclusive command rows are assigned here. Plans 01/02/03 own store command rows, 04 owns env/dispatch rows and 05 owns Git/dot rows. Plan 29 is final cross-domain module integration owner for store.rs, git.rs, env.rs, dispatch.rs and dot_sync.rs. event_store.rs and mission_state.rs receive bounded prerequisite adapter edits here; their final owners remain 17 and 16. atomic_file.rs and workspace_files.rs are read-only Plan 06 producers. All new tests are inline in declared files; no undeclared helper file is needed.

Use Plan 06 with_path_transactions, PathTransactionRequest and PathTransactionLease without replacing the protocol. Proposed local identifiers: source_mutation_paths, git_mutation_paths, env_mutation_paths, dispatch_mutation_paths, dot_mutation_paths and explicit `<existing_writer>_in_transaction` adapters carrying a borrowed lease and validated complete write-set coverage. For detached env work the owned lease must move into the existing worker or admission occurs inside that worker before its first effect. No thread-local ambient lease or implicit reentrant locking. The shared cfg(test) path/stage barrier API is already produced by 06. Original synchronous APIs and registered wrappers remain compatible.

<tasks>
<task type="tracer" tdd="true">
  <name>29-1: Admit real Skills and Git checkout mutations against parent operations</name>
  <files>src-tauri/src/skill_host/store.rs, src-tauri/src/git.rs, src-tauri/src/skill_host/env.rs</files>
  <read_first>src-tauri/src/skill_host/store.rs; src-tauri/src/git.rs; src-tauri/src/skill_host/env.rs; src-tauri/src/skill_host/fs.rs; src-tauri/src/atomic_file.rs; src-tauri/src/workspace_files.rs; docs/SSOT-TIERS.md; docs/BOUNDARIES.md; .planning/phases/08-main-thread-responsiveness/08-01-PLAN.md; .planning/phases/08-main-thread-responsiveness/08-03-PLAN.md; .planning/phases/08-main-thread-responsiveness/08-05-PLAN.md</read_first>
  <behavior>Start red with real store::ipc::skills_save_skill_file versus workspace_files::ipc::rename_workspace_entry on a linked skill under an ordinary nested workspace directory; both entry orders must serialize. Expand to Skills sync, Git pull, trash, aliases, failure cleanup and registry availability.</behavior>
  <action>Wire the tracer from actual registered Skills wrapper through source resolution and shared admission to persisted SKILL.md while the actual Plan 06 parent wrapper competes. Expand admission to every filesystem-writing route reached from store synchronous/IPC/batch/default/bundle/import/install callers, including helper-only registry initialization and reconciliation. Keep Plan 01 nonblocking atomic source-ID/canonical-checkout reservation first: release its bookkeeping mutex before any other lock; duplicate/alias source requests return busy immediately even when the first owner is waiting for path admission. Snapshot registry briefly and release REGISTRY_LOCK before discovering or waiting on paths; then acquire shared admission, recheck source incarnation/config, path aliases, selected existing-parent stable identity, permissions and relevant content revisions before effects. No domain guard is held while waiting for admission. Declare complete source tree or Git worktree top-level (never only the skills subdirectory), resolved Git directory and common Git directory for linked worktrees, clone destination/staging/rollback paths, bundle cache/active/backup paths, managed/imported targets, install links and destination tool roots resolved through host_fs (including CODEX_HOME), and affected registry/sidecar paths. Use lexical plus physical alias keys. Check Git top-level/common-dir resolution again after admission; reject changes before stash/pull/rebase/pop or add/commit/push. Include all Git mutators, including git_commit and helper call paths, and retain fixed argv, approvals and sensitive-path guards.

A network stage reserves checkout/bundle/env mutation paths but never registry.json or a global skills parent that subsumes registry.json. Hold checkout admission through the complete Git mutation/error settlement; retain source ownership until final result. Release this path lease before acquiring the complete commit path set (checkout plus registry and affected install/sidecars) atomically, so there is no nested lease-set extension. Revalidate incarnation/settings and all derived file identities/hashes after reacquisition, rescan current content and commit fresh registry; stale derived results return existing stale/conflict without retry per D-01/D-05. Metadata-only source removal acquires only its short registry metadata set before REGISTRY_LOCK, increments generation and never waits for active checkout paths; deleting active checkout bytes remains disallowed. Registry read/list has no path admission. Other registry mutators precompute full sets from a brief read-only snapshot, drop the registry guard, admit, reload and reject changed targets before writing. Do not put network-capable defaults/env repair under registry lock. Integrate env_status's ensure_dir, bootstrap dry-run status write, no-setup .venv creation, setup subprocess target and final status.json writes, preserving the existing background worker. env_mutation_paths covers host_fs::env_root() subtree, including .venv and status.json, and setup-owned local targets found by its existing contract. env nested calls reuse explicit lease adapters; resolve needed paths before store admission rather than extending a held set. Existing setup network work never owns registry metadata admission.

Inline phase08_29_skills and phase08_29_git tests call actual Skills save/sync and Git pull wrappers against real rename/trash wrappers: every three-by-two pair in both orders, ordinary nested directory plus symlink alias, alias IDs and nested skills subdirectory in one checkout, dirty stash/pull/pop, Git error release, source lease error/unwind release, parent deletion/recreation and no lost/partial writes. Use disposable local bare Git remotes, real changed commits and nonempty SKILL.md, no remote services. Bound barrier waits with timeouts. While network or path admission is blocked, assert list and metadata-only remove finish; duplicate source and Sync All skipped outcomes return without starting/queuing another network call. Inline phase08_29_env tests hold the actual background setup fixture through final status, verify competing parent mutation waits, aliases and launch/failure release. Preserve D-04 operation lifetime and existing errors. Do not require document save/create shared-admission proof here; Plan 07 produces and tests that conversion.</action>
  <acceptance_criteria>All six Skills-save/Skills-sync/Git-pull versus rename/trash pairs pass both orders and alias cases; registry access/removal and duplicate/skip semantics remain available during network/path waits; env lifetime and error release pass; app/CLI signatures compile.</acceptance_criteria>
  <verify>
    <automated>cargo test --manifest-path src-tauri/Cargo.toml --lib phase08_29</automated>
    <fails_when>Nonzero exit, zero tests, missing any produced skills/git/env case, no actual command invocation, waiter enters early, stale parent recreated, lost update, deadlock, registry access/removal blocked by network, duplicate starts/queues work, or skipped case.</fails_when>
    <automated>cargo check --manifest-path src-tauri/Cargo.toml -p maru-cli</automated>
    <fails_when>Nonzero exit.</fails_when>
  </verify>
  <done>All six Skills-save/Skills-sync/Git-pull versus rename/trash pairs pass both orders and alias cases; registry access/removal and duplicate/skip semantics remain available during network/path waits; env lifetime and error release pass; app/CLI signatures compile.</done>
</task>

<task type="auto" tdd="true">
  <name>29-2: Integrate dispatch callbacks and dot local mutation targets</name>
  <files>src-tauri/src/skill_host/dispatch.rs, src-tauri/src/agent_host/event_store.rs, src-tauri/src/mission_state.rs, src-tauri/src/dot_sync.rs</files>
  <read_first>src-tauri/src/skill_host/dispatch.rs; src-tauri/src/agent_host/event_store.rs; src-tauri/src/mission_state.rs; src-tauri/src/dot_sync.rs; src-tauri/src/atomic_file.rs; src-tauri/src/workspace_files.rs; .planning/phases/08-main-thread-responsiveness/08-16-PLAN.md; .planning/phases/08-main-thread-responsiveness/08-17-PLAN.md</read_first>
  <behavior>Real background dispatch event/mission writes and local dot mirror/peer mutations cannot enter while an overlapping parent is admitted; failures release and unrelated siblings progress.</behavior>
  <action>Audit actual dispatch synchronous setup, stream pumps and terminal/error callbacks. Protect run_events_path(cwd, invocation_id), cwd/.maru/runs/skills/invocation_id/events.jsonl, and mission ~/.maru/state/missions/id.json plus id.log before append_run_event_payload, register_mission_with_metadata, touch_output, finish_mission and fail_mission effects. Add explicit lease-taking adapters in event_store.rs and mission_state.rs alongside unchanged public sync outer entry points, so direct callers also admit before append_lock_for or MissionState.missions/pids guards; all persisted mutation paths in these two modules share this ordering. Capture the initiating existing cwd/home parent identity before dispatch and reuse it in callbacks, preventing late callbacks from recreating a moved/deleted workspace. Admit only finite Maru-owned event/mission transactions for proposal-only subprocess work, not a whole cwd for its indefinite lifetime. For approved file-writing subprocess execution, reserve its declared local cwd/add_dirs write roots through actual child exit and stream join; preserve existing approval boundaries, do not invent permission. Include event/mission keys in that complete set and pass the lease into callbacks rather than nested admission. External independently launched editors/daemons are outside in-process exclusion; Maru-launched owned mutation subprocesses are participants. Test bounded fake CLI lifecycle with actual filesystem writes and callback paths, no external credentials.

In dot_sync.rs discover actual action path sets before DOT_ACTION_LOCK via existing versioned status/config reads and validated request values. Cover RunMirror push/pull local source/destination (including local: targets), RunPeer local root and home-path destinations, configuration/filter/home-path files, state/log sidecars and setup/update installation targets. Resolve actual accepted schema fields from installed/source fixtures during execution, record exact source keys; never infer a local path from a remote host string. If a finite local write set cannot be resolved, return the existing contextual action error before launch, with no effects. Re-read the config/target identity after shared admission and DOT_ACTION_LOCK acquisition; reject a changed mapping before any mutation. Keep approval/secret acknowledgment and existing poison recovery. Retain admission through the owned finite subprocess's completion and local sidecar writes. Configure/pause/resume daemon actions protect their immediate files; independent later daemon writes remain explicitly outside process-local scope, not claimed safe. Read-only PeerDiff/ReadFilter/ReadLog paths have positive read-only evidence, not a mutation-lock exemption.

Inline phase08_29_dispatch and phase08_29_dot tests use actual production entry functions and temporary process fixtures against the existing rename/trash wrappers, both orders and alias paths, post-admission changed parent/config rejection, launch/runtime/error/unwind release and unrelated progress. Test no event/mission mutation while a parent is owned, no callback tree recreation, no nested admission deadlock, and dot always takes admission before DOT_ACTION_LOCK. Preserve original signatures via owned helpers and record exact adapters for later Plans 16/17.</action>
  <acceptance_criteria>Dispatch direct/background writes and dot finite local mutations all have complete path sets, admission-before-domain-lock and real contention/error tests; existing process lifetime, approvals and callback contracts remain valid.</acceptance_criteria>
  <verify>
    <automated>cargo test --manifest-path src-tauri/Cargo.toml --lib phase08_29</automated>
    <fails_when>Nonzero exit, zero tests, missing produced dispatch/dot pair/order/alias/callback case, effects before admission, unresolved local target launched, parent recreation, deadlock, lost event/mission record, or skipped case.</fails_when>
    <automated>cargo check --manifest-path src-tauri/Cargo.toml -p maru-cli</automated>
    <fails_when>Nonzero exit.</fails_when>
  </verify>
  <done>Dispatch direct/background writes and dot finite local mutations all have complete path sets, admission-before-domain-lock and real contention/error tests; existing process lifetime, approvals and callback contracts remain valid.</done>
</task>

<task type="auto">
  <name>29-3: Record integration evidence without duplicate command ownership</name>
  <files>docs/performance/phase08-29-integration.json, scripts/check-command-isolation.mjs, scripts/check-command-isolation.test.mjs</files>
  <read_first>scripts/check-command-isolation.mjs; scripts/check-command-isolation.test.mjs; docs/performance/phase08-01.json; docs/performance/phase08-03.json; docs/performance/phase08-04.json; docs/performance/phase08-05.json; .planning/phases/08-main-thread-responsiveness/08-PLAN-MAP.md; .planning/phases/08-main-thread-responsiveness/08-07-PLAN.md; .planning/phases/08-main-thread-responsiveness/08-28-PLAN.md</read_first>
  <action>Produce the integration overlay specified in PLAN-MAP with exact module/symbol/command references, final or staged integration ownership, lexical/alias sets and source keys, parent preconditions, source-reservation and domain-lock order, complete network/commit/background lifetimes, and tests actually run. Existing command shards stay unchanged and exclusive; the overlay updates integration conclusions by reference. Audit every earlier shard's actual writing helperChain including store defaults, nested env, dispatch callbacks and dot subprocess targets; fail if an entry lacks a record. Consume checker support from 04; add real overlay fixture coverage now that helpers exist. --integration 29 rejects missing writer, abbreviated checkout keys, missing alias/parent recheck, registry admission held over network, missing actual parent-pair/order case, duplicate inventory rows and conflicting ownership. Its documentRaceConsumer is exactly 08-07 with named future cases, which is an explicit produced-later obligation, not passing evidence. --all in 28 requires those actual 07 cases and completed 16/17 adapter handoffs. Record original commandCount=365 without adding a commands collection. Re-run source/batch regression tests after the integration to prove P08-01 remains resolved and D-01..D-05 are preserved.</action>
  <acceptance_criteria>Overlay validates all seven modules with five final and two staged owners, zero new command rows; 29's produced tests pass, 07 document evidence is explicitly pending until its producer, and source/batch regressions pass.</acceptance_criteria>
  <verify>
    <automated>node --test scripts/check-command-isolation.test.mjs</automated>
    <fails_when>Nonzero exit, zero tests, invalid overlay fixture accepted, valid staged producer fixture rejected, or skipped expected case.</fails_when>
    <automated>node scripts/check-command-isolation.mjs --integration 29</automated>
    <fails_when>Nonzero exit, missing seven-module coverage, duplicate command ownership, unresolved produced-case evidence, or future document evidence misrepresented as passed.</fails_when>
    <automated>cargo test --manifest-path src-tauri/Cargo.toml --lib phase08_source_transactions</automated>
    <fails_when>Nonzero exit, zero tests, missing nonblocking duplicate or network registry-availability case.</fails_when>
    <automated>cargo test --manifest-path src-tauri/Cargo.toml --lib phase08_batch_transactions</automated>
    <fails_when>Nonzero exit, zero tests, missing actual_wrapper_allows_async_progress or skipped-source case.</fails_when>
  </verify>
  <done>Overlay validates all seven modules with five final and two staged owners, zero new command rows; 29's produced tests pass, 07 document evidence is explicitly pending until its producer, and source/batch regressions pass.</done>
</task>
</tasks>

<threat_model>
OWASP ASVS L1; block on high. No new dependency or external integration.

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-08-29-01 | Tampering | Cross-domain checkout, install and sidecar writes | high | mitigate | 29-1/2 complete lexical/alias path admission and fresh-parent/config checks; real parent pairs in both orders; 07 produces document pairs. |
| T-08-29-02 | Denial of service | Source, path and domain lock ordering | high | mitigate | 29-1 nonblocking duplicate source admission, registry availability during network, no nested set extension; 29-2 callback lease propagation; errors/unwind release. |
| T-08-29-03 | Information disclosure | Subprocess/evidence boundary | medium | mitigate | Local synthetic fixtures, preserved approval/redaction/fixed argv; 29-3 overlay excludes credentials and live paths. |
</threat_model>

<verification>
Execute only after 06; all selectors must match non-skipped cases produced by the current or preceding task. 29 tests existing Skills/Git and parent wrappers; 07 subsequently produces actual document wrappers and the required document races. Cached focused checks target under 60 seconds; no benchmark or product test ran during planning.
</verification>

<success_criteria>
All task acceptance conditions hold. P08-02 has executable integration ownership, complete path/lock/lifetime evidence and real parent-race proof; 07 and 28 consume explicit later-document and final evidence obligations. No claimed protection against independent external processes, new command ownership, automatic retry or changed locked decision.
</success_criteria>

<output>
Create .planning/phases/08-main-thread-responsiveness/08-29-SUMMARY.md after execution with actual checks, staged ownership, threats and the pending 07 consumer obligation.
</output>
