---
phase: 08-main-thread-responsiveness
plan: "03"
type: execute
wave: 3
depends_on: ["08-02"]
files_modified: ["src-tauri/src/skill_host/store.rs","src-tauri/src/skill_host/mod.rs","src-tauri/src/cli.rs","docs/performance/phase08-03.json","src-tauri/src/lib.rs"]
autonomous: true
requirements: ["PERF-02","PERF-01"]
estimate:
  tokens: 45000
  raw_tokens: 45000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "Every remaining registry command executes blocking work inside a blocking worker."
    - "Clone, bundle and reconcile network requests leave REGISTRY_LOCK available."
    - "Source ownership, signed bundle checks and current registry contents survive parallel operations."
  artifacts:
    - path: "src-tauri/src/skill_host/store.rs"
      provides: "Remaining registry commands and lock-free network stages implementation or executable verification"
    - path: "src-tauri/src/skill_host/mod.rs"
      provides: "Remaining registry commands and lock-free network stages implementation or executable verification"
    - path: "src-tauri/src/cli.rs"
      provides: "Remaining registry commands and lock-free network stages implementation or executable verification"
    - path: "src-tauri/src/lib.rs"
      provides: "Remaining registry commands and lock-free network stages implementation or executable verification"
  key_links:
    - from: "src-tauri/src/skill_host/store.rs"
      to: "src-tauri/src/skill_host/mod.rs"
      via: "Existing registered command and explicit transaction/verification flow described in tasks"
  prohibitions:
    - statement: "No automatic sync retry, duplicate queue, new job center, or Phase 9 quit/terminal escalation."
      status: resolved
      verification: explicit
    - statement: "No live workspace, credentials, permission bypass, or native test hooks in production artifacts."
      status: resolved
      verification: explicit
---

<objective>
Remaining registry commands and lock-free network stages. Every remaining registry command executes blocking work inside a blocking worker. Clone, bundle and reconcile network requests leave REGISTRY_LOCK available. Source ownership, signed bundle checks and current registry contents survive parallel operations.
Purpose: satisfy PERF-02 and PERF-01 within the existing command/UI contracts.
Output: tested source changes and exact owned-command evidence.
</objective>

<execution_context>
@.claude/gsd-core/workflows/execute-plan.md
@.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@README.md
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/08-main-thread-responsiveness/08-CONTEXT.md
@.planning/phases/08-main-thread-responsiveness/08-RESEARCH.md
@.planning/phases/08-main-thread-responsiveness/08-PATTERNS.md
@.planning/phases/08-main-thread-responsiveness/08-PLAN-MAP.md
</context>

## Artifacts this phase produces

- Synchronous *_blocking helpers for converted store command names in this plan's ownership table
- Per-source transaction lease use for clone-add/reconcile and builtin bundle operations
- docs/performance/phase08-03.json closure evidence; inline phase08_03 tests
- store::ipc module with same-name wrappers for remaining registered store commands
- Inline test module/name prefix `phase08_03` where tasks specify Rust tests; existing tests stay available.
- New helper identifiers are limited to the explicitly listed names and `<owned_command>_blocking` for each converted command in the table below; these are proposed symbols, not existing APIs.



## Exclusive command ownership

- `src-tauri/src/skill_host/store.rs`: `skills_list_sources`, `skills_add_source`, `skills_remove_source`, `skills_rescan_source`, `skills_list_skills`, `skills_read_skill`, `skills_read_skill_file`, `skills_save_skill_file`, `skills_save_skill_as`, `skills_create_skill`, `skills_delete_skill`, `skills_list_installs`, `skills_install_skill`, `skills_uninstall_skill`, `skills_adopt_external_links`, `skills_reset_registry`, `skills_doctor`, `skills_list_dirty`, `skills_reconcile_skill`, `skills_import_external`, `skills_import_unmanage`, `skills_bundle_status`, `skills_check_bundle_update`, `skills_apply_bundle_update`.


## Earlier-writer integration handoff

This plan closes its exclusive command rows and preserves D-01..D-05. Shared hierarchical path admission is produced by 06 and integrated into these earlier writers by 29, which executes immediately after 06. Source leases and domain guards are prerequisites, not cross-domain exclusion. Record `integrationRequired: "08-29"` and `moduleIntegrationOwner: "08-29"` for store/env/dispatch/git/dot modules; command row ownership stays here. No task here imports or tests an unproduced Plan 06/29 symbol. Its focused checker accepts this explicit staged requirement only for these producer plans; final closure requires the actual 29 overlay and test evidence. Preserve owned synchronous entry points for that later integration, including background callbacks and source snapshot/commit seams.

<tasks>
<task type="auto">
  <name>03-1: Separate clone, reconcile and bundle network from commit</name>
  <reversibility rating="reversible">Internal scheduling and test changes preserve public command names and persisted data.</reversibility>
  <files>src-tauri/src/skill_host/store.rs, src-tauri/src/skill_host/mod.rs, src-tauri/src/cli.rs, src-tauri/src/lib.rs</files>
  <read_first>src-tauri/src/skill_host/store.rs; src-tauri/src/skill_host/mod.rs; src-tauri/src/cli.rs; src-tauri/src/lib.rs (new artifacts are created by this task if absent); README.md; docs/SSOT-TIERS.md; docs/BOUNDARIES.md; .planning/phases/08-main-thread-responsiveness/08-CONTEXT.md; .planning/phases/08-main-thread-responsiveness/08-PATTERNS.md; .planning/phases/08-main-thread-responsiveness/08-COMMAND-INVENTORY.md; src-tauri/src/hwped.rs; src-tauri/src/ipc_error.rs; src-tauri/src/lock_recovery.rs</read_first>
  <action>Use the Plan 01 source lease/snapshot pattern for skills_add_source cloning, skills_reconcile_skill Git work, skills_check_bundle_update and skills_apply_bundle_update network/env-repair phases. Capture/validate under registry_guard, perform network in a blocking worker without global registry lock, then reload and validate current source/path/tier/dirty hashes before guarded commit. Builtin bundle work uses its own stable source key and existing signed bundle swap/recovery semantics; preserve staging and checksums, never publish unverified files. Do not move a network-capable ensure_active_bundle call back under the registry lock. Enumerate all actual source mutators via save_registry_unlocked callers; ensure every configuration replacement and deletion updates Plan 01 generation state, including default source refresh/reset/adopt/import. Offload only IPC entry points, retain original public synchronous command functions for existing CLI/internal callers and add same-name async wrappers under store::ipc; update the corresponding generate_handler paths in lib.rs. Plan 01/02's sync wrappers keep their existing synchronous *_impl helpers. Keep skill_host/mod.rs reexports synchronous for CLI; no block_on. Add phase08_03_network_lock tests with barriers proving global registry access during clone/bundle/reconcile and stale rejection preserves latest source; include denied-owner and dirty-bundle rejection before effects.</action>
  <acceptance_criteria>No network round-trip retains global registry lock in these routes; CLI and app compile; local temporary fixtures exercise source generation through all actual mutation routes.</acceptance_criteria>
  <verify>
    <automated>cargo test --manifest-path src-tauri/Cargo.toml --lib phase08_03_network_lock</automated>
    <fails_when>Nonzero exit, zero matched/passed tests, missing named case, or an expected case skipped.</fails_when>
    <automated>cargo check --manifest-path src-tauri/Cargo.toml -p maru-cli</automated>
    <fails_when>Nonzero exit.</fails_when>
  </verify>
  <done>No network round-trip retains global registry lock in these routes; CLI and app compile; local temporary fixtures exercise source generation through all actual mutation routes.</done>
</task>

<task type="auto">
  <name>03-2: Close remaining registry command inventory and write races</name>
  <reversibility rating="reversible">Internal scheduling and test changes preserve public command names and persisted data.</reversibility>
  <files>src-tauri/src/skill_host/store.rs, docs/performance/phase08-03.json, src-tauri/src/lib.rs</files>
  <read_first>src-tauri/src/skill_host/store.rs; docs/performance/phase08-03.json; src-tauri/src/lib.rs (new artifacts are created by this task if absent); README.md; docs/SSOT-TIERS.md; docs/BOUNDARIES.md; .planning/phases/08-main-thread-responsiveness/08-CONTEXT.md; .planning/phases/08-main-thread-responsiveness/08-PATTERNS.md; .planning/phases/08-main-thread-responsiveness/08-COMMAND-INVENTORY.md; src-tauri/src/hwped.rs; src-tauri/src/ipc_error.rs; src-tauri/src/lock_recovery.rs</read_first>
  <action>Own all remaining store.rs command rows listed in PLAN-MAP, with this plan as blocking-disposition owner and Plan 29 as final cross-domain store integration owner. Convert CONVERT and every discovered blocking AUDIT helper chain to async spawn_blocking; preserve original sync helper use internally. For skills list/read/install/save/create/delete/import/adopt/reset/doctor paths keep current authorization, registry guard, source tier rules and fresh scans. Scope any newly concurrent checkout mutation with source lease admission; do not let clone/reconcile/write mutate a checkout owned by another source operation. Source removal must remain possible during sync and only removes metadata if deleting an active checkout would conflict. Explicitly disposition every AUDIT and existing isolated row with reachable helper chain, platform cfg and bound; no regex-only retention. Add inline phase08_03_boundary tests covering typed/legacy errors, nonempty scan results and parallel same-source writes without lost updates. Write the JSON evidence shard schema specified in PLAN-MAP, including tracer/batch module integration references but not duplicate command ownership. For remaining converted store functions use the same ipc submodule wrapper pattern and update only their lib.rs registration paths, retaining original synchronous functions and signatures.</action>
  <acceptance_criteria>All assigned rows have evidence and final disposition; simultaneous writes keep complete revisions or return existing conflict, not partial/lost records; no original guard is bypassed.</acceptance_criteria>
  <verify>
    <automated>cargo test --manifest-path src-tauri/Cargo.toml --lib phase08_03_boundary</automated>
    <fails_when>Nonzero exit, zero matched/passed tests, missing named case, or an expected case skipped.</fails_when>
    <automated>cargo check --manifest-path src-tauri/Cargo.toml -p maru-cli</automated>
    <fails_when>Nonzero exit.</fails_when>
  </verify>
  <done>All assigned rows have evidence and final disposition; simultaneous writes keep complete revisions or return existing conflict, not partial/lost records; no original guard is bypassed.</done>
</task>
</tasks>

<threat_model>
OWASP ASVS L1; block on high. No new dependency or external integration.

| Trust boundary | Protected invariant |
|---|---|
| Webview IPC to worker | Owned inputs retain backend permission, containment and revision validation |
| Worker to filesystem/process | Scoped serialization preserves current disk state and fixed argv |
| Test fixture to native process | Only disposable roots and compile-time gated observation |

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-08-03-01 | Tampering | Owned mutation paths | high | mitigate | Task 03-1 preserves or adds scoped transaction validation and denied-write tests before external effects. |
| T-08-03-02 | Denial of service | IPC scheduling and lifecycle | high | mitigate | Finite blocking closure, no guard across await, cleanup on failure/unwind; named task tests plus native saturation gate. |
| T-08-03-03 | Information disclosure | Errors and evidence | medium | mitigate | Preserve redaction, record synthetic fixture paths only, and final feature-off binary/frontend guards. |
</threat_model>

<verification>
Run each task's exact commands from the repository root; compiled tests must match at least one named case. Test paths introduced here are produced before their first consuming verification. Plans with native commands are slow integration gates; other focused checks target under 60 seconds with cached compilation. Never report pending execution as passed.
</verification>

<success_criteria>
Every task acceptance condition holds, assigned inventory rows have a final justified disposition, and PERF-02/PERF-01 evidence is recorded without claims about unrelated commands. Descriptor-less prohibitions remain flagged-unverified until verifier supplies explicit evidence; never auto-dismiss them.
</success_criteria>

<output>
Create .planning/phases/08-main-thread-responsiveness/08-03-SUMMARY.md after execution, including actual checks, owned rows and threat dispositions.
</output>
