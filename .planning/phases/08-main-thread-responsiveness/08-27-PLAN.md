---
phase: 08-main-thread-responsiveness
plan: "27"
type: execute
wave: 28
depends_on: ["08-26"]
files_modified: ["src-tauri/src/native_e2e.rs","src-tauri/src/lib.rs","src/lib/nativeE2eBridge.ts","e2e-native/wdio.conf.ts","e2e-native/helpers/fixtureWorkspace.ts","e2e-native/specs/responsiveness.spec.ts","e2e-native/helpers/responsivenessSamples.ts","src-tauri/src/git.rs","src-tauri/src/vault.rs","src-tauri/src/skill_host/store.rs"]
autonomous: true
requirements: ["PERF-01","PERF-02"]
estimate:
  tokens: 30000
  raw_tokens: 30000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "Four actual Git/scan/sync operations overlap while a real production command and same-runtime async probe remain responsive."
    - "D-01..D-05 native behavior is observed with actual source changes and screen navigation."
    - "A deliberately blocking-async control violates frozen latency bounds before any green harness result is accepted."
  artifacts:
    - path: "src-tauri/src/native_e2e.rs"
      provides: "Native saturation and source-race harness implementation or executable verification"
    - path: "src-tauri/src/lib.rs"
      provides: "Native saturation and source-race harness implementation or executable verification"
    - path: "src/lib/nativeE2eBridge.ts"
      provides: "Native saturation and source-race harness implementation or executable verification"
    - path: "e2e-native/wdio.conf.ts"
      provides: "Native saturation and source-race harness implementation or executable verification"
    - path: "e2e-native/helpers/fixtureWorkspace.ts"
      provides: "Native saturation and source-race harness implementation or executable verification"
    - path: "e2e-native/specs/responsiveness.spec.ts"
      provides: "Native saturation and source-race harness implementation or executable verification"
    - path: "e2e-native/helpers/responsivenessSamples.ts"
      provides: "Native saturation and source-race harness implementation or executable verification"
    - path: "src-tauri/src/git.rs"
      provides: "Native saturation and source-race harness implementation or executable verification"
    - path: "src-tauri/src/vault.rs"
      provides: "Native saturation and source-race harness implementation or executable verification"
    - path: "src-tauri/src/skill_host/store.rs"
      provides: "Native saturation and source-race harness implementation or executable verification"
  key_links:
    - from: "src-tauri/src/native_e2e.rs"
      to: "src-tauri/src/lib.rs"
      via: "Existing registered command and explicit transaction/verification flow described in tasks"
  prohibitions:
    - statement: "No automatic sync retry, duplicate queue, new job center, or Phase 9 quit/terminal escalation."
      status: resolved
      verification: explicit
    - statement: "No live workspace, credentials, permission bypass, or native test hooks in production artifacts."
      status: resolved
      verification: explicit
---

<objective>
Native saturation and source-race harness. Four actual Git/scan/sync operations overlap while a real production command and same-runtime async probe remain responsive. D-01..D-05 native behavior is observed with actual source changes and screen navigation. A deliberately blocking-async control violates frozen latency bounds before any green harness result is accepted.
Purpose: satisfy PERF-01 and PERF-02 within the existing command/UI contracts.
Output: tested source changes and exact owned-command evidence.
</objective>

<execution_context>
@.claude/gsd-core/workflows/execute-plan.md
@.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@README.md
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/08-main-thread-responsiveness/08-CONTEXT.md
@.planning/phases/08-main-thread-responsiveness/08-RESEARCH.md
@.planning/phases/08-main-thread-responsiveness/08-PATTERNS.md
@.planning/phases/08-main-thread-responsiveness/08-PLAN-MAP.md
</context>

## Artifacts this phase produces

- src-tauri/src/native_e2e.rs: install_test_runtime, native_e2e_async_probe, native_e2e_load_control, with_load_control; marker MARU_NATIVE_RESPONSIVENESS_HOOK
- Existing native-e2e Cargo feature gates module, commands and worker hooks; no new dependency
- nativeE2eBridge responsiveness allowlist methods; no arbitrary invoke/eval
- e2e-native/specs/responsiveness.spec.ts and helpers/responsivenessSamples.ts
- artifacts/native-responsiveness/calibration.json, thresholds.json, negative-control.json, fixed-run.json
- Inline test module/name prefix `phase08_27` where tasks specify Rust tests; existing tests stay available.
- New helper identifiers are limited to the explicitly listed names and `<owned_command>_blocking` for each converted command in the table below; these are proposed symbols, not existing APIs.

Spec-less flagged assumption PERF-01 concurrency: screen changes do not cancel operations; no app-quit durability claim (Phase 9). Latency SLO is an engineering acceptance bound calibrated before fixed testing, not a user-imposed product threshold. The unresolved fallback entry is kept visible until raw native evidence establishes it. Slow native tests may require per-test timeout 300000ms; do not increase product timeouts.



<tasks>
<task type="auto">
  <name>27-1: Install a real same-runtime probe and gated workload hooks</name>
  <reversibility rating="reversible">Internal scheduling and test changes preserve public command names and persisted data.</reversibility>
  <files>src-tauri/src/native_e2e.rs, src-tauri/src/lib.rs, src/lib/nativeE2eBridge.ts, e2e-native/wdio.conf.ts</files>
  <read_first>src-tauri/src/native_e2e.rs; src-tauri/src/lib.rs; src/lib/nativeE2eBridge.ts; e2e-native/wdio.conf.ts (new artifacts are created by this task if absent); README.md; docs/SSOT-TIERS.md; docs/BOUNDARIES.md; .planning/phases/08-main-thread-responsiveness/08-CONTEXT.md; .planning/phases/08-main-thread-responsiveness/08-PATTERNS.md; .planning/phases/08-main-thread-responsiveness/08-COMMAND-INVENTORY.md; src-tauri/src/hwped.rs; src-tauri/src/ipc_error.rs; src-tauri/src/lock_recovery.rs</read_first>
  <action>Compile every Rust hook and registration only under cfg(feature="native-e2e"). Before any Tauri runtime initialization in run(), call install_test_runtime: set TOKIO_WORKER_THREADS=2 in launcher before process spawn, create tauri::async_runtime::TokioRuntime::new(), assert its handle metrics reports num_workers=2, store Runtime for process lifetime and install its cloned handle with tauri::async_runtime::set. Report observed worker count from native_e2e_async_probe, which performs a std::future::poll_fn one-yield asynchronous handshake on that same runtime and never uses spawn_blocking. Add feature-only control with allowlisted operation IDs, fixture-root validation, overlap counters and mode isolated/blocking_async. Expose nativeE2eBridge narrowly allowlisted calls for git_status, scan_vault, skills_sync_source, skills_sync_all_sources, skills_remove_source and the two test commands using imported invoke; expose no arbitrary command/path. Repeat literal VITE_NATIVE_E2E gate per registration. native_e2e_load_control also exercises existing source mutator seams on fixture-owned IDs for change/revert, not a new source edit command. Keep default builds with no runtime override and preserve Phase 6 runner lifecycle. Create inline phase08_native_harness tests in native_e2e.rs for allowlist refusal, fixture path rejection, worker count, probe yield and control reset; these are produced here before the verification command.</action>
  <acceptance_criteria>Feature-on app reports exactly two workers from installed runtime, async probe yields correctly, arbitrary command/path requests are refused, default build cannot compile references to test module outside cfg.</acceptance_criteria>
  <verify>
    <automated>cargo test --manifest-path src-tauri/Cargo.toml --features native-e2e --lib phase08_native_harness</automated>
    <fails_when>Nonzero exit, zero matched/passed tests, missing named case, or an expected case skipped.</fails_when>
    <automated>pnpm typecheck</automated>
    <fails_when>Nonzero exit.</fails_when>
  </verify>
  <done>Feature-on app reports exactly two workers from installed runtime, async probe yields correctly, arbitrary command/path requests are refused, default build cannot compile references to test module outside cfg.</done>
</task>

<task type="auto">
  <name>27-2: Drive actual workloads with deterministic overlap</name>
  <reversibility rating="reversible">Internal scheduling and test changes preserve public command names and persisted data.</reversibility>
  <files>src-tauri/src/git.rs, src-tauri/src/vault.rs, src-tauri/src/skill_host/store.rs, e2e-native/helpers/fixtureWorkspace.ts, e2e-native/helpers/responsivenessSamples.ts</files>
  <read_first>src-tauri/src/git.rs; src-tauri/src/vault.rs; src-tauri/src/skill_host/store.rs; e2e-native/helpers/fixtureWorkspace.ts; e2e-native/helpers/responsivenessSamples.ts (new artifacts are created by this task if absent); README.md; docs/SSOT-TIERS.md; docs/BOUNDARIES.md; .planning/phases/08-main-thread-responsiveness/08-CONTEXT.md; .planning/phases/08-main-thread-responsiveness/08-PATTERNS.md; .planning/phases/08-main-thread-responsiveness/08-COMMAND-INVENTORY.md; src-tauri/src/hwped.rs; src-tauri/src/ipc_error.rs; src-tauri/src/lock_recovery.rs</read_first>
  <action>At the already converted git_status, scan_vault and skills_sync_source paths call feature-gated with_load_control around the real domain work. Isolated mode holds a 2500ms deterministic pre-work interval inside each real blocking closure; blocking_async control executes the same work/interval directly in its async wrapper, only under native-e2e. Seed two disposable Git repos with modified tracked files, a 2000-file Markdown tree and two cloned skills sources with local bare remotes and pending commits. Start git_status(repo A), git_status(repo B), scan_vault(tree) and skills_sync_source(source A) concurrently through actual registrations, requiring four overlap intervals in isolated mode, nonempty statuses, >=2000 scanned entries and expected updated skill. Measure inside webview performance.now around IPC promises, not WDIO calls; sample probe and unrelated production parse_korean_date_cmd at a 25ms target cadence, retaining actual dispatch/settlement times. Sample 40 warm-up then 120 idle, 120 loaded and 120 recovery observations per round, three rounds. Make held workload repeat per loaded window only as explicitly initiated test work, never an automatic product retry. Preserve raw samples and fixture metadata outside temp cleanup. Call parse_korean_date_cmd with input='내일', nowIso='2026-09-05T09:00:00+09:00' and assert a non-null next-day result. Add it explicitly to the bridge production-command allowlist.</action>
  <acceptance_criteria>Loaded isolated window demonstrates four real active operations; each sample is timed in the webview; nonempty production outcomes prove no-op wrappers cannot pass; fixture paths remain isolated.</acceptance_criteria>
  <verify>
    <automated>cargo test --manifest-path src-tauri/Cargo.toml --features native-e2e --lib phase08_native_harness</automated>
    <fails_when>Nonzero exit, zero matched/passed tests, missing named case, or an expected case skipped.</fails_when>
    <automated>pnpm typecheck</automated>
    <fails_when>Nonzero exit.</fails_when>
  </verify>
  <done>Loaded isolated window demonstrates four real active operations; each sample is timed in the webview; nonempty production outcomes prove no-op wrappers cannot pass; fixture paths remain isolated.</done>
</task>

<task type="auto">
  <name>27-3: Calibrate, falsify and prove responsiveness and navigation</name>
  <reversibility rating="reversible">Internal scheduling and test changes preserve public command names and persisted data.</reversibility>
  <files>e2e-native/specs/responsiveness.spec.ts, e2e-native/helpers/responsivenessSamples.ts</files>
  <read_first>e2e-native/specs/responsiveness.spec.ts; e2e-native/helpers/responsivenessSamples.ts (new artifacts are created by this task if absent); README.md; docs/SSOT-TIERS.md; docs/BOUNDARIES.md; .planning/phases/08-main-thread-responsiveness/08-CONTEXT.md; .planning/phases/08-main-thread-responsiveness/08-PATTERNS.md; .planning/phases/08-main-thread-responsiveness/08-COMMAND-INVENTORY.md; src-tauri/src/hwped.rs; src-tauri/src/ipc_error.rs; src-tauri/src/lock_recovery.rs</read_first>
  <action>Create native specs that first record three idle calibration rounds with the fixed cadence/warm-up above. Freeze thresholds.json before load: for both probe series loaded/recovery p95 <= max(calibrated idle p95*2, calibrated idle p95+25ms) and max stall <=250ms. Include fixed sample count, max missing samples=0, worker_count=2 and nonempty results. If baseline alone exceeds 250ms, report noise/inconclusive and investigate; never widen thresholds until green. Run blocking_async negative control and require actual probe latency-bound violation, not only an overlap or setup failure; persist red evidence, restore isolated mode in finally, reset counters and run three full idle/load/recovery rounds with frozen thresholds. Native D-01..D-05 cases hold real sync, invoke remove/change/revert through real transaction seams, invoke duplicate and Sync All while A runs, and assert B success persisted while one source fails once. Start sync/file processing in existing UI, change mode/workspace, resolve/reject, observe exactly one existing-surface notice and unchanged navigation, then return and verify disk results. Export build revision, runtime metrics, raw samples, medians/p95/max, overlap times, outcomes, fixture counts and threshold hash; no performance success if any case skips.</action>
  <acceptance_criteria>Negative control demonstrably breaks latency bound; restored isolated case meets frozen bounds in all rounds; native decision flows pass without mocks or live credentials.</acceptance_criteria>
  <verify>
    <automated>make test-e2e-native</automated>
    <fails_when>Nonzero exit, zero matched/passed tests, missing named case, or an expected case skipped.</fails_when>
  </verify>
  <done>Negative control demonstrably breaks latency bound; restored isolated case meets frozen bounds in all rounds; native decision flows pass without mocks or live credentials.</done>
</task>
</tasks>

<threat_model>
OWASP ASVS L1; block on high. No new dependency or external integration.

| Trust boundary | Protected invariant |
|---|---|
| Webview IPC to worker | Owned inputs retain backend permission, containment and revision validation |
| Worker to filesystem/process | Scoped serialization preserves current disk state and fixed argv |
| Test fixture to native process | Only disposable roots and compile-time gated observation |

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-08-27-01 | Tampering | Owned mutation paths | high | mitigate | Task 27-1 preserves or adds scoped transaction validation and denied-write tests before external effects. |
| T-08-27-02 | Denial of service | IPC scheduling and lifecycle | high | mitigate | Finite blocking closure, no guard across await, cleanup on failure/unwind; named task tests plus native saturation gate. |
| T-08-27-03 | Information disclosure | Errors and evidence | medium | mitigate | Preserve redaction, record synthetic fixture paths only, and final feature-off binary/frontend guards. |
</threat_model>

<verification>
Run each task's exact commands from the repository root; compiled tests must match at least one named case. Test paths introduced here are produced before their first consuming verification. Plans with native commands are slow integration gates; other focused checks target under 60 seconds with cached compilation. Never report pending execution as passed.
</verification>

<success_criteria>
Every task acceptance condition holds, assigned inventory rows have a final justified disposition, and PERF-01/PERF-02 evidence is recorded without claims about unrelated commands. Descriptor-less prohibitions remain flagged-unverified until verifier supplies explicit evidence; never auto-dismiss them.
</success_criteria>

<output>
Create .planning/phases/08-main-thread-responsiveness/08-27-SUMMARY.md after execution, including actual checks, owned rows and threat dispositions.
</output>
