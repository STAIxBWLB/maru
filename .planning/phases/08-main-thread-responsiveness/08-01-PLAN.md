---
phase: 08-main-thread-responsiveness
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified: ["src-tauri/src/skill_host/store.rs","src-tauri/src/skill_host/fs.rs","src/lib/skills.ts","e2e-native/helpers/fixtureWorkspace.ts","e2e-native/specs/skills-sync.spec.ts","docs/performance/phase08-01.json"]
autonomous: true
requirements: ["PERF-02","PERF-01"]
estimate:
  tokens: 30000
  raw_tokens: 30000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "D-01: Registry access is available during Git pull; deletion and changed source identity reject stale write-back."
    - "D-02 and D-05: Duplicate source sync returns immediately without queuing; failure releases ownership and retry is manual."
    - "The existing Skills UI invokes the real async Rust boundary and a local Git source produces persisted skill records."
  artifacts:
    - path: "src-tauri/src/skill_host/store.rs"
      provides: "Single-source synchronization tracer implementation or executable verification"
    - path: "src/lib/skills.ts"
      provides: "Single-source synchronization tracer implementation or executable verification"
    - path: "e2e-native/helpers/fixtureWorkspace.ts"
      provides: "Single-source synchronization tracer implementation or executable verification"
    - path: "e2e-native/specs/skills-sync.spec.ts"
      provides: "Single-source synchronization tracer implementation or executable verification"
  key_links:
    - from: "src-tauri/src/skill_host/store.rs"
      to: "src/lib/skills.ts"
      via: "Existing registered command and explicit transaction/verification flow described in tasks"
  prohibitions:
    - statement: "No automatic sync retry, duplicate queue, new job center, or Phase 9 quit/terminal escalation."
      status: resolved
      verification: explicit
    - statement: "No live workspace, credentials, permission bypass, or native test hooks in production artifacts."
      status: resolved
      verification: explicit
---

<objective>
Single-source synchronization tracer. D-01: Registry access is available during Git pull; deletion and changed source identity reject stale write-back. D-02 and D-05: Duplicate source sync returns immediately without queuing; failure releases ownership and retry is manual. The existing Skills UI invokes the real async Rust boundary and a local Git source produces persisted skill records.
Purpose: satisfy PERF-02 and PERF-01 within the existing command/UI contracts.
Output: tested source changes and exact owned-command evidence.
</objective>

<execution_context>
@.claude/gsd-core/workflows/execute-plan.md
@.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@README.md
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/08-main-thread-responsiveness/08-CONTEXT.md
@.planning/phases/08-main-thread-responsiveness/08-RESEARCH.md
@.planning/phases/08-main-thread-responsiveness/08-PATTERNS.md
@.planning/phases/08-main-thread-responsiveness/08-PLAN-MAP.md
</context>

## Artifacts this phase produces

- SourceOperationLease and SourceSnapshot in store.rs
- admit_source_operation, capture_source_snapshot, sync_source_transaction and ensure_source_generation in store.rs
- Process-local SourceGeneration { incarnation: Uuid, config_revision: Uuid } with registry-path/source-id keys; no persisted schema change
- e2e-native/specs/skills-sync.spec.ts native happy-path tracer; phase08_source_transactions Rust tests
- docs/performance/phase08-01.json
- SourceOperationLease checkout_key: canonical Git worktree identity alongside source key
- Inline test module/name prefix `phase08_01` where tasks specify Rust tests; existing tests stay available.
- New helper identifiers are limited to the explicitly listed names and `<owned_command>_blocking` for each converted command in the table below; these are proposed symbols, not existing APIs.

Spec-less flagged assumptions: PERF-02 boundary is defined as absent/present source and each transaction admission/network/commit edge; test all three. PERF-02 precision uses opaque UUID equality, no numeric generation arithmetic; empty/singleton batches are tested in 02. PERF-02 concurrency guarantee is in-process app mutations plus fresh disk reload, not cross-process registry serialization. All three unresolved fallback entries remain visible here until execution supplies explicit evidence. Reversibility: reversible process-local metadata; no migration. store.rs is staged across 01, 02, 03 and 29; 03 closes blocking dispositions and 29 owns final cross-domain integration, and each registered command has exactly one row owner in PLAN-MAP.

## Exclusive command ownership

- `src-tauri/src/skill_host/store.rs`: `skills_sync_source`.


## Earlier-writer integration handoff

This plan closes its exclusive command rows and preserves D-01..D-05. Shared hierarchical path admission is produced by 06 and integrated into these earlier writers by 29, which executes immediately after 06. Source leases and domain guards are prerequisites, not cross-domain exclusion. Record `integrationRequired: "08-29"` and `moduleIntegrationOwner: "08-29"` for store/env/dispatch/git/dot modules; command row ownership stays here. No task here imports or tests an unproduced Plan 06/29 symbol. Its focused checker accepts this explicit staged requirement only for these producer plans; final closure requires the actual 29 overlay and test evidence. Preserve owned synchronous entry points for that later integration, including background callbacks and source snapshot/commit seams.

<tasks>
<task type="tracer">
  <name>01-1: Prove one real Skills sync through the native UI</name>
  <reversibility rating="reversible">Internal scheduling and test changes preserve public command names and persisted data.</reversibility>
  <files>src-tauri/src/skill_host/store.rs, src-tauri/src/skill_host/fs.rs, e2e-native/helpers/fixtureWorkspace.ts, e2e-native/specs/skills-sync.spec.ts</files>
  <read_first>src-tauri/src/skill_host/store.rs; e2e-native/helpers/fixtureWorkspace.ts; e2e-native/specs/skills-sync.spec.ts (new artifacts are created by this task if absent); README.md; docs/SSOT-TIERS.md; docs/BOUNDARIES.md; .planning/phases/08-main-thread-responsiveness/08-CONTEXT.md; .planning/phases/08-main-thread-responsiveness/08-PATTERNS.md; .planning/phases/08-main-thread-responsiveness/08-COMMAND-INVENTORY.md; src-tauri/src/hwped.rs; src-tauri/src/ipc_error.rs; src-tauri/src/lock_recovery.rs</read_first>
  <action>Per D-01, D-02 and D-05, make skills_sync_source an async command awaiting spawn_blocking; construct ProgressReporter inside the closure from owned AppHandle/progress_id. Keep skills_sync_source_impl synchronous for internal callers. Implement SourceOperationLease using a fail-closed Mutex<HashSet<(registry_path, source_id)>>: try admission by set insertion, never wait for another source operation or queue duplicates; RAII removes only the owned key on every return and unwind. Capture a process-local incarnation UUID plus config_revision UUID while holding registry_guard; every source creation/removal/config replacement changes these tokens, including ensure_default_sources, upsert_linked_source, reset/adopt/import and bundle replacement seams. Refresh metadata from the latest loaded registry before capture. A settings signature comparison is additional protection, not the ABA detector. Release registry guard before git pull --ff-only, then reacquire, reload registry, require identical incarnation/revision/config/path, rescan the current source under the guard and save that latest registry. A missing source wins; no stale result or automatic retry. Retain String errors here using distinct display messages until Plan 02 introduces typed busy/stale codes. Seed a disposable bare Git remote, cloned source, committed SKILL.md and changed remote commit under native fixture home before launch. Create skills-sync.spec.ts that clicks the existing Skills sync control, observes progress and verifies persisted skill data from the local fixture, without mocks or arbitrary invoke bridge. Preserve root reset/cleanup semantics. Admission additionally reserves the canonical Git worktree root (resolve symlinks with canonicalize and git rev-parse --show-toplevel before the transaction; no lock is held during that subprocess). Distinct source IDs aliasing the same checkout return busy instead of executing concurrent Git mutations. Acquire ID and checkout reservations atomically under one short lease-map guard; lock order is lease bookkeeping then release it, registry guard, with no nested wait. Metadata removal remains allowed; filesystem deletion of an active checkout is rejected. Tests include linked source aliases and nested skills subdirectories in the same repository.</action>
  <acceptance_criteria>Real native sync produces the expected changed skill title; fixture registry contains the same source and new record; no live home is read; an unrelated registry read completes while injected network seam is held in Rust tests.</acceptance_criteria>
  <verify>
    <automated>make test-e2e-native</automated>
    <fails_when>Nonzero exit, zero matched/passed tests, missing named case, or an expected case skipped.</fails_when>
    <automated>cargo test --manifest-path src-tauri/Cargo.toml --lib phase08_source_transactions</automated>
    <fails_when>Nonzero exit, zero matched/passed tests, missing named case, or an expected case skipped.</fails_when>
  </verify>
  <done>Real native sync produces the expected changed skill title; fixture registry contains the same source and new record; no live home is read; an unrelated registry read completes while injected network seam is held in Rust tests.</done>
</task>

<task type="auto">
  <name>01-2: Prove source race edges and legacy caller compatibility</name>
  <reversibility rating="reversible">Internal scheduling and test changes preserve public command names and persisted data.</reversibility>
  <files>src-tauri/src/skill_host/store.rs, src/lib/skills.ts, docs/performance/phase08-01.json</files>
  <read_first>src-tauri/src/skill_host/store.rs; src/lib/skills.ts; docs/performance/phase08-01.json (new artifacts are created by this task if absent); README.md; docs/SSOT-TIERS.md; docs/BOUNDARIES.md; .planning/phases/08-main-thread-responsiveness/08-CONTEXT.md; .planning/phases/08-main-thread-responsiveness/08-PATTERNS.md; .planning/phases/08-main-thread-responsiveness/08-COMMAND-INVENTORY.md; src-tauri/src/hwped.rs; src-tauri/src/ipc_error.rs; src-tauri/src/lock_recovery.rs</read_first>
  <action>Add deterministic barrier tests in store.rs under phase08_source_transactions. Exercise unrelated edit preservation; source delete; identical remove/re-add; config change; config change/revert through upsert_linked_source/actual application mutator seams; duplicate admission has exactly one network call; failure and catch_unwind release the lease; another source can progress. Test immediately before admission, while network is blocked and just before commit. Use UUID equality rather than numeric counters, eliminating overflow/rounding. Process-local tokens explicitly cover mutations in the running application only; compare fresh disk configuration too, but do not claim multi-process transactional locking or offline identical ABA detection. Keep registry_guard's existing Phase 7 recover_guard justification; do not recover the invariant-bearing lease mutex with the same disk-only argument. Keep work mutation running independently of the Rust future's initiating view. Assert error text reaches the existing Skills wrapper unchanged and that synchronous internal calls remain synchronous. Write docs/performance/phase08-01.json with exactly the owned command rows and schema in 08-PLAN-MAP.md; test evidence remains honest and nonempty. Add source alias contention, empty path rejection and canonical symlink identity cases; verify exactly one network call across alias IDs.</action>
  <acceptance_criteria>Every named race case runs with nonempty tests and barrier handshakes, old generation never commits, one explicit retry after failure succeeds, and no new on-disk format or source-edit UI is introduced.</acceptance_criteria>
  <verify>
    <automated>cargo test --manifest-path src-tauri/Cargo.toml --lib phase08_source_transactions</automated>
    <fails_when>Nonzero exit, zero matched/passed tests, missing named case, or an expected case skipped.</fails_when>
    <automated>cargo check --manifest-path src-tauri/Cargo.toml -p maru-cli</automated>
    <fails_when>Nonzero exit.</fails_when>
  </verify>
  <done>Every named race case runs with nonempty tests and barrier handshakes, old generation never commits, one explicit retry after failure succeeds, and no new on-disk format or source-edit UI is introduced.</done>
</task>
</tasks>

<threat_model>
OWASP ASVS L1; block on high. No new dependency or external integration.

| Trust boundary | Protected invariant |
|---|---|
| Webview IPC to worker | Owned inputs retain backend permission, containment and revision validation |
| Worker to filesystem/process | Scoped serialization preserves current disk state and fixed argv |
| Test fixture to native process | Only disposable roots and compile-time gated observation |

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-08-01-01 | Tampering | Owned mutation paths | high | mitigate | Task 01-1 preserves or adds scoped transaction validation and denied-write tests before external effects. |
| T-08-01-02 | Denial of service | IPC scheduling and lifecycle | high | mitigate | Finite blocking closure, no guard across await, cleanup on failure/unwind; named task tests plus native saturation gate. |
| T-08-01-03 | Information disclosure | Errors and evidence | medium | mitigate | Preserve redaction, record synthetic fixture paths only, and final feature-off binary/frontend guards. |
</threat_model>

<verification>
Run each task's exact commands from the repository root; compiled tests must match at least one named case. Test paths introduced here are produced before their first consuming verification. Plans with native commands are slow integration gates; other focused checks target under 60 seconds with cached compilation. Never report pending execution as passed.
</verification>

<success_criteria>
Every task acceptance condition holds, assigned inventory rows have a final justified disposition, and PERF-02/PERF-01 evidence is recorded without claims about unrelated commands. Descriptor-less prohibitions remain flagged-unverified until verifier supplies explicit evidence; never auto-dismiss them.
</success_criteria>

<output>
Create .planning/phases/08-main-thread-responsiveness/08-01-SUMMARY.md after execution, including actual checks, owned rows and threat dispositions.
</output>
