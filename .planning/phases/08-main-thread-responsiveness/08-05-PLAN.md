---
phase: 08-main-thread-responsiveness
plan: "05"
type: execute
wave: 5
depends_on: ["08-04"]
files_modified: ["src-tauri/src/git.rs","src-tauri/src/dot_sync.rs","src-tauri/src/lib.rs","docs/performance/phase08-05.json"]
autonomous: true
requirements: ["PERF-01"]
estimate:
  tokens: 30000
  raw_tokens: 30000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "Git and workspace sync reaches no network, process or unbounded walk on the UI or shared async worker thread."
    - "All 11 owned commands preserve wire types, permission checks and synchronous internal callers."
    - "Concurrent operations preserve lifecycle ordering and current disk state; each retained/native UI exception has positive evidence."
  artifacts:
    - path: "src-tauri/src/git.rs"
      provides: "Git and workspace sync command isolation implementation or executable verification"
    - path: "src-tauri/src/dot_sync.rs"
      provides: "Git and workspace sync command isolation implementation or executable verification"
    - path: "src-tauri/src/lib.rs"
      provides: "Git and workspace sync command isolation implementation or executable verification"
  key_links:
    - from: "src-tauri/src/git.rs"
      to: "src-tauri/src/dot_sync.rs"
      via: "Existing registered command and explicit transaction/verification flow described in tasks"
  prohibitions:
    - statement: "No automatic sync retry, duplicate queue, new job center, or Phase 9 quit/terminal escalation."
      status: resolved
      verification: explicit
    - statement: "No live workspace, credentials, permission bypass, or native test hooks in production artifacts."
      status: resolved
      verification: explicit
---

<objective>
Git and workspace sync command isolation. Git and workspace sync reaches no network, process or unbounded walk on the UI or shared async worker thread. All 11 owned commands preserve wire types, permission checks and synchronous internal callers. Concurrent operations preserve lifecycle ordering and current disk state; each retained/native UI exception has positive evidence.
Purpose: satisfy PERF-01 within the existing command/UI contracts.
Output: tested source changes and exact owned-command evidence.
</objective>

<execution_context>
@.claude/gsd-core/workflows/execute-plan.md
@.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@README.md
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/08-main-thread-responsiveness/08-CONTEXT.md
@.planning/phases/08-main-thread-responsiveness/08-RESEARCH.md
@.planning/phases/08-main-thread-responsiveness/08-PATTERNS.md
@.planning/phases/08-main-thread-responsiveness/08-PLAN-MAP.md
</context>

## Artifacts this phase produces

- src-tauri/src/git.rs: pub mod ipc with async same-name wrappers for each converted owned command; original sync functions retained
- src-tauri/src/dot_sync.rs: pub mod ipc with async same-name wrappers for each converted owned command; original sync functions retained
- docs/performance/phase08-05.json final command evidence
- Inline phase08_05 behavioral tests
- Owned-input <command>_blocking helpers only where State/borrow adaptation is required, retaining original synchronous API signatures
- Inline test module/name prefix `phase08_05` where tasks specify Rust tests; existing tests stay available.
- New helper identifiers are limited to the explicitly listed names and `<owned_command>_blocking` for each converted command in the table below; these are proposed symbols, not existing APIs.

Registration edits share lib.rs, so this plan depends on the immediately preceding tranche. This deliberately avoids parallel same-file conflicts while preserving independently green app/CLI builds. No extra dispatcher or build infrastructure is introduced to manufacture parallelism. Existing implementation helper modules are read-only unless explicitly listed; place necessary boundary serialization in the owned controller or reuse the existing domain guard.

## Exclusive command ownership

- `src-tauri/src/git.rs`: `git_status`, `git_changes`, `git_diff`, `git_generate_commit_message`, `git_sync_scan`, `list_workspace_submodules`, `git_sync_pull_rebase`, `git_sync_commit_push`, `git_commit`.
- `src-tauri/src/dot_sync.rs`: `dot_sync_overview`, `dot_sync_run`.


## Earlier-writer integration handoff

This plan closes its exclusive command rows and preserves D-01..D-05. Shared hierarchical path admission is produced by 06 and integrated into these earlier writers by 29, which executes immediately after 06. Source leases and domain guards are prerequisites, not cross-domain exclusion. Record `integrationRequired: "08-29"` and `moduleIntegrationOwner: "08-29"` for store/env/dispatch/git/dot modules; command row ownership stays here. No task here imports or tests an unproduced Plan 06/29 symbol. Its focused checker accepts this explicit staged requirement only for these producer plans; final closure requires the actual 29 overlay and test evidence. Preserve owned synchronous entry points for that later integration, including background callbacks and source snapshot/commit seams.

<tasks>
<task type="auto">
  <name>05-1: Isolate src/git.rs, src/dot_sync.rs</name>
  <reversibility rating="reversible">Internal scheduling and test changes preserve public command names and persisted data.</reversibility>
  <files>src-tauri/src/git.rs, src-tauri/src/dot_sync.rs, src-tauri/src/lib.rs</files>
  <read_first>src-tauri/src/git.rs; src-tauri/src/dot_sync.rs; src-tauri/src/lib.rs (new artifacts are created by this task if absent); README.md; docs/SSOT-TIERS.md; docs/BOUNDARIES.md; .planning/phases/08-main-thread-responsiveness/08-CONTEXT.md; .planning/phases/08-main-thread-responsiveness/08-PATTERNS.md; .planning/phases/08-main-thread-responsiveness/08-COMMAND-INVENTORY.md; src-tauri/src/hwped.rs; src-tauri/src/ipc_error.rs; src-tauri/src/lock_recovery.rs</read_first>
  <action>Own and close these exact command rows: src-tauri/src/git.rs: git_status, git_changes, git_diff, git_generate_commit_message, git_sync_scan, list_workspace_submodules, git_sync_pull_rebase, git_sync_commit_push, git_commit; src-tauri/src/dot_sync.rs: dot_sync_overview, dot_sync_run. Read every body and all reached helpers including cfg/platform branches, traits/callbacks and command(async) bodies. For blocking synchronous functions, retain the original public synchronous function and its name for Rust/CLI callers; remove its command attribute and add a same-name async #[tauri::command] function inside this module's new pub mod ipc. That wrapper owns its inputs and awaits tauri::async_runtime::spawn_blocking calling super::the_original_name. Update only corresponding generate_handler paths in lib.rs to module::ipc::command; the wire name is unchanged. Existing genuinely async/isolated/UI entries keep their original registration path unless their body needs a finite blocking substep. Construct ProgressReporter inside worker; clone State's owned Arc/handle rather than carrying borrowed State or guards across await. Preserve Result<T,String> versus Result<T,IpcError>; inner typed errors pass through untouched, while JoinError becomes contextual display-only error of the same outer type. Do not use block_on or an executor/dispatch registry. Keep dot_sync's DOT_ACTION_LOCK recovery and approval checks; use local repositories with fixed argv. Preserve git fast-forward/rebase and commit/push policy without invoking remote services in tests. Add inline tests with prefix phase08_05 in modified modules: nonempty real fixture result, unchanged typed/legacy rejection, and at least one deterministic overlap case where a blocked worker still permits a same-runtime async yield. For every newly parallel write, inventory its transaction key and test same-target contention; keep authorization and revision validation inside the worker/transaction. Per D-04, annotate each user-started processing row's frontend call sites and existing operation completion ownership in this shard; Plan 25/26 implements the shared lifetime path, not a new job service. For a synchronous function that accepts State or another borrowed runtime object, extract an owned-input synchronous <command>_blocking helper for the actual work; both the unchanged public synchronous function and the async ipc wrapper call that helper. The wrapper clones the Arc/handle from State before awaiting. Do not fabricate a State or send a borrowed guard/reporter into spawn_blocking. Define and list exact owned argument types in the implementation summary.</action>
  <acceptance_criteria>All commands named by this task compile with unchanged wire names and synchronous Rust call sites. Named phase08_05 tests run; blocking body and lock waits execute in a blocking worker; no lost update or permission bypass. </acceptance_criteria>
  <verify>
    <automated>cargo test --manifest-path src-tauri/Cargo.toml --lib phase08_05</automated>
    <fails_when>Nonzero exit, zero matched/passed tests, missing named case, or an expected case skipped.</fails_when>
    <automated>cargo check --manifest-path src-tauri/Cargo.toml -p maru-cli</automated>
    <fails_when>Nonzero exit.</fails_when>
  </verify>
  <done>All commands named by this task compile with unchanged wire names and synchronous Rust call sites. Named phase08_05 tests run; blocking body and lock waits execute in a blocking worker; no lost update or permission bypass. </done>
</task>

<task type="auto">
  <name>05-2: Prove lifecycle and error contracts</name>
  <reversibility rating="reversible">Internal scheduling and test changes preserve public command names and persisted data.</reversibility>
  <files>src-tauri/src/git.rs, docs/performance/phase08-05.json</files>
  <read_first>src-tauri/src/git.rs; docs/performance/phase08-05.json (new artifacts are created by this task if absent); README.md; docs/SSOT-TIERS.md; docs/BOUNDARIES.md; .planning/phases/08-main-thread-responsiveness/08-CONTEXT.md; .planning/phases/08-main-thread-responsiveness/08-PATTERNS.md; .planning/phases/08-main-thread-responsiveness/08-COMMAND-INVENTORY.md; src-tauri/src/hwped.rs; src-tauri/src/ipc_error.rs; src-tauri/src/lock_recovery.rs</read_first>
  <action>Extend the already converted module's inline tests to cover error/cleanup and concurrent operations through the actual wrapper. Keep dot_sync's DOT_ACTION_LOCK recovery and approval checks; use local repositories with fixed argv. Preserve git fast-forward/rebase and commit/push policy without invoking remote services in tests. Add inline tests with prefix phase08_05 in modified modules: nonempty real fixture result, unchanged typed/legacy rejection, and at least one deterministic overlap case where a blocked worker still permits a same-runtime async yield. For every newly parallel write, inventory its transaction key and test same-target contention; keep authorization and revision validation inside the worker/transaction. Write docs/performance/phase08-05.json with every owned row exactly once, final disposition and decisive helper/affinity/bound plus tests and current registration path. Zero AUDIT; retained/UI/ISOLATED rows require positive evidence too. Preserve other shards and the research inventory; final merge is Plan 28. Per D-04, annotate each user-started processing row's frontend call sites and existing operation completion ownership in this shard; Plan 25/26 implements the shared lifetime path, not a new job service. For a synchronous function that accepts State or another borrowed runtime object, extract an owned-input synchronous <command>_blocking helper for the actual work; both the unchanged public synchronous function and the async ipc wrapper call that helper. The wrapper clones the Arc/handle from State before awaiting. Do not fabricate a State or send a borrowed guard/reporter into spawn_blocking. Define and list exact owned argument types in the implementation summary.</action>
  <acceptance_criteria>All commands named by this task compile with unchanged wire names and synchronous Rust call sites. Named phase08_05 tests run; blocking body and lock waits execute in a blocking worker; no lost update or permission bypass. Owned shard passes exact coverage and zero AUDIT checks.</acceptance_criteria>
  <verify>
    <automated>cargo test --manifest-path src-tauri/Cargo.toml --lib phase08_05</automated>
    <fails_when>Nonzero exit, zero matched/passed tests, missing named case, or an expected case skipped.</fails_when>
    <automated>cargo check --manifest-path src-tauri/Cargo.toml -p maru-cli</automated>
    <fails_when>Nonzero exit.</fails_when>
    <automated>node scripts/check-command-isolation.mjs --plan 05</automated>
    <fails_when>Nonzero exit.</fails_when>
  </verify>
  <done>All commands named by this task compile with unchanged wire names and synchronous Rust call sites. Named phase08_05 tests run; blocking body and lock waits execute in a blocking worker; no lost update or permission bypass. Owned shard passes exact coverage and zero AUDIT checks.</done>
</task>
</tasks>

<threat_model>
OWASP ASVS L1; block on high. No new dependency or external integration.

| Trust boundary | Protected invariant |
|---|---|
| Webview IPC to worker | Owned inputs retain backend permission, containment and revision validation |
| Worker to filesystem/process | Scoped serialization preserves current disk state and fixed argv |
| Test fixture to native process | Only disposable roots and compile-time gated observation |

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-08-05-01 | Tampering | Owned mutation paths | high | mitigate | Task 05-1 preserves or adds scoped transaction validation and denied-write tests before external effects. |
| T-08-05-02 | Denial of service | IPC scheduling and lifecycle | high | mitigate | Finite blocking closure, no guard across await, cleanup on failure/unwind; named task tests plus native saturation gate. |
| T-08-05-03 | Information disclosure | Errors and evidence | medium | mitigate | Preserve redaction, record synthetic fixture paths only, and final feature-off binary/frontend guards. |
</threat_model>

<verification>
Run each task's exact commands from the repository root; compiled tests must match at least one named case. Test paths introduced here are produced before their first consuming verification. Plans with native commands are slow integration gates; other focused checks target under 60 seconds with cached compilation. Never report pending execution as passed.
</verification>

<success_criteria>
Every task acceptance condition holds, assigned inventory rows have a final justified disposition, and PERF-01 evidence is recorded without claims about unrelated commands. Descriptor-less prohibitions remain flagged-unverified until verifier supplies explicit evidence; never auto-dismiss them.
</success_criteria>

<output>
Create .planning/phases/08-main-thread-responsiveness/08-05-SUMMARY.md after execution, including actual checks, owned rows and threat dispositions.
</output>
