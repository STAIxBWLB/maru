---
phase: 08-main-thread-responsiveness
plan: "25"
type: execute
wave: 26
depends_on: ["08-24","08-02"]
files_modified: ["src/lib/processingOperations.ts","src/lib/processingOperations.test.ts","src/lib/api.ts","src/lib/api.test.ts","src/lib/export.ts","src/lib/export.test.ts","src/lib/studio.ts","src/lib/studio.test.ts"]
autonomous: true
requirements: ["PERF-01"]
estimate:
  tokens: 30000
  raw_tokens: 30000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "D-04: Existing user-started file processing settles and notifies after navigation."
    - "D-05: Rejected and fulfilled partial/all-failed outcomes report actionable reasons exactly once, retain successes and never start an automatic retry."
    - "Processing notices belong to the initiating workspace; ordinary reads do not generate completion notices."
  artifacts:
    - path: "src/lib/processingOperations.ts"
      provides: "File-processing completion ownership implementation or executable verification"
    - path: "src/lib/processingOperations.test.ts"
      provides: "File-processing completion ownership implementation or executable verification"
    - path: "src/lib/api.ts"
      provides: "File-processing completion ownership implementation or executable verification"
    - path: "src/lib/api.test.ts"
      provides: "File-processing completion ownership implementation or executable verification"
    - path: "src/lib/export.ts"
      provides: "File-processing completion ownership implementation or executable verification"
    - path: "src/lib/export.test.ts"
      provides: "File-processing completion ownership implementation or executable verification"
    - path: "src/lib/studio.ts"
      provides: "File-processing completion ownership implementation or executable verification"
    - path: "src/lib/studio.test.ts"
      provides: "File-processing completion ownership implementation or executable verification"
  key_links:
    - from: "src/lib/processingOperations.ts"
      to: "src/lib/processingOperations.test.ts"
      via: "Existing registered command and explicit transaction/verification flow described in tasks"
  prohibitions:
    - statement: "No automatic sync retry, duplicate queue, new job center, or Phase 9 quit/terminal escalation."
      status: resolved
      verification: explicit
    - statement: "No live workspace, credentials, permission bypass, or native test hooks in production artifacts."
      status: resolved
      verification: explicit
---

<objective>
File-processing completion ownership. D-04: Existing user-started file processing settles and notifies after navigation. D-05: Failure reports its reason exactly once and never starts an automatic retry. Processing notices belong to the initiating workspace; ordinary reads do not generate completion notices.
Purpose: satisfy PERF-01 within the existing command/UI contracts.
Output: tested source changes and exact owned-command evidence.
</objective>

<execution_context>
@.claude/gsd-core/workflows/execute-plan.md
@.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@README.md
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/08-main-thread-responsiveness/08-CONTEXT.md
@.planning/phases/08-main-thread-responsiveness/08-RESEARCH.md
@.planning/phases/08-main-thread-responsiveness/08-PATTERNS.md
@.planning/phases/08-main-thread-responsiveness/08-PLAN-MAP.md
</context>

## Artifacts this phase produces

- src/lib/processingOperations.ts: runProcessingOperation, ProcessingOperationContext, ProcessingCompletion, getProcessingOperationsSnapshot
- src/lib/api.ts: classifyWorkspaceMutationCompletion, classifyInboxCompletion
- src/lib/export.ts: classifyExportDispatchCompletion
- src/lib/studio.ts: classifyTemplatePrepareCompletion, classifyTemplateFillCompletion
- src/lib/processingOperations.test.ts
- src/lib/studio.test.ts if absent; extension to existing API/export tests
- Inline test module/name prefix `phase08_25` where tasks specify Rust tests; existing tests stay available.
- New helper identifiers are limited to the explicitly listed names and `<owned_command>_blocking` for each converted command in the table below; these are proposed symbols, not existing APIs.





<tasks>
<task type="auto">
  <name>25-1: Own existing Files and Inbox processing promises</name>
  <reversibility rating="reversible">Internal scheduling and test changes preserve public command names and persisted data.</reversibility>
  <files>src/lib/processingOperations.ts, src/lib/processingOperations.test.ts, src/lib/api.ts, src/lib/api.test.ts</files>
  <read_first>src/lib/processingOperations.ts; src/lib/processingOperations.test.ts; src/lib/api.ts; src/lib/api.test.ts (new artifacts are created by this task if absent); README.md; docs/SSOT-TIERS.md; docs/BOUNDARIES.md; .planning/phases/08-main-thread-responsiveness/08-CONTEXT.md; .planning/phases/08-main-thread-responsiveness/08-PATTERNS.md; .planning/phases/08-main-thread-responsiveness/08-COMMAND-INVENTORY.md; src-tauri/src/hwped.rs; src-tauri/src/ipc_error.rs; src-tauri/src/lock_recovery.rs; src/lib/types.ts; src/lib/export.ts; src/lib/studio.ts; src/lib/errorStore.ts; src/App.tsx</read_first>
  <action>Per D-04/D-05, implement runProcessingOperation(context, run, classifyResult) using the existing OperationNotice publisher and immutable external-store pattern from Plan 02. The surviving owning wrapper must supply an explicit typed fulfilled-result classifier; promise resolution alone is never success. ProcessingCompletion records all-success, partial-success or all-failed, retained successful item/output references, failed item labels and actionable reasons. Publish success only when every requested item succeeded, info for partial success with counts/reasons, and error for all failed; empty/no-op results are informational, not fabricated success. Keep payloads and rejection objects returned to callers unchanged, preserve successful siblings, and leave retry manual. Classify and publish once in the module owner before any workspace/view-admission check, so unsubscribe or A->B->A cannot discard failure reasons. Component-local setters may update currently admitted detail state, but only the operation owner publishes the terminal toast. Context captures operation ID, initiating workspace path, user-visible label and optional progress ID before invocation. Promise lifetime is independent from subscribers; settle once; listener cleanup handles late registration; no cancellation or retry on unmount. Wrap existing top-level processing wrappers in api.ts for apply_file_queue, duplicate_workspace_entries, paste_workspace_entries, trash_workspace_entries, stage_inbox_drop_files, accept_inbox_item(s), reject_inbox_item(s), apply_inbox_decisions, prepare_share_outbox_files, binary_viewer_extract_hwpx and existing file move/trash/batch copy operations. Derive complete final allowlist from the reviewed processing rows in all shards; enumerate it as a literal command-to-context map in tests, covering every row marked user-started-processing, including singleton and batch wrappers. Preserve wrapper return types and normalizeIpcError boundary; one user action calls one owning wrapper to avoid nested double notices. Ordinary scans/reads stay unwrapped. In api.ts, classifyWorkspaceMutationCompletion reads singleton/array WorkspaceMutationOutcome.status=done/error and error; classifyInboxCompletion reads singleton/array InboxDecisionOutcome, InboxDropStageOutcome and InboxTrashOutcome.ok and error. Other allowlisted wrappers must name their actual typed classifier or a proven rejection-only contract in the command-to-context map; no generic truthiness default. Controlled-promise tests through actual API wrappers resolve all-success, mixed and all-failed payloads after unsubscribe, A->B and A->B->A; assert correct severity/counts/reasons, retained successful targets, unchanged returned payload identity, exactly one notice and zero retries. Retain rejection, independent-operation and late-listener tests.</action>
  <acceptance_criteria>Every processing row routed through api.ts has a tested owner; promise settlement survives unsubscribe, original result/error returns unchanged, mixed/all-failed payloads retain successful results and actionable errors, and exactly one correctly classified notice carries the initiating workspace.</acceptance_criteria>
  <verify>
    <automated>pnpm exec vitest run src/lib/processingOperations.test.ts src/lib/api.test.ts</automated>
    <fails_when>Nonzero exit, zero tests, missing mixed/all-failed fulfilled-payload-after-navigation cases, false success, lost successful output/reason, changed payload, duplicate notice, automatic retry, or an expected case skipped.</fails_when>
    <automated>pnpm typecheck</automated>
    <fails_when>Nonzero exit.</fails_when>
  </verify>
  <done>Every processing row routed through api.ts has a tested owner; promise settlement survives unsubscribe, original result/error returns unchanged, mixed/all-failed payloads retain successful results and actionable errors, and exactly one correctly classified notice carries the initiating workspace.</done>
</task>

<task type="auto">
  <name>25-2: Extend completion ownership to export and template wrappers</name>
  <reversibility rating="reversible">Internal scheduling and test changes preserve public command names and persisted data.</reversibility>
  <files>src/lib/export.ts, src/lib/export.test.ts, src/lib/studio.ts, src/lib/studio.test.ts</files>
  <read_first>src/lib/export.ts; src/lib/export.test.ts; src/lib/studio.ts; src/lib/studio.test.ts (new artifacts are created by this task if absent); README.md; docs/SSOT-TIERS.md; docs/BOUNDARIES.md; .planning/phases/08-main-thread-responsiveness/08-CONTEXT.md; .planning/phases/08-main-thread-responsiveness/08-PATTERNS.md; .planning/phases/08-main-thread-responsiveness/08-COMMAND-INVENTORY.md; src-tauri/src/hwped.rs; src-tauri/src/ipc_error.rs; src-tauri/src/lock_recovery.rs; src/lib/types.ts; src/lib/export.ts; src/lib/studio.ts; src/lib/errorStore.ts; src/App.tsx</read_first>
  <action>Wrap exportDispatch, templatePrepareHwpxTemplate and templateFillHwpx (confirm exact exported function spelling in studio.ts before edit) with runProcessingOperation, capturing workspace from the existing parameters. Preserve existing browser fallback behavior and source/result contracts. The surviving owning wrapper must supply an explicit typed fulfilled-result classifier; promise resolution alone is never success. ProcessingCompletion records all-success, partial-success or all-failed, retained successful item/output references, failed item labels and actionable reasons. Publish success only when every requested item succeeded, info for partial success with counts/reasons, and error for all failed; empty/no-op results are informational, not fabricated success. Keep payloads and rejection objects returned to callers unchanged, preserve successful siblings, and leave retry manual. Classify and publish once in the module owner before any workspace/view-admission check, so unsubscribe or A->B->A cannot discard failure reasons. Component-local setters may update currently admitted detail state, but only the operation owner publishes the terminal toast.  Use outer Studio operation IDs when one user flow calls template then export so only the user flow owner emits the terminal notice. Implement classifyExportDispatchCompletion from results[].success, output_path, format and reason, retaining summarizeDispatch's partial-failure detail and manifest/validation. Implement classifyTemplatePrepareCompletion from status/preparedPath/reason: manualFallback remains informational with its reason and manual next step, never a success claim. Implement classifyTemplateFillCompletion from validationOk, validationChecks, unmatchedFields and outputPath: retain generated outputs while reporting failed validation/actionable reasons, never equate output existence with full success. Outer Studio flow ownership aggregates nested classifications without losing early partial failures; inner wrappers suppress only their notice when an explicit outer owner is provided. Add actual-wrapper controlled-promise tests resolving mixed/all-failed export results and unsuccessful template payloads after disposal/workspace change; assert exactly one correctly classified terminal notice, successful output retention and original payload identity. Preserve String/typed rejection tests and no retries. Cover processing rows in export/dispatch, hwp_cli_template, template_fill and hwped used through these wrappers; hwped callers outside these files are assigned to Plan 26's completion ownership audit.</action>
  <acceptance_criteria>Export and template completion remains visible on another screen; fulfilled partial/all-failed payloads preserve reasons and successes with exactly one correctly classified notice, including nested processing; focused tests match at least one case per file.</acceptance_criteria>
  <verify>
    <automated>pnpm exec vitest run src/lib/export.test.ts src/lib/studio.test.ts src/lib/processingOperations.test.ts</automated>
    <fails_when>Nonzero exit, zero tests, missing mixed/all-failed fulfilled-payload-after-navigation cases, false success, lost successful output/reason, changed payload, duplicate notice, automatic retry, or an expected case skipped.</fails_when>
    <automated>pnpm typecheck</automated>
    <fails_when>Nonzero exit.</fails_when>
  </verify>
  <done>Export and template completion remains visible on another screen; fulfilled partial/all-failed payloads preserve reasons and successes with exactly one correctly classified notice, including nested processing; focused tests match at least one case per file.</done>
</task>
</tasks>

<threat_model>
OWASP ASVS L1; block on high. No new dependency or external integration.

| Trust boundary | Protected invariant |
|---|---|
| Webview IPC to worker | Owned inputs retain backend permission, containment and revision validation |
| Worker to filesystem/process | Scoped serialization preserves current disk state and fixed argv |
| Test fixture to native process | Only disposable roots and compile-time gated observation |

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-08-25-01 | Tampering | Owned mutation paths | high | mitigate | Task 25-1 preserves or adds scoped transaction validation and denied-write tests before external effects. |
| T-08-25-02 | Denial of service | IPC scheduling and lifecycle | high | mitigate | Finite blocking closure, no guard across await, cleanup on failure/unwind; named task tests plus native saturation gate. |
| T-08-25-03 | Information disclosure | Errors and evidence | medium | mitigate | Preserve redaction, record synthetic fixture paths only, and final feature-off binary/frontend guards. |
</threat_model>

<verification>
Run each task's exact commands from the repository root; compiled tests must match at least one named case. Test paths introduced here are produced before their first consuming verification. Plans with native commands are slow integration gates; other focused checks target under 60 seconds with cached compilation. Never report pending execution as passed.
</verification>

<success_criteria>
Every task acceptance condition holds, assigned inventory rows have a final justified disposition, and PERF-01 evidence is recorded without claims about unrelated commands. Descriptor-less prohibitions remain flagged-unverified until verifier supplies explicit evidence; never auto-dismiss them.
</success_criteria>

<output>
Create .planning/phases/08-main-thread-responsiveness/08-25-SUMMARY.md after execution, including actual checks, owned rows and threat dispositions.
</output>
