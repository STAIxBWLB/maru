---
phase: 08-main-thread-responsiveness
plan: "28"
type: execute
wave: 29
depends_on: ["08-27"]
files_modified: ["scripts/check-command-isolation.mjs","scripts/check-command-isolation.test.mjs","scripts/check-native-e2e-isolation.mjs","Makefile","docs/native-e2e.md","README.md",".planning/phases/08-main-thread-responsiveness/08-COMMAND-INVENTORY.md","docs/performance/phase08-final.json"]
autonomous: true
requirements: ["PERF-01","PERF-02"]
estimate:
  tokens: 30000
  raw_tokens: 30000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "All 365 original production commands and any explicitly added production rows have exactly one final disposition; zero AUDIT remains."
    - "Native evidence proves real isolation and all five decisions; Phase 6/7 guardrails remain passing."
    - "Normal frontend and default-feature binary contain no responsiveness probe/control or WebDriver hooks."
  artifacts:
    - path: "scripts/check-command-isolation.mjs"
      provides: "Exhaustive closure and production isolation implementation or executable verification"
    - path: "scripts/check-command-isolation.test.mjs"
      provides: "Exhaustive closure and production isolation implementation or executable verification"
    - path: "scripts/check-native-e2e-isolation.mjs"
      provides: "Exhaustive closure and production isolation implementation or executable verification"
    - path: "Makefile"
      provides: "Exhaustive closure and production isolation implementation or executable verification"
    - path: "docs/native-e2e.md"
      provides: "Exhaustive closure and production isolation implementation or executable verification"
    - path: "README.md"
      provides: "Exhaustive closure and production isolation implementation or executable verification"
    - path: ".planning/phases/08-main-thread-responsiveness/08-COMMAND-INVENTORY.md"
      provides: "Exhaustive closure and production isolation implementation or executable verification"
  key_links:
    - from: "scripts/check-command-isolation.mjs"
      to: "scripts/check-command-isolation.test.mjs"
      via: "Existing registered command and explicit transaction/verification flow described in tasks"
  prohibitions:
    - statement: "No automatic sync retry, duplicate queue, new job center, or Phase 9 quit/terminal escalation."
      status: resolved
      verification: explicit
    - statement: "No live workspace, credentials, permission bypass, or native test hooks in production artifacts."
      status: resolved
      verification: explicit
---

<objective>
Exhaustive closure and production isolation. All 365 original production commands and any explicitly added production rows have exactly one final disposition; zero AUDIT remains. Native evidence proves real isolation and all five decisions; Phase 6/7 guardrails remain passing. Normal frontend and default-feature binary contain no responsiveness probe/control or WebDriver hooks.
Purpose: satisfy PERF-01 and PERF-02 within the existing command/UI contracts.
Output: tested source changes and exact owned-command evidence.
</objective>

<execution_context>
@.claude/gsd-core/workflows/execute-plan.md
@.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@README.md
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/08-main-thread-responsiveness/08-CONTEXT.md
@.planning/phases/08-main-thread-responsiveness/08-RESEARCH.md
@.planning/phases/08-main-thread-responsiveness/08-PATTERNS.md
@.planning/phases/08-main-thread-responsiveness/08-PLAN-MAP.md
</context>

## Artifacts this phase produces

- docs/performance/phase08-final.json merged evidence and decision/source audit
- Final 08-COMMAND-INVENTORY.md with zero AUDIT and current registration paths
- Produced default-feature frontend/binary isolation evidence
- README/native runner documentation for exact verification commands
- Inline test module/name prefix `phase08_28` where tasks specify Rust tests; existing tests stay available.
- New helper identifiers are limited to the explicitly listed names and `<owned_command>_blocking` for each converted command in the table below; these are proposed symbols, not existing APIs.

No completion claims until native fixed-run/negative-control artifacts are present and parsed. Full validation is slow and intentionally separate from each tranche's cached focused tests.



<tasks>
<task type="auto">
  <name>28-1: Merge exact command and decision evidence with regression gate</name>
  <reversibility rating="reversible">Internal scheduling and test changes preserve public command names and persisted data.</reversibility>
  <files>scripts/check-command-isolation.mjs, scripts/check-command-isolation.test.mjs, .planning/phases/08-main-thread-responsiveness/08-COMMAND-INVENTORY.md, docs/performance/phase08-final.json</files>
  <read_first>scripts/check-command-isolation.mjs; scripts/check-command-isolation.test.mjs; .planning/phases/08-main-thread-responsiveness/08-COMMAND-INVENTORY.md; docs/performance/phase08-final.json (new artifacts are created by this task if absent); README.md; docs/SSOT-TIERS.md; docs/BOUNDARIES.md; .planning/phases/08-main-thread-responsiveness/08-CONTEXT.md; .planning/phases/08-main-thread-responsiveness/08-PATTERNS.md; .planning/phases/08-main-thread-responsiveness/08-COMMAND-INVENTORY.md; src-tauri/src/hwped.rs; src-tauri/src/ipc_error.rs; src-tauri/src/lock_recovery.rs; .planning/phases/08-main-thread-responsiveness/08-PLAN-MAP.md; .planning/phases/08-main-thread-responsiveness/08-VALIDATION.md</read_first>
  <action>Merge only each shard's exclusively owned rows and update research inventory with final path/disposition/helper/tests. Compare live generate_handler commands with all 365 original names; removal or new production entry requires explicit reconciled justification, never hide it to pass the count. Test-only feature commands are separately allowlisted and absent in default artifact. Reject zero rows, duplicates, AUDIT, missing sync-helper caller evidence or unresolved processing-callers row. Add checker fixtures that reject a synchronous skills_sync_all_sources boundary or missing actual-wrapper same-runtime batch test, exact-path-only mutation exclusion or existing-domain-guard exemptions, missing 07 parent/child pair/order evidence, and a processing row without its fulfilled-domain classifier/success-retention/reason/single-notice owner. Reconcile each mutation row's complete lexical/alias write set and shared-admission-before-domain-lock order against the Shared mutation admission contract in 08-PLAN-MAP.md. Merge docs/performance/phase08-29-integration.json as a reference overlay, never as additional command rows. Require its store/env/dispatch/git/dot final module ownership and staged event_store/mission_state handoffs to 17/16. Reject early integrationRequired obligations still pending, missing full checkout/common-Git/install/bundle/env/dot/event/mission path sets, missing source reservation-before-path-wait proof, and any REGISTRY_LOCK or registry.json path admission held over network. Require all Skills-save/Skills-sync/Git-pull versus parent rename/trash pairs from 29, plus their save/create pairs from 07, both orders, aliases, error release and unchanged registry availability; final checker fixtures deliberately remove each proof and must fail. Require real cross-domain contention and error-release evidence from every affected tranche; unresolved writers require plan correction, not claimed safety. Require mixed/all-failed post-navigation payload tests from 25-26 across both notice channels. Run exact coverage plus Rust/CLI/full hermetic checks. Evaluate all four spec-less assumptions from Plans 01/27 using actual evidence; retain flagged-unverified if evidence is insufficient. Confirm D-01..D-05 each has deterministic Rust/frontend and applicable native proof, per-plan high threats are mitigated, and no promised source item is silently dropped.</action>
  <acceptance_criteria>All owned evidence joins once, 365 baseline commands retained, zero AUDIT/unowned/unresolved completion or mutation-protocol rows; batch-wrapper, Plan 29 earlier-writer parent races, Plan 07 earlier-writer document races (both orders/aliases/release/registry availability), hierarchical domain-contention and fulfilled mixed/all-failed notice evidence is present, make verify passes; no native proof replaced by browser mock evidence.</acceptance_criteria>
  <verify>
    <automated>node --test scripts/check-command-isolation.test.mjs</automated>
    <fails_when>Nonzero exit, zero matched/passed tests, missing named case, or an expected case skipped.</fails_when>
    <automated>node scripts/check-command-isolation.mjs --all --expected-count 365</automated>
    <fails_when>Nonzero exit.</fails_when>
    <automated>make verify</automated>
    <fails_when>Nonzero exit.</fails_when>
    <automated>cargo check --manifest-path src-tauri/Cargo.toml -p maru-cli</automated>
    <fails_when>Nonzero exit.</fails_when>
  </verify>
  <done>All owned evidence joins once, 365 baseline commands retained, zero AUDIT/unowned/unresolved completion or mutation-protocol rows; batch-wrapper, Plan 29 earlier-writer parent races, Plan 07 earlier-writer document races (both orders/aliases/release/registry availability), hierarchical domain-contention and fulfilled mixed/all-failed notice evidence is present, make verify passes; no native proof replaced by browser mock evidence.</done>
</task>

<task type="auto">
  <name>28-2: Prove test hooks absent from normal produced artifacts</name>
  <reversibility rating="reversible">Internal scheduling and test changes preserve public command names and persisted data.</reversibility>
  <files>scripts/check-native-e2e-isolation.mjs, Makefile, docs/native-e2e.md, README.md</files>
  <read_first>scripts/check-native-e2e-isolation.mjs; Makefile; docs/native-e2e.md; README.md (new artifacts are created by this task if absent); README.md; docs/SSOT-TIERS.md; docs/BOUNDARIES.md; .planning/phases/08-main-thread-responsiveness/08-CONTEXT.md; .planning/phases/08-main-thread-responsiveness/08-PATTERNS.md; .planning/phases/08-main-thread-responsiveness/08-COMMAND-INVENTORY.md; src-tauri/src/hwped.rs; src-tauri/src/ipc_error.rs; src-tauri/src/lock_recovery.rs; .planning/phases/08-main-thread-responsiveness/08-PLAN-MAP.md; .planning/phases/08-main-thread-responsiveness/08-VALIDATION.md</read_first>
  <action>Extend existing produced-artifact binary scan for MARU_NATIVE_RESPONSIVENESS_HOOK and both native_e2e command symbols, plus frontend allowlist marker. First prove feature-on native artifacts are rejected and record exit code/markers, then rebuild ordinary frontend via pnpm build:frontend and normal no-native-feature Tauri binary using existing release-checks build recipe (touch build.rs as existing recipe requires fresh embedded assets). Run the extended guard on actual normal binary and dist; a manifest-only assertion is insufficient. Wire command-isolation into hermetic make verify after native artifact cleanup and keep native runtime gate in existing release-preflight placement. Document fixed fixtures, two-worker runtime, calibration/negative control, artifact paths and exact normal rebuild commands. Re-run Phase 7 sanitizer/poison/watcher tests through make verify and use release-checks for CLI/default binary verification. Preserve raw evidence outside fixture teardown and confirm no test process/root leaks.</action>
  <acceptance_criteria>Feature-on artifacts are observably rejected, normal frontend/default-feature binary pass actual artifact scan, source guards and full release checks pass, docs describe commands actually executed.</acceptance_criteria>
  <verify>
    <automated>pnpm build:frontend</automated>
    <fails_when>Nonzero exit.</fails_when>
    <automated>make release-checks</automated>
    <fails_when>Nonzero exit.</fails_when>
    <automated>node scripts/check-command-isolation.mjs --all --expected-count 365</automated>
    <fails_when>Nonzero exit.</fails_when>
  </verify>
  <done>Feature-on artifacts are observably rejected, normal frontend/default-feature binary pass actual artifact scan, source guards and full release checks pass, docs describe commands actually executed.</done>
</task>
</tasks>

<threat_model>
OWASP ASVS L1; block on high. No new dependency or external integration.

| Trust boundary | Protected invariant |
|---|---|
| Webview IPC to worker | Owned inputs retain backend permission, containment and revision validation |
| Worker to filesystem/process | Scoped serialization preserves current disk state and fixed argv |
| Test fixture to native process | Only disposable roots and compile-time gated observation |

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-08-28-01 | Tampering | Owned mutation paths | high | mitigate | Task 28-1 preserves or adds scoped transaction validation and denied-write tests before external effects. |
| T-08-28-02 | Denial of service | IPC scheduling and lifecycle | high | mitigate | Finite blocking closure, no guard across await, cleanup on failure/unwind; named task tests plus native saturation gate. |
| T-08-28-03 | Information disclosure | Errors and evidence | medium | mitigate | Preserve redaction, record synthetic fixture paths only, and final feature-off binary/frontend guards. |
</threat_model>

<verification>
Run each task's exact commands from the repository root; compiled tests must match at least one named case. Test paths introduced here are produced before their first consuming verification. Plans with native commands are slow integration gates; other focused checks target under 60 seconds with cached compilation. Never report pending execution as passed.
</verification>

<success_criteria>
Every task acceptance condition holds, assigned inventory rows have a final justified disposition, and PERF-01/PERF-02 evidence is recorded without claims about unrelated commands. Descriptor-less prohibitions remain flagged-unverified until verifier supplies explicit evidence; never auto-dismiss them.
</success_criteria>

<output>
Create .planning/phases/08-main-thread-responsiveness/08-28-SUMMARY.md after execution, including actual checks, owned rows and threat dispositions.
</output>
