---
phase: 08-main-thread-responsiveness
plan: "24"
type: execute
wave: 25
depends_on: ["08-23"]
files_modified: ["src-tauri/src/html_editor.rs","src-tauri/src/browser_passkeys.rs","src-tauri/src/site_view.rs","src-tauri/src/today_notify.rs","src-tauri/src/approval.rs","src-tauri/src/korean_date.rs","src-tauri/src/linter/gaejosik.rs","src-tauri/src/lib.rs","docs/performance/phase08-24.json"]
autonomous: true
requirements: ["PERF-01"]
estimate:
  tokens: 30000
  raw_tokens: 30000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "Native UI and bounded parsers reaches no network, process or unbounded walk on the UI or shared async worker thread."
    - "All 21 owned commands preserve wire types, permission checks and synchronous internal callers."
    - "Concurrent operations preserve lifecycle ordering and current disk state; each retained/native UI exception has positive evidence."
  artifacts:
    - path: "src-tauri/src/html_editor.rs"
      provides: "Native UI and bounded parsers command isolation implementation or executable verification"
    - path: "src-tauri/src/browser_passkeys.rs"
      provides: "Native UI and bounded parsers command isolation implementation or executable verification"
    - path: "src-tauri/src/site_view.rs"
      provides: "Native UI and bounded parsers command isolation implementation or executable verification"
    - path: "src-tauri/src/today_notify.rs"
      provides: "Native UI and bounded parsers command isolation implementation or executable verification"
    - path: "src-tauri/src/approval.rs"
      provides: "Native UI and bounded parsers command isolation implementation or executable verification"
    - path: "src-tauri/src/korean_date.rs"
      provides: "Native UI and bounded parsers command isolation implementation or executable verification"
    - path: "src-tauri/src/linter/gaejosik.rs"
      provides: "Native UI and bounded parsers command isolation implementation or executable verification"
    - path: "src-tauri/src/lib.rs"
      provides: "Native UI and bounded parsers command isolation implementation or executable verification"
  key_links:
    - from: "src-tauri/src/html_editor.rs"
      to: "src-tauri/src/browser_passkeys.rs"
      via: "Existing registered command and explicit transaction/verification flow described in tasks"
  prohibitions:
    - statement: "No automatic sync retry, duplicate queue, new job center, or Phase 9 quit/terminal escalation."
      status: resolved
      verification: explicit
    - statement: "No live workspace, credentials, permission bypass, or native test hooks in production artifacts."
      status: resolved
      verification: explicit
---

<objective>
Native UI and bounded parsers command isolation. Native UI and bounded parsers reaches no network, process or unbounded walk on the UI or shared async worker thread. All 21 owned commands preserve wire types, permission checks and synchronous internal callers. Concurrent operations preserve lifecycle ordering and current disk state; each retained/native UI exception has positive evidence.
Purpose: satisfy PERF-01 within the existing command/UI contracts.
Output: tested source changes and exact owned-command evidence.
</objective>

<execution_context>
@.claude/gsd-core/workflows/execute-plan.md
@.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@README.md
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/08-main-thread-responsiveness/08-CONTEXT.md
@.planning/phases/08-main-thread-responsiveness/08-RESEARCH.md
@.planning/phases/08-main-thread-responsiveness/08-PATTERNS.md
@.planning/phases/08-main-thread-responsiveness/08-PLAN-MAP.md
</context>

## Artifacts this phase produces

- src-tauri/src/html_editor.rs: pub mod ipc with async same-name wrappers for each converted owned command; original sync functions retained
- src-tauri/src/browser_passkeys.rs: pub mod ipc with async same-name wrappers for each converted owned command; original sync functions retained
- src-tauri/src/site_view.rs: pub mod ipc with async same-name wrappers for each converted owned command; original sync functions retained
- src-tauri/src/today_notify.rs: pub mod ipc with async same-name wrappers for each converted owned command; original sync functions retained
- src-tauri/src/approval.rs: pub mod ipc with async same-name wrappers for each converted owned command; original sync functions retained
- src-tauri/src/korean_date.rs: pub mod ipc with async same-name wrappers for each converted owned command; original sync functions retained
- src-tauri/src/linter/gaejosik.rs: pub mod ipc with async same-name wrappers for each converted owned command; original sync functions retained
- docs/performance/phase08-24.json final command evidence
- Inline phase08_24 behavioral tests
- Owned-input <command>_blocking helpers only where State/borrow adaptation is required, retaining original synchronous API signatures
- Inline test module/name prefix `phase08_24` where tasks specify Rust tests; existing tests stay available.
- New helper identifiers are limited to the explicitly listed names, `<owned_command>_in_transaction` adapters and `<owned_command>_blocking` for each converted command in the table below; these are proposed symbols, not existing APIs.

Registration edits share lib.rs, so this plan depends on the immediately preceding tranche. This deliberately avoids parallel same-file conflicts while preserving independently green app/CLI builds. No extra dispatcher or build infrastructure is introduced to manufacture parallelism. Existing implementation helper modules are read-only unless explicitly listed; place shared admission in the owned mutation entry, before existing domain guards; no existing-guard exemption. Read src-tauri/src/atomic_file.rs before adapting any write; Plan 06 produces the shared path transaction helper.

## Exclusive command ownership

- `src-tauri/src/html_editor.rs`: `prepare_html_editor_assets`.
- `src-tauri/src/browser_passkeys.rs`: `browser_passkey_status`, `browser_passkey_request_authorization`.
- `src-tauri/src/site_view.rs`: `site_view_open`, `site_view_navigate`, `site_view_set_bounds`, `site_view_show`, `site_view_hide`, `site_view_close`, `site_view_close_all`, `site_view_reload`, `site_view_back`, `site_view_forward`, `site_view_open_external`, `site_view_open_safari`, `site_view_take_opened_urls`.
- `src-tauri/src/today_notify.rs`: `today_notify_new_day`.
- `src-tauri/src/approval.rs`: `prepare_approval`, `record_approval`.
- `src-tauri/src/korean_date.rs`: `parse_korean_date_cmd`.
- `src-tauri/src/linter/gaejosik.rs`: `gaejosik_lint`.

<tasks>
<task type="auto">
  <name>24-1: Isolate src/html_editor.rs, src/browser_passkeys.rs, src/site_view.rs</name>
  <reversibility rating="reversible">Internal scheduling and test changes preserve public command names and persisted data.</reversibility>
  <files>src-tauri/src/html_editor.rs, src-tauri/src/browser_passkeys.rs, src-tauri/src/site_view.rs, src-tauri/src/lib.rs</files>
  <read_first>src-tauri/src/html_editor.rs; src-tauri/src/browser_passkeys.rs; src-tauri/src/site_view.rs; src-tauri/src/lib.rs (new artifacts are created by this task if absent); README.md; docs/SSOT-TIERS.md; docs/BOUNDARIES.md; .planning/phases/08-main-thread-responsiveness/08-CONTEXT.md; .planning/phases/08-main-thread-responsiveness/08-PATTERNS.md; .planning/phases/08-main-thread-responsiveness/08-COMMAND-INVENTORY.md; src-tauri/src/hwped.rs; src-tauri/src/ipc_error.rs; src-tauri/src/lock_recovery.rs; src-tauri/src/atomic_file.rs; .planning/phases/08-main-thread-responsiveness/08-PLAN-MAP.md</read_first>
  <action>Own and close these exact command rows: src-tauri/src/html_editor.rs: prepare_html_editor_assets; src-tauri/src/browser_passkeys.rs: browser_passkey_status, browser_passkey_request_authorization; src-tauri/src/site_view.rs: site_view_open, site_view_navigate, site_view_set_bounds, site_view_show, site_view_hide, site_view_close, site_view_close_all, site_view_reload, site_view_back, site_view_forward, site_view_open_external, site_view_open_safari, site_view_take_opened_urls. Read every body and all reached helpers including cfg/platform branches, traits/callbacks and command(async) bodies. For blocking synchronous functions, retain the original public synchronous function and its name for Rust/CLI callers; remove its command attribute and add a same-name async #[tauri::command] function inside this module's new pub mod ipc. That wrapper owns its inputs and awaits tauri::async_runtime::spawn_blocking calling super::the_original_name. Update only corresponding generate_handler paths in lib.rs to module::ipc::command; the wire name is unchanged. Existing genuinely async/isolated/UI entries keep their original registration path unless their body needs a finite blocking substep. Construct ProgressReporter inside worker; clone State's owned Arc/handle rather than carrying borrowed State or guards across await. Preserve Result<T,String> versus Result<T,IpcError>; inner typed errors pass through untouched, while JoinError becomes contextual display-only error of the same outer type. Do not use block_on or an executor/dispatch registry. Keep native webview/passkey/notification UI calls on Tauri's required thread; split incidental disk/process work before native dispatch. site_view_open must not hold shared registry guards during slow native creation; retain lifetime identity during publication. Offload site_view_open_external/open_safari process launch. Parser/model bounds must include input size and helper behavior; absent regex hits do not justify retention. Add inline tests with prefix phase08_24 in modified modules: nonempty real fixture result, unchanged typed/legacy rejection, and at least one deterministic overlap case where a blocked worker still permits a same-runtime async yield. For every newly parallel write, inventory its transaction key and test same-target contention; keep authorization and revision validation inside the worker/transaction. Per D-04, annotate each user-started processing row's frontend call sites and existing operation completion ownership in this shard; Plan 25/26 implements the shared lifetime path, not a new job service. For a synchronous function that accepts State or another borrowed runtime object, extract an owned-input synchronous <command>_blocking helper for the actual work; both the unchanged public synchronous function and the async ipc wrapper call that helper. The wrapper clones the Arc/handle from State before awaiting. Do not fabricate a State or send a borrowed guard/reporter into spawn_blocking. Define and list exact owned argument types in the implementation summary. For every filesystem mutation, follow the Shared mutation admission contract in 08-PLAN-MAP.md using atomic_file::with_path_transactions from Plan 06, including ancestor/descendant and symlink-alias conflicts. Declare the complete source/destination/sidecar write set; acquire shared admission inside the blocking worker before existing domain guards, then revalidate lexical containment, required existing parents and revisions. Existing guards remain nested participants, never exemptions. Nested calls use the admitted PathTransactionLease via <owned_command>_in_transaction adapters without reacquiring guards. Hold admission through write/rekey/rollback; no old parent recreation. Record the shared entry and lock order for actual sync/IPC/background writers and add deterministic real-command cross-domain contention and error-release cases.</action>
  <acceptance_criteria>All commands named by this task compile with unchanged wire names and synchronous Rust call sites. Named phase08_24 tests run; blocking body and lock waits execute in a blocking worker; no lost update or permission bypass; shared hierarchical admission precedes domain locks, supported symlinks still work, and cross-domain contention/error-release cases pass. </acceptance_criteria>
  <verify>
    <automated>cargo test --manifest-path src-tauri/Cargo.toml --lib phase08_24</automated>
    <fails_when>Nonzero exit, zero matched tests, missing named cross-domain/lock-order/error-release case for a mutation, parent recreation, partial rekey, deadlock, or an expected case skipped.</fails_when>
    <automated>cargo check --manifest-path src-tauri/Cargo.toml -p maru-cli</automated>
    <fails_when>Nonzero exit.</fails_when>
  </verify>
  <done>All commands named by this task compile with unchanged wire names and synchronous Rust call sites. Named phase08_24 tests run; blocking body and lock waits execute in a blocking worker; no lost update or permission bypass; shared hierarchical admission precedes domain locks, supported symlinks still work, and cross-domain contention/error-release cases pass. </done>
</task>

<task type="auto">
  <name>24-2: Isolate src/today_notify.rs, src/approval.rs, src/korean_date.rs</name>
  <reversibility rating="reversible">Internal scheduling and test changes preserve public command names and persisted data.</reversibility>
  <files>src-tauri/src/today_notify.rs, src-tauri/src/approval.rs, src-tauri/src/korean_date.rs, src-tauri/src/lib.rs</files>
  <read_first>src-tauri/src/today_notify.rs; src-tauri/src/approval.rs; src-tauri/src/korean_date.rs; src-tauri/src/lib.rs (new artifacts are created by this task if absent); README.md; docs/SSOT-TIERS.md; docs/BOUNDARIES.md; .planning/phases/08-main-thread-responsiveness/08-CONTEXT.md; .planning/phases/08-main-thread-responsiveness/08-PATTERNS.md; .planning/phases/08-main-thread-responsiveness/08-COMMAND-INVENTORY.md; src-tauri/src/hwped.rs; src-tauri/src/ipc_error.rs; src-tauri/src/lock_recovery.rs; src-tauri/src/atomic_file.rs; .planning/phases/08-main-thread-responsiveness/08-PLAN-MAP.md</read_first>
  <action>Own and close these exact command rows: src-tauri/src/today_notify.rs: today_notify_new_day; src-tauri/src/approval.rs: prepare_approval, record_approval; src-tauri/src/korean_date.rs: parse_korean_date_cmd. Read every body and all reached helpers including cfg/platform branches, traits/callbacks and command(async) bodies. For blocking synchronous functions, retain the original public synchronous function and its name for Rust/CLI callers; remove its command attribute and add a same-name async #[tauri::command] function inside this module's new pub mod ipc. That wrapper owns its inputs and awaits tauri::async_runtime::spawn_blocking calling super::the_original_name. Update only corresponding generate_handler paths in lib.rs to module::ipc::command; the wire name is unchanged. Existing genuinely async/isolated/UI entries keep their original registration path unless their body needs a finite blocking substep. Construct ProgressReporter inside worker; clone State's owned Arc/handle rather than carrying borrowed State or guards across await. Preserve Result<T,String> versus Result<T,IpcError>; inner typed errors pass through untouched, while JoinError becomes contextual display-only error of the same outer type. Do not use block_on or an executor/dispatch registry. Keep native webview/passkey/notification UI calls on Tauri's required thread; split incidental disk/process work before native dispatch. site_view_open must not hold shared registry guards during slow native creation; retain lifetime identity during publication. Offload site_view_open_external/open_safari process launch. Parser/model bounds must include input size and helper behavior; absent regex hits do not justify retention. Add inline tests with prefix phase08_24 in modified modules: nonempty real fixture result, unchanged typed/legacy rejection, and at least one deterministic overlap case where a blocked worker still permits a same-runtime async yield. For every newly parallel write, inventory its transaction key and test same-target contention; keep authorization and revision validation inside the worker/transaction. Per D-04, annotate each user-started processing row's frontend call sites and existing operation completion ownership in this shard; Plan 25/26 implements the shared lifetime path, not a new job service. For a synchronous function that accepts State or another borrowed runtime object, extract an owned-input synchronous <command>_blocking helper for the actual work; both the unchanged public synchronous function and the async ipc wrapper call that helper. The wrapper clones the Arc/handle from State before awaiting. Do not fabricate a State or send a borrowed guard/reporter into spawn_blocking. Define and list exact owned argument types in the implementation summary. For every filesystem mutation, follow the Shared mutation admission contract in 08-PLAN-MAP.md using atomic_file::with_path_transactions from Plan 06, including ancestor/descendant and symlink-alias conflicts. Declare the complete source/destination/sidecar write set; acquire shared admission inside the blocking worker before existing domain guards, then revalidate lexical containment, required existing parents and revisions. Existing guards remain nested participants, never exemptions. Nested calls use the admitted PathTransactionLease via <owned_command>_in_transaction adapters without reacquiring guards. Hold admission through write/rekey/rollback; no old parent recreation. Record the shared entry and lock order for actual sync/IPC/background writers and add deterministic real-command cross-domain contention and error-release cases.</action>
  <acceptance_criteria>All commands named by this task compile with unchanged wire names and synchronous Rust call sites. Named phase08_24 tests run; blocking body and lock waits execute in a blocking worker; no lost update or permission bypass; shared hierarchical admission precedes domain locks, supported symlinks still work, and cross-domain contention/error-release cases pass. </acceptance_criteria>
  <verify>
    <automated>cargo test --manifest-path src-tauri/Cargo.toml --lib phase08_24</automated>
    <fails_when>Nonzero exit, zero matched tests, missing named cross-domain/lock-order/error-release case for a mutation, parent recreation, partial rekey, deadlock, or an expected case skipped.</fails_when>
    <automated>cargo check --manifest-path src-tauri/Cargo.toml -p maru-cli</automated>
    <fails_when>Nonzero exit.</fails_when>
  </verify>
  <done>All commands named by this task compile with unchanged wire names and synchronous Rust call sites. Named phase08_24 tests run; blocking body and lock waits execute in a blocking worker; no lost update or permission bypass; shared hierarchical admission precedes domain locks, supported symlinks still work, and cross-domain contention/error-release cases pass. </done>
</task>

<task type="auto">
  <name>24-3: Isolate linter/gaejosik.rs</name>
  <reversibility rating="reversible">Internal scheduling and test changes preserve public command names and persisted data.</reversibility>
  <files>src-tauri/src/linter/gaejosik.rs, src-tauri/src/lib.rs, docs/performance/phase08-24.json</files>
  <read_first>src-tauri/src/linter/gaejosik.rs; src-tauri/src/lib.rs; docs/performance/phase08-24.json (new artifacts are created by this task if absent); README.md; docs/SSOT-TIERS.md; docs/BOUNDARIES.md; .planning/phases/08-main-thread-responsiveness/08-CONTEXT.md; .planning/phases/08-main-thread-responsiveness/08-PATTERNS.md; .planning/phases/08-main-thread-responsiveness/08-COMMAND-INVENTORY.md; src-tauri/src/hwped.rs; src-tauri/src/ipc_error.rs; src-tauri/src/lock_recovery.rs; src-tauri/src/atomic_file.rs; .planning/phases/08-main-thread-responsiveness/08-PLAN-MAP.md</read_first>
  <action>Own and close these exact command rows: src-tauri/src/linter/gaejosik.rs: gaejosik_lint. Read every body and all reached helpers including cfg/platform branches, traits/callbacks and command(async) bodies. For blocking synchronous functions, retain the original public synchronous function and its name for Rust/CLI callers; remove its command attribute and add a same-name async #[tauri::command] function inside this module's new pub mod ipc. That wrapper owns its inputs and awaits tauri::async_runtime::spawn_blocking calling super::the_original_name. Update only corresponding generate_handler paths in lib.rs to module::ipc::command; the wire name is unchanged. Existing genuinely async/isolated/UI entries keep their original registration path unless their body needs a finite blocking substep. Construct ProgressReporter inside worker; clone State's owned Arc/handle rather than carrying borrowed State or guards across await. Preserve Result<T,String> versus Result<T,IpcError>; inner typed errors pass through untouched, while JoinError becomes contextual display-only error of the same outer type. Do not use block_on or an executor/dispatch registry. Keep native webview/passkey/notification UI calls on Tauri's required thread; split incidental disk/process work before native dispatch. site_view_open must not hold shared registry guards during slow native creation; retain lifetime identity during publication. Offload site_view_open_external/open_safari process launch. Parser/model bounds must include input size and helper behavior; absent regex hits do not justify retention. Add inline tests with prefix phase08_24 in modified modules: nonempty real fixture result, unchanged typed/legacy rejection, and at least one deterministic overlap case where a blocked worker still permits a same-runtime async yield. For every newly parallel write, inventory its transaction key and test same-target contention; keep authorization and revision validation inside the worker/transaction. Write docs/performance/phase08-24.json with every owned row exactly once, final disposition and decisive helper/affinity/bound plus tests and current registration path. Zero AUDIT; retained/UI/ISOLATED rows require positive evidence too. Preserve other shards and the research inventory; final merge is Plan 28. Per D-04, annotate each user-started processing row's frontend call sites and existing operation completion ownership in this shard; Plan 25/26 implements the shared lifetime path, not a new job service. For a synchronous function that accepts State or another borrowed runtime object, extract an owned-input synchronous <command>_blocking helper for the actual work; both the unchanged public synchronous function and the async ipc wrapper call that helper. The wrapper clones the Arc/handle from State before awaiting. Do not fabricate a State or send a borrowed guard/reporter into spawn_blocking. Define and list exact owned argument types in the implementation summary. For every filesystem mutation, follow the Shared mutation admission contract in 08-PLAN-MAP.md using atomic_file::with_path_transactions from Plan 06, including ancestor/descendant and symlink-alias conflicts. Declare the complete source/destination/sidecar write set; acquire shared admission inside the blocking worker before existing domain guards, then revalidate lexical containment, required existing parents and revisions. Existing guards remain nested participants, never exemptions. Nested calls use the admitted PathTransactionLease via <owned_command>_in_transaction adapters without reacquiring guards. Hold admission through write/rekey/rollback; no old parent recreation. Record the shared entry and lock order for actual sync/IPC/background writers and add deterministic real-command cross-domain contention and error-release cases.</action>
  <acceptance_criteria>All commands named by this task compile with unchanged wire names and synchronous Rust call sites. Named phase08_24 tests run; blocking body and lock waits execute in a blocking worker; no lost update or permission bypass; shared hierarchical admission precedes domain locks, supported symlinks still work, and cross-domain contention/error-release cases pass. Owned shard passes exact coverage and zero AUDIT checks.</acceptance_criteria>
  <verify>
    <automated>cargo test --manifest-path src-tauri/Cargo.toml --lib phase08_24</automated>
    <fails_when>Nonzero exit, zero matched tests, missing named cross-domain/lock-order/error-release case for a mutation, parent recreation, partial rekey, deadlock, or an expected case skipped.</fails_when>
    <automated>cargo check --manifest-path src-tauri/Cargo.toml -p maru-cli</automated>
    <fails_when>Nonzero exit.</fails_when>
    <automated>node scripts/check-command-isolation.mjs --plan 24</automated>
    <fails_when>Nonzero exit.</fails_when>
  </verify>
  <done>All commands named by this task compile with unchanged wire names and synchronous Rust call sites. Named phase08_24 tests run; blocking body and lock waits execute in a blocking worker; no lost update or permission bypass; shared hierarchical admission precedes domain locks, supported symlinks still work, and cross-domain contention/error-release cases pass. Owned shard passes exact coverage and zero AUDIT checks.</done>
</task>
</tasks>

<threat_model>
OWASP ASVS L1; block on high. No new dependency or external integration.

| Trust boundary | Protected invariant |
|---|---|
| Webview IPC to worker | Owned inputs retain backend permission, containment and revision validation |
| Worker to filesystem/process | Scoped serialization preserves current disk state and fixed argv |
| Test fixture to native process | Only disposable roots and compile-time gated observation |

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-08-24-01 | Tampering | Owned mutation paths | high | mitigate | Task 24-1 preserves or adds scoped transaction validation and denied-write tests before external effects. |
| T-08-24-02 | Denial of service | IPC scheduling and lifecycle | high | mitigate | Finite blocking closure, no guard across await, cleanup on failure/unwind; named task tests plus native saturation gate. |
| T-08-24-03 | Information disclosure | Errors and evidence | medium | mitigate | Preserve redaction, record synthetic fixture paths only, and final feature-off binary/frontend guards. |
</threat_model>

<verification>
Run each task's exact commands from the repository root; compiled tests must match at least one named case. Test paths introduced here are produced before their first consuming verification. Plans with native commands are slow integration gates; other focused checks target under 60 seconds with cached compilation. Never report pending execution as passed.
</verification>

<success_criteria>
Every task acceptance condition holds, assigned inventory rows have a final justified disposition, and PERF-01 evidence is recorded without claims about unrelated commands. Descriptor-less prohibitions remain flagged-unverified until verifier supplies explicit evidence; never auto-dismiss them.
</success_criteria>

<output>
Create .planning/phases/08-main-thread-responsiveness/08-24-SUMMARY.md after execution, including actual checks, owned rows and threat dispositions.
</output>
