---
phase: 08-main-thread-responsiveness
plan: "04"
type: execute
wave: 4
depends_on: ["08-03"]
files_modified: ["src-tauri/src/skill_host/env.rs","src-tauri/src/skill_host/dispatch.rs","docs/performance/phase08-04.json","scripts/check-command-isolation.mjs","scripts/check-command-isolation.test.mjs","src-tauri/src/lib.rs"]
autonomous: true
requirements: ["PERF-01"]
estimate:
  tokens: 30000
  raw_tokens: 30000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "Environment setup and dispatch setup never block shared async workers."
    - "All command ownership evidence is machine-checkable without asserting that a text scan proves thread safety."
  artifacts:
    - path: "src-tauri/src/skill_host/env.rs"
      provides: "Environment and dispatch blocking boundaries implementation or executable verification"
    - path: "src-tauri/src/skill_host/dispatch.rs"
      provides: "Environment and dispatch blocking boundaries implementation or executable verification"
    - path: "scripts/check-command-isolation.mjs"
      provides: "Environment and dispatch blocking boundaries implementation or executable verification"
    - path: "scripts/check-command-isolation.test.mjs"
      provides: "Environment and dispatch blocking boundaries implementation or executable verification"
    - path: "src-tauri/src/lib.rs"
      provides: "Environment and dispatch blocking boundaries implementation or executable verification"
  key_links:
    - from: "src-tauri/src/skill_host/env.rs"
      to: "src-tauri/src/skill_host/dispatch.rs"
      via: "Existing registered command and explicit transaction/verification flow described in tasks"
  prohibitions:
    - statement: "No automatic sync retry, duplicate queue, new job center, or Phase 9 quit/terminal escalation."
      status: resolved
      verification: explicit
    - statement: "No live workspace, credentials, permission bypass, or native test hooks in production artifacts."
      status: resolved
      verification: explicit
---

<objective>
Environment and dispatch blocking boundaries. Environment setup and dispatch setup never block shared async workers. All command ownership evidence is machine-checkable without asserting that a text scan proves thread safety.
Purpose: satisfy PERF-01 within the existing command/UI contracts.
Output: tested source changes and exact owned-command evidence.
</objective>

<execution_context>
@.claude/gsd-core/workflows/execute-plan.md
@.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@README.md
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/08-main-thread-responsiveness/08-CONTEXT.md
@.planning/phases/08-main-thread-responsiveness/08-RESEARCH.md
@.planning/phases/08-main-thread-responsiveness/08-PATTERNS.md
@.planning/phases/08-main-thread-responsiveness/08-PLAN-MAP.md
</context>

## Artifacts this phase produces

- *_blocking sync helpers for assigned env/dispatch commands
- scripts/check-command-isolation.mjs --plan/--all/--expected-count flags
- scripts/check-command-isolation.test.mjs
- docs/performance/phase08-04.json
- env::ipc and dispatch::ipc same-name wrappers
- Inline test module/name prefix `phase08_04` where tasks specify Rust tests; existing tests stay available.
- New helper identifiers are limited to the explicitly listed names and `<owned_command>_blocking` for each converted command in the table below; these are proposed symbols, not existing APIs.



## Exclusive command ownership

- `src-tauri/src/skill_host/dispatch.rs`: `skills_runtime_status`, `skills_dispatch_compose`, `skills_dispatch_terminal`, `skills_dispatch_background`.
- `src-tauri/src/skill_host/env.rs`: `skills_env_status`, `skills_env_bootstrap`, `skills_env_repair`.


## Earlier-writer integration handoff

This plan closes its exclusive command rows and preserves D-01..D-05. Shared hierarchical path admission is produced by 06 and integrated into these earlier writers by 29, which executes immediately after 06. Source leases and domain guards are prerequisites, not cross-domain exclusion. Record `integrationRequired: "08-29"` and `moduleIntegrationOwner: "08-29"` for store/env/dispatch/git/dot modules; command row ownership stays here. No task here imports or tests an unproduced Plan 06/29 symbol. Its focused checker accepts this explicit staged requirement only for these producer plans; final closure requires the actual 29 overlay and test evidence. Preserve owned synchronous entry points for that later integration, including background callbacks and source snapshot/commit seams.

<tasks>
<task type="auto">
  <name>04-1: Offload environment and dispatch finite work</name>
  <reversibility rating="reversible">Internal scheduling and test changes preserve public command names and persisted data.</reversibility>
  <files>src-tauri/src/skill_host/env.rs, src-tauri/src/skill_host/dispatch.rs, docs/performance/phase08-04.json, src-tauri/src/lib.rs</files>
  <read_first>src-tauri/src/skill_host/env.rs; src-tauri/src/skill_host/dispatch.rs; docs/performance/phase08-04.json; src-tauri/src/lib.rs (new artifacts are created by this task if absent); README.md; docs/SSOT-TIERS.md; docs/BOUNDARIES.md; .planning/phases/08-main-thread-responsiveness/08-CONTEXT.md; .planning/phases/08-main-thread-responsiveness/08-PATTERNS.md; .planning/phases/08-main-thread-responsiveness/08-COMMAND-INVENTORY.md; src-tauri/src/hwped.rs; src-tauri/src/ipc_error.rs; src-tauri/src/lock_recovery.rs</read_first>
  <action>Convert the exact seven env/dispatch rows in the ownership table. Keep long-running spawned background worker ownership; offload environment inspection/repair, command resolution, filesystem and subprocess setup before returning. Preserve approvals before launch and revalidate owned target inside worker. Construct owned State handles and AppHandle outside await; guards and borrowed reporters stay inside closure. Preserve String or IpcError as currently declared; JoinError only adds contextual legacy display text. Retain synchronous domain helpers for store/CLI callers. Add inline phase08_04 tests for successful local setup, denied dispatch with zero process launch and failure cleanup. Fill own evidence shard. Preserve original synchronous env/dispatch functions; remove their command attributes and add same-name async wrappers in each pub mod ipc, changing the corresponding lib.rs registration paths. Already async isolated functions retain their registration. This keeps all store and CLI callers synchronous and compiling.</action>
  <acceptance_criteria>Seven rows closed, including both AUDIT dispatch commands; existing runtime status/dispatch callers compile and denied dispatch has no effect.</acceptance_criteria>
  <verify>
    <automated>cargo test --manifest-path src-tauri/Cargo.toml --lib phase08_04</automated>
    <fails_when>Nonzero exit, zero matched/passed tests, missing named case, or an expected case skipped.</fails_when>
    <automated>cargo check --manifest-path src-tauri/Cargo.toml -p maru-cli</automated>
    <fails_when>Nonzero exit.</fails_when>
  </verify>
  <done>Seven rows closed, including both AUDIT dispatch commands; existing runtime status/dispatch callers compile and denied dispatch has no effect.</done>
</task>

<task type="auto">
  <name>04-2: Make exhaustive command evidence fail closed</name>
  <reversibility rating="reversible">Internal scheduling and test changes preserve public command names and persisted data.</reversibility>
  <files>scripts/check-command-isolation.mjs, scripts/check-command-isolation.test.mjs</files>
  <read_first>scripts/check-command-isolation.mjs; scripts/check-command-isolation.test.mjs (new artifacts are created by this task if absent); README.md; docs/SSOT-TIERS.md; docs/BOUNDARIES.md; .planning/phases/08-main-thread-responsiveness/08-CONTEXT.md; .planning/phases/08-main-thread-responsiveness/08-PATTERNS.md; .planning/phases/08-main-thread-responsiveness/08-COMMAND-INVENTORY.md; src-tauri/src/hwped.rs; src-tauri/src/ipc_error.rs; src-tauri/src/lock_recovery.rs</read_first>
  <action>Create a checker that reads lib.rs generate_handler registrations, the research inventory and docs/performance/phase08-NN.json shards. --plan NN validates only that plan's exact assigned command set; --all requires every current production registration exactly once, baseline 365 preserved unless an explicitly recorded new production registration exists. Reject unknown/duplicate/unowned rows, empty evidence, AUDIT dispositions, missing source/helper paths, missing behavior-test names and a CONVERT row lacking async boundary plus spawn_blocking evidence. Account native feature-only commands separately. Treat function-body scans as drift alarms with explicit human-reviewed chain records, never proof that unknown callbacks are safe. Test using miniature fixture sources that missing/duplicate/AUDIT, blocking command(async), and omitted indirect helper each produce nonzero exit; valid bounded UI/model and isolated examples pass. Tests must not inspect live credentials. Implement --integration 29 to validate the separate docs/performance/phase08-29-integration.json overlay schema in PLAN-MAP; it contains integration records referencing existing command keys, not command inventory rows. --plan 01/02/03/04/05 permits their explicit integrationRequired=08-29 obligations before that artifact exists. --integration 29 requires its own produced tests and module/path/admission evidence, but accepts document-race evidence as an explicit consumer obligation owned by 07; it must not require unproduced document wrappers. --all requires both the 29 overlay and 07 cross-domain document evidence, rejects pending integration and treats integration IDs separately from the 365 command rows. Add miniature fixtures for early-stage acceptance, missing final overlay rejection, duplicate command injection via overlay rejection, and missing 07 consumer evidence rejection.</action>
  <acceptance_criteria>Checker red fixtures all rejected, valid fixture accepted; --plan 04 matches seven rows with no AUDIT.</acceptance_criteria>
  <verify>
    <automated>node --test scripts/check-command-isolation.test.mjs</automated>
    <fails_when>Nonzero exit, zero matched/passed tests, missing named case, or an expected case skipped.</fails_when>
    <automated>node scripts/check-command-isolation.mjs --plan 04</automated>
    <fails_when>Nonzero exit.</fails_when>
  </verify>
  <done>Checker red fixtures all rejected, valid fixture accepted; --plan 04 matches seven rows with no AUDIT.</done>
</task>
</tasks>

<threat_model>
OWASP ASVS L1; block on high. No new dependency or external integration.

| Trust boundary | Protected invariant |
|---|---|
| Webview IPC to worker | Owned inputs retain backend permission, containment and revision validation |
| Worker to filesystem/process | Scoped serialization preserves current disk state and fixed argv |
| Test fixture to native process | Only disposable roots and compile-time gated observation |

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-08-04-01 | Tampering | Owned mutation paths | high | mitigate | Task 04-1 preserves or adds scoped transaction validation and denied-write tests before external effects. |
| T-08-04-02 | Denial of service | IPC scheduling and lifecycle | high | mitigate | Finite blocking closure, no guard across await, cleanup on failure/unwind; named task tests plus native saturation gate. |
| T-08-04-03 | Information disclosure | Errors and evidence | medium | mitigate | Preserve redaction, record synthetic fixture paths only, and final feature-off binary/frontend guards. |
</threat_model>

<verification>
Run each task's exact commands from the repository root; compiled tests must match at least one named case. Test paths introduced here are produced before their first consuming verification. Plans with native commands are slow integration gates; other focused checks target under 60 seconds with cached compilation. Never report pending execution as passed.
</verification>

<success_criteria>
Every task acceptance condition holds, assigned inventory rows have a final justified disposition, and PERF-01 evidence is recorded without claims about unrelated commands. Descriptor-less prohibitions remain flagged-unverified until verifier supplies explicit evidence; never auto-dismiss them.
</success_criteria>

<output>
Create .planning/phases/08-main-thread-responsiveness/08-04-SUMMARY.md after execution, including actual checks, owned rows and threat dispositions.
</output>
