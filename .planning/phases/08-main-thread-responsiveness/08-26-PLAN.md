---
phase: 08-main-thread-responsiveness
plan: "26"
type: execute
wave: 27
depends_on: ["08-25","08-03"]
files_modified: ["src/App.tsx","src/components/settings/tabs/SkillsTab.tsx","src/components/studio/StudioMode.tsx","src/lib/processingOperations.test.ts","src/lib/skillOperations.test.ts","docs/performance/phase08-processing-callers.json"]
autonomous: true
requirements: ["PERF-01","PERF-02"]
estimate:
  tokens: 30000
  raw_tokens: 30000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "D-04: Background completion never forces navigation or publishes another workspace's data."
    - "D-01/D-05: Skills stale and failed operations notify once and leave retry manual."
    - "Every in-scope file-processing caller has a concrete lifetime and refresh owner."
  artifacts:
    - path: "src/App.tsx"
      provides: "Scope visible refreshes and close processing callers implementation or executable verification"
    - path: "src/components/settings/tabs/SkillsTab.tsx"
      provides: "Scope visible refreshes and close processing callers implementation or executable verification"
    - path: "src/components/studio/StudioMode.tsx"
      provides: "Scope visible refreshes and close processing callers implementation or executable verification"
    - path: "src/lib/processingOperations.test.ts"
      provides: "Scope visible refreshes and close processing callers implementation or executable verification"
    - path: "src/lib/skillOperations.test.ts"
      provides: "Scope visible refreshes and close processing callers implementation or executable verification"
  key_links:
    - from: "src/App.tsx"
      to: "src/components/settings/tabs/SkillsTab.tsx"
      via: "Existing registered command and explicit transaction/verification flow described in tasks"
  prohibitions:
    - statement: "No automatic sync retry, duplicate queue, new job center, or Phase 9 quit/terminal escalation."
      status: resolved
      verification: explicit
    - statement: "No live workspace, credentials, permission bypass, or native test hooks in production artifacts."
      status: resolved
      verification: explicit
---

<objective>
Scope visible refreshes and close processing callers. D-04: Background completion never forces navigation or publishes another workspace's data. D-01/D-05: Skills stale and failed operations notify once and leave retry manual. Every in-scope file-processing caller has a concrete lifetime and refresh owner.
Purpose: satisfy PERF-01 and PERF-02 within the existing command/UI contracts.
Output: tested source changes and exact owned-command evidence.
</objective>

<execution_context>
@.claude/gsd-core/workflows/execute-plan.md
@.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@README.md
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/08-main-thread-responsiveness/08-CONTEXT.md
@.planning/phases/08-main-thread-responsiveness/08-RESEARCH.md
@.planning/phases/08-main-thread-responsiveness/08-PATTERNS.md
@.planning/phases/08-main-thread-responsiveness/08-PLAN-MAP.md
</context>

## Artifacts this phase produces

- Workspace/request generation guards at App, SkillsTab and StudioMode processing refresh sites
- docs/performance/phase08-processing-callers.json command/caller/owner/fulfilled-classifier/success-retention/failure-reasons/notice/refresh evidence
- Inline test module/name prefix `phase08_26` where tasks specify Rust tests; existing tests stay available.
- New helper identifiers are limited to the explicitly listed names and `<owned_command>_blocking` for each converted command in the table below; these are proposed symbols, not existing APIs.

The caller audit closes D-04, not just component unmount tests. The final native gate fails if phase08-processing-callers.json contains an unresolved row. No new UI surface or UI-SPEC is required.



<tasks>
<task type="auto">
  <name>26-1: Guard App and Skills visible refreshes</name>
  <reversibility rating="reversible">Internal scheduling and test changes preserve public command names and persisted data.</reversibility>
  <files>src/App.tsx, src/components/settings/tabs/SkillsTab.tsx, src/lib/processingOperations.test.ts, src/lib/skillOperations.test.ts</files>
  <read_first>src/App.tsx; src/components/settings/tabs/SkillsTab.tsx; src/lib/processingOperations.test.ts; src/lib/skillOperations.test.ts (new artifacts are created by this task if absent); README.md; docs/SSOT-TIERS.md; docs/BOUNDARIES.md; .planning/phases/08-main-thread-responsiveness/08-CONTEXT.md; .planning/phases/08-main-thread-responsiveness/08-PATTERNS.md; .planning/phases/08-main-thread-responsiveness/08-COMMAND-INVENTORY.md; src-tauri/src/hwped.rs; src-tauri/src/ipc_error.rs; src-tauri/src/lock_recovery.rs; src/lib/processingOperations.ts; src/lib/api.ts; src/lib/export.ts; src/lib/studio.ts; src/lib/errorStore.ts</read_first>
  <action>Per D-04, capture initiating workspace and request-generation token before each user-started processing promise in App's Files/Inbox/export handlers and SkillsTab's create/import/save/delete/install/reconcile/bundle handlers. Delegate mutation/notification lifetime and Plan 25 fulfilled-result classification to the stores; component cleanup only invalidates view publication. Before any refresh/setter ensure both workspace and request token still match; changing A->B->A creates a new generation. On revisit re-read disk and reattach progress. Use useRef or existing scoped store generation, adding no MainApp useState/useEffect. Route direct skills processing operations through startSkillOperation and avoid a second local completion toast. Consolidate existing component error and success notices under that same terminal owner, including App's fulfilled Inbox outcomes.filter(!ok) setError branch and WorkspaceMutationOutcome.status=error handling. Keep successful Inbox carry updates and local admitted detail state, but remove duplicate terminal setError/toast publication for owned operations; rejected errors follow the same rule. Preflight/approval failures before an operation starts retain their existing owner. The surviving owning wrapper must supply an explicit typed fulfilled-result classifier; promise resolution alone is never success. ProcessingCompletion records all-success, partial-success or all-failed, retained successful item/output references, failed item labels and actionable reasons. Publish success only when every requested item succeeded, info for partial success with counts/reasons, and error for all failed; empty/no-op results are informational, not fabricated success. Keep payloads and rejection objects returned to callers unchanged, preserve successful siblings, and leave retry manual. Classify and publish once in the module owner before any workspace/view-admission check, so unsubscribe or A->B->A cannot discard failure reasons. Component-local setters may update currently admitted detail state, but only the operation owner publishes the terminal toast. Preserve preflight approval and disk guards. Extend the controlled-promise tests with the exact production ownership/guard selectors for successful and failing unmount cases, duplicate remount and stale A result while B is active. Resolve mixed and all-failed Inbox/WorkspaceMutation/export payloads after unsubscribe and workspace switch; observe both OperationNotice and the existing error toast channel to assert exactly one correctly classified terminal notice, preserved reasons/successful results and no stale view publication.</action>
  <acceptance_criteria>Persisted mutation can finish after navigation but no stale component data overwrites B or later A; fulfilled mixed/all-failed results produce exactly one correctly classified notice across both toast channels with retained reasons/successes, and native final tests observe unchanged selected mode.</acceptance_criteria>
  <verify>
    <automated>pnpm exec vitest run src/lib/processingOperations.test.ts src/lib/skillOperations.test.ts</automated>
    <fails_when>Nonzero exit, zero tests, missing fulfilled mixed/all-failed after-navigation cases, duplicate terminal notice across error/operation channels, incorrect severity, lost reason/success, stale view publication, or an expected case skipped.</fails_when>
    <automated>pnpm typecheck</automated>
    <fails_when>Nonzero exit.</fails_when>
  </verify>
  <done>Persisted mutation can finish after navigation but no stale component data overwrites B or later A; fulfilled mixed/all-failed results produce exactly one correctly classified notice across both toast channels with retained reasons/successes, and native final tests observe unchanged selected mode.</done>
</task>

<task type="auto">
  <name>26-2: Guard Studio and reconcile all completion call sites</name>
  <reversibility rating="reversible">Internal scheduling and test changes preserve public command names and persisted data.</reversibility>
  <files>src/components/studio/StudioMode.tsx, src/lib/processingOperations.test.ts, docs/performance/phase08-processing-callers.json</files>
  <read_first>src/components/studio/StudioMode.tsx; src/lib/processingOperations.test.ts; docs/performance/phase08-processing-callers.json (new artifacts are created by this task if absent); README.md; docs/SSOT-TIERS.md; docs/BOUNDARIES.md; .planning/phases/08-main-thread-responsiveness/08-CONTEXT.md; .planning/phases/08-main-thread-responsiveness/08-PATTERNS.md; .planning/phases/08-main-thread-responsiveness/08-COMMAND-INVENTORY.md; src-tauri/src/hwped.rs; src-tauri/src/ipc_error.rs; src-tauri/src/lock_recovery.rs; src/lib/processingOperations.ts; src/lib/api.ts; src/lib/export.ts; src/lib/studio.ts; src/lib/errorStore.ts</read_first>
  <action>Apply the same captured workspace/request generation to Studio template/export and managed file-processing refreshes. Record every processing row from the 365-command evidence as command, frontend caller path/function, owning wrapper/store, terminal notice dedupe key, fulfilled-result classifier and domain fields, successful-result retention, actionable failure reason source, progress ownership and view guard. Audit Studio local success/error notices and keep one outer-flow terminal owner, preserving partial export summaries and unsuccessful template outcomes after navigation. Follow hwped/export/template callers and event-based mission flows; existing mission owners are valid only with explicit settlement/notification evidence. No processing row may be marked covered merely because its promise survives. For commands with no current UI caller (including dormant hwped exports), record the checked symbol references and 'no current user-started UI entry'; do not invent a UI. For existing event-driven missions, record the module-level mission owner and terminal event path rather than claiming a component promise alone provides feedback. Any unresolved existing caller is a failing acceptance condition requiring plan correction, never an exemption. Extend tests so every classified caller is represented and no ordinary read produces a success notice. Include controlled mixed/all-failed Studio/export completion after unsubscribe/workspace switch and assert exactly one correctly classified terminal notice across store/component channels.</action>
  <acceptance_criteria>All processing rows map to concrete owner and notice evidence with zero unresolved caller/classifier or duplicate terminal publisher; late partial/all-failed results retain reasons and successful outputs; selected Studio/workspace view is unchanged by late result.</acceptance_criteria>
  <verify>
    <automated>pnpm exec vitest run src/lib/processingOperations.test.ts</automated>
    <fails_when>Nonzero exit, zero tests, missing fulfilled mixed/all-failed after-navigation cases, duplicate terminal notice across error/operation channels, incorrect severity, lost reason/success, stale view publication, or an expected case skipped.</fails_when>
    <automated>pnpm typecheck</automated>
    <fails_when>Nonzero exit.</fails_when>
  </verify>
  <done>All processing rows map to concrete owner and notice evidence with zero unresolved caller/classifier or duplicate terminal publisher; late partial/all-failed results retain reasons and successful outputs; selected Studio/workspace view is unchanged by late result.</done>
</task>
</tasks>

<threat_model>
OWASP ASVS L1; block on high. No new dependency or external integration.

| Trust boundary | Protected invariant |
|---|---|
| Webview IPC to worker | Owned inputs retain backend permission, containment and revision validation |
| Worker to filesystem/process | Scoped serialization preserves current disk state and fixed argv |
| Test fixture to native process | Only disposable roots and compile-time gated observation |

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-08-26-01 | Tampering | Owned mutation paths | high | mitigate | Task 26-1 preserves or adds scoped transaction validation and denied-write tests before external effects. |
| T-08-26-02 | Denial of service | IPC scheduling and lifecycle | high | mitigate | Finite blocking closure, no guard across await, cleanup on failure/unwind; named task tests plus native saturation gate. |
| T-08-26-03 | Information disclosure | Errors and evidence | medium | mitigate | Preserve redaction, record synthetic fixture paths only, and final feature-off binary/frontend guards. |
</threat_model>

<verification>
Run each task's exact commands from the repository root; compiled tests must match at least one named case. Test paths introduced here are produced before their first consuming verification. Plans with native commands are slow integration gates; other focused checks target under 60 seconds with cached compilation. Never report pending execution as passed.
</verification>

<success_criteria>
Every task acceptance condition holds, assigned inventory rows have a final justified disposition, and PERF-01/PERF-02 evidence is recorded without claims about unrelated commands. Descriptor-less prohibitions remain flagged-unverified until verifier supplies explicit evidence; never auto-dismiss them.
</success_criteria>

<output>
Create .planning/phases/08-main-thread-responsiveness/08-26-SUMMARY.md after execution, including actual checks, owned rows and threat dispositions.
</output>
