---
phase: 08-main-thread-responsiveness
plan: "02"
type: execute
wave: 2
depends_on: ["08-01"]
files_modified: ["src-tauri/Cargo.toml","src-tauri/src/skill_host/store.rs","src-tauri/src/ipc_error.rs","src/lib/types.ts","src/lib/skills.ts","src/lib/skills.test.ts","src/lib/skillOperations.ts","src/lib/skillOperations.test.ts","src/lib/errorStore.ts","src/lib/errorStore.test.tsx","src/App.tsx","src/components/settings/tabs/SkillsTab.tsx","src/lib/i18n/locales/ko.ts","src/lib/i18n/locales/en.ts","docs/performance/phase08-02.json"]
autonomous: true
requirements: ["PERF-02","PERF-01"]
estimate:
  tokens: 30000
  raw_tokens: 30000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "The entire Sync All IPC operation, including defaults and lock admission, executes in an awaited blocking worker."
    - "D-03: Sync All skips an in-flight source immediately and synchronizes other sources."
    - "D-01 and D-05: Stale and failed outcomes preserve current settings and successful siblings; no automatic retry starts."
    - "D-04: Skills work settles after unmount and emits exactly one existing-surface notice without overwriting another workspace."
  artifacts:
    - path: "src-tauri/src/skill_host/store.rs"
      provides: "Sync All outcomes and surviving completion implementation or executable verification"
    - path: "src-tauri/src/ipc_error.rs"
      provides: "Sync All outcomes and surviving completion implementation or executable verification"
    - path: "src/lib/types.ts"
      provides: "Sync All outcomes and surviving completion implementation or executable verification"
    - path: "src/lib/skills.ts"
      provides: "Sync All outcomes and surviving completion implementation or executable verification"
    - path: "src/lib/skills.test.ts"
      provides: "Sync All outcomes and surviving completion implementation or executable verification"
    - path: "src/lib/skillOperations.ts"
      provides: "Sync All outcomes and surviving completion implementation or executable verification"
    - path: "src/lib/skillOperations.test.ts"
      provides: "Sync All outcomes and surviving completion implementation or executable verification"
    - path: "src/lib/errorStore.ts"
      provides: "Sync All outcomes and surviving completion implementation or executable verification"
    - path: "src/lib/errorStore.test.tsx"
      provides: "Sync All outcomes and surviving completion implementation or executable verification"
    - path: "src/App.tsx"
      provides: "Sync All outcomes and surviving completion implementation or executable verification"
    - path: "src/components/settings/tabs/SkillsTab.tsx"
      provides: "Sync All outcomes and surviving completion implementation or executable verification"
    - path: "src/lib/i18n/locales/ko.ts"
      provides: "Sync All outcomes and surviving completion implementation or executable verification"
    - path: "src/lib/i18n/locales/en.ts"
      provides: "Sync All outcomes and surviving completion implementation or executable verification"
  key_links:
    - from: "src-tauri/src/skill_host/store.rs"
      to: "src-tauri/src/ipc_error.rs"
      via: "Existing registered command and explicit transaction/verification flow described in tasks"
  prohibitions:
    - statement: "No automatic sync retry, duplicate queue, new job center, or Phase 9 quit/terminal escalation."
      status: resolved
      verification: explicit
    - statement: "No live workspace, credentials, permission bypass, or native test hooks in production artifacts."
      status: resolved
      verification: explicit
---

<objective>
Sync All outcomes and surviving completion. D-03: Sync All skips an in-flight source immediately and synchronizes other sources. D-01 and D-05: Stale and failed outcomes preserve current settings and successful siblings; no automatic retry starts. D-04: Skills work settles after unmount and emits exactly one existing-surface notice without overwriting another workspace.
Purpose: satisfy PERF-02 and PERF-01 within the existing command/UI contracts.
Output: tested source changes and exact owned-command evidence.
</objective>

<execution_context>
@.claude/gsd-core/workflows/execute-plan.md
@.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@README.md
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/08-main-thread-responsiveness/08-CONTEXT.md
@.planning/phases/08-main-thread-responsiveness/08-RESEARCH.md
@.planning/phases/08-main-thread-responsiveness/08-PATTERNS.md
@.planning/phases/08-main-thread-responsiveness/08-PLAN-MAP.md
</context>

## Artifacts this phase produces

- SKILLS_SOURCE_BUSY and SKILLS_SOURCE_STALE Rust/TypeScript codes
- skills_sync_all_sources async IPC wrapper awaiting spawn_blocking; existing skills_sync_all_sources_impl remains synchronous
- phase08_batch_transactions::actual_wrapper_allows_async_progress
- SyncSourceResult.skipped and SyncAllOutcome.skipped additive fields
- src/lib/skillOperations.ts with startSkillOperation, subscribeSkillOperations, getSkillOperationsSnapshot
- src/lib/skillOperations.test.ts controlled-promise lifetime tests
- OperationNotice, publishOperationNotice, useOperationNotice in errorStore.ts
- docs/performance/phase08-02.json
- Inline test module/name prefix `phase08_02` where tasks specify Rust tests; existing tests stay available.
- New helper identifiers are limited to the explicitly listed names and `<owned_command>_blocking` for each converted command in the table below; these are proposed symbols, not existing APIs.

Reversibility: costly coordinated additive IPC outcome/error contract; existing fields and String error text remain compatible, no persisted schema migration. Tasks intentionally separated at five modified files each. Native navigation settlement is reverified in final native plans.

## Exclusive command ownership

- `src-tauri/src/skill_host/store.rs`: `skills_sync_all_sources`.

## Cross-domain integration handoff

Keep this plan's reviewed whole-batch worker boundary and payload/notice contracts. Its store command row remains owned by 02; final store integration owner is 29 after helper producer 06. Record integrationRequired=08-29 in the shard; no future helper import or check is required here. Plan 29 integrates the existing shared per-source/batch/default paths and reruns phase08_batch_transactions::actual_wrapper_allows_async_progress. Source duplicates and busy batch skips remain nonblocking before any path wait.

<tasks>
<task type="auto">
  <name>02-1: Make batch transactions independently durable with typed outcomes</name>
  <reversibility rating="costly">Coordinated additive IPC and local UI contract; no removal or data migration.</reversibility>
  <files>src-tauri/Cargo.toml, src-tauri/src/skill_host/store.rs, src-tauri/src/ipc_error.rs, src/lib/types.ts, src/lib/skills.ts, src/lib/skills.test.ts</files>
  <read_first>src-tauri/src/skill_host/store.rs; src-tauri/src/ipc_error.rs; src/lib/types.ts; src/lib/skills.ts; src/lib/skills.test.ts (new artifacts are created by this task if absent); README.md; docs/SSOT-TIERS.md; docs/BOUNDARIES.md; .planning/phases/08-main-thread-responsiveness/08-CONTEXT.md; .planning/phases/08-main-thread-responsiveness/08-PATTERNS.md; .planning/phases/08-main-thread-responsiveness/08-COMMAND-INVENTORY.md; src-tauri/src/hwped.rs; src-tauri/src/ipc_error.rs; src-tauri/src/lock_recovery.rs</read_first>
  <action>Convert the existing ordinary synchronous #[tauri::command] skills_sync_all_sources into an async IPC wrapper that awaits tauri::async_runtime::spawn_blocking. Move owned AppHandle and progress_id into that closure and construct ProgressReporter there; call synchronous skills_sync_all_sources_impl from inside it, retaining that implementation and its sync Rust/CLI callers. The worker owns the entire batch: ensuring defaults, registry snapshot/admission, all lock waits, per-source transactions, network/subprocess work, scans and result assembly. Preserve the inner typed or legacy result unchanged; map only the outer JoinError contextually. Per D-01/D-02/D-03/D-05, snapshot source identities/generations briefly after ensuring defaults, then call the tracer transaction with the expected identity for each source. Busy results have skipped=true, ok=false, skills=0 and no new last_synced_at; stale/deleted sources produce failed results instead of resolving replacements. Persist each success independently. Require total=results.length=succeeded+failed+skipped, including 0 and 1 source. Add skipped fields compatibly; preserve existing successful result fields and legacy String error text. Single source busy/stale failures become IpcError with known codes skills_source_busy/skills_source_stale; add Rust constants, TS known-code union, normalizer use in skills.ts and structured-error guard coverage. Normalize JoinError to display-only IpcError with empty code and contextual message. Update browser fallback and every batch consumer in the subsequent task; do not parse messages or perform retries. Tests phase08_batch_transactions cover A busy while B persists and C fails, missing/replaced identity before its turn, batch failure cleanup and manual retry. Add phase08_batch_transactions::actual_wrapper_allows_async_progress using the actual async command wrapper and a barrier in the real batch path, with a temporary registry and local source. While batch work is held, schedule an unrelated yielding task on the same Tauri async runtime and require it to complete before releasing the barrier; after release require a nonempty batch outcome. Hold defaults/admission and a per-source work stage in separate cases so pre-worker setup cannot escape coverage. Restore/release barriers on every test exit; no sleep-only or helper-only scheduling proof.</action>
  <acceptance_criteria>The actual Sync All wrapper allows same-runtime async progress while defaults/admission and batch work are held; no batch work or lock wait precedes spawn_blocking. Busy is not counted as succeeded or failed; every snapshot identity gets exactly one result; CLI compiles; native and browser wrapper error normalization preserve typed codes and legacy text.</acceptance_criteria>
  <verify>
    <automated>cargo test --manifest-path src-tauri/Cargo.toml --lib phase08_batch_transactions</automated>
    <fails_when>Nonzero exit, zero tests, missing actual_wrapper_allows_async_progress cases, same-runtime probe cannot finish before batch barrier release, empty result, or any expected case skipped.</fails_when>
    <automated>pnpm exec vitest run src/lib/skills.test.ts</automated>
    <fails_when>Nonzero exit, zero matched/passed tests, missing named case, or an expected case skipped.</fails_when>
    <automated>cargo check --manifest-path src-tauri/Cargo.toml -p maru-cli</automated>
    <fails_when>Nonzero exit.</fails_when>
  </verify>
  <done>The actual Sync All wrapper allows same-runtime async progress while defaults/admission and batch work are held; no batch work or lock wait precedes spawn_blocking. Busy is not counted as succeeded or failed; every snapshot identity gets exactly one result; CLI compiles; native and browser wrapper error normalization preserve typed codes and legacy text.</done>
</task>

<task type="auto">
  <name>02-2: Own Skills operation lifetime beyond component subscriptions</name>
  <reversibility rating="costly">Coordinated additive IPC and local UI contract; no removal or data migration.</reversibility>
  <files>src/lib/skillOperations.ts, src/lib/skillOperations.test.ts, src/lib/errorStore.ts, src/lib/errorStore.test.tsx</files>
  <read_first>src/lib/skillOperations.ts; src/lib/skillOperations.test.ts; src/lib/errorStore.ts; src/lib/errorStore.test.tsx (new artifacts are created by this task if absent); README.md; docs/SSOT-TIERS.md; docs/BOUNDARIES.md; .planning/phases/08-main-thread-responsiveness/08-CONTEXT.md; .planning/phases/08-main-thread-responsiveness/08-PATTERNS.md; .planning/phases/08-main-thread-responsiveness/08-COMMAND-INVENTORY.md; src-tauri/src/hwped.rs; src-tauri/src/ipc_error.rs; src-tauri/src/lock_recovery.rs</read_first>
  <action>Per D-04/D-05, create the narrow skillOperations external store keyed by initiating workspace and operation ID/source ID. Store immutable snapshots and own the mutation promise and skills-op://progress listener until settlement, even with no subscribers. Listener registration failure must still allow mutation result reporting and dispose a late listener; filter events by progress_id. Add an OperationNotice channel in the existing errorStore with success/info/error and deduplication by operation ID; preserve setError/useError behavior. Exactly one terminal notice is published, with source and workspace label, reason on failure, and manual retry language. Store no job persistence, queue or retry scheduler. Controlled promise tests cover unmount then resolve/reject, remount duplicate, independent sources, A->B and A->B->A, listener race and exactly-once notices.</action>
  <acceptance_criteria>Disposed view subscriptions never cancel mutations; a result emits one notice; subscriptions cannot mutate current workspace snapshots with stale generations.</acceptance_criteria>
  <verify>
    <automated>pnpm exec vitest run src/lib/skillOperations.test.ts src/lib/errorStore.test.tsx</automated>
    <fails_when>Nonzero exit, zero matched/passed tests, missing named case, or an expected case skipped.</fails_when>
  </verify>
  <done>Disposed view subscriptions never cancel mutations; a result emits one notice; subscriptions cannot mutate current workspace snapshots with stale generations.</done>
</task>

<task type="auto">
  <name>02-3: Connect existing progress and toast UI to lifetime ownership</name>
  <reversibility rating="costly">Coordinated additive IPC and local UI contract; no removal or data migration.</reversibility>
  <files>src/App.tsx, src/components/settings/tabs/SkillsTab.tsx, src/lib/i18n/locales/ko.ts, src/lib/i18n/locales/en.ts, docs/performance/phase08-02.json</files>
  <read_first>src/App.tsx; src/components/settings/tabs/SkillsTab.tsx; src/lib/i18n/locales/ko.ts; src/lib/i18n/locales/en.ts; docs/performance/phase08-02.json (new artifacts are created by this task if absent); README.md; docs/SSOT-TIERS.md; docs/BOUNDARIES.md; .planning/phases/08-main-thread-responsiveness/08-CONTEXT.md; .planning/phases/08-main-thread-responsiveness/08-PATTERNS.md; .planning/phases/08-main-thread-responsiveness/08-COMMAND-INVENTORY.md; src-tauri/src/hwped.rs; src-tauri/src/ipc_error.rs; src-tauri/src/lock_recovery.rs</read_first>
  <action>Wire SkillsTab sync and Sync All actions through startSkillOperation. Preserve current progress UI, show explicit skipped count, disable duplicate source actions while backend remains authoritative, and keep errors actionable. Guard refresh publication with initiating workspace plus monotonically changed view request token; A->B->A must invalidate the earlier view refresh, while revisiting reads persisted results and reattaches operation progress. In App's existing toast region render useOperationNotice with the current notice styling; do not add MainApp useState/useEffect or a new surface. Add localized skills.operation.success, skills.operation.skipped, skills.operation.stale and skills.operation.failed strings in both locales. Extend the operation-store tests to verify selector/admission inputs used by SkillsTab and use existing native skills-sync smoke to observe success notice. Write docs/performance/phase08-02.json with exactly the owned command rows and schema in 08-PLAN-MAP.md; test evidence remains honest and nonempty.</action>
  <acceptance_criteria>D-01..D-05 appear in user-visible existing feedback; navigation stays on selected mode; typecheck and i18n checks pass.</acceptance_criteria>
  <verify>
    <automated>pnpm typecheck</automated>
    <fails_when>Nonzero exit.</fails_when>
    <automated>pnpm lint:i18n</automated>
    <fails_when>Nonzero exit.</fails_when>
    <automated>pnpm exec vitest run src/lib/skillOperations.test.ts</automated>
    <fails_when>Nonzero exit, zero matched/passed tests, missing named case, or an expected case skipped.</fails_when>
  </verify>
  <done>D-01..D-05 appear in user-visible existing feedback; navigation stays on selected mode; typecheck and i18n checks pass.</done>
</task>
</tasks>

<threat_model>
OWASP ASVS L1; block on high. No new dependency or external integration.

| Trust boundary | Protected invariant |
|---|---|
| Webview IPC to worker | Owned inputs retain backend permission, containment and revision validation |
| Worker to filesystem/process | Scoped serialization preserves current disk state and fixed argv |
| Test fixture to native process | Only disposable roots and compile-time gated observation |

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-08-02-01 | Tampering | Owned mutation paths | high | mitigate | Task 02-1 preserves or adds scoped transaction validation and denied-write tests before external effects. |
| T-08-02-02 | Denial of service | IPC scheduling and lifecycle | high | mitigate | Finite blocking closure, no guard across await, cleanup on failure/unwind; named task tests plus native saturation gate. |
| T-08-02-03 | Information disclosure | Errors and evidence | medium | mitigate | Preserve redaction, record synthetic fixture paths only, and final feature-off binary/frontend guards. |
</threat_model>

<verification>
Run each task's exact commands from the repository root; compiled tests must match at least one named case. Test paths introduced here are produced before their first consuming verification. Plans with native commands are slow integration gates; other focused checks target under 60 seconds with cached compilation. Never report pending execution as passed.
</verification>

<success_criteria>
Every task acceptance condition holds, assigned inventory rows have a final justified disposition, and PERF-02/PERF-01 evidence is recorded without claims about unrelated commands. Descriptor-less prohibitions remain flagged-unverified until verifier supplies explicit evidence; never auto-dismiss them.
</success_criteria>

<output>
Create .planning/phases/08-main-thread-responsiveness/08-02-SUMMARY.md after execution, including actual checks, owned rows and threat dispositions.
</output>

## Execution scope correction

- Dev-only existing Tauri test feature enables an actual command wrapper test with MockRuntime; generic AppHandle and narrow progress emitter preserve the wire contract. No new dependency package is introduced.
- sync_source_transaction accepts an optional expected snapshot; skills_sync_all_sources_blocking is the extracted per-snapshot batch loop for empty/singleton and identity coverage. SyncSourceResult.errorCode is additive for typed partial failure classification.
