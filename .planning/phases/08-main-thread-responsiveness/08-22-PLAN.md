---
phase: 08-main-thread-responsiveness
plan: "22"
type: execute
wave: 23
depends_on: ["08-21"]
files_modified: ["src-tauri/src/maru_dir.rs","src-tauri/src/vault_list.rs","src-tauri/src/workspace.rs","src-tauri/src/lib.rs","docs/performance/phase08-22.json"]
autonomous: true
requirements: ["PERF-01"]
estimate:
  tokens: 30000
  raw_tokens: 30000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "Workspace configuration reaches no network, process or unbounded walk on the UI or shared async worker thread."
    - "All 30 owned commands preserve wire types, permission checks and synchronous internal callers."
    - "Concurrent operations preserve lifecycle ordering and current disk state; each retained/native UI exception has positive evidence."
  artifacts:
    - path: "src-tauri/src/maru_dir.rs"
      provides: "Workspace configuration command isolation implementation or executable verification"
    - path: "src-tauri/src/vault_list.rs"
      provides: "Workspace configuration command isolation implementation or executable verification"
    - path: "src-tauri/src/workspace.rs"
      provides: "Workspace configuration command isolation implementation or executable verification"
    - path: "src-tauri/src/lib.rs"
      provides: "Workspace configuration command isolation implementation or executable verification"
  key_links:
    - from: "src-tauri/src/maru_dir.rs"
      to: "src-tauri/src/vault_list.rs"
      via: "Existing registered command and explicit transaction/verification flow described in tasks"
  prohibitions:
    - statement: "No automatic sync retry, duplicate queue, new job center, or Phase 9 quit/terminal escalation."
      status: resolved
      verification: explicit
    - statement: "No live workspace, credentials, permission bypass, or native test hooks in production artifacts."
      status: resolved
      verification: explicit
---

<objective>
Workspace configuration command isolation. Workspace configuration reaches no network, process or unbounded walk on the UI or shared async worker thread. All 30 owned commands preserve wire types, permission checks and synchronous internal callers. Concurrent operations preserve lifecycle ordering and current disk state; each retained/native UI exception has positive evidence.
Purpose: satisfy PERF-01 within the existing command/UI contracts.
Output: tested source changes and exact owned-command evidence.
</objective>

<execution_context>
@.claude/gsd-core/workflows/execute-plan.md
@.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@README.md
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/08-main-thread-responsiveness/08-CONTEXT.md
@.planning/phases/08-main-thread-responsiveness/08-RESEARCH.md
@.planning/phases/08-main-thread-responsiveness/08-PATTERNS.md
@.planning/phases/08-main-thread-responsiveness/08-PLAN-MAP.md
</context>

## Artifacts this phase produces

- src-tauri/src/maru_dir.rs: pub mod ipc with async same-name wrappers for each converted owned command; original sync functions retained
- src-tauri/src/vault_list.rs: pub mod ipc with async same-name wrappers for each converted owned command; original sync functions retained
- src-tauri/src/workspace.rs: pub mod ipc with async same-name wrappers for each converted owned command; original sync functions retained
- docs/performance/phase08-22.json final command evidence
- Inline phase08_22 behavioral tests
- Owned-input <command>_blocking helpers only where State/borrow adaptation is required, retaining original synchronous API signatures
- Inline test module/name prefix `phase08_22` where tasks specify Rust tests; existing tests stay available.
- New helper identifiers are limited to the explicitly listed names, `<owned_command>_in_transaction` adapters and `<owned_command>_blocking` for each converted command in the table below; these are proposed symbols, not existing APIs.

Registration edits share lib.rs, so this plan depends on the immediately preceding tranche. This deliberately avoids parallel same-file conflicts while preserving independently green app/CLI builds. No extra dispatcher or build infrastructure is introduced to manufacture parallelism. Existing implementation helper modules are read-only unless explicitly listed; place shared admission in the owned mutation entry, before existing domain guards; no existing-guard exemption. Read src-tauri/src/atomic_file.rs before adapting any write; Plan 06 produces the shared path transaction helper.

## Exclusive command ownership

- `src-tauri/src/maru_dir.rs`: `read_maru_workspace`, `update_maru_workspace`, `bootstrap_maru_dir`, `read_maru_ignore`, `save_maru_ignore`, `list_maru_rules`, `read_maru_rule`, `save_maru_rule`, `delete_maru_rule`, `list_maru_templates`, `read_maru_template`, `save_maru_template`, `delete_maru_template`, `read_maru_mcp`, `save_maru_mcp`, `read_maru_projects`, `list_workspace_projects`, `save_maru_projects`, `read_maru_skills`, `read_maru_settings`, `save_maru_settings`.
- `src-tauri/src/vault_list.rs`: `list_workspace_roots`, `add_workspace_root`, `refresh_workspace_capabilities`, `remove_workspace_root`, `set_active_workspace_root`.
- `src-tauri/src/workspace.rs`: `detect_workspace`, `read_workspace_config`, `register_workspace_roots`, `list_workspaces`.

<tasks>
<task type="auto">
  <name>22-1: Isolate src/maru_dir.rs, src/vault_list.rs, src/workspace.rs</name>
  <reversibility rating="reversible">Internal scheduling and test changes preserve public command names and persisted data.</reversibility>
  <files>src-tauri/src/maru_dir.rs, src-tauri/src/vault_list.rs, src-tauri/src/workspace.rs, src-tauri/src/lib.rs</files>
  <read_first>src-tauri/src/maru_dir.rs; src-tauri/src/vault_list.rs; src-tauri/src/workspace.rs; src-tauri/src/lib.rs (new artifacts are created by this task if absent); README.md; docs/SSOT-TIERS.md; docs/BOUNDARIES.md; .planning/phases/08-main-thread-responsiveness/08-CONTEXT.md; .planning/phases/08-main-thread-responsiveness/08-PATTERNS.md; .planning/phases/08-main-thread-responsiveness/08-COMMAND-INVENTORY.md; src-tauri/src/hwped.rs; src-tauri/src/ipc_error.rs; src-tauri/src/lock_recovery.rs; src-tauri/src/atomic_file.rs; .planning/phases/08-main-thread-responsiveness/08-PLAN-MAP.md</read_first>
  <action>Own and close these exact command rows: src-tauri/src/maru_dir.rs: read_maru_workspace, update_maru_workspace, bootstrap_maru_dir, read_maru_ignore, save_maru_ignore, list_maru_rules, read_maru_rule, save_maru_rule, delete_maru_rule, list_maru_templates, read_maru_template, save_maru_template, delete_maru_template, read_maru_mcp, save_maru_mcp, read_maru_projects, list_workspace_projects, save_maru_projects, read_maru_skills, read_maru_settings, save_maru_settings; src-tauri/src/vault_list.rs: list_workspace_roots, add_workspace_root, refresh_workspace_capabilities, remove_workspace_root, set_active_workspace_root; src-tauri/src/workspace.rs: detect_workspace, read_workspace_config, register_workspace_roots, list_workspaces. Read every body and all reached helpers including cfg/platform branches, traits/callbacks and command(async) bodies. For blocking synchronous functions, retain the original public synchronous function and its name for Rust/CLI callers; remove its command attribute and add a same-name async #[tauri::command] function inside this module's new pub mod ipc. That wrapper owns its inputs and awaits tauri::async_runtime::spawn_blocking calling super::the_original_name. Update only corresponding generate_handler paths in lib.rs to module::ipc::command; the wire name is unchanged. Existing genuinely async/isolated/UI entries keep their original registration path unless their body needs a finite blocking substep. Construct ProgressReporter inside worker; clone State's owned Arc/handle rather than carrying borrowed State or guards across await. Preserve Result<T,String> versus Result<T,IpcError>; inner typed errors pass through untouched, while JoinError becomes contextual display-only error of the same outer type. Do not use block_on or an executor/dispatch registry. Read each AUDIT helper including defaults/bootstrap. Finite config reads can block on disk or locks and should use a worker unless a positive runtime bound is documented. Serialize workspace registry read-modify-write; do not overwrite concurrently added roots or mutate caller-provided foreign directories. Add inline tests with prefix phase08_22 in modified modules: nonempty real fixture result, unchanged typed/legacy rejection, and at least one deterministic overlap case where a blocked worker still permits a same-runtime async yield. For every newly parallel write, inventory its transaction key and test same-target contention; keep authorization and revision validation inside the worker/transaction. Per D-04, annotate each user-started processing row's frontend call sites and existing operation completion ownership in this shard; Plan 25/26 implements the shared lifetime path, not a new job service. For a synchronous function that accepts State or another borrowed runtime object, extract an owned-input synchronous <command>_blocking helper for the actual work; both the unchanged public synchronous function and the async ipc wrapper call that helper. The wrapper clones the Arc/handle from State before awaiting. Do not fabricate a State or send a borrowed guard/reporter into spawn_blocking. Define and list exact owned argument types in the implementation summary. For every filesystem mutation, follow the Shared mutation admission contract in 08-PLAN-MAP.md using atomic_file::with_path_transactions from Plan 06, including ancestor/descendant and symlink-alias conflicts. Declare the complete source/destination/sidecar write set; acquire shared admission inside the blocking worker before existing domain guards, then revalidate lexical containment, required existing parents and revisions. Existing guards remain nested participants, never exemptions. Nested calls use the admitted PathTransactionLease via <owned_command>_in_transaction adapters without reacquiring guards. Hold admission through write/rekey/rollback; no old parent recreation. Record the shared entry and lock order for actual sync/IPC/background writers and add deterministic real-command cross-domain contention and error-release cases.</action>
  <acceptance_criteria>All commands named by this task compile with unchanged wire names and synchronous Rust call sites. Named phase08_22 tests run; blocking body and lock waits execute in a blocking worker; no lost update or permission bypass; shared hierarchical admission precedes domain locks, supported symlinks still work, and cross-domain contention/error-release cases pass. </acceptance_criteria>
  <verify>
    <automated>cargo test --manifest-path src-tauri/Cargo.toml --lib phase08_22</automated>
    <fails_when>Nonzero exit, zero matched tests, missing named cross-domain/lock-order/error-release case for a mutation, parent recreation, partial rekey, deadlock, or an expected case skipped.</fails_when>
    <automated>cargo check --manifest-path src-tauri/Cargo.toml -p maru-cli</automated>
    <fails_when>Nonzero exit.</fails_when>
  </verify>
  <done>All commands named by this task compile with unchanged wire names and synchronous Rust call sites. Named phase08_22 tests run; blocking body and lock waits execute in a blocking worker; no lost update or permission bypass; shared hierarchical admission precedes domain locks, supported symlinks still work, and cross-domain contention/error-release cases pass. </done>
</task>

<task type="auto">
  <name>22-2: Prove lifecycle and error contracts</name>
  <reversibility rating="reversible">Internal scheduling and test changes preserve public command names and persisted data.</reversibility>
  <files>src-tauri/src/maru_dir.rs, docs/performance/phase08-22.json</files>
  <read_first>src-tauri/src/maru_dir.rs; docs/performance/phase08-22.json (new artifacts are created by this task if absent); README.md; docs/SSOT-TIERS.md; docs/BOUNDARIES.md; .planning/phases/08-main-thread-responsiveness/08-CONTEXT.md; .planning/phases/08-main-thread-responsiveness/08-PATTERNS.md; .planning/phases/08-main-thread-responsiveness/08-COMMAND-INVENTORY.md; src-tauri/src/hwped.rs; src-tauri/src/ipc_error.rs; src-tauri/src/lock_recovery.rs; src-tauri/src/atomic_file.rs; .planning/phases/08-main-thread-responsiveness/08-PLAN-MAP.md</read_first>
  <action>Extend the already converted module's inline tests to cover error/cleanup and concurrent operations through the actual wrapper. Read each AUDIT helper including defaults/bootstrap. Finite config reads can block on disk or locks and should use a worker unless a positive runtime bound is documented. Serialize workspace registry read-modify-write; do not overwrite concurrently added roots or mutate caller-provided foreign directories. Add inline tests with prefix phase08_22 in modified modules: nonempty real fixture result, unchanged typed/legacy rejection, and at least one deterministic overlap case where a blocked worker still permits a same-runtime async yield. For every newly parallel write, inventory its transaction key and test same-target contention; keep authorization and revision validation inside the worker/transaction. Write docs/performance/phase08-22.json with every owned row exactly once, final disposition and decisive helper/affinity/bound plus tests and current registration path. Zero AUDIT; retained/UI/ISOLATED rows require positive evidence too. Preserve other shards and the research inventory; final merge is Plan 28. Per D-04, annotate each user-started processing row's frontend call sites and existing operation completion ownership in this shard; Plan 25/26 implements the shared lifetime path, not a new job service. For a synchronous function that accepts State or another borrowed runtime object, extract an owned-input synchronous <command>_blocking helper for the actual work; both the unchanged public synchronous function and the async ipc wrapper call that helper. The wrapper clones the Arc/handle from State before awaiting. Do not fabricate a State or send a borrowed guard/reporter into spawn_blocking. Define and list exact owned argument types in the implementation summary. For every filesystem mutation, follow the Shared mutation admission contract in 08-PLAN-MAP.md using atomic_file::with_path_transactions from Plan 06, including ancestor/descendant and symlink-alias conflicts. Declare the complete source/destination/sidecar write set; acquire shared admission inside the blocking worker before existing domain guards, then revalidate lexical containment, required existing parents and revisions. Existing guards remain nested participants, never exemptions. Nested calls use the admitted PathTransactionLease via <owned_command>_in_transaction adapters without reacquiring guards. Hold admission through write/rekey/rollback; no old parent recreation. Record the shared entry and lock order for actual sync/IPC/background writers and add deterministic real-command cross-domain contention and error-release cases.</action>
  <acceptance_criteria>All commands named by this task compile with unchanged wire names and synchronous Rust call sites. Named phase08_22 tests run; blocking body and lock waits execute in a blocking worker; no lost update or permission bypass; shared hierarchical admission precedes domain locks, supported symlinks still work, and cross-domain contention/error-release cases pass. Owned shard passes exact coverage and zero AUDIT checks.</acceptance_criteria>
  <verify>
    <automated>cargo test --manifest-path src-tauri/Cargo.toml --lib phase08_22</automated>
    <fails_when>Nonzero exit, zero matched tests, missing named cross-domain/lock-order/error-release case for a mutation, parent recreation, partial rekey, deadlock, or an expected case skipped.</fails_when>
    <automated>cargo check --manifest-path src-tauri/Cargo.toml -p maru-cli</automated>
    <fails_when>Nonzero exit.</fails_when>
    <automated>node scripts/check-command-isolation.mjs --plan 22</automated>
    <fails_when>Nonzero exit.</fails_when>
  </verify>
  <done>All commands named by this task compile with unchanged wire names and synchronous Rust call sites. Named phase08_22 tests run; blocking body and lock waits execute in a blocking worker; no lost update or permission bypass; shared hierarchical admission precedes domain locks, supported symlinks still work, and cross-domain contention/error-release cases pass. Owned shard passes exact coverage and zero AUDIT checks.</done>
</task>
</tasks>

<threat_model>
OWASP ASVS L1; block on high. No new dependency or external integration.

| Trust boundary | Protected invariant |
|---|---|
| Webview IPC to worker | Owned inputs retain backend permission, containment and revision validation |
| Worker to filesystem/process | Scoped serialization preserves current disk state and fixed argv |
| Test fixture to native process | Only disposable roots and compile-time gated observation |

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-08-22-01 | Tampering | Owned mutation paths | high | mitigate | Task 22-1 preserves or adds scoped transaction validation and denied-write tests before external effects. |
| T-08-22-02 | Denial of service | IPC scheduling and lifecycle | high | mitigate | Finite blocking closure, no guard across await, cleanup on failure/unwind; named task tests plus native saturation gate. |
| T-08-22-03 | Information disclosure | Errors and evidence | medium | mitigate | Preserve redaction, record synthetic fixture paths only, and final feature-off binary/frontend guards. |
</threat_model>

<verification>
Run each task's exact commands from the repository root; compiled tests must match at least one named case. Test paths introduced here are produced before their first consuming verification. Plans with native commands are slow integration gates; other focused checks target under 60 seconds with cached compilation. Never report pending execution as passed.
</verification>

<success_criteria>
Every task acceptance condition holds, assigned inventory rows have a final justified disposition, and PERF-01 evidence is recorded without claims about unrelated commands. Descriptor-less prohibitions remain flagged-unverified until verifier supplies explicit evidence; never auto-dismiss them.
</success_criteria>

<output>
Create .planning/phases/08-main-thread-responsiveness/08-22-SUMMARY.md after execution, including actual checks, owned rows and threat dispositions.
</output>
