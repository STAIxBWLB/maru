---
phase: 08-main-thread-responsiveness
plan: "06"
type: execute
wave: 6
depends_on: ["08-05"]
files_modified: ["src-tauri/src/vault.rs","src-tauri/src/workspace_files.rs","src-tauri/src/content_search.rs","src-tauri/src/calendar_search.rs","src-tauri/src/lib.rs","docs/performance/phase08-06.json","src-tauri/src/atomic_file.rs"]
autonomous: true
requirements: ["PERF-01"]
estimate:
  tokens: 30000
  raw_tokens: 30000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "Workspace scan and file queue reaches no network, process or unbounded walk on the UI or shared async worker thread."
    - "All 15 owned commands preserve wire types, permission checks and synchronous internal callers."
    - "Concurrent operations preserve lifecycle ordering and current disk state; each retained/native UI exception has positive evidence."
  artifacts:
    - path: "src-tauri/src/vault.rs"
      provides: "Workspace scan and file queue command isolation implementation or executable verification"
    - path: "src-tauri/src/workspace_files.rs"
      provides: "Workspace scan and file queue command isolation implementation or executable verification"
    - path: "src-tauri/src/content_search.rs"
      provides: "Workspace scan and file queue command isolation implementation or executable verification"
    - path: "src-tauri/src/calendar_search.rs"
      provides: "Workspace scan and file queue command isolation implementation or executable verification"
    - path: "src-tauri/src/lib.rs"
      provides: "Workspace scan and file queue command isolation implementation or executable verification"
    - path: "src-tauri/src/atomic_file.rs"
      provides: "Workspace scan and file queue command isolation implementation or executable verification"
  key_links:
    - from: "src-tauri/src/vault.rs"
      to: "src-tauri/src/workspace_files.rs"
      via: "Existing registered command and explicit transaction/verification flow described in tasks"
  prohibitions:
    - statement: "No automatic sync retry, duplicate queue, new job center, or Phase 9 quit/terminal escalation."
      status: resolved
      verification: explicit
    - statement: "No live workspace, credentials, permission bypass, or native test hooks in production artifacts."
      status: resolved
      verification: explicit
---

<objective>
Workspace scan and file queue command isolation. Workspace scan and file queue reaches no network, process or unbounded walk on the UI or shared async worker thread. All 15 owned commands preserve wire types, permission checks and synchronous internal callers. Concurrent operations preserve lifecycle ordering and current disk state; each retained/native UI exception has positive evidence.
Purpose: satisfy PERF-01 within the existing command/UI contracts.
Output: tested source changes and exact owned-command evidence.
</objective>

<execution_context>
@.claude/gsd-core/workflows/execute-plan.md
@.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@README.md
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/08-main-thread-responsiveness/08-CONTEXT.md
@.planning/phases/08-main-thread-responsiveness/08-RESEARCH.md
@.planning/phases/08-main-thread-responsiveness/08-PATTERNS.md
@.planning/phases/08-main-thread-responsiveness/08-PLAN-MAP.md
</context>

## Artifacts this phase produces

- src-tauri/src/vault.rs: pub mod ipc with async same-name wrappers for each converted owned command; original sync functions retained
- src-tauri/src/workspace_files.rs: pub mod ipc with async same-name wrappers for each converted owned command; original sync functions retained
- src-tauri/src/content_search.rs: pub mod ipc with async same-name wrappers for each converted owned command; original sync functions retained
- src-tauri/src/calendar_search.rs: pub mod ipc with async same-name wrappers for each converted owned command; original sync functions retained
- docs/performance/phase08-06.json final command evidence
- Inline phase08_06 behavioral tests
- Owned-input <command>_blocking helpers only where State/borrow adaptation is required, retaining original synchronous API signatures
- src-tauri/src/atomic_file.rs: with_path_transactions, PathTransactionRequest, PathTransactionLease and Mutex/Condvar atomic path-set admission; cfg(test) admission/pre-effect barrier seam and inline phase08_06 contention tests
- Inline test module/name prefix `phase08_06` where tasks specify Rust tests; existing tests stay available.
- New helper identifiers are limited to the explicitly listed names, `<owned_command>_in_transaction` adapters and `<owned_command>_blocking` for each converted command in the table below; these are proposed symbols, not existing APIs.

Registration edits share lib.rs, so this plan depends on the immediately preceding tranche. This deliberately avoids parallel same-file conflicts while preserving independently green app/CLI builds. No extra dispatcher or build infrastructure is introduced to manufacture parallelism. Existing implementation helper modules are read-only unless explicitly listed; place necessary boundary serialization in the owned controller or reuse the existing domain guard.

## Exclusive command ownership

- `src-tauri/src/vault.rs`: `sample_workspace_path`, `scan_vault`, `scan_vault_paths`, `read_vault_cache`.
- `src-tauri/src/workspace_files.rs`: `scan_workspace_files`, `scan_workspace_entries`, `create_workspace_directory`, `rename_workspace_entry`, `duplicate_workspace_entries`, `paste_workspace_entries`, `trash_workspace_entries`, `describe_file_queue_sources`, `apply_file_queue`.
- `src-tauri/src/content_search.rs`: `search_workspace_contents`.
- `src-tauri/src/calendar_search.rs`: `search_calendar_notes`.

<tasks>
<task type="auto">
  <name>06-1: Isolate src/vault.rs, src/workspace_files.rs, src/content_search.rs</name>
  <reversibility rating="reversible">Internal scheduling and test changes preserve public command names and persisted data.</reversibility>
  <files>src-tauri/src/vault.rs, src-tauri/src/workspace_files.rs, src-tauri/src/content_search.rs, src-tauri/src/lib.rs, src-tauri/src/atomic_file.rs</files>
  <read_first>src-tauri/src/vault.rs; src-tauri/src/workspace_files.rs; src-tauri/src/content_search.rs; src-tauri/src/lib.rs; src-tauri/src/atomic_file.rs (new artifacts are created by this task if absent); README.md; docs/SSOT-TIERS.md; docs/BOUNDARIES.md; .planning/phases/08-main-thread-responsiveness/08-CONTEXT.md; .planning/phases/08-main-thread-responsiveness/08-PATTERNS.md; .planning/phases/08-main-thread-responsiveness/08-COMMAND-INVENTORY.md; src-tauri/src/hwped.rs; src-tauri/src/ipc_error.rs; src-tauri/src/lock_recovery.rs</read_first>
  <action>Own and close these exact command rows: src-tauri/src/vault.rs: sample_workspace_path, scan_vault, scan_vault_paths, read_vault_cache; src-tauri/src/workspace_files.rs: scan_workspace_files, scan_workspace_entries, create_workspace_directory, rename_workspace_entry, duplicate_workspace_entries, paste_workspace_entries, trash_workspace_entries, describe_file_queue_sources, apply_file_queue; src-tauri/src/content_search.rs: search_workspace_contents. Read every body and all reached helpers including cfg/platform branches, traits/callbacks and command(async) bodies. For blocking synchronous functions, retain the original public synchronous function and its name for Rust/CLI callers; remove its command attribute and add a same-name async #[tauri::command] function inside this module's new pub mod ipc. That wrapper owns its inputs and awaits tauri::async_runtime::spawn_blocking calling super::the_original_name. Update only corresponding generate_handler paths in lib.rs to module::ipc::command; the wire name is unchanged. Existing genuinely async/isolated/UI entries keep their original registration path unless their body needs a finite blocking substep. Construct ProgressReporter inside worker; clone State's owned Arc/handle rather than carrying borrowed State or guards across await. Preserve Result<T,String> versus Result<T,IpcError>; inner typed errors pass through untouched, while JoinError becomes contextual display-only error of the same outer type. Do not use block_on or an executor/dispatch registry. Preserve Phase 7 generated/non-document-root pruning for full scan, targeted scan and cache reads. Serialize apply_file_queue and destination mutations against concurrent rename/paste/trash using a workspace/path transaction key; existing revision checks and atomic writes stay inside that guard. Add inline tests with prefix phase08_06 in modified modules: nonempty real fixture result, unchanged typed/legacy rejection, and at least one deterministic overlap case where a blocked worker still permits a same-runtime async yield. For every newly parallel write, inventory its transaction key and test same-target contention; keep authorization and revision validation inside the worker/transaction. Per D-04, annotate each user-started processing row's frontend call sites and existing operation completion ownership in this shard; Plan 25/26 implements the shared lifetime path, not a new job service. For a synchronous function that accepts State or another borrowed runtime object, extract an owned-input synchronous <command>_blocking helper for the actual work; both the unchanged public synchronous function and the async ipc wrapper call that helper. The wrapper clones the Arc/handle from State before awaiting. Do not fabricate a State or send a borrowed guard/reporter into spawn_blocking. Define and list exact owned argument types in the implementation summary. Implement the Shared mutation admission contract in 08-PLAN-MAP.md exactly: proposed PathTransactionRequest declares complete lexical plus physical-alias source/destination/sidecar paths and required existing-parent stable filesystem-identity preconditions (not mtime), rechecked after admission to reject same-path removal/recreation; with_path_transactions admits the whole conflicting-subtree set atomically under a short Mutex/Condvar and passes an RAII PathTransactionLease to the closure. Use component-wise ancestry, release bookkeeping during work, and retain intentional symlinks under lexical authorization. Revalidate parents, aliases, permissions and revisions after admission before mutation. Shared admission precedes all existing domain guards; nested <owned_command>_in_transaction adapters reuse the lease and validate coverage. Hold it through complete file queue/rename/trash/rekey/rollback. Add cfg(test)-only admission and pre-effect barrier seams in atomic_file.rs for Plans 29 and 07 real-wrapper races; no production IPC test port. Tests phase08_06 cover same-target and parent/child exclusion, unrelated sibling progress, reversed multi-path requests, successful symlink alias operations, missing/changed parent rejection, error and unwind release; require no recreated old tree or partial rekey. Expose the existing cfg(test) admission/pre-effect barrier seam as pub(crate) for inline tests in store.rs, git.rs, env.rs, dispatch.rs, dot_sync.rs and document.rs; test APIs carry explicit paths and stage labels, never process-global unscoped barriers. Plan 29 consumes these APIs immediately after this plan, before document isolation. Plain write_atomic stays a byte-write primitive: guard the whole transaction, not only its final write.</action>
  <acceptance_criteria>All commands named by this task compile with unchanged wire names and synchronous Rust call sites. Named phase08_06 tests run; blocking body and lock waits execute in a blocking worker; no lost update, parent recreation, partial rekey or permission bypass; hierarchical/alias admission and domain lock-order cases pass. </acceptance_criteria>
  <verify>
    <automated>cargo test --manifest-path src-tauri/Cargo.toml --lib phase08_06</automated>
    <fails_when>Nonzero exit, zero tests, missing hierarchical/alias/order/error-release cases, domain-guard exemption, old parent recreation, partial rekey, deadlock, or an expected case skipped.</fails_when>
    <automated>cargo check --manifest-path src-tauri/Cargo.toml -p maru-cli</automated>
    <fails_when>Nonzero exit.</fails_when>
  </verify>
  <done>All commands named by this task compile with unchanged wire names and synchronous Rust call sites. Named phase08_06 tests run; blocking body and lock waits execute in a blocking worker; no lost update, parent recreation, partial rekey or permission bypass; hierarchical/alias admission and domain lock-order cases pass. </done>
</task>

<task type="auto">
  <name>06-2: Isolate src/calendar_search.rs</name>
  <reversibility rating="reversible">Internal scheduling and test changes preserve public command names and persisted data.</reversibility>
  <files>src-tauri/src/calendar_search.rs, src-tauri/src/lib.rs, docs/performance/phase08-06.json</files>
  <read_first>src-tauri/src/calendar_search.rs; src-tauri/src/lib.rs; docs/performance/phase08-06.json (new artifacts are created by this task if absent); README.md; docs/SSOT-TIERS.md; docs/BOUNDARIES.md; .planning/phases/08-main-thread-responsiveness/08-CONTEXT.md; .planning/phases/08-main-thread-responsiveness/08-PATTERNS.md; .planning/phases/08-main-thread-responsiveness/08-COMMAND-INVENTORY.md; src-tauri/src/hwped.rs; src-tauri/src/ipc_error.rs; src-tauri/src/lock_recovery.rs</read_first>
  <action>Own and close these exact command rows: src-tauri/src/calendar_search.rs: search_calendar_notes. Read every body and all reached helpers including cfg/platform branches, traits/callbacks and command(async) bodies. For blocking synchronous functions, retain the original public synchronous function and its name for Rust/CLI callers; remove its command attribute and add a same-name async #[tauri::command] function inside this module's new pub mod ipc. That wrapper owns its inputs and awaits tauri::async_runtime::spawn_blocking calling super::the_original_name. Update only corresponding generate_handler paths in lib.rs to module::ipc::command; the wire name is unchanged. Existing genuinely async/isolated/UI entries keep their original registration path unless their body needs a finite blocking substep. Construct ProgressReporter inside worker; clone State's owned Arc/handle rather than carrying borrowed State or guards across await. Preserve Result<T,String> versus Result<T,IpcError>; inner typed errors pass through untouched, while JoinError becomes contextual display-only error of the same outer type. Do not use block_on or an executor/dispatch registry. Preserve Phase 7 generated/non-document-root pruning for full scan, targeted scan and cache reads. Serialize apply_file_queue and destination mutations against concurrent rename/paste/trash using a workspace/path transaction key; existing revision checks and atomic writes stay inside that guard. Add inline tests with prefix phase08_06 in modified modules: nonempty real fixture result, unchanged typed/legacy rejection, and at least one deterministic overlap case where a blocked worker still permits a same-runtime async yield. For every newly parallel write, inventory its transaction key and test same-target contention; keep authorization and revision validation inside the worker/transaction. Write docs/performance/phase08-06.json with every owned row exactly once, final disposition and decisive helper/affinity/bound plus tests and current registration path. Zero AUDIT; retained/UI/ISOLATED rows require positive evidence too. Preserve other shards and the research inventory; final merge is Plan 28. Per D-04, annotate each user-started processing row's frontend call sites and existing operation completion ownership in this shard; Plan 25/26 implements the shared lifetime path, not a new job service. For a synchronous function that accepts State or another borrowed runtime object, extract an owned-input synchronous <command>_blocking helper for the actual work; both the unchanged public synchronous function and the async ipc wrapper call that helper. The wrapper clones the Arc/handle from State before awaiting. Do not fabricate a State or send a borrowed guard/reporter into spawn_blocking. Define and list exact owned argument types in the implementation summary.</action>
  <acceptance_criteria>All commands named by this task compile with unchanged wire names and synchronous Rust call sites. Named phase08_06 tests run; blocking body and lock waits execute in a blocking worker; no lost update, parent recreation, partial rekey or permission bypass; hierarchical/alias admission and domain lock-order cases pass. Owned shard passes exact coverage and zero AUDIT checks.</acceptance_criteria>
  <verify>
    <automated>cargo test --manifest-path src-tauri/Cargo.toml --lib phase08_06</automated>
    <fails_when>Nonzero exit, zero tests, missing hierarchical/alias/order/error-release cases, domain-guard exemption, old parent recreation, partial rekey, deadlock, or an expected case skipped.</fails_when>
    <automated>cargo check --manifest-path src-tauri/Cargo.toml -p maru-cli</automated>
    <fails_when>Nonzero exit.</fails_when>
    <automated>node scripts/check-command-isolation.mjs --plan 06</automated>
    <fails_when>Nonzero exit.</fails_when>
  </verify>
  <done>All commands named by this task compile with unchanged wire names and synchronous Rust call sites. Named phase08_06 tests run; blocking body and lock waits execute in a blocking worker; no lost update, parent recreation, partial rekey or permission bypass; hierarchical/alias admission and domain lock-order cases pass. Owned shard passes exact coverage and zero AUDIT checks.</done>
</task>
</tasks>

<threat_model>
OWASP ASVS L1; block on high. No new dependency or external integration.

| Trust boundary | Protected invariant |
|---|---|
| Webview IPC to worker | Owned inputs retain backend permission, containment and revision validation |
| Worker to filesystem/process | Scoped serialization preserves current disk state and fixed argv |
| Test fixture to native process | Only disposable roots and compile-time gated observation |

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-08-06-01 | Tampering | Owned mutation paths | high | mitigate | Task 06-1 preserves or adds scoped transaction validation and denied-write tests before external effects. |
| T-08-06-02 | Denial of service | IPC scheduling and lifecycle | high | mitigate | Finite blocking closure, no guard across await, cleanup on failure/unwind; named task tests plus native saturation gate. |
| T-08-06-03 | Information disclosure | Errors and evidence | medium | mitigate | Preserve redaction, record synthetic fixture paths only, and final feature-off binary/frontend guards. |
</threat_model>

<verification>
Run each task's exact commands from the repository root; compiled tests must match at least one named case. Test paths introduced here are produced before their first consuming verification. Plans with native commands are slow integration gates; other focused checks target under 60 seconds with cached compilation. Never report pending execution as passed.
</verification>

<success_criteria>
Every task acceptance condition holds, assigned inventory rows have a final justified disposition, and PERF-01 evidence is recorded without claims about unrelated commands. Descriptor-less prohibitions remain flagged-unverified until verifier supplies explicit evidence; never auto-dismiss them.
</success_criteria>

<output>
Create .planning/phases/08-main-thread-responsiveness/08-06-SUMMARY.md after execution, including actual checks, owned rows and threat dispositions.
</output>
