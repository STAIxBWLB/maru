No external API integration: this phase builds a local macOS test runner (WebdriverIO driving the app's own WKWebView, PTY and menu-command surfaces) and deliberately leaves every outbound dependency unconfigured per D-11 - no credentials, no provider IO, no updater. The detector fires on the words "Playwright API" and "Tauri API" inside `<read_first>` notes about in-repo test code, not on a service integration.
