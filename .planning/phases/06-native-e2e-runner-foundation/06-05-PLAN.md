---
phase: 06-native-e2e-runner-foundation
plan: 05
type: execute
wave: 4
depends_on: ["06-03", "06-04"]
files_modified:
  - .github/workflows/ci.yml
  - .github/workflows/native-e2e.yml
  - .github/workflows/native-e2e-spike.yml
  - Makefile
  - docs/native-e2e.md
  - .planning/PROJECT.md
autonomous: false
requirements: [TEST-01]

estimate:
  tokens: 44000
  raw_tokens: 44000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "A macOS CI job compiles and typechecks the runner code on every pull request and does not execute the suite, so the runner cannot rot between releases (D-03)."
    - "`make release-preflight` fails when the native suite fails, in both branches of the verdict (D-03)."
    - "If and only if the spike proved all three of D-01's conditions, the full native suite runs on pushes to `main` and on release tags, and never on an ordinary pull request (D-16)."
    - "A developer can read, in one document under `docs/`, what the native runner is, how to run it, which parts run unattended, and which parts a human must do (D-04)."
    - "`.planning/PROJECT.md`'s `CI reality` constraint states the verdict rather than the pre-phase assumption that macOS-native changes ship unverified by CI (D-04)."
    - "The verdict is not recorded as CI-viable unless a WebDriver session established with no interactive prompt, real PTY output was read through the canvas surface, and a full run completed with no human present - all three (D-01)."
    - "The document names which of D-08's two artifacts covers what, so the boundary between automated proof and human attendance is explicit rather than implied."
    - statement: "A reader of `docs/native-e2e.md` can tell which make target runs the mocked-IPC Playwright suite and which runs the real-backend native suite, so a green `make test-e2e` is not misread as full e2e coverage."
      verification: backstop
  prohibitions:
    - statement: "The recorded verdict must not claim CI viability on a partial pass; if any one of D-01's three conditions fails, the runner is recorded as local-only."
    - statement: "The native suite must not be presented as covering the macOS menu bar or real OS-level IME composition if the sub-spikes show it cannot; the document must name what stays human-attended."
  artifacts:
    - path: "docs/native-e2e.md"
      provides: "D-04's native-runner document: scope, how to run, coverage boundary, CI placement, evidence"
      contains: "## Human-attended checklist"
    - path: ".planning/PROJECT.md"
      provides: "Rewritten CI reality constraint matching the verdict"
      contains: "CI reality"
    - path: ".github/workflows/ci.yml"
      provides: "D-03's macOS compile-and-typecheck job, every PR, suite not executed"
      contains: "native-e2e-compile"
    - path: "Makefile"
      provides: "release-preflight blocking on the native suite"
  key_links:
    - from: "Makefile"
      to: "e2e-native/wdio.conf.ts"
      via: "release-preflight invokes test-e2e-native, which runs the wdio config"
      pattern: "test-e2e-native"
    - from: "docs/native-e2e.md"
      to: ".planning/PROJECT.md"
      via: "both record the same verdict; Phase 8 and Phase 9 plan against these two, not against CONTEXT.md"
      pattern: "CI reality"
---

<objective>
Turn the runner from a set of files into a settled fact: place it in CI according to what
the spike actually proved, make it blocking in the local release gate either way, and write
the verdict down in the two places the next two phases will read.

Purpose: D-04 is explicit that Phase 8 and Phase 9 plan their verification against the
`docs/` document and the rewritten `PROJECT.md` constraint, not against this phase's
CONTEXT. ROADMAP criterion 5 asks for the CI-vs-local scope to be recorded as a settled
fact rather than an assumption carried forward. D-01's three-condition rule is what decides
which branch gets written, and this plan is the first point in the phase where all three
conditions have evidence: session establishment from plan 06-01, PTY readability from plan
06-02, and a full unattended run from the suite as it now stands.

Output: the D-03 compile job, the D-16 gated full-suite workflow if and only if the verdict
allows it, `release-preflight` wiring, the completed `docs/native-e2e.md`, and the rewritten
`CI reality` constraint.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-native-e2e-runner-foundation/06-CONTEXT.md
@.planning/phases/06-native-e2e-runner-foundation/06-PATTERNS.md
@.planning/phases/06-native-e2e-runner-foundation/06-01-SUMMARY.md
@.planning/phases/06-native-e2e-runner-foundation/06-03-SUMMARY.md
@.planning/phases/06-native-e2e-runner-foundation/06-04-SUMMARY.md
</context>

## Artifacts this phase produces

Newly created symbols and paths. Nothing below exists in the tree before Phase 6, so a
drift check must not treat any of them as an unacknowledged rename of an existing symbol.

- Cargo feature `native-e2e`; optional cargo dependency `tauri-plugin-wdio-webdriver`
- Environment variables `MARU_NATIVE_E2E_HOME`, `MARU_NATIVE_E2E_CONFIG_DIR`
- Vite env flag `VITE_NATIVE_E2E`
- Rust constants `paths::NATIVE_E2E_HOME_VAR`, `paths::NATIVE_E2E_CONFIG_DIR_VAR`;
  Rust functions `paths::resolve_native_e2e_dir`, `paths::native_e2e_dir_override`
- Frontend module `src/lib/nativeE2eBridge.ts`, exporting `nativeE2eEnabled`,
  `registerTerminalTextReader`, `registerMenuCommandDispatcher`, and the type
  `MaruNativeE2eBridge`
- Browser global `window.__MARU_NATIVE_E2E__`, with members `terminalText` and `menuCommand`
- Helper exports `seedFixtureWorkspace`, `resetFixtureWorkspace`, `cleanupFixtureWorkspace`,
  `readTerminalText`, `assertTerminalInk`, `INK_CHANNEL_TOLERANCE`, `INK_MIN_RATIO`
- Guard script `scripts/check-native-e2e-isolation.mjs`
- Make targets `test-e2e-native`, `check-native-e2e-isolation`
- pnpm scripts `test:e2e:native`, `build:frontend:native-e2e`, `check:native-e2e-isolation`
- New files: `e2e-native/wdio.conf.ts`, `e2e-native/helpers/fixtureWorkspace.ts`,
  `e2e-native/helpers/ptyAssertions.ts`, `e2e-native/specs/webview.spec.ts`,
  `e2e-native/specs/pty.spec.ts`, `e2e-native/specs/ime.spec.ts`,
  `e2e-native/specs/menu.spec.ts`, `tsconfig.e2e-native.json`,
  `src/lib/nativeE2eBridge.ts`, `src/lib/nativeE2eBridge.test.ts`,
  `scripts/check-native-e2e-isolation.mjs`, `docs/native-e2e.md`,
  `.github/workflows/native-e2e-spike.yml`, `.github/workflows/native-e2e.yml`
- CI job ids: `spike` in `native-e2e-spike.yml`, `native-e2e-compile` in `ci.yml`, and
  `native-e2e-suite` in `native-e2e.yml` (created only in the CI-viable branch of D-01)

<flagged_assumptions>
The spec-less edge-coverage probe returned one row for this phase (TEST-01, `unclassified`,
`unresolved`). It is surfaced in full in `06-01-PLAN.md` under `<flagged_assumptions>`, and
this plan is where it resolves: TEST-01's own acceptance condition is the verdict Task 3
ratifies. If the spike surfaced an edge outside the three D-01 conditions plus D-02's
failure taxonomy, it belongs in `docs/native-e2e.md` and in a gap-closure plan; it is not
dropped.
</flagged_assumptions>

<tasks>

<task type="auto">
  <name>Task 1: CI placement per the verdict, and the blocking local release gate</name>
  <precondition>`make test-e2e-native` passes locally on macOS with all four D-13 surface
  specs, and `docs/native-e2e.md` carries the spike log from plan 06-01 plus the sub-spike
  verdicts from plan 06-03, so which branch to write is a matter of reading the record
  rather than deciding now.</precondition>
  <reversibility rating="costly">D-03 rates the CI job and the preflight wiring costly:
  both are referenced by the release process and by this milestone's evidence trail, so
  removing them later means re-arguing the evidence standard rather than editing one
  file.</reversibility>
  <files>.github/workflows/ci.yml, .github/workflows/native-e2e.yml, .github/workflows/native-e2e-spike.yml, Makefile</files>
  <read_first>
    - `.github/workflows/ci.yml` (whole file) - the `decision` dedupe job and its
      `run_full` output, and the `verify` job's `needs`/`if` gating plus its checkout,
      pnpm, Node, Rust and `Swatinem/rust-cache` step shape
    - `.github/workflows/native-e2e-spike.yml` from plan 06-01 - the macOS runner setup this
      task reuses, and the spike's recorded outcome
    - `.github/workflows/release-bundles.yml` - the existing `macos-latest` matrix leg and
      how release tags are triggered in this repository today
    - `Makefile` lines 200-230 (`test-e2e`, `verify-integration` and its exclusion comment)
      and lines 258-271 (`release-preflight-core`, `release-preflight`)
    - `docs/native-e2e.md` - the spike log and both sub-spike verdicts, which decide the branch
    - `.planning/phases/06-native-e2e-runner-foundation/06-CONTEXT.md` decisions D-03 and D-16
  </read_first>
  <action>
    Wire the local gate first, because it is identical in both branches. In the `Makefile`,
    add `$(MAKE) test-e2e-native` to `release-preflight`'s recipe alongside the existing
    `release-preflight-core`, `cli-smoke` and `test-e2e` lines. Do not add it to
    `release-preflight-core`, which `release-checks` and CI both reach, and do not add it to
    `verify`; the native target launches a real macOS application and cannot be hermetic,
    which is the same reason `verify-integration` sits outside `verify`. D-03 makes this
    gate blocking in both branches of the verdict, so this edit does not depend on what the
    spike returned.

    Add the D-03 compile job to `.github/workflows/ci.yml` with job id `native-e2e-compile`,
    running on `macos-14`. Give it the same `needs: decision` and
    `if: needs.decision.outputs.run_full == 'true'` gating the existing jobs use, so the
    dedupe path keeps working. Copy the `verify` job's checkout, `pnpm/action-setup@v6` at
    9.15.0, `actions/setup-node@v5` at 22.22.3 with the pnpm cache,
    `dtolnay/rust-toolchain@stable` and `Swatinem/rust-cache@v2` steps, with a distinct
    `shared-key`, and drop the Linux-only `libwebkit2gtk` / `libxdo` apt step. Its steps are
    `pnpm install --frozen-lockfile`, `pnpm typecheck`, `pnpm lint`, and a cargo check of
    the runner-gated code with the feature on plus `--locked`. Use `--locked` here rather
    than the `--offline` this repository's local sandbox needs: a hosted runner has no warm
    registry cache on a cold job, and `--offline` would fail for a reason unrelated to the
    code. This job must not run the suite - D-03 is explicit that it compiles and
    typechecks only, and adding a run step here would put a real macOS application launch on
    every pull request, which D-16 rejects on cost and on early-flakiness grounds.

    Then branch on the recorded verdict, and only on it.

    If `docs/native-e2e.md` records that all three D-01 conditions held - a session
    established with no interactive prompt, PTY output was read through the canvas surface,
    and a full run completed with no human present - create
    `.github/workflows/native-e2e.yml` with job id `native-e2e-suite` on `macos-14`,
    triggered by `push` to `main`, by `push` on release tags matching this repository's
    existing tag convention, and by `workflow_dispatch`. It runs `make test-e2e-native` and
    uploads the wdio output on failure. Put it in its own workflow file rather than in
    `ci.yml`, because `ci.yml`'s `push` trigger is scoped to `branches: [main]` and adding a
    tag trigger there would change when the entire pipeline runs. Then delete
    `.github/workflows/native-e2e-spike.yml`: this workflow supersedes it.

    If the verdict is either local-only branch, do not create `native-e2e.yml` at all. Keep
    `native-e2e-spike.yml` in place instead, and add a header comment recording the observed
    failure class and that it exists so a future macOS runner image can be re-probed
    without rebuilding the spike from scratch - D-02 anticipates exactly that re-attempt.
    The `native-e2e-compile` job and the `release-preflight` wiring stay in this branch too;
    they are what stop the runner rotting while it is human-attended.

    In both branches, do not weaken any existing gate and do not touch the `verify` or `e2e`
    jobs.
  </action>
  <verify>
    <automated>node -e "const fs=require('node:fs');const ci=fs.readFileSync('.github/workflows/ci.yml','utf8');if(!/native-e2e-compile:/.test(ci))throw new Error('D-03 compile job missing from ci.yml');if(!/macos-14/.test(ci))throw new Error('compile job must run on macos-14');if(/make test-e2e-native/.test(ci))throw new Error('the PR compile job must not execute the suite');const mk=fs.readFileSync('Makefile','utf8');const pre=mk.split('release-preflight:')[1].split('\n.PHONY')[0];if(!/test-e2e-native/.test(pre))throw new Error('release-preflight does not block on the native suite');const verifyLine=mk.split('\n').find(l=>l.startsWith('verify:'));if(/test-e2e-native/.test(verifyLine))throw new Error('the native target must stay out of make verify');"</automated>
    <automated>node -e "const fs=require('node:fs');const doc=fs.readFileSync('docs/native-e2e.md','utf8');const ciViable=/ci-viable/.test(doc)&amp;&amp;!/local-only/.test(doc);const hasSuite=fs.existsSync('.github/workflows/native-e2e.yml');const hasSpike=fs.existsSync('.github/workflows/native-e2e-spike.yml');if(ciViable&amp;&amp;!hasSuite)throw new Error('verdict is CI-viable but no gated suite workflow exists');if(ciViable&amp;&amp;hasSpike)throw new Error('the spike workflow should be superseded in the CI-viable branch');if(!ciViable&amp;&amp;hasSuite)throw new Error('verdict is local-only but a gated CI suite workflow exists');if(!ciViable&amp;&amp;!hasSpike)throw new Error('local-only branch should keep the spike workflow for a later re-probe');"</automated>
    <automated>pnpm exec js-yaml .github/workflows/ci.yml > /dev/null 2>&amp;1 || node -e "const y=require('node:fs').readFileSync('.github/workflows/ci.yml','utf8');if(/\t/.test(y))throw new Error('tab character in YAML');"</automated>
  </verify>
  <acceptance_criteria>
    - `.github/workflows/ci.yml` has a `native-e2e-compile` job on `macos-14`, gated by
      `needs: decision` and `run_full`, whose steps are install, typecheck, lint and a
      feature-on cargo check, and which contains no invocation of the native suite.
    - The `native-e2e-compile` job's cargo step uses `--locked`, not `--offline`.
    - The `verify` and `e2e` jobs in `ci.yml` are unchanged.
    - `Makefile`'s `release-preflight` recipe invokes `test-e2e-native`, and `verify`'s
      prerequisite list does not.
    - Exactly one of these holds, matching the recorded verdict: (CI-viable)
      `.github/workflows/native-e2e.yml` exists with job id `native-e2e-suite` triggered on
      `main` pushes, release tags and `workflow_dispatch`, and `native-e2e-spike.yml` is
      deleted; or (local-only) `native-e2e.yml` does not exist and `native-e2e-spike.yml`
      survives with a header comment naming the observed failure class.
    - No workflow other than the one created by this task is triggered on release tags.
  </acceptance_criteria>
  <done>
    The native suite blocks `make release-preflight` on every machine, a macOS PR job keeps
    the runner compiling, and the suite's CI placement matches what the spike proved rather
    than what anyone hoped.
  </done>
</task>

<task type="auto">
  <name>Task 2: Write the verdict into the two places Phase 8 and Phase 9 will read</name>
  <files>docs/native-e2e.md, .planning/PROJECT.md</files>
  <read_first>
    - `docs/native-e2e.md` as it stands - the `## Spike log` from plan 06-01 and the
      `## IME sub-spike` and `## macOS menu bar` sections from plan 06-03
    - `docs/macos-passkeys.md` lines 1-40 - the structural analog: a scope paragraph that
      says what the default build is unaffected by, then process, then evidence
    - `.planning/PROJECT.md` lines 230-236 - the `CI reality` bullet to rewrite, and the
      bullets around it for tone and length
    - `.planning/codebase/TESTING.md` "Run Commands" and "E2E Tests" - the existing three
      test layers this document must not contradict
    - `.planning/phases/06-native-e2e-runner-foundation/06-CONTEXT.md` decisions D-01, D-04,
      D-08, D-13 and D-16
    - `.planning/phases/06-native-e2e-runner-foundation/06-RESEARCH.md` Pitfall 5
  </read_first>
  <action>
    Complete `docs/native-e2e.md` around the three sections that already exist. Follow the
    shape `docs/macos-passkeys.md` uses: open with what this is and what the default build
    is unaffected by, then how to run it, then the boundary, then the evidence.

    Write these sections. A scope paragraph saying the native runner drives the real Maru
    application - the real WKWebView DOM, a real PTY, and IME input - against the real Rust
    backend, that it is macOS-only, that it lives in `e2e-native/` and is separate from the
    Playwright suite in `e2e/`, and that nothing it adds reaches a shipped build. A `## How
    to run` section giving `make test-e2e-native` and stating what it builds first and why
    the `dist/` it leaves behind is deliberately not shippable. A section stating plainly
    which target runs which suite: `make test-e2e` is Playwright against Vite with mocked
    IPC, `make test-e2e-native` is WebdriverIO against the real backend, and a green result
    from one says nothing about the other. A `## CI placement` section stating the verdict
    and its consequence: which jobs exist, what runs on a pull request, what runs on `main`
    and on release tags, and what a human must run. A `## Human-attended checklist` section
    consolidating the fixed items - the macOS menu-bar click-through from plan 06-03, and,
    if the IME sub-spike came back negative for a surface, the Korean two-set composition
    check for that surface, written as concrete steps with an expected result rather than as
    "verify IME works". A `## Known limits` section recording that the runner targets debug
    builds, and that a hardened-runtime notarized `.app` restricts automation mechanisms a
    debug build does not, so the runner's result does not transfer to a signed artifact
    without re-verification.

    Be exact about D-08's split. State which of the two artifacts covers what: the
    synthetic-composition specs inside the runner guard the app's own composition-handling
    logic at the native level, and the human checklist covers the OS input method itself.
    Naming which one covers what is the point of D-08; a document that says "IME is
    partially covered" fails it.

    Do not overstate. D-01 is a three-condition rule and a partial pass is not promoted, so
    if any condition failed, the document says local-only and names which condition and why.
    The value of this document to Phase 8 and Phase 9 is entirely in it being accurate about
    what does not work.

    Then rewrite the `CI reality` bullet in `.planning/PROJECT.md`. It currently asserts
    that `make verify` runs on `ubuntu-22.04` only, that e2e runs Chromium against Vite with
    mocked IPC, and that macOS-native changes ship unverified by CI. Keep the first two
    clauses, which are still true, and replace the third with what is now true: which macOS
    job runs on a pull request, whether the native suite runs unattended anywhere, what
    `make release-preflight` now blocks on, and a pointer to `docs/native-e2e.md`. Match the
    surrounding bullets' length and tone - two or three lines, not a paragraph. Do not touch
    any other constraint.
  </action>
  <verify>
    <automated>node -e "const d=require('node:fs').readFileSync('docs/native-e2e.md','utf8'); const need=['## How to run','## CI placement','## Human-attended checklist','## Known limits','## Spike log','## IME sub-spike','## macOS menu bar']; const miss=need.filter(h=>!d.includes(h)); if(miss.length) throw new Error('missing sections: '+miss.join(', '));"</automated>
    <automated>node -e "const d=require('node:fs').readFileSync('docs/native-e2e.md','utf8'); if(!d.includes('make test-e2e-native')) throw new Error('run command missing'); if(!/make test-e2e[^-]/.test(d)) throw new Error('the two suites must be named by their distinct make targets');"</automated>
    <automated>node -e "const p=require('node:fs').readFileSync('.planning/PROJECT.md','utf8'); if(p.includes('macOS-native changes ship unverified by')) throw new Error('CI reality constraint still carries the pre-phase claim'); if(!/docs\/native-e2e\.md/.test(p)) throw new Error('CI reality constraint must point at the runner document');"</automated>
  </verify>
  <acceptance_criteria>
    - `docs/native-e2e.md` contains all seven required sections: `## How to run`,
      `## CI placement`, `## Human-attended checklist`, `## Known limits`, `## Spike log`,
      `## IME sub-spike`, `## macOS menu bar`.
    - The document names both `make test-e2e` and `make test-e2e-native` and says which
      premise each runs under.
    - `## CI placement` states the verdict and, if it is local-only, names which of D-01's
      three conditions failed and why.
    - `## Human-attended checklist` lists concrete steps with expected results, not
      instructions to "verify" something.
    - The document states which of D-08's two artifacts covers the app's own composition
      logic and which covers the OS input method.
    - `.planning/PROJECT.md` no longer asserts that macOS-native changes ship unverified by
      CI, points at `docs/native-e2e.md`, and its `CI reality` bullet is at most three lines.
    - No constraint in `.planning/PROJECT.md` other than `CI reality` is modified, verified
      by reading `git diff .planning/PROJECT.md`.
  </acceptance_criteria>
  <done>
    Both records exist and agree: `docs/native-e2e.md` says what the runner covers, how to
    run it, and what a human must still do, and `.planning/PROJECT.md`'s `CI reality`
    constraint says the same thing in one bullet.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Ratify D-01's three conditions and the recorded verdict</name>
  <what-built>
    The complete native runner: four D-13 surface specs, the fail-closed workspace
    isolation, the ship-isolation guard inside `make verify`, the macOS PR compile job, the
    `release-preflight` gate, and both verdict records. What a human must settle here is the
    one thing no command can: whether D-01's three conditions actually held, since the
    absence of an interactive permission prompt is only observable by looking.
  </what-built>
  <how-to-verify>
    1. Read `docs/native-e2e.md`'s `## Spike log` and open the run URLs it names. For each,
       check the job's own timing signature and the uploaded screenshot artifact. A modal
       permission dialog shows up as a job that hangs to its timeout with a dialog visible
       in the capture; a clean run shows neither.
    2. Confirm condition one: a WebDriver session established on the hosted macOS runner
       with no interactive or TCC prompt. If a prompt was observed, the verdict must be
       local-only and the log must show that no further attempts were made after it.
    3. Confirm condition two: `e2e-native/specs/pty.spec.ts` asserted real PTY output
       through both the text mirror and the canvas ink check, and the asserted string was
       produced by the shell rather than typed.
    4. Confirm condition three: a full run of the suite completed with no human present.
       On the CI-viable branch this is the `native-e2e-suite` job's own green status. On the
       local-only branch it is not satisfiable by definition, which is what makes the
       verdict local-only.
    5. Run `make release-preflight` on a macOS machine and record its exit code. This is the
       blocking local gate in both branches.
    6. Work the `## Human-attended checklist` in `docs/native-e2e.md` end to end - the
       macOS menu-bar click-through, and the Korean two-set composition check on whichever
       surfaces the IME sub-spike left to a human. Record a per-item observation, not a
       single overall yes. Milestone v1.0's retrospective records that a bare approval
       marker is not reusable verification evidence, and Phase 9 will need these
       observations.
    7. Confirm the CI placement matches the verdict: on the CI-viable branch,
       `native-e2e.yml` exists and `native-e2e-spike.yml` is gone; on the local-only branch,
       the reverse.
  </how-to-verify>
  <acceptance_criteria>
    - Each of D-01's three conditions is answered yes or no individually, with the evidence
      named for each; an overall approval without the three separate answers is not accepted.
    - The recorded verdict in `docs/native-e2e.md` matches those three answers, and is
      local-only if any one of them is no.
    - `make release-preflight`'s exit code is recorded.
    - Every `## Human-attended checklist` item has its own observation recorded, not a
      blanket approval.
  </acceptance_criteria>
  <resume-signal>Type "approved" with the three condition answers, the preflight exit code, and the per-item checklist observations, or describe what did not hold</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Recorded scope -> downstream phases | Phase 8 and Phase 9 plan their verification against this phase's two records |
| Local release gate -> shipped release | `release-preflight` is the last check before a release tag |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-04 | Repudiation | The verdict in `docs/native-e2e.md` and `.planning/PROJECT.md` | medium | mitigate | D-01's three-condition rule is applied item by item at the blocking checkpoint, with the evidence named per condition, so a partial pass cannot be recorded as CI viability. The two records are written in the same task so they cannot drift apart |
| T-06-08 | Repudiation | The human-attended checklist | medium | mitigate | Per-item observations are required rather than a single approval marker, which is the specific failure milestone v1.0's retrospective recorded and Phase 5 had to re-run a UAT to correct |
| T-06-11 | Denial of Service | `native-e2e-suite` on `main` and release tags | low | accept | A flaky native run can block a release tag. Accepted because D-16 already weighed this against per-PR execution and chose the narrower trigger, and `release-preflight` is human-run so a real blocker is visible before the tag |
| T-06-12 | Elevation of Privilege | `native-e2e-compile` on `macos-14` for every PR | low | accept | The job compiles and typechecks only and never executes the suite or the app, so an untrusted PR cannot launch a process on the runner beyond the compilers this repository already runs on its Linux legs |
</threat_model>

<verification>
- `make verify` green, unchanged in composition by this plan
- `make release-preflight` run on macOS with its exit code recorded
- `native-e2e-compile` present in `ci.yml`, on `macos-14`, executing no suite run
- Exactly one of `native-e2e.yml` / `native-e2e-spike.yml` present, matching the verdict
- `docs/native-e2e.md` carries all seven required sections
- `.planning/PROJECT.md`'s `CI reality` bullet states the verdict and points at the document,
  with `git diff .planning/PROJECT.md` touching no other constraint
- The blocking checkpoint answered D-01's three conditions individually with named evidence
</verification>

<success_criteria>
- The runner's CI-vs-local scope is a settled, written fact that Phase 8 and Phase 9 can
  plan against without reading this phase's CONTEXT
- `make release-preflight` blocks on the native suite regardless of which branch the verdict
  took
- The runner compiles on every pull request on a real macOS runner and executes nowhere it
  was not proven to
- Nothing is claimed as covered that a human was not asked to check, and nothing a human
  checked was recorded as a bare yes
</success_criteria>

<output>
Create `.planning/phases/06-native-e2e-runner-foundation/06-05-SUMMARY.md` when done
</output>
    <automated>node -e "const d=require('node:fs').readFileSync('docs/native-e2e.md','utf8');for(const h of ['## How to run','## CI placement','## Human-attended checklist','## Known limits','## Spike log','## IME sub-spike','## macOS menu bar']){if(!d.includes(h))throw new Error('missing section: '+h);}if(!/make test-e2e-native/.test(d))throw new Error('run command missing');if(!/make test-e2e\b/.test(d))throw new Error('the two suites must be distinguished by target name');"