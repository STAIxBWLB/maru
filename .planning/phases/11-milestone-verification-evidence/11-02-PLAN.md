---
phase: 11-milestone-verification-evidence
plan: 02
type: execute
wave: 2
depends_on: [11-01]
files_modified:
  - .github/workflows/coverage.yml
  - README.md
autonomous: true
requirements: [TEST-02]

estimate:
  tokens: 35000
  raw_tokens: 35000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "`.github/workflows/coverage.yml` triggers only on `push` to `main`, with the same `paths-ignore` list as `ci.yml`; it has no pull-request trigger and no manual-dispatch trigger (D-01)."
    - "The workflow runs `make coverage`, appends the table printed by `scripts/coverage-summary.mjs` to `$GITHUB_STEP_SUMMARY`, and uploads `coverage/` (TypeScript and Rust HTML) as the `coverage-report` artifact (D-02)."
    - "No job in any workflow `needs:` the coverage job, `ci.yml` is unchanged on this branch (no diff from its merge-base with `origin/main`), and branch protection is not touched, so coverage cannot become a merge gate (D-01)."
    - "`llvm-tools` is added inside the checkout, so it attaches to the toolchain `rust-toolchain.toml` pins; `rust-toolchain.toml` itself is unchanged."
    - "`actionlint` reports no findings for `coverage.yml`, and the summary step replayed locally against a real `coverage/` writes the totals table into a stand-in `GITHUB_STEP_SUMMARY` file."
    - "README.md documents `make coverage` and the coverage workflow as non-gating, with no threshold."
  artifacts:
    - path: ".github/workflows/coverage.yml"
      provides: "Non-gating `coverage` workflow: push-to-main only, minimal permissions, llvm-tools on the pinned toolchain, `make coverage`, Job Summary table, `coverage-report` artifact (30-day retention)"
    - path: "README.md"
      provides: "`make coverage` line in the commands block and a short coverage paragraph in `## Verification and CI`"
  key_links:
    - from: ".github/workflows/coverage.yml"
      to: "Makefile `coverage` target"
      via: "`run: make coverage` step"
      pattern: "make coverage"
    - from: ".github/workflows/coverage.yml"
      to: "scripts/coverage-summary.mjs"
      via: "`node scripts/coverage-summary.mjs >> \"$GITHUB_STEP_SUMMARY\"` step"
      pattern: "GITHUB_STEP_SUMMARY"
  prohibitions:
    - statement: "The coverage workflow never runs on pull requests, is never a required status check, and no other job depends on it; branch protection settings are not changed in this phase (D-01)."
      requirement_id: TEST-02
    - statement: "The workflow sends coverage data nowhere but the run's own artifact and Job Summary: no external coverage service action, no upload token, no secret referenced (D-02)."
      requirement_id: TEST-02
  flagged_assumptions:
    - "The workflow's first real run happens on the first push to `main` after the phase PR merges. The executor cannot observe it: D-01 rules out PR and dispatch triggers, and GitHub only dispatches a workflow that already exists on the default branch. Its run URL, Job Summary table and artifact are a post-merge check, which 11-06 records in 11-EVIDENCE.md as pending."
    - "RESEARCH assumption A2 is corrected here. `rust-toolchain.toml` sits at the repo root and pins 1.98.0 with only clippy and rustfmt, so a `components: llvm-tools` input on `dtolnay/rust-toolchain@stable` would attach llvm-tools to `stable`, not to the pin. The workflow instead runs `cargo --version`, which makes rustup install the pinned toolchain, and then `rustup component add llvm-tools` inside the checkout. The first run proves it; if it fails, the result is a red non-required job, not a blocked merge."
---

<objective>
Land the CI half of TEST-02. A separate `coverage` workflow runs `make coverage` on every
push to `main`, puts the totals table in the run's Job Summary, and uploads both HTML
reports as an artifact. It is never a PR check and never a required check (D-01, D-02).
README.md documents the target and the workflow.

Purpose: coverage is refreshed on every main push without adding a gate or a PR cost.

Output: `.github/workflows/coverage.yml` and the README update. This plan reuses plan
11-01's `make coverage` target and `scripts/coverage-summary.mjs` unchanged.
</objective>

<execution_context>
@/Users/yj.lee/workspace/work/dev/maru/.claude/gsd-core/workflows/execute-plan.md
@/Users/yj.lee/workspace/work/dev/maru/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-milestone-verification-evidence/11-CONTEXT.md
@.planning/phases/11-milestone-verification-evidence/11-RESEARCH.md
@.planning/phases/11-milestone-verification-evidence/11-PATTERNS.md
@.planning/phases/11-milestone-verification-evidence/11-01-SUMMARY.md
</context>

## Artifacts this phase produces

Plan 11-02 scope only.

- New workflow file `.github/workflows/coverage.yml`: workflow `name: coverage`, one job
  with id `coverage`, concurrency group `coverage-${{ github.ref }}`, artifact
  `coverage-report` (path `coverage/`, `retention-days: 30`).
- README.md: one commented `make coverage` line in the commands block and one paragraph
  in `## Verification and CI`.

The separate-file choice follows the `native-e2e.yml` precedent. That file is its own
workflow because `ci.yml`'s triggers and `decision`/`run_full` dedup job serve a
different purpose. Keeping coverage out of `ci.yml` leaves the PR path byte-identical.

<tasks>

<task type="tracer">
  <name>Task 1: Tracer, push-to-main coverage workflow, actionlint-clean, with the Job Summary step replayed locally</name>
  <precondition>`coverage/ts/coverage-summary.json` and `coverage/rust/coverage.json` exist from plan 11-01's green `make coverage` run (re-run `make coverage` with the LLVM env from 11-01 Task 3 if they were cleaned).</precondition>
  <files>.github/workflows/coverage.yml</files>
  <read_first>
    - `.github/workflows/native-e2e.yml` lines 1-60: the header-comment voice (why the file is separate, trigger-scope rationale), `permissions`, `concurrency`, `env`, and job layout to mirror
    - `.github/workflows/ci.yml` lines 3-20 (the `paths-ignore` list to copy verbatim for the push trigger) and lines 101-186 (the `verify` job's checkout, pnpm 9.15.0, Node 22.22.3 with pnpm cache, `dtolnay/rust-toolchain@stable`, `Swatinem/rust-cache@v2`, Linux Tauri apt list, `pnpm install --frozen-lockfile` steps to copy)
    - `.github/workflows/ci.yml` lines 221-229 (`actions/upload-artifact@v4` step shape)
    - `rust-toolchain.toml` (repo root: channel 1.98.0, components clippy and rustfmt only)
    - `Makefile` `coverage` target and `scripts/coverage-summary.mjs` as landed in plan 11-01
  </read_first>
  <action>
    Implements D-01 and D-02 for CI. Create `.github/workflows/coverage.yml`.

    1. Header comment, in the `native-e2e.yml` voice. It names TEST-02 and says:
       - Coverage is non-gating evidence. It has no threshold (D-04) and is not a
         required check.
       - It runs on every push to `main` and never for PRs (D-01).
       - Every main push refreshes the baseline, including pushes whose tree a PR already
         validated. That is why it does not reuse `ci.yml`'s `decision` dedup job.
       - It is a separate file so `ci.yml`'s PR path stays untouched.

    2. `name: coverage`. The trigger is `on.push.branches: [main]`, with the `paths-ignore`
       list copied verbatim from `ci.yml`'s push trigger: `README.md`, `CHANGELOG.md`,
       `docs/**`, `.planning/**`, `.github/**/*.md`. Add no other trigger.

    3. Top level: `permissions: contents: read`,
       `concurrency: group: coverage-${{ github.ref }}` with `cancel-in-progress: true`,
       and `env: CARGO_TERM_COLOR: always`.

    4. One job, id `coverage`, named `coverage report (non-gating)`, with
       `runs-on: ubuntu-22.04` and `timeout-minutes: 60`. It depends on no other job.
       Its steps:
       - Checkout: `actions/checkout@v5`.
       - Setup pnpm: `pnpm/action-setup@v6` with version 9.15.0.
       - Setup Node: `actions/setup-node@v5`, Node 22.22.3, `cache: pnpm`.
       - Setup Rust: `dtolnay/rust-toolchain@stable`, identical to `verify`.
       - A step named `Add llvm-tools to the pinned toolchain` that runs `cargo --version`
         and then `rustup component add llvm-tools` from the repo root. Its comment says
         `rust-toolchain.toml` pins the channel with only clippy and rustfmt, so a
         components input on the stable step would attach llvm-tools to the wrong
         toolchain. Running inside the checkout targets the pin, and
         `rust-toolchain.toml` is not edited.
       - Cache Rust build: `Swatinem/rust-cache@v2` with `workspaces: src-tauri -> target`
         and `shared-key: ubuntu-22.04-coverage`. The key is distinct so instrumented
         artifacts never mix into the verify cache.
       - Install cargo-llvm-cov: `taiki-e/install-action@cargo-llvm-cov`.
       - Install Linux Tauri dependencies: the apt list verbatim from `verify`.
       - Install frontend dependencies: `pnpm install --frozen-lockfile`.
       - Run coverage: `make coverage`.
       - Write coverage totals to the job summary:
         `node scripts/coverage-summary.mjs >> "$GITHUB_STEP_SUMMARY"`.
       - Upload coverage report: `actions/upload-artifact@v4` with `name: coverage-report`,
         `path: coverage/` and `retention-days: 30`. It has no failure-only condition,
         because the report is evidence on success.

       Reference no secret and no third-party coverage-service action.

    5. Run `actionlint .github/workflows/coverage.yml`. It is installed at
       `/opt/homebrew/bin/actionlint` and is clean on `ci.yml` and `native-e2e.yml` today.

    6. Replay the Job Summary step locally against the real `coverage/` from plan 11-01.
       Point `GITHUB_STEP_SUMMARY` at a temporary file, run
       `node scripts/coverage-summary.mjs >> "$GITHUB_STEP_SUMMARY"`, and confirm the file
       holds the `### Coverage totals` table with all four data rows. Record the replay
       output and the actionlint result in 11-02-SUMMARY.md.
  </action>
  <verify>
    <automated>actionlint .github/workflows/coverage.yml</automated>
    <fails_when>non-zero exit or any finding line printed</fails_when>
    <automated>test "$(grep -v '^[[:space:]]*#' .github/workflows/coverage.yml | grep -cE 'pull_request|workflow_dispatch|needs:|secrets\.')" = 0 && grep -q 'make coverage' .github/workflows/coverage.yml && grep -q 'GITHUB_STEP_SUMMARY' .github/workflows/coverage.yml && grep -q 'rustup component add llvm-tools' .github/workflows/coverage.yml</automated>
    <fails_when>non-zero exit, meaning a PR or dispatch trigger, a needs dependency or a secret reference exists in non-comment lines, or a required step is missing</fails_when>
    <automated>S=$(mktemp) && GITHUB_STEP_SUMMARY="$S" sh -c 'node scripts/coverage-summary.mjs >> "$GITHUB_STEP_SUMMARY"' && grep -q '^| Rust workspace total |' "$S" && grep -q '^| TypeScript (src + scripts) |' "$S"</automated>
    <fails_when>non-zero exit, or the stand-in summary file lacks the TypeScript or Rust workspace total row</fails_when>
    <automated>git diff --quiet "$(git merge-base HEAD origin/main)" -- .github/workflows/ci.yml rust-toolchain.toml</automated>
    <fails_when>non-zero exit, meaning ci.yml or rust-toolchain.toml changed</fails_when>
  </verify>
  <acceptance_criteria>
    - `.github/workflows/coverage.yml` exists; its only trigger is `push` on `main` with the five `paths-ignore` entries copied from `ci.yml`.
    - `permissions` is exactly `contents: read`; there is one job and it has no `needs:`.
    - The step order is: toolchain, `cargo --version` then `rustup component add llvm-tools`, cache, install cargo-llvm-cov, apt deps, `pnpm install --frozen-lockfile`, `make coverage`, Job Summary append, `coverage-report` upload with `retention-days: 30`.
    - `actionlint` exits 0 with no output.
    - The local replay writes both the TypeScript and Rust workspace total rows into the stand-in summary file.
    - `ci.yml` and `rust-toolchain.toml` are unchanged on this branch (no diff from the merge-base with `origin/main`).
  </acceptance_criteria>
  <done>
    The coverage workflow is complete, statically valid, and its Job Summary step is
    proven locally against real coverage output. It will run for the first time on the
    first push to `main` after merge.
  </done>
</task>

<task type="auto">
  <name>Task 2: Document `make coverage` and the coverage workflow in README.md</name>
  <files>README.md</files>
  <read_first>
    - `README.md` lines 315-350 (the commands block: `make verify`, `make release-checks`, `make verify-integration` with one-line comments)
    - `README.md` lines 359-386 (`## Verification and CI`: what `make verify` covers and how PR vs main-push CI behaves)
    - `.github/workflows/coverage.yml` as written in Task 1
  </read_first>
  <action>
    Per CLAUDE.md, README.md is the source of truth for gates and CI, so the new
    non-gating target and workflow are documented there (D-01, D-02).

    1. In the commands block, after the two `make verify-integration` lines, add a
       comment line saying the TypeScript and Rust coverage report is non-gating, is not
       part of verify, and needs cargo-llvm-cov plus llvm-tools. Follow it with a
       `make coverage` line.

    2. In `## Verification and CI`, after the paragraph about Chromium e2e, add one short
       paragraph. It says:
       - Coverage is measured by `make coverage` locally and by the `coverage` workflow
         on every push to `main`.
       - It never runs for pull requests and is not a required check.
       - Both HTML reports upload as the `coverage-report` artifact with 30-day
         retention, and the per-language and per-crate totals table appears in the run's
         Job Summary.
       - There is no threshold, so a lower number never fails anything.

    Use ASCII punctuation only; the ascii-lint hook rejects em-dashes, curly quotes and
    ellipsis in Markdown. Change nothing else in README.md.

    Then run `make verify` once and paste the output in 11-02-SUMMARY.md. The workflow
    and README do not enter verify, but the phase contract requires a green run per code
    plan.
  </action>
  <verify>
    <automated>grep -q '^make coverage$' README.md && grep -q 'coverage-report' README.md</automated>
    <fails_when>non-zero exit, meaning the command line or the artifact paragraph is missing</fails_when>
    <automated>git diff --check -- README.md .github/workflows/coverage.yml</automated>
    <fails_when>non-zero exit or any whitespace-error line printed</fails_when>
    <automated>make verify</automated>
    <fails_when>non-zero exit, or make stops at any target before `check-command-isolation` completes</fails_when>
  </verify>
  <acceptance_criteria>
    - README.md contains a `make coverage` line in the commands block with a comment marking it non-gating.
    - `## Verification and CI` contains a paragraph naming the `coverage` workflow, push-to-main only, not a required check, the `coverage-report` artifact, the Job Summary table, and no threshold.
    - `git diff --stat -- README.md` shows only additions (no deleted lines).
    - `make verify` exits 0 and its output is pasted in 11-02-SUMMARY.md.
  </acceptance_criteria>
  <done>
    README.md documents the coverage target and the workflow as non-gating, and the tree
    still passes `make verify`.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GitHub-hosted runner -> third-party actions | `taiki-e/install-action`, `dtolnay/rust-toolchain`, `Swatinem/rust-cache` run with the job's token |
| CI run -> `coverage-report` artifact and Job Summary | Coverage HTML of repository source is published with the run |
| Workflow -> merge gating | A new job could accidentally become a required or depended-on check |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-11-04 | Tampering | Third-party actions in `coverage.yml` (tag-pinned) | medium | accept | Same tag-pinning convention as the existing `ci.yml` and `native-e2e.yml` jobs; the workflow grants only `contents: read`, references no secret, and runs only on pushes to `main` (trusted code), so a compromised action gets a read-only token and no credentials |
| T-11-05 | Elevation of Privilege | Workflow token scope | medium | mitigate | Top-level `permissions: contents: read`; no `pull_request_target`, no PR trigger, no dispatch inputs; Task 1's grep gate rejects `secrets.` references and extra triggers |
| T-11-06 | Denial of Service (developer velocity) | Coverage job gating merges | medium | mitigate | No `needs:` on or toward the job, `ci.yml` unchanged on this branch, branch protection untouched (main has no required status checks today), and no threshold; asserted by Task 1's verify commands |
| T-11-07 | Information Disclosure | `coverage-report` artifact contents | low | accept | The repository is public; the HTML renders source that is already published, and test fixtures are synthetic (same precedent as the Playwright trace artifact, 01-SECURITY.md AR-01-03) |
</threat_model>

<verification>
- `actionlint .github/workflows/coverage.yml` clean
- Local Job Summary replay writes the full totals table into a stand-in `GITHUB_STEP_SUMMARY`
- `ci.yml` and `rust-toolchain.toml` unchanged versus the branch merge-base with `origin/main`
- README.md documents target and workflow; `make verify` green, output in the SUMMARY
</verification>

<success_criteria>
- TEST-02 CI half (D-01, D-02): a non-gating, push-to-main-only job that publishes both HTML reports and a Job Summary totals table, ready to run on the first post-merge push
- Nothing about PR CI, required checks, or `make verify` changes
</success_criteria>

<output>
Create `.planning/phases/11-milestone-verification-evidence/11-02-SUMMARY.md` when done, recording the actionlint result, the local Job Summary replay, and the post-merge check that remains (first `coverage` run on `main`).
</output>
