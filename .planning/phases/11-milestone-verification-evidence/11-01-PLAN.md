---
phase: 11-milestone-verification-evidence
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - pnpm-lock.yaml
  - Makefile
  - scripts/coverage-summary.mjs
  - scripts/coverage-summary.test.ts
autonomous: false
requirements: [TEST-02]

estimate:
  tokens: 60000
  raw_tokens: 60000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "`make coverage` runs Vitest with the v8 provider over the same scope as `pnpm test` (`src` + `scripts`, `check-command-isolation.test.mjs` excluded) and writes an HTML report at `coverage/ts/index.html` plus `coverage/ts/coverage-summary.json` (D-02, D-03)."
    - "`make coverage` runs cargo-llvm-cov over the whole Cargo workspace (the `maru` lib and `maru-cli`) and writes an HTML report under `coverage/rust/` plus `coverage/rust/coverage.json` (D-03)."
    - "The last thing `make coverage` prints is a totals table: one TypeScript row, one Rust row per crate (`maru`, `maru-cli`), and a Rust workspace total, each with lines and functions (D-02, D-03)."
    - "With cargo-llvm-cov absent, `make coverage` exits non-zero before any report is produced and prints an install hint; it never finishes with a TypeScript-only report."
    - "`make verify` and its prerequisite list are unchanged, and no minimum-percentage option exists in the Makefile, the Vitest invocation, or the summary script (D-01, D-04)."
    - "`@vitest/coverage-v8` is pinned exactly at `4.1.5`, the peer version the installed `vitest@4.1.5` names, and was installed only after the blocking-human legitimacy checkpoint (D-03)."
    - "The first measured totals (TypeScript plus per-crate Rust) are recorded once as the baseline in 11-01-SUMMARY.md under `## Evidence: coverage baseline`, with the commit SHA and toolchain versions they were measured on (D-04)."
  artifacts:
    - path: "Makefile"
      provides: "`coverage` target (`.PHONY`, `##` description) under a not-in-verify comment block naming TEST-02 and D-01/D-02/D-04: cargo-llvm-cov preflight with install hint, Vitest v8 half, cargo-llvm-cov half, totals table"
    - path: "scripts/coverage-summary.mjs"
      provides: "Exported `summarizeCoverage({ ts, rust })` plus a CLI that reads `coverage/ts/coverage-summary.json` and `coverage/rust/coverage.json` and prints the markdown totals table; exits 1 naming any missing or unparseable input"
    - path: "scripts/coverage-summary.test.ts"
      provides: "Vitest behavior test for `summarizeCoverage`: TS row math, per-crate grouping, workspace total, zero-denominator and no-files cases"
    - path: "package.json"
      provides: "`@vitest/coverage-v8` devDependency pinned exactly at 4.1.5"
    - path: "pnpm-lock.yaml"
      provides: "Lockfile regenerated by `pnpm add`, never hand-edited"
  key_links:
    - from: "Makefile `coverage` recipe"
      to: "scripts/coverage-summary.mjs"
      via: "final recipe line `$(NODE) scripts/coverage-summary.mjs`"
      pattern: "coverage-summary.mjs"
    - from: "Makefile `coverage` recipe"
      to: "Vitest json-summary reporter output consumed by the summary script"
      via: "`--coverage.reporter=json-summary` with `--coverage.reportsDirectory=coverage/ts`"
      pattern: "json-summary"
    - from: "scripts/coverage-summary.mjs"
      to: "cargo-llvm-cov JSON export `data[0].files[].filename`"
      via: "per-crate grouping on the `/src-tauri/maru-cli/` and `/src-tauri/src/` path segments"
      pattern: "src-tauri/maru-cli"
  prohibitions:
    - statement: "Coverage never becomes a gate: `coverage` is not added to `verify` or any verify prerequisite, no minimum-percentage option is configured in Vitest or cargo-llvm-cov, and no target or job depends on coverage succeeding (D-01, D-04)."
      requirement_id: TEST-02
    - statement: "No coverage data leaves the machine or the CI run: no lcov output and no upload to an external coverage service (D-02)."
      requirement_id: TEST-02
    - statement: "The Rust half is never silently skipped: a missing cargo-llvm-cov fails `make coverage` loudly with an install hint instead of reporting TypeScript alone as success."
      requirement_id: TEST-02
  flagged_assumptions:
    - "TEST-02 edge-probe row (unclassified, unresolved; surfaced, not dropped): the planner reads D-03's 'same scope as `pnpm test`' as Vitest's default denominator, which counts only files a test imports. Files no test imports are absent from the TypeScript percentage. `--coverage.include` is the switch if the user wants an all-files denominator; this plan does not add it."
    - "The local Rust baseline is measured with this machine's Homebrew rustc 1.97.1 (there is no rustup here, so `rust-toolchain.toml`'s 1.98.0 pin is not applied locally), using `LLVM_COV`/`LLVM_PROFDATA` from Homebrew llvm whose major version must equal the LLVM major in `rustc --version --verbose` (both 22 at planning time). CI measures on the pinned 1.98.0. Small drift between the two is expected and recorded, not reconciled. If cargo-llvm-cov cannot run on this toolchain even with those two variables set, Task 3 stops and reports. The fallback is the user's call, for example taking the Rust baseline from the first post-merge CI run."
    - "The lib and CLI test suites pass under `-C instrument-coverage`. A test that fails only under instrumentation stops the task and is reported; no skip flag, filter, or `#[ignore]` is added to get a green run."
---

<objective>
Land the local half of TEST-02: a `make coverage` target that measures TypeScript
coverage (Vitest, v8 provider) and Rust coverage (cargo-llvm-cov, whole workspace) and
ends with one totals table, kept outside `make verify` with no threshold (D-01..D-04).

Purpose: coverage becomes visible evidence without becoming a gate. The first measured
totals are recorded as the phase baseline so later runs can be compared by eye (D-04).

Output: the `@vitest/coverage-v8@4.1.5` devDependency (behind a blocking-human
legitimacy checkpoint), the `coverage` Make target, `scripts/coverage-summary.mjs` with
its Vitest test, and the recorded baseline. The CI job that runs the same target on
pushes to `main` is plan 11-02.
</objective>

<execution_context>
@/Users/yj.lee/workspace/work/dev/maru/.claude/gsd-core/workflows/execute-plan.md
@/Users/yj.lee/workspace/work/dev/maru/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-milestone-verification-evidence/11-CONTEXT.md
@.planning/phases/11-milestone-verification-evidence/11-RESEARCH.md
@.planning/phases/11-milestone-verification-evidence/11-PATTERNS.md
</context>

## Artifacts this phase produces

Plan 11-01 scope only; the other plans declare their own.

- New Make target `coverage` (`.PHONY`, `##` help text) in `Makefile`, placed with the
  other deliberately-not-in-verify targets (`test-e2e-native`, `verify-integration`).
- New file `scripts/coverage-summary.mjs`: ESM, node builtins only, exported function
  `summarizeCoverage({ ts, rust })` returning the markdown totals table, plus a CLI
  entry guarded by the `import.meta.url === pathToFileURL(process.argv[1]).href` idiom.
- New file `scripts/coverage-summary.test.ts`: Vitest behavior test (runs inside the
  existing `pnpm test`, so inside `make verify` as an ordinary unit test).
- New devDependency `@vitest/coverage-v8` at exactly `4.1.5` in `package.json`, with the
  regenerated `pnpm-lock.yaml`.
- Output paths (gitignored by the existing `coverage/` rule, never committed):
  `coverage/ts/index.html`, `coverage/ts/coverage-summary.json`,
  `coverage/rust/html/index.html`, `coverage/rust/coverage.json`.
- Machine-level tool (not a repo dependency, approved at Task 1): `cargo-llvm-cov` in
  `~/.cargo/bin`, removable with `cargo uninstall cargo-llvm-cov`.

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Package legitimacy check for @vitest/coverage-v8 and approval of the local cargo-llvm-cov install</name>
  <files>package.json</files>
  <read_first>
    - `.planning/phases/11-milestone-verification-evidence/11-RESEARCH.md` section "Package Legitimacy Audit" (the verdict table and why `@vitest/coverage-v8` is flagged SUS)
    - `package.json` `devDependencies` block (`vitest` is `^4.1.5`)
    - `pnpm-lock.yaml` around line 4150 (the `vitest@4.1.5` peerDependencies entry naming `'@vitest/coverage-v8': 4.1.5`)
  </read_first>
  <what-built>
    Nothing yet. This gate runs before any install. RESEARCH.md's legitimacy audit gave
    `@vitest/coverage-v8` a SUS verdict from the checker's `too-new` heuristic, which
    fired on the package's latest tag (5.x, published within the freshness window), not
    on the pinned 4.1.5 release. The package is the first-party coverage provider from
    the same monorepo as the already-trusted `vitest`, with tens of millions of weekly
    downloads. A SUS verdict is never auto-approvable, so the developer confirms it.

    The same stop asks for approval of one machine-level install: `cargo install
    cargo-llvm-cov --locked` (crates.io, maintainer taiki-e, OK verdict, first published
    2021). It is not a repo dependency; it lands in `~/.cargo/bin` so the Rust half of
    `make coverage` can run and produce the local baseline (Task 3). Without it, the
    Rust baseline would only exist after the first post-merge CI run.
  </what-built>
  <how-to-verify>
    1. Open https://www.npmjs.com/package/@vitest/coverage-v8 and confirm the repository
       field points at github.com/vitest-dev/vitest and version 4.1.5 is published.
    2. Confirm 4.1.5 is the exact version `vitest@4.1.5` names as its
       `@vitest/coverage-v8` peer (pnpm-lock.yaml around line 4150). The install will
       pin it exactly (no caret) so a future `vitest` bump forces a matched bump.
    3. Open https://crates.io/crates/cargo-llvm-cov and confirm the repository is
       github.com/taiki-e/cargo-llvm-cov. Approve or decline the local
       `cargo install cargo-llvm-cov --locked`.
  </how-to-verify>
  <resume-signal>Type "approved" to approve both, or describe what to change (for example "npm only, no local cargo install")</resume-signal>
  <acceptance_criteria>
    - The developer confirms the npm page shows github.com/vitest-dev/vitest as the source and 4.1.5 exists.
    - The developer states whether the local `cargo install cargo-llvm-cov --locked` is approved.
    - 11-01-SUMMARY.md records both answers verbatim.
  </acceptance_criteria>
</task>

<task type="tracer" tdd="true">
  <name>Task 2: Tracer, `make coverage` end-to-end for the TypeScript half, from Vitest v8 JSON to the terminal totals table</name>
  <precondition>`git rev-parse --abbrev-ref HEAD` prints a branch other than `main`, and Task 1 was answered "approved" for `@vitest/coverage-v8`.</precondition>
  <files>package.json, pnpm-lock.yaml, Makefile, scripts/coverage-summary.mjs, scripts/coverage-summary.test.ts</files>
  <read_first>
    - `package.json` line 31, the `test` script: the source of truth for the Vitest scope (`vitest run src scripts`) and the `--exclude '**/check-command-isolation.test.mjs'` flag (RESEARCH Pitfall 3)
    - `Makefile` lines 1-35 (variables `PNPM`, `CARGO`, `NODE`, `TAURI_DIR`, `ICON_PATH`) and lines 226-270 (`test-e2e-native` and `verify-integration` with their "Deliberately NOT part of `verify`" comment blocks, the convention the new target copies)
    - `Makefile` line 376, the `verify:` prerequisite list, which must not change
    - `scripts/check-mode-css-ownership.mjs` lines 50-56 (REPO_ROOT via `fileURLToPath(import.meta.url)`) and line 480 (CLI main guard), plus `scripts/check-mode-css-ownership.test.ts` lines 70-95 (dynamic `await import("./check-mode-css-ownership.mjs")` of an exported function)
    - `vite.config.ts` (no `test:` block exists; coverage options go on the CLI, RESEARCH Pitfall 1)
  </read_first>
  <behavior>
    - `summarizeCoverage({ ts })` where `ts.total.lines` is `{ total: 200, covered: 150 }` and `ts.total.functions` is `{ total: 40, covered: 10 }` returns markdown containing the row `| TypeScript (src + scripts) | 75.00% (150/200) | 25.00% (10/40) |`.
    - The table starts with the line `### Coverage totals` followed by the header `| Scope | Lines | Functions |`.
    - A zero denominator renders `n/a (0/0)` instead of dividing by zero.
    - Percentages are recomputed from covered/total to two decimals, not copied from the input's `pct` field.
  </behavior>
  <action>
    Implements D-02, D-03 and D-04 for the TypeScript half.

    1. Install the approved provider with `pnpm add -D -E @vitest/coverage-v8@4.1.5` so
       `package.json` records exactly `4.1.5` (no caret) and `pnpm-lock.yaml` regenerates.
       Never hand-edit the lockfile. If pnpm reports a peer mismatch with the installed
       vitest, stop and report instead of forcing it.

    2. RED: create `scripts/coverage-summary.test.ts` from the behavior list above. Import
       the function with `await import("./coverage-summary.mjs")`, as
       `check-mode-css-ownership.test.ts` does. Run it and confirm it fails because the
       module does not exist yet.

    3. GREEN: create `scripts/coverage-summary.mjs`. It is ESM and imports only
       `node:fs`, `node:path` and `node:url`. It exports `summarizeCoverage({ ts })`,
       which returns the table as one string. The CLI part sits behind the
       `process.argv[1] && import.meta.url === pathToFileURL(process.argv[1]).href`
       guard and resolves paths from REPO_ROOT, the same way as the precedent module.
       It reads `coverage/ts/coverage-summary.json`. If that file is missing or is not
       valid JSON, it prints a `coverage-summary:` line to stderr naming the path and
       telling the reader to run `make coverage`, then exits 1. Otherwise it prints the
       table to stdout. The Rust rows are added in Task 3.

    4. Add the `coverage` target to `Makefile` right after the `verify-integration`
       block. Put a comment block above it in the same voice as that block. The comment
       names TEST-02 and says why the target is outside `verify`: coverage is a
       diagnostic report, not a gate. It has no threshold and no fail condition (D-04),
       and its CI job runs only on pushes to `main` (D-01, plan 11-02). Declare
       `.PHONY: coverage`. The prerequisites are `node_modules $(ICON_PATH)`, matching
       `test-rust`, because Task 3 adds the Rust half. Add a `##` help string. The
       recipe has two lines:
       - `$(PNPM) exec vitest run src scripts --exclude '**/check-command-isolation.test.mjs' --coverage --coverage.reporter=html --coverage.reporter=json-summary --coverage.reportsDirectory=coverage/ts`
       - `$(NODE) scripts/coverage-summary.mjs`

       Keep the default denominator: add no `--coverage.include`, per the flagged
       assumption. Do not add a `vitest.config.ts` or a `test:` block to
       `vite.config.ts`. Do not touch the `verify:` line.

    5. Run `make coverage` and confirm `coverage/ts/index.html` exists and the output
       ends with the table.

    6. Run `pnpm typecheck`. `scripts/` is type-checked through
       `tsconfig.scripts.json`, which has `checkJs` on.
  </action>
  <verify>
    <automated>pnpm exec vitest run scripts/coverage-summary.test.ts</automated>
    <fails_when>non-zero exit, or the summary line reports "No test files found" or 0 passed</fails_when>
    <automated>make coverage && test -f coverage/ts/index.html && test -f coverage/ts/coverage-summary.json</automated>
    <fails_when>non-zero exit, or the output lacks a line starting with "| TypeScript (src + scripts) |"</fails_when>
    <automated>node -e "const p=require('./package.json'); if (p.devDependencies['@vitest/coverage-v8'] !== '4.1.5') process.exit(1)"</automated>
    <fails_when>non-zero exit, meaning the dependency is missing or carries a range instead of the exact 4.1.5</fails_when>
    <automated>pnpm typecheck</automated>
    <fails_when>non-zero exit or any "error TS" line</fails_when>
  </verify>
  <acceptance_criteria>
    - `package.json` `devDependencies["@vitest/coverage-v8"]` is exactly `4.1.5`; the `pnpm-lock.yaml` diff comes only from `pnpm add`.
    - `grep -c '^coverage:' Makefile` returns 1 and `grep -E '^verify:' Makefile | grep -c coverage` returns 0.
    - The `coverage` recipe carries the identical `--exclude '**/check-command-isolation.test.mjs'` flag as the `test` script in `package.json`.
    - `make coverage` exits 0, writes `coverage/ts/index.html` and `coverage/ts/coverage-summary.json`, and its last output lines are the `### Coverage totals` table with the TypeScript row.
    - `git status --short` lists no file under `coverage/` (the existing `.gitignore` rule holds).
    - `pnpm exec vitest run scripts/coverage-summary.test.ts` and `pnpm typecheck` exit 0.
  </acceptance_criteria>
  <done>
    One real path works end to end: `make coverage` runs the `pnpm test` scope under the
    v8 provider, writes HTML and JSON under `coverage/ts/`, and prints the totals table
    from the JSON. `make verify` is untouched.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Rust half, cargo-llvm-cov preflight with install hint, per-crate totals, and the recorded baseline</name>
  <precondition>Task 1 approved the local `cargo install cargo-llvm-cov --locked`, and `/opt/homebrew/opt/llvm/bin/llvm-cov --version` reports the same LLVM major version as `rustc --version --verbose`.</precondition>
  <files>Makefile, scripts/coverage-summary.mjs, scripts/coverage-summary.test.ts</files>
  <read_first>
    - `Makefile` `coverage` target as written in Task 2, and lines 212-216 (`test-rust` uses `cd $(TAURI_DIR) && $(CARGO) ...`, the idiom the Rust lines copy)
    - `src-tauri/Cargo.toml` lines 19-22 (`[workspace] members = [".", "maru-cli"]`, `default-members = ["."]`, so `--workspace` is needed to cover `maru-cli`)
    - `src-tauri/maru-cli/src/main.rs` (4 lines, no tests; its 0% row is truthful, not a defect)
    - `rust-toolchain.toml` at the repo root (`channel = "1.98.0"`, `components = ["clippy", "rustfmt"]`; this plan does not edit it)
    - `scripts/coverage-summary.mjs` and `scripts/coverage-summary.test.ts` as written in Task 2
  </read_first>
  <behavior>
    - Given a cargo-llvm-cov JSON export whose `data[0].files` holds `/w/src-tauri/src/a.rs` (lines count 10, covered 5; functions count 2, covered 1) and `/w/src-tauri/maru-cli/src/main.rs` (lines count 4, covered 0; functions count 1, covered 0), the table contains `| Rust maru (src-tauri/src) | 50.00% (5/10) | 50.00% (1/2) |`, `| Rust maru-cli (src-tauri/maru-cli) | 0.00% (0/4) | 0.00% (0/1) |` and `| Rust workspace total | 35.71% (5/14) | 33.33% (1/3) |`.
    - A crate with no files in the export renders `no files in report` in both cells instead of disappearing from the table.
    - Windows-style backslash separators in `filename` group the same way as forward slashes.
    - The TypeScript row from Task 2 is still the first data row.
  </behavior>
  <action>
    Implements D-02, D-03 and D-04 for the Rust half, and fails loudly when the tool is
    missing.

    1. RED: extend `scripts/coverage-summary.test.ts` with the Rust cases above and confirm
       they fail.

    2. GREEN: extend `summarizeCoverage` to take `{ ts, rust }`. The grouping works like
       this:
       - Normalize backslashes in each `rust.data[0].files[].filename`.
       - A path containing `/src-tauri/maru-cli/` belongs to `maru-cli`.
       - A path containing `/src-tauri/src/` belongs to `maru`.
       - Anything else counts only toward the workspace total.
       - Sum `summary.lines.count` and `.covered`, and `summary.functions.count` and
         `.covered`, then recompute the percentages from those sums.

       Row labels are `Rust maru (src-tauri/src)`, `Rust maru-cli (src-tauri/maru-cli)`
       and `Rust workspace total`, in that order, after the TypeScript row. The CLI now
       also requires `coverage/rust/coverage.json`, with the same missing or unparseable
       exit-1 message naming the path.

    3. Add a preflight as the first line of the `coverage` recipe. It runs
       `@$(CARGO) llvm-cov --version >/dev/null 2>&1`. On failure it echoes a
       `coverage:` message and exits 1. The message says cargo-llvm-cov is not installed,
       so the Rust half cannot run. It names `cargo install cargo-llvm-cov --locked`, then
       says to either run `rustup component add llvm-tools` (rustup toolchains) or export
       `LLVM_COV` and `LLVM_PROFDATA` pointing at an LLVM whose major version matches
       `rustc --version --verbose` (for example Homebrew llvm). The preflight uses
       `$(CARGO) llvm-cov`, not a PATH lookup: cargo also searches `~/.cargo/bin` for
       subcommands, and this shell's PATH does not include it.

    4. Between the Vitest line and the summary line, add two recipe lines:
       - `cd $(TAURI_DIR) && $(CARGO) llvm-cov --workspace --html --output-dir ../coverage/rust`
       - `cd $(TAURI_DIR) && $(CARGO) llvm-cov report --json --summary-only --output-path ../coverage/rust/coverage.json`

       The second line reuses the first run's profile data, the documented
       `cargo llvm-cov report` pattern. After the install in step 6, confirm both flag sets
       against `cargo llvm-cov --help` and `cargo llvm-cov report --help`. Also confirm the
       HTML lands at `coverage/rust/html/index.html`. If the path differs, record it and
       use the real path in the acceptance checks.

    5. RED run, before installing the tool: run `make coverage` and confirm it exits
       non-zero within seconds, prints the install hint, and runs neither Vitest nor
       cargo. Keep the output for the SUMMARY.

    6. Install per the Task 1 approval with `cargo install cargo-llvm-cov --locked`, then
       confirm `cargo llvm-cov --version` exits 0.

    7. GREEN run. This machine has no rustup, so export
       `LLVM_COV=/opt/homebrew/opt/llvm/bin/llvm-cov` and
       `LLVM_PROFDATA=/opt/homebrew/opt/llvm/bin/llvm-profdata` in the same shell
       invocation, then run `make coverage`. The first instrumented build of the maru lib
       can exceed the 10-minute tool timeout, so run it in the background and poll. If a
       test fails only under instrumentation, stop and report it, per the flagged
       assumption.

    8. Record the baseline in 11-01-SUMMARY.md under the heading
       `## Evidence: coverage baseline`, so plan 11-06 can transcribe it into
       11-EVIDENCE.md. Record:
       - The totals table exactly as printed.
       - `git rev-parse --short HEAD`.
       - The installed `vitest` and `@vitest/coverage-v8` versions.
       - `cargo llvm-cov --version`.
       - `rustc --version --verbose`, including the LLVM line.
       - The two HTML entry points.
       - The RED-run hint output from step 5.

       Use ASCII punctuation only (no em-dash, curly quotes, or ellipsis).

    9. Run `make verify` and paste its output into the SUMMARY. The new test is part of
       `pnpm test`; nothing else in verify changes.
  </action>
  <verify>
    <automated>pnpm exec vitest run scripts/coverage-summary.test.ts</automated>
    <fails_when>non-zero exit, or fewer passing tests than the combined Task 2 and Task 3 behavior cases</fails_when>
    <automated>LLVM_COV=/opt/homebrew/opt/llvm/bin/llvm-cov LLVM_PROFDATA=/opt/homebrew/opt/llvm/bin/llvm-profdata make coverage && test -f coverage/ts/index.html && test -f coverage/rust/html/index.html && test -f coverage/rust/coverage.json</automated>
    <fails_when>non-zero exit, or the output lacks a line starting with "| Rust maru (src-tauri/src) |" or "| Rust workspace total |"</fails_when>
    <automated>test "$(grep -E '^verify:' Makefile | grep -c coverage)" = 0 && test "$(grep -v '^[[:space:]]*#' Makefile | grep -cE 'coverage\.thresholds|fail-under|lcov')" = 0 && test "$(grep -vE '^[[:space:]]*(//|\*)' scripts/coverage-summary.mjs | grep -cE 'coverage\.thresholds|fail-under|lcov')" = 0</automated>
    <fails_when>non-zero exit, meaning coverage joined the verify prerequisites or a minimum-percentage or lcov option appeared in non-comment lines</fails_when>
    <automated>make verify</automated>
    <fails_when>non-zero exit, or make stops at any target before `check-command-isolation` completes</fails_when>
  </verify>
  <acceptance_criteria>
    - With cargo-llvm-cov absent (before step 6), `make coverage` exits non-zero and its output contains `cargo install cargo-llvm-cov --locked`, `LLVM_COV` and `LLVM_PROFDATA`; `coverage/rust/` is not created by that run.
    - With the tool installed and the LLVM env exported, `make coverage` exits 0 and the final table has four data rows in this order: TypeScript, Rust maru, Rust maru-cli, Rust workspace total.
    - `coverage/rust/coverage.json` contains filenames under both `src-tauri/src/` and `src-tauri/maru-cli/src/`, or the maru-cli row reads `no files in report` and the SUMMARY says so.
    - 11-01-SUMMARY.md contains `## Evidence: coverage baseline` with the table, the commit SHA, and the rustc, LLVM, cargo-llvm-cov, vitest and @vitest/coverage-v8 versions.
    - `rust-toolchain.toml` is unchanged (`git diff --quiet -- rust-toolchain.toml`).
    - `make verify` exits 0 and its output is pasted in the SUMMARY.
  </acceptance_criteria>
  <done>
    `make coverage` measures both languages, fails loudly and early when the Rust tool is
    missing, ends with a per-language and per-crate totals table, and the first measured
    numbers are on record as the D-04 baseline. `make verify` is still green and still
    free of coverage.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry -> `node_modules` and the lockfile | A new devDependency (`@vitest/coverage-v8`) enters the build and test toolchain |
| crates.io -> developer machine (`~/.cargo/bin`) | A cargo subcommand (`cargo-llvm-cov`) is compiled and installed locally |
| Test run -> `coverage/` HTML | Coverage HTML renders repository source line by line |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-11-SC | Tampering | `pnpm add -D -E @vitest/coverage-v8@4.1.5`, `cargo install cargo-llvm-cov --locked` | high | mitigate | Package Legitimacy Audit (RESEARCH.md) plus the Task 1 blocking-human checkpoint before either install; exact pin 4.1.5 matching the vitest peer; lockfile only from `pnpm add`, and CI installs with `--frozen-lockfile`; `cargo install --locked` uses the crate's own lockfile |
| T-11-01 | Denial of Service (developer velocity) | `make verify` prerequisite list | medium | mitigate | `coverage` is not a verify prerequisite and carries no minimum-percentage option; Task 3's grep gate asserts both, and the not-in-verify comment block documents why |
| T-11-02 | Information Disclosure | `coverage/` HTML output | low | accept | The output is local and gitignored (`coverage/`), and the repository is public, so the rendered source discloses nothing new |
| T-11-03 | Repudiation | Coverage report claiming completeness | medium | mitigate | The preflight fails the target when cargo-llvm-cov is missing, so a TypeScript-only run can never exit 0; the RED run in Task 3 proves it |
</threat_model>

<verification>
- `pnpm exec vitest run scripts/coverage-summary.test.ts` green, including the Rust grouping cases
- `make coverage` RED (tool missing, install hint) then GREEN (both HTML reports, four-row totals table), both transcripts in 11-01-SUMMARY.md
- `grep -E '^verify:' Makefile` unchanged, no minimum-percentage or lcov option anywhere in the new code
- `make verify` green, output pasted in the SUMMARY
</verification>

<success_criteria>
- TEST-02 local half: TypeScript and Rust coverage measured by one non-gating command, readable as HTML after the run, totals printed at the end (ROADMAP Phase 11 success criterion 1)
- D-04 baseline recorded once, with the commit and toolchain it was measured on
- `make verify` unchanged and green
</success_criteria>

<output>
Create `.planning/phases/11-milestone-verification-evidence/11-01-SUMMARY.md` when done, including the `## Evidence: coverage baseline` section described in Task 3.
</output>
