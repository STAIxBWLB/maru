---
phase: 11-milestone-verification-evidence
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/11-milestone-verification-evidence/11-EVIDENCE.md
autonomous: true
requirements: [GATE-08]

estimate:
  tokens: 30000
  raw_tokens: 30000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "A fresh `workflow_dispatch` run of `ci.yml` on a throwaway branch, carrying exactly one failing assertion in `e2e/startup.spec.ts`, failed its `playwright e2e` job on that test (D-05, D-06)."
    - "The failing test's `trace.zip` from that run's `playwright-report` artifact was downloaded, and its `unzip -l` listing (entry names, byte sizes, totals) is recorded verbatim in 11-EVIDENCE.md with the run URL and ID, artifact name, failing test name, and config commit `2d2e8660`, compared against v1.0's 123,399 bytes / 6 entries (D-07)."
    - "`playwright.config.ts` on the probe branch is byte-identical to `origin/main` and to `2d2e8660`, so the evidence is about the shipped narrowed trace config (`retain-on-failure`, snapshots and screenshots off)."
    - "The probe branch is deleted locally and on `origin`, its worktree is removed, no PR was opened, and the probe commit is an ancestor of neither `origin/main` nor the phase branch (D-06)."
    - "No trace binary or downloaded artifact is committed (D-07)."
  artifacts:
    - path: ".planning/phases/11-milestone-verification-evidence/11-EVIDENCE.md"
      provides: "Phase 11 evidence record, created here with the `## GATE-08: narrowed Playwright trace re-proof` section; plan 11-06 appends the remaining sections"
  key_links:
    - from: "11-EVIDENCE.md GATE-08 section"
      to: "the GitHub Actions run of ci.yml on the probe branch"
      via: "recorded run URL and run ID"
      pattern: "actions/runs/"
    - from: "11-EVIDENCE.md GATE-08 section"
      to: "playwright.config.ts trace setting at commit 2d2e8660"
      via: "recorded config commit and the verbatim trace setting line"
      pattern: "2d2e8660"
  prohibitions:
    - statement: "The probe commit never reaches `main` or the phase PR branch: it lives only on a throwaway branch in a separate worktree, no PR is opened, and the branch is deleted locally and on the remote after capture (D-06)."
      requirement_id: GATE-08
    - statement: "The evidence is only about the shipped config: `playwright.config.ts` and `.github/workflows/ci.yml` are not edited to produce or shape the trace, and the downloaded trace binary is never committed (D-05, D-07)."
      requirement_id: GATE-08
  flagged_assumptions:
    - "GATE-08 edge-probe row (unclassified, unresolved; surfaced, not dropped): the planner reads 'actually produces a trace' as the failing test's `retain-on-failure` `trace.zip` being present in the uploaded artifact with a non-empty listing that includes the action timeline (`0-trace.trace`). `playwright.config.ts` sets no `retries`, so the first-attempt trace is the one captured. If unrelated specs also fail in the same run, their traces are ignored and the run still counts, but the probe test must be among the failures or the evidence is void."
    - "Dispatch semantics (orchestrator note): `paths-ignore` filters do not apply to `workflow_dispatch`, and the `decision` job sets `run_full=true` for any non-push event, so `verify`, `e2e` and `native-e2e-compile` all run on the dispatch. The e2e job is the only one this evidence needs; the others' results are recorded but do not gate it."
---

<objective>
Retire the GATE-04 evidence debt accepted at v1.0 closeout (GATE-08). A fresh, deliberate
CI failure must prove that the narrowed Playwright trace configuration shipped in v1.0
still produces a real trace. The proof is recorded in the repo so it outlives the 7-day
CI artifact retention, which is what left the v1.0 proof unverifiable (D-05..D-07).

Purpose: replace an expired, unaccepted artifact (run 32569215249) with committed,
checkable evidence.

Output: the `## GATE-08` section of the new 11-EVIDENCE.md. The probe branch, its commit
and its worktree are created and destroyed inside this plan. The user approved the push,
the dispatch and the deletion (D-06).
</objective>

<execution_context>
@/Users/yj.lee/workspace/work/dev/maru/.claude/gsd-core/workflows/execute-plan.md
@/Users/yj.lee/workspace/work/dev/maru/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-milestone-verification-evidence/11-CONTEXT.md
@.planning/phases/11-milestone-verification-evidence/11-RESEARCH.md
</context>

## Artifacts this phase produces

Plan 11-03 scope only.

- New file `.planning/phases/11-milestone-verification-evidence/11-EVIDENCE.md`, the phase
  evidence record, with its `## GATE-08: narrowed Playwright trace re-proof` section.
  Evidence goes here and not into 11-VERIFICATION.md because gsd-verifier creates
  11-VERIFICATION.md with a whole-file write at the end of execution, which would
  discard anything a plan wrote into it. The verifier cites 11-EVIDENCE.md instead.
- Transient only, all removed before the plan ends:
  - branch `test/gate08-trace-probe`, local and on `origin`
  - one probe commit on that branch
  - a git worktree under the executor's session scratchpad
  - the downloaded `playwright-report` artifact directory

<tasks>

<task type="tracer">
  <name>Task 1: Tracer, probe branch to dispatched CI failure to downloaded trace listing</name>
  <precondition>`gh auth status` exits 0, and `git ls-remote --heads origin test/gate08-trace-probe` prints nothing.</precondition>
  <files>e2e/startup.spec.ts (edited only inside the throwaway worktree on branch test/gate08-trace-probe, never on the phase branch)</files>
  <read_first>
    - `playwright.config.ts` lines 12-30: the narrowed trace setting and its comment, including the measured v1.0 trace contents (~123 KB across 6 entries, `0-trace.network` 0 bytes)
    - `e2e/startup.spec.ts` (27 lines): the test `keeps the full terminal renderer out of the collapsed startup path` and its final assertion on `terminal:full-mount-request`
    - `.github/workflows/ci.yml` lines 3-21 (`workflow_dispatch` trigger), lines 36-99 (the `decision` job: `run_full=true` for any non-push event) and lines 189-229 (the `e2e` job, `make test-e2e`, and the failure-only `playwright-report` upload with `retention-days: 7`)
    - `.planning/milestones/v1.0-phases/01-trustworthy-verify-signal/01-VERIFICATION.md` "Human Verification Required" item 1: the 2026-08-22 probe method and figures this run supersedes
  </read_first>
  <action>
    Implements D-05 and D-06 exactly as approved. There is no PR, and the phase branch
    and `main` are never touched.

    1. `git fetch origin main`. Confirm that `git log -1 --format=%h origin/main --
       playwright.config.ts` prints `2d2e8660`, and that `git diff --quiet 2d2e8660
       origin/main -- playwright.config.ts` exits 0. This proves the config is unchanged
       since v1.0. If either check fails, stop and report: the evidence would be about a
       different config.

    2. Create the probe worktree outside the maru checkout, under your session scratchpad
       directory, with `git worktree add <scratchpad>/gate08-probe -b
       test/gate08-trace-probe origin/main`. Never place it inside the repo: `vitest run
       src scripts` matches path substrings and would pick up the worktree's test files.

    3. In the worktree only, flip the polarity of the last assertion in
       `e2e/startup.spec.ts`. The `terminal:full-mount-request` assertion loses its
       `.not`, so the test now requires a mark that the collapsed startup path never
       emits. That makes the failure clean and deterministic. Make the edit with a
       one-line in-place substitution. Then confirm `git -C <worktree> diff --stat` shows
       exactly one file with one insertion and one deletion, and that `git -C <worktree>
       diff --name-only origin/main` would list only `e2e/startup.spec.ts` after commit.
       Commit in the worktree with a conventional message marking it a do-not-merge
       GATE-08 probe. Record the probe commit SHA as PROBE_SHA.

    4. Push with `git -C <worktree> push -u origin test/gate08-trace-probe`. The push
       triggers no workflow: `ci.yml`'s push trigger is `main` only and no other workflow
       triggers on branch pushes.

    5. Dispatch with `gh workflow run ci.yml --ref test/gate08-trace-probe`. Then poll
       `gh run list --workflow ci.yml --branch test/gate08-trace-probe --event
       workflow_dispatch --limit 1 --json databaseId,url,headSha,status,conclusion` until
       a run whose `headSha` equals PROBE_SHA appears. Record its id as RUN_ID and its
       URL.

    6. Poll `gh run view RUN_ID --json status,conclusion,jobs` until the job named
       `playwright e2e` has completed. Expect about 6-15 minutes. Poll with short calls or
       in the background, and never hold one blocking call longer than the tool timeout.
       The e2e job's conclusion must be `failure`. Use `gh run view RUN_ID --log-failed`
       to confirm that the failing test is `e2e/startup.spec.ts` >
       `keeps the full terminal renderer out of the collapsed startup path`, and note any
       other failing specs. If the probe test did not fail, stop and report, because the
       evidence is void.

    7. Download the artifact with `gh run download RUN_ID -n playwright-report -D
       <scratchpad>/gate08-artifact`. Find the probe's trace with `find` under
       `test-results/`: it is a directory whose name starts with `startup-`. Confirm the
       directory name carries no `-retry` suffix. Capture `unzip -l` of that `trace.zip`
       verbatim, and record the relative path to it inside the artifact. Also capture
       `gh api repos/STAIxBWLB/maru/actions/runs/RUN_ID/artifacts --jq '.artifacts[] |
       {name, id, size_in_bytes, expires_at}'` for the artifact name, id, size and expiry.
       Once the run completes, capture the conclusions of the `make verify` and
       `native e2e compile check` jobs for the record. They do not gate this evidence.

    Keep every captured output for Task 2. Nothing from this task is committed to the
    phase branch.
  </action>
  <verify>
    <automated>test "$(gh run list --workflow ci.yml --branch test/gate08-trace-probe --event workflow_dispatch --limit 1 --json conclusion --jq '.[0].conclusion')" = failure</automated>
    <fails_when>non-zero exit; empty output means no dispatched run was found, "success" means the probe did not fail</fails_when>
    <automated>git diff --quiet "$(git merge-base HEAD origin/main)" -- e2e/startup.spec.ts playwright.config.ts .github/workflows/ci.yml</automated>
    <fails_when>non-zero exit, meaning the probe edit or a config edit leaked into the phase branch</fails_when>
  </verify>
  <acceptance_criteria>
    - `git log -1 --format=%h origin/main -- playwright.config.ts` printed `2d2e8660`, and the config diff against `2d2e8660` was empty.
    - The probe commit changes exactly one line in `e2e/startup.spec.ts` and nothing else.
    - The dispatched run's `headSha` equals PROBE_SHA, and its `playwright e2e` job conclusion is `failure`, with `keeps the full terminal renderer out of the collapsed startup path` among the failures.
    - The `playwright-report` artifact downloaded, and a `trace.zip` under a `startup-` test-results directory with no `-retry` suffix was found and listed with `unzip -l`.
    - The phase branch has not changed `e2e/startup.spec.ts`, `playwright.config.ts` or `.github/workflows/ci.yml` (no diff from its merge-base with `origin/main`).
  </acceptance_criteria>
  <done>
    The whole evidence path ran once for real, from throwaway branch to dispatched CI
    failure to artifact download to trace listing, against the unchanged shipped config.
  </done>
</task>

<task type="auto">
  <name>Task 2: Record the GATE-08 evidence in 11-EVIDENCE.md, then delete the probe branch, worktree and artifact</name>
  <files>.planning/phases/11-milestone-verification-evidence/11-EVIDENCE.md</files>
  <read_first>
    - The outputs captured in Task 1 (run URL and ID, PROBE_SHA, failing test, `unzip -l` listing, artifact metadata, other job conclusions)
    - `.planning/milestones/v1.0-phases/01-trustworthy-verify-signal/01-VERIFICATION.md` "Human Verification Required" item 1: the comparison shape (narrowed 123,399 bytes / 6 entries vs wide 1,752,382 bytes / 14 entries)
    - `playwright.config.ts` line 29 (the `trace:` setting line to quote verbatim)
  </read_first>
  <action>
    Implements D-07, then completes D-06's cleanup.

    1. Create 11-EVIDENCE.md with the title `# Phase 11: Evidence Record`. Add one intro
       paragraph saying this file holds evidence that must outlive CI artifact retention.
       It is kept separate from 11-VERIFICATION.md, which the verifier regenerates.

    2. Add a section headed exactly `## GATE-08: narrowed Playwright trace re-proof`. It
       holds a two-column table (Item | Value) with these rows:
       - run URL
       - run ID
       - event `workflow_dispatch`
       - probe branch `test/gate08-trace-probe`, deleted
       - PROBE_SHA, not on `main`
       - config commit `2d2e8660` with the verbatim `trace:` line from
         `playwright.config.ts`
       - the failing test (file and title)
       - the `playwright e2e` job conclusion
       - the other jobs' conclusions
       - artifact name `playwright-report`, id, size and expiry
       - the trace path inside the artifact

    3. Add a subsection `### Trace listing` with the `unzip -l` output verbatim inside a
       fenced text block.

    4. Add a subsection `### Comparison with v1.0` with a table of three rows:
       - v1.0 narrowed config, 2026-08-22, run 32569215249: 123,399 bytes, 6 entries.
       - v1.0 wide config: 1,752,382 bytes, 14 entries.
       - This run: the measured total bytes and entry count.

       Follow the table with a one-line verdict. It says whether the narrowed config still
       produces a non-trivial trace that contains the action timeline (`0-trace.trace`)
       and the stack file, and whether the network entry is empty, as the config comment
       documents. If the size or entry count is far from about 123 KB and 6 entries, state
       that plainly as a finding. Do not smooth it over.

    5. Clean up in this order:
       - Confirm `git -C <worktree> status --porcelain` is empty.
       - `git push origin --delete test/gate08-trace-probe`.
       - `git worktree remove <worktree>`, then `git worktree prune`.
       - `git branch -D test/gate08-trace-probe`. The branch is unmerged by design and
         D-06 approved deleting it.
       - Delete the downloaded artifact directory so the trace binary never enters the
         repo (D-07).

    6. Add a subsection `### Cleanup` recording:
       - The empty `git ls-remote --heads origin test/gate08-trace-probe`.
       - The empty `git branch --list test/gate08-trace-probe`.
       - The worktree's absence from `git worktree list`.
       - That `git merge-base --is-ancestor PROBE_SHA origin/main` and
         `git merge-base --is-ancestor PROBE_SHA HEAD` both exit 1.

    Use ASCII punctuation only outside the fenced listing; the ascii-lint hook rejects
    em-dashes, curly quotes and ellipsis in Markdown prose.
  </action>
  <verify>
    <automated>test -z "$(git ls-remote --heads origin test/gate08-trace-probe)" && test -z "$(git branch --list test/gate08-trace-probe)"</automated>
    <fails_when>non-zero exit, meaning the probe branch still exists on origin or locally</fails_when>
    <automated>F=.planning/phases/11-milestone-verification-evidence/11-EVIDENCE.md; grep -q '^## GATE-08: narrowed Playwright trace re-proof$' "$F" && grep -q '2d2e8660' "$F" && grep -q '0-trace.trace' "$F" && grep -q 'playwright-report' "$F" && grep -q 'actions/runs/' "$F" && grep -q '123,399' "$F"</automated>
    <fails_when>non-zero exit, meaning the section, config commit, trace listing, artifact name, run URL or v1.0 comparison is missing</fails_when>
    <automated>test "$(git ls-files | grep -c 'trace\.zip')" = 0 && git diff --quiet "$(git merge-base HEAD origin/main)" -- e2e/startup.spec.ts playwright.config.ts</automated>
    <fails_when>non-zero exit, meaning a trace binary is tracked or the probe edit reached the phase branch</fails_when>
  </verify>
  <acceptance_criteria>
    - 11-EVIDENCE.md exists with `## GATE-08: narrowed Playwright trace re-proof` holding the run URL and ID, PROBE_SHA, config commit `2d2e8660`, failing test name, artifact name, id and expiry, and the verbatim `unzip -l` listing.
    - The comparison table carries the v1.0 figures (123,399 / 6 and 1,752,382 / 14) next to this run's measured totals, plus a one-line verdict.
    - `git ls-remote --heads origin test/gate08-trace-probe` and `git branch --list test/gate08-trace-probe` both print nothing, and `git worktree list` no longer shows the probe worktree.
    - `git merge-base --is-ancestor PROBE_SHA origin/main` and `git merge-base --is-ancestor PROBE_SHA HEAD` both exit 1.
    - No `trace.zip` is tracked, and the task's commit touches only 11-EVIDENCE.md.
  </acceptance_criteria>
  <done>
    GATE-08's proof is committed as durable evidence that no longer depends on CI
    retention, and every transient probe artifact (branch, worktree, commit reference,
    downloaded trace) is gone.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Local probe branch -> origin (public repo) | A deliberately failing commit is pushed to a shared remote |
| Dispatched CI run -> public artifact | The Playwright report and trace are uploaded with 7-day retention |
| Probe worktree -> phase branch | The throwaway edit must never mix with the phase's real changes |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-11-08 | Tampering | Probe commit reaching `main` or the phase PR | high | mitigate | Separate worktree off `origin/main` outside the checkout, no PR opened, branch deleted locally and remotely, ancestry checks (`git merge-base --is-ancestor` exits 1 against both `origin/main` and `HEAD`) recorded in 11-EVIDENCE.md; Task 1 and Task 2 verify commands assert the phase branch never changed the spec or config (diff from its merge-base with `origin/main`) |
| T-11-09 | Repudiation | Evidence integrity (trace from a modified config) | medium | mitigate | Config commit `2d2e8660` and an empty `playwright.config.ts` diff against it are checked before the probe; the probe diff is limited to one line in `e2e/startup.spec.ts`; the run's `headSha` must equal PROBE_SHA |
| T-11-10 | Information Disclosure | `playwright-report` artifact on a public repo | low | accept | The e2e suite runs Chromium against Vite with mocked IPC and synthetic fixtures (01-SECURITY.md AR-01-03 precedent); the artifact expires in 7 days and the binary is never committed |
</threat_model>

<verification>
- Dispatched run on the probe branch concluded `failure` on the probe test, against config commit `2d2e8660`
- 11-EVIDENCE.md GATE-08 section holds the run, artifact, failing test, verbatim `unzip -l` listing and the v1.0 comparison
- Probe branch absent locally and on origin, worktree removed, probe SHA not an ancestor of `origin/main` or `HEAD`, no tracked `trace.zip`
</verification>

<success_criteria>
- ROADMAP Phase 11 success criterion 2: a deliberate CI failure against the narrowed trace config produced a trace, and the proof is committed where CI retention cannot expire it
</success_criteria>

<output>
Create `.planning/phases/11-milestone-verification-evidence/11-03-SUMMARY.md` when done, linking the 11-EVIDENCE.md GATE-08 section and listing the cleanup checks.
</output>
