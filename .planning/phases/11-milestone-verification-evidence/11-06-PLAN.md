---
phase: 11-milestone-verification-evidence
plan: 06
type: execute
wave: 3
depends_on: [11-01, 11-02, 11-03, 11-04, 11-05]
files_modified:
  - .planning/phases/11-milestone-verification-evidence/11-EVIDENCE.md
  - .planning/milestones/v1.0-MILESTONE-AUDIT.md
  - .planning/REQUIREMENTS.md
autonomous: true
requirements: [TEST-02, GATE-08, VALID-01, SEC-03]

estimate:
  tokens: 45000
  raw_tokens: 45000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "11-EVIDENCE.md holds, besides 11-03's GATE-08 section, a `## TEST-02: coverage baseline` section and a `## VALID-01 and SEC-03: reconciled records` section. The TEST-02 section transcribes the totals table, commit and toolchain from 11-01-SUMMARY.md and the CI-readiness record from 11-02-SUMMARY.md, and marks the first `coverage` run on `main` as a post-merge check. The VALID-01/SEC-03 section indexes the three VALIDATION.md files and 02-SECURITY.md with their measured status values."
    - "v1.0-MILESTONE-AUDIT.md ends with `## Resolved in v1.1 Phase 11`, placed after `## Closeout Recommendation`. It has one entry each for the GATE-04 trace re-proof, Nyquist metadata 01-03, and the Phase 02 security report, each citing its evidence path, plus a note that 03-SECURITY.md (commit `4fd3ea3b`, 2026-08-28) had already resolved the Phase 03 half. No pre-existing line is removed or modified (D-11)."
    - "REQUIREMENTS.md shows TEST-02, GATE-08, VALID-01 and SEC-03 as `[x]` and as `Complete` in the Traceability table. Each flips only after its evidence gate command passed in this plan (D-14)."
    - "`make verify` passes on the final tree and its output is pasted in 11-06-SUMMARY.md."
  artifacts:
    - path: ".planning/phases/11-milestone-verification-evidence/11-EVIDENCE.md"
      provides: "Complete phase evidence record: GATE-08 (from 11-03), TEST-02 baseline and CI readiness, VALID-01/SEC-03 index"
    - path: ".planning/milestones/v1.0-MILESTONE-AUDIT.md"
      provides: "Appended `## Resolved in v1.1 Phase 11` section; original frontmatter, verdict and debt list untouched"
    - path: ".planning/REQUIREMENTS.md"
      provides: "Four Phase 11 requirement checkboxes and Traceability rows flipped to Complete"
  key_links:
    - from: "v1.0-MILESTONE-AUDIT.md `## Resolved in v1.1 Phase 11`"
      to: "11-EVIDENCE.md, the three VALIDATION.md files, 02-SECURITY.md, 03-SECURITY.md"
      via: "backticked repo-root-relative paths, the audit's own citation style"
      pattern: "11-EVIDENCE.md"
    - from: "REQUIREMENTS.md Traceability rows"
      to: "11-EVIDENCE.md sections"
      via: "per-ID evidence gate run before each flip"
      pattern: "\\| (TEST-02|GATE-08|VALID-01|SEC-03) \\| Phase 11 \\| Complete \\|"
  prohibitions:
    - statement: "The v1.0 audit's history is not rewritten: its frontmatter (status, scores, gaps, nyquist, tech_debt), `## Verdict` and `## Technical Debt Accepted at Closeout` stay byte-identical, and the resolution is append-only (D-11)."
      requirement_id: VALID-01
    - statement: "No requirement is marked Complete on intent: each of the four flips waits for its evidence gate to pass, and a failing gate leaves that row Pending with the reason recorded (D-14)."
      requirement_id: TEST-02
  flagged_assumptions:
    - "TEST-02 flips Complete on the local evidence: `make coverage` measuring both languages with a recorded baseline (11-01), plus a statically validated, locally replayed CI workflow (11-02). The workflow's first run on `main` can only happen after the phase PR merges, so 11-EVIDENCE.md lists it as a post-merge check (run URL, Job Summary table, `coverage-report` artifact) for the orchestrator or user to confirm. If the user wants TEST-02 held until that run is observed, this flip moves to a post-merge follow-up."
    - "The evidence record lives in 11-EVIDENCE.md, not 11-VERIFICATION.md. gsd-verifier creates 11-VERIFICATION.md with a whole-file write after execution, which would discard plan-written sections. D-07's 'phase verification artifact' is satisfied by the dedicated, committed evidence file that the verifier cites."
---

<objective>
Close the phase's evidence loop. This plan completes 11-EVIDENCE.md, appends the
resolution record to the v1.0 milestone audit without rewriting its verdict (D-11), and
flips the four Phase 11 requirements only where their evidence exists (D-14). It ends
with the phase-gate `make verify`.

Purpose: the v1.0 audit's three accepted debt items each point to durable evidence, and
the v1.1 requirement ledger measures evidence, not intent.

Output: the completed 11-EVIDENCE.md, the appended audit section, and four flipped
REQUIREMENTS.md rows.
</objective>

<execution_context>
@/Users/yj.lee/workspace/work/dev/maru/.claude/gsd-core/workflows/execute-plan.md
@/Users/yj.lee/workspace/work/dev/maru/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-milestone-verification-evidence/11-CONTEXT.md
@.planning/phases/11-milestone-verification-evidence/11-01-SUMMARY.md
@.planning/phases/11-milestone-verification-evidence/11-02-SUMMARY.md
@.planning/phases/11-milestone-verification-evidence/11-03-SUMMARY.md
@.planning/phases/11-milestone-verification-evidence/11-04-SUMMARY.md
@.planning/phases/11-milestone-verification-evidence/11-05-SUMMARY.md
</context>

## Artifacts this phase produces

Plan 11-06 scope only.

- In `11-EVIDENCE.md`, which plan 11-03 created: new sections `## TEST-02: coverage
  baseline` and `## VALID-01 and SEC-03: reconciled records`.
- In `.planning/milestones/v1.0-MILESTONE-AUDIT.md`: new final section `## Resolved in
  v1.1 Phase 11`.
- In `.planning/REQUIREMENTS.md`: four checkbox lines and four Traceability rows changed
  from Pending to Complete.

<tasks>

<task type="tracer">
  <name>Task 1: Tracer, complete 11-EVIDENCE.md from the plan SUMMARYs and the reconciled records</name>
  <precondition>`.planning/phases/11-milestone-verification-evidence/11-EVIDENCE.md` exists with a `## GATE-08` section (plan 11-03), and 11-01-SUMMARY.md contains `## Evidence: coverage baseline`.</precondition>
  <files>.planning/phases/11-milestone-verification-evidence/11-EVIDENCE.md</files>
  <read_first>
    - `.planning/phases/11-milestone-verification-evidence/11-EVIDENCE.md` (the intro and GATE-08 section written by 11-03; keep both unchanged)
    - `.planning/phases/11-milestone-verification-evidence/11-01-SUMMARY.md` section `## Evidence: coverage baseline` (totals table, commit, toolchain versions, RED-run hint)
    - `.planning/phases/11-milestone-verification-evidence/11-02-SUMMARY.md` (actionlint result, local Job Summary replay, the pending post-merge check)
    - Frontmatter of the three VALIDATION.md files (`status`, `nyquist_compliant`) and their `## Validation Audit 2026-09-25` tables, plus the frontmatter of `02-SECURITY.md` (`status`, `threats_open`)
  </read_first>
  <action>
    Implements D-04 (baseline recorded in the phase artifacts) and D-07's durability goal
    for the rest of the evidence. Append two sections to 11-EVIDENCE.md, after the GATE-08
    section, and leave the existing text untouched.

    1. `## TEST-02: coverage baseline`. Transcribe the totals table exactly as printed in
       11-01-SUMMARY.md. Follow it with a small table recording:
       - measured-at commit
       - rustc and LLVM versions (local Homebrew toolchain)
       - cargo-llvm-cov version
       - vitest and @vitest/coverage-v8 versions
       - the HTML entry points `coverage/ts/index.html` and `coverage/rust/html/index.html`

       Then a line saying the baseline is recorded once, for comparison by eye, with no
       threshold (D-04). Then a `### CI readiness` subsection: the actionlint result and
       the local Job Summary replay from 11-02-SUMMARY.md. Last, a `### Post-merge check`
       subsection: the first `coverage` workflow run on `main` after this phase's PR
       merges should show the totals table in its Job Summary and a `coverage-report`
       artifact; record its run URL here when observed. Mark this as pending, never as
       passed.

    2. `## VALID-01 and SEC-03: reconciled records`. Add a table with columns Record |
       Path | status | nyquist_compliant or threats_open | Audit date. It has one row each
       for 01-, 02- and 03-VALIDATION.md and for 02-SECURITY.md, with values read from
       each file's frontmatter now, not copied from SUMMARY prose. Add a row for
       03-SECURITY.md (status verified, threats_open 0, commit `4fd3ea3b`, 2026-08-28),
       noting that it predates this phase.

    3. Confirm the GATE-04 pointer written by plan 11-04 into 01-VALIDATION.md now
       resolves: 11-EVIDENCE.md exists and contains the GATE-08 heading.

    Use ASCII punctuation only; the ascii-lint hook rejects em-dashes, curly quotes and
    ellipsis.
  </action>
  <verify>
    <automated>F=.planning/phases/11-milestone-verification-evidence/11-EVIDENCE.md; for h in '## GATE-08: narrowed Playwright trace re-proof' '## TEST-02: coverage baseline' '### Post-merge check' '## VALID-01 and SEC-03: reconciled records'; do grep -q "^$h" "$F" || exit 1; done && grep -q '| Rust workspace total |' "$F" && grep -q '02-SECURITY.md' "$F" && grep -q '4fd3ea3b' "$F"</automated>
    <fails_when>non-zero exit, meaning a required section, the transcribed Rust total row, the 02-SECURITY.md index row or the 03-SECURITY.md commit note is missing</fails_when>
    <automated>grep -q '11-EVIDENCE.md' .planning/milestones/v1.0-phases/01-trustworthy-verify-signal/01-VALIDATION.md && test -f .planning/phases/11-milestone-verification-evidence/11-EVIDENCE.md</automated>
    <fails_when>non-zero exit, meaning 01-VALIDATION.md's GATE-04 pointer does not resolve to the evidence file</fails_when>
  </verify>
  <acceptance_criteria>
    - 11-EVIDENCE.md contains the four headings `## GATE-08: narrowed Playwright trace re-proof`, `## TEST-02: coverage baseline`, `### Post-merge check` and `## VALID-01 and SEC-03: reconciled records`, and the GATE-08 section is unchanged from 11-03's commit.
    - The TEST-02 table matches 11-01-SUMMARY.md's baseline table verbatim, and the post-merge check is marked pending.
    - The records table lists the three VALIDATION.md files and 02-SECURITY.md with values matching their current frontmatter, plus the 03-SECURITY.md note citing `4fd3ea3b`.
  </acceptance_criteria>
  <done>
    One committed file holds every piece of Phase 11 evidence or points to it, with
    nothing depending on CI retention.
  </done>
</task>

<task type="auto">
  <name>Task 2: Append `## Resolved in v1.1 Phase 11` to the v1.0 milestone audit without touching its history</name>
  <files>.planning/milestones/v1.0-MILESTONE-AUDIT.md</files>
  <read_first>
    - `.planning/milestones/v1.0-MILESTONE-AUDIT.md` (whole file, 156 lines: frontmatter `tech_debt` list, `## Security Coverage`, `## Technical Debt Accepted at Closeout`, and the final `## Closeout Recommendation`)
    - `.planning/phases/11-milestone-verification-evidence/11-EVIDENCE.md` as completed in Task 1 (the anchors and figures to cite)
    - `.planning/phases/11-milestone-verification-evidence/11-RESEARCH.md` section "v1.0 Audit Append Location" (the entry shape)
  </read_first>
  <action>
    Implements D-11. Append a new final section, `## Resolved in v1.1 Phase 11`, after the
    last line of `## Closeout Recommendation`, with one blank line between them. Change no
    other line: frontmatter, `## Verdict`, `## Nyquist Coverage`, `## Security Coverage`
    and `## Technical Debt Accepted at Closeout` all stay as they are.

    The section opens with one sentence: the three debt items accepted on 2026-08-28 were
    resolved in milestone v1.1 Phase 11, and the verdict above is kept as the historical
    record. Then one bullet per debt item, citing evidence as backticked repo-root-relative
    paths (the audit's own style):
    - GATE-04 trace re-proof: a fresh deliberate CI failure (run ID and URL from
      11-EVIDENCE.md) against the unchanged narrowed config at `2d2e8660` produced a
      trace (bytes and entries as measured). Evidence path:
      `.planning/phases/11-milestone-verification-evidence/11-EVIDENCE.md`, GATE-08
      section. Requirement GATE-08.
    - Nyquist metadata, Phases 01-03: the three VALIDATION.md paths with their current
      `status` and `nyquist_compliant` values. Reconciliation re-ran each row's command,
      and the drafts were stale metadata, not missing tests. Requirement VALID-01.
    - Phase 02 security report:
      `.planning/milestones/v1.0-phases/02-shared-scanner-and-path-invariants/02-SECURITY.md`
      with its `threats_open` value. Requirement SEC-03.

    Close with a note that the claim "Phases 02-03 have no SECURITY.md" was already half
    resolved before v1.1 Phase 11: `03-SECURITY.md` landed in commit `4fd3ea3b` on
    2026-08-28.

    Use ASCII punctuation only (no em-dash, curly quotes or ellipsis).
  </action>
  <verify>
    <automated>F=.planning/milestones/v1.0-MILESTONE-AUDIT.md; test "$(git diff --numstat "$(git merge-base HEAD origin/main)" -- "$F" | awk '{print $2}')" = 0 && test "$(grep -c '^## Resolved in v1.1 Phase 11$' "$F")" = 1 && test "$(grep -n '^## Closeout Recommendation$' "$F" | cut -d: -f1)" -lt "$(grep -n '^## Resolved in v1.1 Phase 11$' "$F" | cut -d: -f1)"</automated>
    <fails_when>non-zero exit, meaning a pre-existing line was removed or modified, the section is missing or duplicated, or it sits above Closeout Recommendation</fails_when>
    <automated>F=.planning/milestones/v1.0-MILESTONE-AUDIT.md; grep -q '11-EVIDENCE.md' "$F" && grep -q '02-SECURITY.md' "$F" && grep -q '4fd3ea3b' "$F" && grep -q '01-VALIDATION.md' "$F" && grep -q '^status: tech_debt$' "$F"</automated>
    <fails_when>non-zero exit, meaning an evidence citation is missing or the original frontmatter status changed</fails_when>
  </verify>
  <acceptance_criteria>
    - `git diff "$(git merge-base HEAD origin/main)" -- .planning/milestones/v1.0-MILESTONE-AUDIT.md` shows only added lines, all at the end of the file.
    - Phase-wide D-10 scope holds: `git diff --name-only "$(git merge-base HEAD origin/main)" -- .planning/milestones/ | grep -vE '0[123]-VALIDATION\.md$|02-SECURITY\.md$|v1\.0-MILESTONE-AUDIT\.md$'` prints nothing.
    - The frontmatter still reads `status: tech_debt`, `closeout: accepted_tech_debt`, and `overall: not_validated` under `nyquist`.
    - The new section has one bullet per debt item (GATE-04 trace, Nyquist 01-03, Phase 02 security report), each with a backticked evidence path and requirement ID, plus the `4fd3ea3b` note.
  </acceptance_criteria>
  <done>
    The v1.0 audit reads as its original verdict followed by a dated, evidence-linked
    resolution, with history kept intact.
  </done>
</task>

<task type="auto">
  <name>Task 3: Flip the four Phase 11 requirements behind per-ID evidence gates, then the phase-gate `make verify`</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - `.planning/REQUIREMENTS.md` lines 108-140 (the `- [ ] **SEC-03**`, `**TEST-02**`, `**GATE-08**`, `**VALID-01**` entries) and lines 183-210 (Traceability table rows `| SEC-03 | Phase 11 | Pending |` etc.)
    - `.planning/phases/11-milestone-verification-evidence/11-EVIDENCE.md` as completed in Task 1
  </read_first>
  <action>
    Implements D-14. For each ID, run its evidence gate. Only if the gate exits 0, change
    that ID's `- [ ]` to `- [x]` and its Traceability cell `Pending` to `Complete`. Below,
    E stands for `.planning/phases/11-milestone-verification-evidence/11-EVIDENCE.md`.

    - TEST-02 gate: `grep -q '^coverage:' Makefile && grep -q 'coverage-summary.mjs' Makefile && test -f .github/workflows/coverage.yml && grep -q '^## TEST-02: coverage baseline' E`
    - GATE-08 gate: `grep -q '^## GATE-08' E && grep -q '0-trace.trace' E && test -z "$(git ls-remote --heads origin test/gate08-trace-probe)"`
    - VALID-01 gate: all three VALIDATION.md files match `^status: validated$`.
    - SEC-03 gate: 02-SECURITY.md exists with `^threats_open: [0-9]+$` and one register row for each of T-02-01..T-02-08.

    If a gate fails, leave that ID Pending and record the failing gate and reason in the
    SUMMARY. Do not edit the evidence to make a gate pass. Change nothing else in
    REQUIREMENTS.md.

    Then run `make verify` as the phase gate and paste the full output in
    11-06-SUMMARY.md. `make` stops at the first failing target, so a run that reaches
    `check-command-isolation` is the healthy signal.
  </action>
  <verify>
    <automated>F=.planning/REQUIREMENTS.md; for id in TEST-02 GATE-08 VALID-01 SEC-03; do grep -q "^- \[x\] \*\*$id\*\*" "$F" && grep -q "^| $id | Phase 11 | Complete |$" "$F" || exit 1; done</automated>
    <fails_when>non-zero exit, meaning an ID's checkbox or Traceability row was not flipped (acceptable only if its evidence gate failed and the SUMMARY records why)</fails_when>
    <automated>git diff --numstat "$(git merge-base HEAD origin/main)" -- .planning/REQUIREMENTS.md | awk '{ if ($1 > 8 || $2 > 8) exit 1 }'</automated>
    <fails_when>non-zero exit, meaning this branch changed REQUIREMENTS.md beyond the four checkbox lines and four table rows (more than 8 added or 8 removed lines)</fails_when>
    <automated>make verify</automated>
    <fails_when>non-zero exit, or make stops at any target before `check-command-isolation` completes</fails_when>
  </verify>
  <acceptance_criteria>
    - Each of TEST-02, GATE-08, VALID-01 and SEC-03 reads `- [x]` and `| ID | Phase 11 | Complete |`, and each flip was preceded by its gate command exiting 0, with the commands and exits recorded in the SUMMARY.
    - The REQUIREMENTS.md diff touches only those eight lines.
    - `make verify` exits 0 on the final tree and its output is pasted in 11-06-SUMMARY.md.
  </acceptance_criteria>
  <done>
    The requirement ledger reflects evidence that exists, the v1.0 debt is closed with
    links, and the tree passes the full gate.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Plan SUMMARYs -> evidence record | Evidence is transcribed from executor-written SUMMARYs into the durable record |
| Evidence record -> requirement ledger and milestone audit | Completion claims rest on the evidence record |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-11-17 | Repudiation | REQUIREMENTS.md completion flips | medium | mitigate | Per-ID evidence gate commands run before each flip and recorded in the SUMMARY; a failing gate leaves the row Pending; the Task 3 diff check caps the change at the eight target lines |
| T-11-18 | Tampering | v1.0 audit history rewritten | medium | mitigate | Append-only: Task 2 asserts zero removed lines against the branch merge-base with `origin/main`, the section placed after `## Closeout Recommendation`, and the original frontmatter `status: tech_debt` intact (D-11) |
| T-11-19 | Repudiation | Transcription drift between SUMMARY and evidence record | low | mitigate | The TEST-02 table is copied verbatim and the records table is read from live frontmatter, not SUMMARY prose; Task 1's acceptance compares them |
</threat_model>

<verification>
- 11-EVIDENCE.md complete (GATE-08, TEST-02 with a pending post-merge check, VALID-01/SEC-03 index)
- v1.0 audit append-only with the resolution section last
- Four requirement rows flipped behind passing gates; `make verify` green with output in the SUMMARY
</verification>

<success_criteria>
- All four ROADMAP Phase 11 success criteria trace to committed evidence in 11-EVIDENCE.md and the archive
- D-11 and D-14 honored: history preserved, ledger flipped only on evidence
</success_criteria>

<output>
Create `.planning/phases/11-milestone-verification-evidence/11-06-SUMMARY.md` when done, including each evidence-gate command and exit status, the pending post-merge coverage check, and the pasted `make verify` output.
</output>
