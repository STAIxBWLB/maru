---
phase: 11-milestone-verification-evidence
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/milestones/v1.0-phases/02-shared-scanner-and-path-invariants/02-SECURITY.md
autonomous: true
requirements: [SEC-03]

estimate:
  tokens: 40000
  raw_tokens: 40000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "`.planning/milestones/v1.0-phases/02-shared-scanner-and-path-invariants/02-SECURITY.md` exists in the 03-SECURITY.md format. Frontmatter holds `phase: 02`, `slug: shared-scanner-and-path-invariants`, `status`, `threats_open`, `asvs_level: 1` and `created: 2026-09-25`. The body holds `## Trust Boundaries`, `## Threat Register`, `## Accepted Risks Log`, `## Security Audit Trail`, `## Verification Evidence` and `## Sign-Off` (D-12)."
    - "Each of the 8 threats T-02-01..T-02-08 declared in the `<threat_model>` blocks of 02-01..02-03-PLAN.md has exactly one register row. Its status is backed by a file:line citation grepped at HEAD and, for `mitigate` dispositions, by a covering test run green in this plan."
    - "`threats_open` is computed from the classification (OPEN threats at or above the `high` block threshold), not copied from research. T-02-03 and T-02-06 (disposition `accept`) appear in the Accepted Risks Log."
    - "An OPEN threat follows D-13. A small local fix lands with a test in this plan. A larger one stays open in the report with a drafted issue, and no GitHub issue is created without the user's explicit approval."
    - "No archived file other than the new 02-SECURITY.md changes under `.planning/milestones/` in this plan (D-10)."
  artifacts:
    - path: ".planning/milestones/v1.0-phases/02-shared-scanner-and-path-invariants/02-SECURITY.md"
      provides: "Retroactive Phase 02 security report: 8-threat register verified against HEAD, accepted risks, audit trail, evidence, sign-off"
  key_links:
    - from: "02-SECURITY.md Threat Register rows"
      to: "src-tauri/src/paths.rs, content_search.rs, vault.rs, evidence_binder.rs, inbox.rs, secrets.rs, skill_host/fs.rs"
      via: "file:line mitigation citations grepped at HEAD"
      pattern: "paths.rs|skill_host/fs.rs|evidence_binder.rs"
    - from: "02-SECURITY.md"
      to: "02-01..02-03-PLAN.md threat_model blocks"
      via: "one register row per declared T-02-* threat"
      pattern: "T-02-0[1-8]"
  prohibitions:
    - statement: "No threat is marked closed without HEAD code evidence and, for mitigations, a passing covering test run in this plan; the report is not transcribed from 11-RESEARCH.md's table (D-12)."
      requirement_id: SEC-03
    - statement: "No GitHub issue is created, and no other external registration made, without the user's explicit approval: a large open threat is only drafted and proposed (D-13, workspace external-registration rule)."
      requirement_id: SEC-03
  flagged_assumptions:
    - "SEC-03 edge-probe row (unclassified, unresolved; surfaced, not dropped): 'has a security report on file' means 02-SECURITY.md in the 03-SECURITY.md shape with a verdict per declared T-02-* threat. The register was authored at plan time (all three PLAN files carry parseable `<threat_model>` blocks), so the secure-phase rules limit the audit to verifying that register. It does not scan for new threats."
    - "Executor agents cannot spawn the gsd-security-auditor subagent. The audit runs secure-phase's steps inline. With `threats_open: 0`, `register_authored_at_plan_time: true` and `asvs_level: 1`, the workflow's own short-circuit skips the auditor anyway (L1 grep-depth is sufficient). If any of those three conditions fails, the executor performs the auditor's verify-mitigations pass itself."
    - "RESEARCH expects `threats_open: 0`. The D-13 branches (small fix, or open threat plus proposed issue) are conditional, not planned work."
---

<objective>
Retire the v1.0 Phase 02 security-report gap (SEC-03). The secure-phase audit runs
retroactively over the 8 `T-02-*` threats declared at plan time, each is verified against
today's code, and the result is written as `02-SECURITY.md` in the `03-SECURITY.md`
format, so every v1.0 phase has a security report (D-12, D-13).

Purpose: the v1.0 audit found no code-level security break. The gap was a missing record.
This plan produces the record from evidence, not from assumption.

Output: `02-SECURITY.md`. Any code change happens only under D-13's small-fix branch,
which is not expected.
</objective>

<execution_context>
@/Users/yj.lee/workspace/work/dev/maru/.claude/gsd-core/workflows/execute-plan.md
@/Users/yj.lee/workspace/work/dev/maru/.claude/gsd-core/templates/summary.md
@/Users/yj.lee/workspace/work/dev/maru/.claude/gsd-core/workflows/secure-phase.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/11-milestone-verification-evidence/11-CONTEXT.md
@.planning/phases/11-milestone-verification-evidence/11-RESEARCH.md
</context>

## Artifacts this phase produces

Plan 11-05 scope only.

- New file `.planning/milestones/v1.0-phases/02-shared-scanner-and-path-invariants/02-SECURITY.md`.
- Conditional only (D-13 small-fix branch): a local fix plus test in the affected
  `src-tauri/src/` module. RESEARCH predicts none.
- Conditional only (D-13 large-threat branch): a drafted GitHub issue title and body in
  11-05-SUMMARY.md, never submitted by the executor.

<tasks>

<task type="tracer">
  <name>Task 1: Tracer, secure-phase audit of the T-02 register against HEAD, written out as 02-SECURITY.md</name>
  <precondition>`gsd_run query init.phase-op 02` returns `phase_found: true` with `phase_dir` ending in `v1.0-phases/02-shared-scanner-and-path-invariants`, and no `02-SECURITY.md` exists there yet (State B).</precondition>
  <files>.planning/milestones/v1.0-phases/02-shared-scanner-and-path-invariants/02-SECURITY.md</files>
  <read_first>
    - `.planning/milestones/v1.0-phases/03-typed-ipc-error-contract/03-SECURITY.md` (whole file, 66 lines: the format to copy exactly, section order and table columns)
    - `.planning/milestones/v1.0-phases/01-trustworthy-verify-signal/01-SECURITY.md` (second format reference; status and severity legend lines under the register)
    - The `<threat_model>` blocks of `02-01-PLAN.md`, `02-02-PLAN.md` and `02-03-PLAN.md` in `.planning/milestones/v1.0-phases/02-shared-scanner-and-path-invariants/` (extract with awk between `<threat_model>` and `</threat_model>`; do not read the whole plans)
    - The `## Threat Flags` sections of `02-01..02-03-SUMMARY.md` (all three state "None")
    - `.claude/gsd-core/workflows/secure-phase.md` steps 1-3 and 6 (classification table and the short-circuit rule)
    - `.planning/phases/11-milestone-verification-evidence/11-RESEARCH.md` section "SEC-03 Threat Register Audit" (expected evidence locations; line numbers may have drifted, so re-grep)
  </read_first>
  <action>
    Implements D-12. Run the secure-phase workflow inline for phase 02.

    1. Step 0. Run `gsd_run query init.phase-op 02`, and read
       `workflow.security_asvs_level` (1) and `workflow.security_block_on` (high) with
       `gsd_run query config-get`.

    2. Step 2. Build the register from the three `<threat_model>` blocks: threat_id,
       category, component, severity, disposition, mitigation plan and trust boundaries.
       Also bring in the SUMMARY threat flags. Set `register_authored_at_plan_time: true`.

    3. Step 3. Verify each threat at HEAD with a fresh grep, recording file:line, and a
       covering test run:
       - T-02-01: `ensure_within` in `src-tauri/src/paths.rs` and its
         descendant/equal/escape/unrelated tests. Run `cd src-tauri && cargo test --lib paths::`.
       - T-02-02: the `GENERATED_DIRS` union in `paths.rs` includes `.git` and `.venv`, and
         `exclude_git: true` appears in `src-tauri/src/content_search.rs`. Run
         `cargo test --lib content_search::`.
       - T-02-03 (accept): the design-rationale comment in `src-tauri/src/vault.rs`
         explaining lexical containment over `canonicalize()`, and `lexical_normalize`
         still blocking `..`.
       - T-02-04: the `.maru` exclusion ORed with the shared union in
         `src-tauri/src/evidence_binder.rs` `is_excluded_dir`. Run
         `cargo test --lib evidence_binder::`.
       - T-02-05: `generated_dirs` stays an injected parameter in
         `vault.rs` `ScanFilter::is_excluded_path`, and `src-tauri/src/inbox.rs` keeps its
         empty-slice call sites. Run `cargo test --lib -- vault:: inbox::`.
       - T-02-06 (accept): `should_prune` in `src-tauri/src/secrets.rs` keeps its
         `.maru/secrets` / `.secrets` prefix rules next to the union. Run
         `cargo test --lib secrets::`.
       - T-02-07: both `maru_home()` and `install_root_base()` in
         `src-tauri/src/skill_host/fs.rs` return through `require_absolute`, and the test
         `maru_home_rejects_relative_test_home` exists. Run
         `cargo test --lib skill_host::fs`.
       - T-02-08: that test holds `test_maru_home_lock()` and restores `MARU_TEST_HOME` on
         every path.

       Classify each threat CLOSED or OPEN using the step 3 criteria. Apply the
       short-circuit rule: if `threats_open` is 0, the register was authored at plan time,
       and ASVS is 1, go straight to step 6.

    4. Step 6, State B create. Write 02-SECURITY.md in the 03-SECURITY.md shape:
       - Frontmatter: `phase: 02`, `slug: shared-scanner-and-path-invariants`,
         `status: verified` (or `open` if an OPEN threat remains), the computed
         `threats_open`, `asvs_level: 1`, `created: 2026-09-25`.
       - Heading `# Phase 2 Security`, then a one-line note saying this is a retroactive
         verification, run in v1.1 Phase 11 (SEC-03), of the register declared in
         02-01..02-03-PLAN.md, against HEAD (the short SHA).
       - `## Trust Boundaries`: Boundary / Description / Data Crossing, taken from the
         three plans' trust-boundary tables.
       - `## Threat Register`: Threat ID / Category / Component / Severity / Disposition /
         Mitigation / Status. There is one row per T-02-01..T-02-08, and each Mitigation
         cell cites the HEAD file:line and the covering test.
       - `## Accepted Risks Log`: AR-02-01 for T-02-03 and AR-02-02 for T-02-06, each with
         its plan-time rationale, "Phase 2 design decision" as accepted by, and the
         plan-time date from the PLAN frontmatter or SUMMARY.
       - `## Security Audit Trail`: 2026-09-25 | 8 | closed count | open count |
         "inline secure-phase audit, v1.1 Phase 11 (L1, ASVS 1)".
       - `## Verification Evidence`: each cargo command with its `test result:` line and
         the HEAD SHA.
       - `## Sign-Off`: checklist plus `**Approval:** verified 2026-09-25`, or a pending
         approval if a threat stays open.

       Use ASCII punctuation only. The ascii-lint hook rejects em-dashes, curly quotes and
       ellipsis in Markdown.

    5. Skip the workflow's own step 7 commit and commit through the executor's per-task
       protocol.
  </action>
  <verify>
    <automated>cd src-tauri && cargo test --lib paths:: && cargo test --lib content_search:: && cargo test --lib evidence_binder:: && cargo test --lib -- vault:: inbox:: && cargo test --lib secrets:: && cargo test --lib skill_host::fs</automated>
    <fails_when>non-zero exit, or any invocation prints "running 0 tests" for its filter</fails_when>
    <automated>F=.planning/milestones/v1.0-phases/02-shared-scanner-and-path-invariants/02-SECURITY.md; test "$(grep -cE '^\| T-02-0[1-8] \|' "$F")" = 8 && grep -qE '^threats_open: [0-9]+$' "$F" && grep -q '^asvs_level: 1$' "$F" && for s in 'Trust Boundaries' 'Threat Register' 'Accepted Risks Log' 'Security Audit Trail' 'Verification Evidence' 'Sign-Off'; do grep -q "^## $s" "$F" || exit 1; done</automated>
    <fails_when>non-zero exit, meaning the register does not hold exactly 8 T-02 rows, the frontmatter lacks threats_open or asvs_level 1, or a required section is missing</fails_when>
  </verify>
  <acceptance_criteria>
    - 02-SECURITY.md exists with the frontmatter keys `phase`, `slug`, `status`, `threats_open`, `asvs_level`, `created` and the six body sections, in 03-SECURITY.md's order.
    - The Threat Register holds exactly eight rows, T-02-01..T-02-08, each with a HEAD file:line citation. Every `mitigate` row names a covering test that ran green in this task.
    - T-02-03 and T-02-06 appear in the Accepted Risks Log.
    - `threats_open` equals the number of OPEN rows at or above `high` severity.
    - The Verification Evidence section lists each cargo command's `test result:` line and the HEAD SHA.
  </acceptance_criteria>
  <done>
    Phase 02 has a security report produced by the secure-phase procedure, with every
    declared threat verified against the code that ships today.
  </done>
</task>

<task type="auto">
  <name>Task 2: D-13 disposition of any open threat, and the archive-scope check</name>
  <files>.planning/milestones/v1.0-phases/02-shared-scanner-and-path-invariants/02-SECURITY.md</files>
  <read_first>
    - `02-SECURITY.md` as written in Task 1 (the Threat Register status column and `threats_open`)
    - `.planning/phases/11-milestone-verification-evidence/11-CONTEXT.md` D-13 (small local fix lands now; larger one stays open and is proposed as an issue, registered only after user approval)
    - The implementing file of any OPEN threat, if one exists
  </read_first>
  <action>
    Implements D-13 and D-10.

    D-13 applies to any row whose Status is `open`, whatever its severity. `threats_open`
    counts only rows at or above `high`, so the case is chosen from the register rows,
    not from `threats_open` alone.

    Case A, every register row is `closed` (the expected outcome). Add one line under the
    Security Audit Trail table: "D-13 not triggered: no open threat." Make no code change.

    Case B, an OPEN threat with a small local fix. Small means one module, a mitigation of
    a few lines, and no signature change visible to other modules. Write a failing test for
    the threat first, then the fix, and confirm both with the module's
    `cargo test --lib <module>::`. Re-run Task 1's check for that threat, flip its row to
    `closed` citing the fix commit, recompute `threats_open`, and update the frontmatter
    `status` and Sign-Off. Run `make verify` and paste its output in 11-05-SUMMARY.md.

    Case C, an OPEN threat whose fix is larger than Case B:
    - Keep the row `open` and keep `threats_open` greater than 0.
    - Draft an issue title and body in 11-05-SUMMARY.md: threat, evidence, proposed
      mitigation and affected files.
    - STOP and return a checkpoint to the orchestrator asking the user whether to register
      the issue.
    - Do not run any GitHub issue-creation command yourself. Registration happens only
      after the user explicitly approves it.

    In every case, confirm the archive scope. For this plan's commits, `git diff
    --name-only "$(git merge-base HEAD origin/main)" -- .planning/milestones/v1.0-phases/02-shared-scanner-and-path-invariants/` must list only 02-SECURITY.md, plus 02-VALIDATION.md if plan 11-04 already landed
    (D-10). Code files are allowed only in Case B.
  </action>
  <verify>
    <automated>test "$(git diff --name-only "$(git merge-base HEAD origin/main)" -- .planning/milestones/v1.0-phases/02-shared-scanner-and-path-invariants/ | grep -vcE '02-(SECURITY|VALIDATION)\.md$')" = 0 && grep -qE '^threats_open: [0-9]+$' .planning/milestones/v1.0-phases/02-shared-scanner-and-path-invariants/02-SECURITY.md</automated>
    <fails_when>non-zero exit, meaning another Phase 02 archive file changed or threats_open is missing</fails_when>
    <automated>F=.planning/milestones/v1.0-phases/02-shared-scanner-and-path-invariants/02-SECURITY.md; o=$(grep -E '^\| T-02-0[1-8] \|' "$F" | grep -cE '\| open \|$'); if [ "$o" = 0 ]; then grep -q 'D-13 not triggered' "$F" && grep -q '^status: verified$' "$F" && grep -q '^threats_open: 0$' "$F"; else grep -q 'D-13' "$F"; fi</automated>
    <fails_when>non-zero exit, meaning the D-13 disposition line, the status, or threats_open does not match the register's open rows</fails_when>
  </verify>
  <acceptance_criteria>
    - If every register row is `closed`: `threats_open: 0`, `status: verified` and the "D-13 not triggered" line are present, and no file outside 02-SECURITY.md changed.
    - If Case B ran: the fix and its test landed in one module, the row is `closed` citing the fix commit, and `make verify` passed with its output pasted in the SUMMARY.
    - If Case C ran: the row stays `open`, a drafted issue is in the SUMMARY, the plan stopped with a checkpoint, and no issue was created.
    - Under `.planning/milestones/v1.0-phases/02-shared-scanner-and-path-invariants/`, only 02-SECURITY.md (and 02-VALIDATION.md, owned by plan 11-04) differ from the branch merge-base with `origin/main`.
  </acceptance_criteria>
  <done>
    D-13 is resolved explicitly (not triggered, fixed, or proposed and pending approval),
    and the v1.0 archive changed only by the new report.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Security report -> milestone audit | The audit trusts 02-SECURITY.md's register as evidence that Phase 02's threats are handled |
| Executor -> GitHub (external registration) | Creating an issue is an external action that needs explicit user approval |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-11-14 | Repudiation | 02-SECURITY.md threat statuses | medium | mitigate | Every status rests on a HEAD file:line grep plus a covering cargo test run in Task 1, recorded in Verification Evidence; the register row count is asserted at exactly 8 |
| T-11-15 | Spoofing / Elevation of Privilege | Unapproved external action (GitHub issue creation) | medium | mitigate | Case C stops with a checkpoint and a drafted issue; the executor never runs an issue-creation command; approval comes only from the user |
| T-11-16 | Tampering | v1.0 archive history | low | mitigate | Task 2's scope check: only 02-SECURITY.md (plus plan 11-04's 02-VALIDATION.md) differs from the branch merge-base inside the Phase 02 archive directory (D-10) |
</threat_model>

<verification>
- Six scoped cargo test runs green, with results recorded in 02-SECURITY.md's Verification Evidence
- 02-SECURITY.md has eight T-02 rows, computed `threats_open`, `asvs_level: 1`, and all six sections
- D-13 disposition recorded; no issue created without approval; only 02-SECURITY.md added to the archive
</verification>

<success_criteria>
- ROADMAP Phase 11 success criterion 4: v1.0 Phase 02 has a security report, so every v1.0 phase has security evidence on file
</success_criteria>

<output>
Create `.planning/phases/11-milestone-verification-evidence/11-05-SUMMARY.md` when done, with the per-threat verdict table, the cargo results, and the D-13 case taken.
</output>
