---
phase: 09-durability-and-session-lifecycle
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/REQUIREMENTS.md
autonomous: true
requirements: [REL-04]

estimate:
  tokens: 15000
  raw_tokens: 15000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "A job whose program.env PATH carries several tilde entries produces a launchd plist in which every entry is absolute and no literal `~/` survives, proven by the existing `plist_env_expands_colon_separated_tildes` test passing on the current tree (D-13, success criterion 4)."
    - "Every `:`-separated tilde segment expands (`~/a:~/b:/usr/bin` becomes `{home}/a:{home}/b:/usr/bin`), a colon-bearing non-path value (`https://example.com/x`, `12:30`) and a no-tilde multi-path value are byte-identical, proven by the existing `env_value_expands_every_tilde_segment` test (D-13)."
    - "Segment order is preserved: the expanded value lists its segments in the same order as the input (`~/a:~/b:/usr/bin` keeps a, b, /usr/bin in order), proven by the same test's first assertion (edge probe REL-04 ordering, resolved explicit)."
    - "A bare `~` segment that touches a separator (`~:/usr/bin`) expands to `{home}:/usr/bin`; the segment boundary, not a `~/` prefix, decides expansion, proven by the same test's last assertion (edge probe REL-04 adjacency, resolved explicit)."
    - statement: "An empty env value stays the empty string and a single-element value expands exactly like `expand_tilde` would (no stray separator is added or removed); the single-element half is asserted (`~/bin/tools`), the empty-string half has no test and D-13 forbids adding one outside a success criterion (edge probe REL-04 empty)."
      verification: backstop
    - statement: "A segment carrying non-ASCII path characters passes through byte-identical apart from its tilde prefix, because `split(':')` splits on the ASCII colon byte and `expand_tilde` only rewrites a leading `~` / `~/` (edge probe REL-04 encoding)."
      verification: backstop
    - "`src-tauri/src/jobs.rs` is not modified by this phase: REL-04 is verify-only and shipped in #328 (commit 86e7074f)."
  artifacts:
    - path: ".planning/REQUIREMENTS.md"
      provides: "REL-04 checkbox ticked and traceability row reading Complete"
      contains: "| REL-04 | Phase 9 | Complete |"
  key_links:
    - from: "src-tauri/src/jobs.rs::plist_for"
      to: "src-tauri/src/jobs.rs::expand_tilde_segments"
      via: "program.env values pass through segment-wise tilde expansion before being written into the plist EnvironmentVariables dict"
      pattern: "expand_tilde_segments"
  prohibitions: []
  flagged_assumptions:
    - "REL-04 prohibition recall produced zero kept prohibitions: segment-wise tilde expansion is a pure utility with no values, safety, or ethics surface (clean-utility precision outcome, not a dropped item)."
---

<objective>
Confirm success criterion 4 against the already-shipped fix and close REL-04 in
traceability, with no code change.

Purpose: REL-04 landed as commit 86e7074f in #328 (issue #295, released v1.1.8).
D-13 makes this phase verify-only for it: run the named regression tests, map each
criterion component to an assertion, and mark REL-04 complete. A new test is added
only if a criterion component turns out to have no assertion (research found all
three covered).

Output: a passing test run recorded in the SUMMARY with a criterion-to-assertion
map, and REL-04 marked Complete in `.planning/REQUIREMENTS.md`.
</objective>

<execution_context>
@/Users/yj.lee/workspace/work/dev/maru/.claude/gsd-core/workflows/execute-plan.md
@/Users/yj.lee/workspace/work/dev/maru/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-durability-and-session-lifecycle/09-CONTEXT.md
@.planning/phases/09-durability-and-session-lifecycle/09-RESEARCH.md
</context>

## Artifacts this phase produces

Plan 09-01 scope only:

- No new symbols, files, commands, flags, or menu ids. `src-tauri/src/jobs.rs` is read, not edited.
- Changed rows in `.planning/REQUIREMENTS.md`: the REL-04 checkbox (`- [x] **REL-04**`) and the traceability row (`| REL-04 | Phase 9 | Complete |`).

<tasks>

<task type="tracer">
  <name>Task 1: Run the shipped REL-04 regression tests end to end and map each criterion component to its assertion</name>
  <files>none - verification only; src-tauri/src/jobs.rs is read, not modified</files>
  <read_first>
    - `src-tauri/src/jobs.rs:185-222` - `expand_tilde` and `expand_tilde_segments`, the shipped fix
    - `src-tauri/src/jobs.rs:1066-1123` - `env_value_expands_every_tilde_segment` (unit) and `plist_env_expands_colon_separated_tildes` (plist XML integration)
    - `.planning/phases/09-durability-and-session-lifecycle/09-RESEARCH.md` "REL-04 verify-only" code example - the research's criterion-to-assertion mapping to re-check, not trust
    - `.planning/ROADMAP.md` Phase 9 success criterion 4 - the exact wording being verified
  </read_first>
  <action>
    Per D-13, do not edit `src-tauri/src/jobs.rs`. Run the jobs test module and
    confirm both named tests pass on the current tree. Then read both test bodies
    and write, into the plan SUMMARY, a three-row map from success criterion 4's
    components to the exact assertion that covers each: (1) every tilde segment
    becomes absolute - the `~/a:~/b:/usr/bin` assertion plus the plist test's
    `<string>` containment and its no-literal-`~/` assertion; (2) a colon-bearing
    non-path value is byte-identical - the `https://example.com/x` and `12:30`
    assertions; (3) a single-path value is byte-identical to the pre-fix output -
    the `~/bin/tools` and `/usr/bin:/opt/homebrew/bin` assertions. Also record the
    two explicit edge rows (ordering: first assertion keeps segment order;
    adjacency: `~:/usr/bin`) and the two backstop edge rows (empty string,
    non-ASCII segment) as untested-by-design under D-13. If, contrary to research,
    any of the three criterion components has no assertion, add exactly one
    assertion for that component to `env_value_expands_every_tilde_segment` and
    say so in the SUMMARY; otherwise add nothing.
  </action>
  <verify>
    <automated>set -o pipefail; cargo test --manifest-path src-tauri/Cargo.toml --lib jobs::tests 2>&1 | grep -cE "(env_value_expands_every_tilde_segment|plist_env_expands_colon_separated_tildes) \.\.\. ok" | grep -qx 2</automated>
    <fails_when>non-zero exit: cargo failed, or fewer than 2 of the named tests printed "... ok" (a renamed or filtered-out test fails this gate instead of passing silently)</fails_when>
    <automated>git diff --exit-code -- src-tauri/src/jobs.rs</automated>
    <fails_when>non-zero exit, meaning jobs.rs was modified without a documented missing-criterion reason in the SUMMARY</fails_when>
  </verify>
  <acceptance_criteria>
    - Both `env_value_expands_every_tilde_segment` and `plist_env_expands_colon_separated_tildes` print `... ok` in the cargo output.
    - The SUMMARY contains a three-row criterion-to-assertion table quoting each covering assertion's input and expected output.
    - `git diff --exit-code -- src-tauri/src/jobs.rs` exits 0 (unless a missing criterion assertion was found and documented).
  </acceptance_criteria>
  <done>Success criterion 4 is shown covered by named, passing assertions, with no source change.</done>
</task>

<task type="auto">
  <name>Task 2: Mark REL-04 complete in REQUIREMENTS.md traceability</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - `.planning/REQUIREMENTS.md` REL-04 bullet (Durability and Session Lifecycle section) and the Traceability table row `| REL-04 | Phase 9 | Pending |`
    - `/Users/yj.lee/workspace/work/dev/maru/.claude/gsd-core/workflows/execute-plan.md` step that runs `requirements.mark-complete` - the standard path that performs this edit
  </read_first>
  <action>
    Per D-13, mark REL-04 complete through the standard execute-plan step
    (`gsd_run query requirements.mark-complete REL-04`) once Task 1's SUMMARY
    evidence exists. If the tool is unavailable, edit only the two REL-04 lines by
    hand: tick the `**REL-04**` checkbox and change its traceability status to
    Complete. Do not touch REL-01, REL-02, or REL-03 rows; their plans close them.
  </action>
  <verify>
    <automated>grep -F "| REL-04 | Phase 9 | Complete |" .planning/REQUIREMENTS.md && grep -F -- "- [x] **REL-04**" .planning/REQUIREMENTS.md</automated>
    <fails_when>either grep exits non-zero (row not flipped or checkbox not ticked)</fails_when>
    <automated>grep -F "| REL-01 | Phase 9 | Pending |" .planning/REQUIREMENTS.md</automated>
    <fails_when>non-zero exit, meaning a sibling requirement row was changed by this plan</fails_when>
  </verify>
  <acceptance_criteria>
    - `.planning/REQUIREMENTS.md` reads `| REL-04 | Phase 9 | Complete |` and `- [x] **REL-04**`.
    - REL-01, REL-02, and REL-03 rows are unchanged by this plan.
  </acceptance_criteria>
  <done>REL-04 reads Complete in traceability and its checkbox is ticked.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| none new | Verify-only plan; no code path, input, or IPC surface changes |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-09-01-01 | Tampering | `src-tauri/src/jobs.rs` launchd plist env values | low | accept | Shipped behavior is unchanged; this plan only runs its existing tests and gates on `git diff --exit-code` for jobs.rs |
| T-09-01-SC | Tampering | package installs | low | accept | No npm/cargo installs in this plan; package legitimacy gate not triggered |
</threat_model>

<verification>
- `cargo test --manifest-path src-tauri/Cargo.toml --lib jobs::tests` passes with both named REL-04 tests reported `ok`.
- `git diff --exit-code -- src-tauri/src/jobs.rs` is clean.
- REQUIREMENTS.md shows REL-04 Complete.
</verification>

<success_criteria>
Success criterion 4 is verified against named passing tests, REL-04 is Complete in
traceability, and no production code changed.
</success_criteria>

<output>
Create `.planning/phases/09-durability-and-session-lifecycle/09-01-SUMMARY.md` when done
</output>
