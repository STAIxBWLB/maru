---
phase: 09-durability-and-session-lifecycle
plan: 07
type: execute
wave: 2
depends_on: ["09-05"]
files_modified:
  - src/components/meetings/MeetingSourceWorkbench.tsx
  - src/components/meetings/MeetingSourceWorkbench.test.tsx
  - src/components/studio/StudioMode.tsx
  - src/components/graph/GraphView.tsx
  - src/lib/teardownSave.surfaces.test.ts
  - src/components/today/TodayBrainDump.tsx
  - src/components/today/TodayPrepare.test.tsx
autonomous: true
requirements: [REL-02, REL-03]

estimate:
  tokens: 70000
  raw_tokens: 70000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "A meeting-source edit made inside the 700 ms autosave window is saved when the source editor unmounts, instead of the timer being cleared (D-01, REL-02)."
    - "Meeting-source autosave still waits for Korean IME composition to finish, still stops auto-retrying after a failed save, and still keeps the visible edit after a failure (existing MeetingSourceWorkbench tests pass unchanged)."
    - "A Studio draft change made inside the 600 ms window is saved when Studio unmounts, and switching Studio to another document inside that window saves the old document's state instead of coalescing it away (D-01)."
    - "A graph layout change made inside the 1500 ms window is saved to `.maru/cache/graph-layout.json` when the graph view unmounts (D-01; research inventory's borderline surface included for consistency)."
    - "The Today brain dump keeps flushing its typed tail on unmount and on Finish/Quick skip, now through the shared saver, and Undo still discards unsaved typing (D-01)."
    - "Every in-scope autosave surface (Scratchpad, Studio, Today brain dump, meeting source, graph layout) calls `useTeardownFlush` with a describe callback that names a real file label and the unsaved content, so plan 09-06's reporter can keep a recovery copy for any of them (D-07, D-08)."
    - "`HtmlVisualEditor` is left as is: its 300 ms timer serializes the iframe DOM into parent state (not a disk save) and its unmount cleanup already runs the serializer (D-01 'conform if cheap': not converted, pinned as compliant by the surfaces test)."
    - "The surfaces pin test fails if any in-scope surface stops calling `useTeardownFlush` or reintroduces its hand-rolled save timer."
  artifacts:
    - path: "src/components/meetings/MeetingSourceWorkbench.tsx"
      provides: "SourceEditor autosave on createDebouncedSaver keyed by editor, unmount flush via useTeardownFlush"
      contains: "useTeardownFlush("
    - path: "src/components/studio/StudioMode.tsx"
      provides: "Studio state autosave on createDebouncedSaver keyed by workspaceRoot, flush before document switch, unmount flush"
      contains: "useTeardownFlush("
    - path: "src/components/graph/GraphView.tsx"
      provides: "graph layout autosave on createDebouncedSaver keyed by workspacePath, unmount flush"
      contains: "useTeardownFlush("
    - path: "src/components/today/TodayBrainDump.tsx"
      provides: "brain dump autosave on createDebouncedSaver, registered flush and unmount flush through the saver"
      contains: "useTeardownFlush("
    - path: "src/lib/teardownSave.surfaces.test.ts"
      provides: "source-level pin: five surfaces call useTeardownFlush and createDebouncedSaver; HtmlVisualEditor keeps its unmount serialize"
  key_links:
    - from: "src/components/meetings/MeetingSourceWorkbench.tsx SourceEditor"
      to: "src/lib/meetingSourceEditorStore.ts::MeetingSourceEditorStore.flush"
      via: "saver save callback awaits editor.flush(); rejection surfaces as a failed settlement"
      pattern: "createDebouncedSaver"
    - from: "src/components/studio/StudioMode.tsx"
      to: "src/lib/studio.ts::studioStateSave"
      via: "enqueueStudioSave inside the saver's save callback, rethrowing after its existing bookkeeping"
      pattern: "enqueueStudioSave\\("
  prohibitions:
    - statement: "MUST NOT drop a pending edit on any autosave surface's teardown to make navigation faster; an intentional discard (Undo, IME deferral, stop-after-failure) stays an explicit, visible user state (REL-02 values; recalled prohibition, judgment-tier, no wired check)."
  flagged_assumptions:
    - "Studio and GraphView have no component test files and heavy dependencies (sigma/graphology, the Studio pipeline). Their behavior is covered by the shared hook tests from 09-05 plus the source-level surfaces pin here, not by new component tests."
    - "When the Today provider's `mutate` returns null for a real failure, the thrown reason is the stable code `today_brain_dump_save_failed` unless the provider exposes a richer message; degraded read-only mode (no snapshot) stays a no-op, as today."
---

<objective>
Move the remaining debounced autosave surfaces from research's inventory onto the
shared saver and the teardown hook, so each one performs its pending save on
unmount (D-01) and is ready for recovery reporting (D-07/D-08).

Purpose: research Pitfall 1 found cancel-only unmounts in MeetingSourceWorkbench
(700 ms), Studio (600 ms), and GraphView (1500 ms), and a correct but hand-rolled
flush in TodayBrainDump (800 ms). D-01 locks every such surface onto
`src/lib/debouncedSave.ts`. Excluded with reasons (research inventory): diagram
auto-snapshots (best-effort versioning, not the document save), the vault index
delta timer (derived cache), Drafts and the main editor (no debounce; explicit save
plus the dirty-draft guard), and the App settings saver (already on the helper and
flushed by the close guard).

Output: four converted surfaces, their tests where a harness exists, and a
source-level pin test.
</objective>

<execution_context>
@/Users/yj.lee/workspace/work/dev/maru/.claude/gsd-core/workflows/execute-plan.md
@/Users/yj.lee/workspace/work/dev/maru/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/09-durability-and-session-lifecycle/09-CONTEXT.md
@.planning/phases/09-durability-and-session-lifecycle/09-RESEARCH.md
@.planning/phases/09-durability-and-session-lifecycle/09-PATTERNS.md
@.planning/phases/09-durability-and-session-lifecycle/09-05-SUMMARY.md

<interfaces>
From plan 09-05 (`src/lib/debouncedSave.ts`, `src/lib/teardownSave.ts`):
- `createDebouncedSaver<T>(save, delayMs, onError?) : SettlingDebouncedSaver<T>` with `schedule`, `flush`, `cancel`, `flushSettled`
- `useTeardownFlush<T>(saver: SettlingDebouncedSaver<T> | null, describe: (value: T) => TeardownSaveTarget | null, t: Translate): void` - registers while mounted; unmount or saver swap settles the old saver
- `TeardownSaveTarget = { workPath: string; filePath: string; content: string }`

From src/components/meetings/MeetingSourceWorkbench.tsx:204-250 (SourceEditor): `useSyncExternalStore(editor.subscribe, editor.getSnapshot)` yields `{ session, draft, dirty, saving, error: saveError }`; autosave effect at :247-250 arms `window.setTimeout(() => { void editor.flush().catch(() => {}); }, 700)` when `dirty && !composing && !busy && !saveError` and clears it in cleanup; `act()` calls `editor.flush()` itself. Store: `src/lib/meetingSourceEditorStore.ts` (`flush(): Promise<SourceSession>` rejects on failure and keeps `dirty`; `workPath` is public; `hasDirtyMeetingSourceDrafts()` feeds the quit guard). Session storage lives under `.maru/meetings/source-reviews` (`src-tauri/src/meeting_sources.rs:210`).

From src/components/studio/StudioMode.tsx: `saveTimerRef` (:127); `enqueueStudioSave(nextState)` (:192-220) serializes on `saveQueueRef`, catches errors into `setError` when the revision is current, never rejects; state-load effect keyed on `activeDocId`, `activeDocument?.path`, `workspaceRoot` (:222-258); 600 ms timer effect with cancel-only cleanup (:262-270); `saveNow()` (:335-339) clears the timer and awaits `enqueueStudioSave(state)`, called as `void saveNow()` (:829); `const { t } = useTranslation()` (:113).

From src/components/graph/GraphView.tsx: `saveTimerRef` (:395); settle effect (:600-616) builds `{ version: 2, positions, pinnedIds }` at fire time and calls `void vaultGraphLayoutSave(workspacePath, ...)` after `SAVE_DEBOUNCE_MS` (1500), cancel-only cleanup; layout cache path `.maru/cache/graph-layout.json` (`src-tauri/src/vault_graph.rs:24`); uses `useTranslation` (:47).

From src/components/today/TodayBrainDump.tsx (218 lines): `save(value)` (:69-86) calls `mutate({ type: "setBrainDump", brainDump: value })`, returns early without a snapshot; unmount effect already flushes via `saveRef` (:89-96); `handleChange` arms an 800 ms timer (:98-112); `flush` (:114-121) registered through `onRegisterFlush` (:127); `handleUndo` clears pending typing (:129-141); context `useToday()` exposes `workPath` and `snapshot`.

From src/components/HtmlVisualEditor.tsx:242-260: serialize timer with an unmount cleanup that runs `serializeNowRef.current()` (already compliant).
</interfaces>
</context>

## Artifacts this phase produces

Plan 09-07 scope only:

- Per-surface stable savers (`createDebouncedSaver`) and `useTeardownFlush(...)` calls in `MeetingSourceWorkbench.tsx` (SourceEditor, saver keyed by `editor`), `StudioMode.tsx` (keyed by `workspaceRoot`), `GraphView.tsx` (keyed by `workspacePath`), `TodayBrainDump.tsx` (one per mount)
- Describe labels: meeting `.maru/meetings/source-reviews/<session id>` with the draft JSON as content; Studio the active document path (or `studio/<docId>`) with the state JSON; graph `.maru/cache/graph-layout.json` with the layout JSON; brain dump `today-brain-dump-<snapshot date>.txt` with the text
- New error code thrown by the brain-dump saver on a null mutate result: `today_brain_dump_save_failed`
- New file `src/lib/teardownSave.surfaces.test.ts` (source-level pin)
- Removed: the hand-rolled save timers and cancel-only cleanups in these four files

<tasks>

<task type="tracer">
  <name>Task 1: A meeting-source edit made just before the editor closes is saved</name>
  <files>src/components/meetings/MeetingSourceWorkbench.tsx, src/components/meetings/MeetingSourceWorkbench.test.tsx</files>
  <read_first>
    - `src/components/meetings/MeetingSourceWorkbench.tsx:200-275` (SourceEditor state, autosave effect, `act`)
    - `src/lib/meetingSourceEditorStore.ts` (whole file, 83 lines)
    - `src/components/meetings/MeetingSourceWorkbench.test.tsx` (mount/edit helpers and the three autosave-related tests)
    - `src/lib/teardownSave.ts` and `src/lib/debouncedSave.ts` as plan 09-05 left them
  </read_first>
  <reversibility rating="costly">D-01 moves every autosave surface's save path onto the shared helper; undoing it touches each surface again.</reversibility>
  <action>
    Implements D-01 for the meeting-source surface.

    In SourceEditor, create the saver with `useMemo` keyed on `editor`:
    `createDebouncedSaver<MeetingSourceEditorStore>(async (store) => { await store.flush(); }, 700)`
    (a rejection becomes a failed settlement; the store keeps `dirty` and its
    `error`, as now). Replace the timer effect with one that calls
    `saver.cancel()` when `!dirty || composing || busy || saveError` and otherwise
    `saver.schedule(editor)`, keeping today's dependency list plus `saver`, so IME
    deferral and stop-after-failure behave exactly as before. Call
    `useTeardownFlush(saver, describe, t)` where `describe(store)` returns
    `{ workPath: store.workPath, filePath: ".maru/meetings/source-reviews/" + session id, content: JSON.stringify(store.getSnapshot().draft, null, 2) }`.
    A saver swap (different `editor`) settles the old store's pending save through
    the hook. Add a test: edit, unmount inside 700 ms, settle, and assert the
    mocked `saveMeetingSourceDraft` received the edited draft once.
  </action>
  <verify>
    <automated>pnpm exec vitest run src/components/meetings/MeetingSourceWorkbench.test.tsx</automated>
    <fails_when>non-zero exit, or "0 passed", or the IME-deferral or failure-preservation test fails</fails_when>
    <automated>pnpm typecheck && pnpm lint</automated>
    <fails_when>non-zero exit, or any ESLint warning</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -c "useTeardownFlush(" src/components/meetings/MeetingSourceWorkbench.tsx` is 1 and `grep -c "createDebouncedSaver" src/components/meetings/MeetingSourceWorkbench.tsx` is at least 1.
    - The new unmount-save test passes, and `defers autosave until Korean IME composition finishes` and `preserves visible edits when autosave fails and never reports confirmation` still pass.
  </acceptance_criteria>
  <done>Closing the meeting-source editor inside the autosave window saves the edit.</done>
</task>

<task type="auto">
  <name>Task 2: Studio and the graph layout save on unmount, pinned by a source-level surfaces test</name>
  <files>src/components/studio/StudioMode.tsx, src/components/graph/GraphView.tsx, src/lib/teardownSave.surfaces.test.ts</files>
  <read_first>
    - `src/components/studio/StudioMode.tsx:100-130`, `:185-345`, `:820-835`
    - `src/components/graph/GraphView.tsx:380-400` and `:596-620`
    - `src/components/HtmlVisualEditor.tsx:242-260` (the compliant unmount serialize the pin test asserts)
    - `scripts/check-csp-blob.test.ts` or `scripts/check-dom-sanitizer.test.ts` - the repo's readFileSync source-assertion test style
  </read_first>
  <reversibility rating="costly">D-01 moves every autosave surface's save path onto the shared helper; undoing it touches each surface again.</reversibility>
  <action>
    Implements D-01 for Studio and the graph layout.

    StudioMode: create the saver with `useMemo` keyed on `workspaceRoot` (null when
    there is no root) whose save callback awaits `enqueueStudioSave(state)`; make
    `enqueueStudioSave` rethrow the save error after its existing bookkeeping
    (setError when the revision is current, setSaving false) so the saver can see
    a failure, and make `saveNow` catch that rejection since its error is already
    on screen. Replace the 600 ms timer effect with one that calls
    `saver.schedule(state)` when there is a root, a state, and no load in progress,
    with no cancelling cleanup. At the start of the state-load effect (document or
    root switch), call `void saver?.flush()` before loading so the previous
    document's pending state is saved rather than overwritten by the next
    schedule. In `saveNow`, replace the manual timer clear with `saver?.cancel()`.
    Call `useTeardownFlush(saver, describe, t)` with `describe(state)` returning
    `{ workPath: workspaceRoot, filePath: activeDocument?.path ?? "studio/" + docId, content: JSON.stringify(state, null, 2) }`.

    GraphView: create the saver with `useMemo` keyed on `workspacePath` whose save
    callback returns `vaultGraphLayoutSave(workspacePath, layout)`. In the settle
    effect keep every existing guard (settled node identity, finite positions),
    build the `{ version: 2, positions, pinnedIds }` payload at schedule time, and
    call `saver.schedule(payload)` with no cancelling cleanup. Call
    `useTeardownFlush(saver, describe, t)` with `describe(layout)` returning
    `{ workPath: workspacePath, filePath: ".maru/cache/graph-layout.json", content: JSON.stringify(layout) }`.
    Remove both components' hand-rolled save timer refs.

    Create `src/lib/teardownSave.surfaces.test.ts` in the readFileSync
    source-assertion style: for each of `src/components/ScratchpadPane.tsx`,
    `src/components/studio/StudioMode.tsx`,
    `src/components/meetings/MeetingSourceWorkbench.tsx`, and
    `src/components/graph/GraphView.tsx`, assert the file contains
    `useTeardownFlush(` and `createDebouncedSaver`, and assert each file's former
    timer identifier is gone (ScratchpadPane, StudioMode, and GraphView each had
    one named save/autosave timer ref; name them in the test). Also assert
    `src/components/HtmlVisualEditor.tsx` still runs its serializer in an unmount
    cleanup. Task 3 adds the TodayBrainDump case, so every committed state of this
    file is green.
  </action>
  <verify>
    <automated>pnpm typecheck && pnpm lint</automated>
    <fails_when>non-zero exit, or any ESLint warning (react-hooks/exhaustive-deps on the new effects included)</fails_when>
    <automated>pnpm exec vitest run src/lib/teardownSave.surfaces.test.ts</automated>
    <fails_when>non-zero exit, or "0 passed" in the vitest summary</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -c "useTeardownFlush(" src/components/studio/StudioMode.tsx` is 1 and `grep -c "useTeardownFlush(" src/components/graph/GraphView.tsx` is 1.
    - `grep -c "saveTimerRef" src/components/studio/StudioMode.tsx` is 0 and `grep -c "saveTimerRef" src/components/graph/GraphView.tsx` is 0.
    - The surfaces pin test (Studio, Graph, Scratchpad, Meeting, HtmlVisualEditor cases) passes.
  </acceptance_criteria>
  <done>Studio and the graph layout perform their pending save on unmount, and a pin test guards all converted surfaces.</done>
</task>

<task type="auto">
  <name>Task 3: The Today brain dump runs on the shared saver with its flush-on-unmount, Finish/Quick-skip flush, and Undo discard intact</name>
  <files>src/components/today/TodayBrainDump.tsx, src/components/today/TodayPrepare.test.tsx, src/lib/teardownSave.surfaces.test.ts</files>
  <read_first>
    - `src/components/today/TodayBrainDump.tsx` (whole file, 218 lines)
    - `src/components/today/todayContext.ts` (TodayContextValue: `workPath`, `snapshot`, `mutate` returning null on failure)
    - `src/components/today/TodayPrepare.tsx:170-190` (`onRegisterFlush={registerBrainDumpFlush}`)
    - `src/components/today/TodayPrepare.test.tsx:195-245` (brain-dump typing, autosave, and undo tests)
  </read_first>
  <reversibility rating="costly">D-01 moves every autosave surface's save path onto the shared helper; undoing it touches each surface again.</reversibility>
  <action>
    Implements D-01 for the brain dump (behavior was already correct; this is the
    helper conversion D-01 requires).

    Create one saver per mount with `createDebouncedSaver<string>(value =>
    saveRef.current(value), AUTOSAVE_DEBOUNCE_MS)`. In `save`, keep the no-snapshot
    early return (degraded mode stays a no-op) and throw
    `new Error("today_brain_dump_save_failed")` when `mutate` resolves null, after
    setting status back to idle. `handleChange` calls `saver.schedule(capped)`;
    the registered `flush` becomes `() => saver.flush()`; `handleUndo` calls
    `saver.cancel()` before the undo mutation (undo still supersedes unsaved
    typing). Replace the hand-written unmount effect and the pending-text ref with
    `useTeardownFlush(saver, describe, t)` where `describe(text)` returns
    `{ workPath, filePath: "today-brain-dump-" + snapshot date + ".txt", content: text }`
    and returns null when there is no `workPath`. Remove the hand-rolled timer ref.
    Add a TodayPrepare test: type, unmount the Prepare panel inside 800 ms, and
    assert `mutate` was called once with the typed text. Add the TodayBrainDump
    case to `src/lib/teardownSave.surfaces.test.ts` (calls `useTeardownFlush(` and
    `createDebouncedSaver`; its former timer ref identifier is gone).
  </action>
  <verify>
    <automated>pnpm exec vitest run src/components/today/TodayPrepare.test.tsx src/lib/teardownSave.surfaces.test.ts</automated>
    <fails_when>non-zero exit, or "0 passed", or an existing brain-dump autosave/undo test fails</fails_when>
    <automated>pnpm typecheck && pnpm lint && pnpm test</automated>
    <fails_when>non-zero exit, or any ESLint warning, or any failing vitest case in the full suite</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -c "useTeardownFlush(" src/components/today/TodayBrainDump.tsx` is 1 and `grep -c "createDebouncedSaver" src/components/today/TodayBrainDump.tsx` is 1.
    - The full surfaces pin test (all five surfaces plus HtmlVisualEditor) passes.
    - Existing tests `updates the brain dump text and counter while typing, then autosaves` and `enables undo only after a save lands, then disables it when the backend reports nothing to undo` pass, plus the new unmount test.
  </acceptance_criteria>
  <done>All five in-scope autosave surfaces run on the shared saver and flush on unmount, and the pin test is fully green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| webview -> Rust IPC (existing save commands) | `saveMeetingSourceDraft`, `studio_state_save`, `vault_graph_layout_save`, the Today mutation; unchanged commands, now also called on unmount |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-09-07-01 | Tampering | saving one workspace's state into another after a switch | high | mitigate | Studio and Graph savers are keyed by their root/path (a switch swaps the saver and settles the old one against its own root); the meeting saver carries its store, whose `workPath` is fixed |
| T-09-07-02 | Tampering | saving a stale Studio document over a newer one | medium | mitigate | The load effect flushes the previous document's pending state before loading the next, and `enqueueStudioSave`'s serialized queue keeps order |
| T-09-07-03 | Information Disclosure | recovery content for non-authored data (graph layout JSON) | low | accept | The layout is workspace metadata already stored under `.maru/cache`; a recovery copy stays inside the same workspace's `.maru` |
| T-09-07-SC | Tampering | package installs | low | accept | No new dependencies; package legitimacy gate not triggered |
</threat_model>

<verification>
- `pnpm exec vitest run src/components/meetings/MeetingSourceWorkbench.test.tsx src/components/today/TodayPrepare.test.tsx src/lib/teardownSave.surfaces.test.ts` passes.
- `pnpm test`, `pnpm typecheck`, and `pnpm lint` pass.
</verification>

<success_criteria>
Every debounced autosave surface in research's inventory either performs its pending
save on unmount through the shared helper or is excluded with a recorded reason, and
a pin test keeps it that way.
</success_criteria>

<output>
Create `.planning/phases/09-durability-and-session-lifecycle/09-07-SUMMARY.md` when done
</output>
