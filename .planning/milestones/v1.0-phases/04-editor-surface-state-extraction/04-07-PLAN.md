---
phase: 04-editor-surface-state-extraction
plan: "07"
type: execute
wave: 6
depends_on: ["04-06"]
files_modified:
  - src/App.tsx
  - src/components/DocumentList.tsx
  - src/components/TerminalPanel.tsx
  - src/lib/shellSurfaceRenderProbe.ts
  - src/__tests__/editorSurfaceRenderIsolation.test.tsx
autonomous: true
gap_closure: true
requirements: [SHELL-01, SHELL-02, SHELL-03, SHELL-04]
estimate:
  tokens: 48000
  raw_tokens: 48000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "[SHELL-01/D-01..D-12/D-16] The verified four-prop OutlinePane facade, keyed ownership, persistence, cleanup, and command-port contracts remain unchanged while the shell isolation gap is closed."
    - "[SHELL-02/D-01..D-12/D-16] The verified four-prop EditorPane facade remains the canonical draft-render path; tab badges, dirty state, active documents, split groups, transient state, and persisted view modes stay fresh."
    - "[SHELL-03/D-13] A real updateTabDraft() dispatched through editorTabsStore may re-execute MainApp, but the production DocumentList, TerminalPanel, and extracted production activity rail do not re-render when their actual props/state are unchanged."
    - "[SHELL-03/D-13] The render-counter regression mounts the production boundary used by MainApp, uses the actual three named shell surfaces, and proves both left and right real tab updates; it contains no inert ShellProbe substitutes."
    - "[SHELL-04/D-14] The existing EditorPane preview-mark class and exact DOM-node identity regression remains green without changing preview markup ownership or memoization."
    - "[D-15] Focused tests, make verify, browser E2E, bundle/lazy-chunk budgets, git diff hygiene, and a fresh Phase 4 verifier report all pass with pixel-identical output."
  artifacts:
    - path: "src/App.tsx"
      provides: "Memoized production activity-rail boundary, stable DocumentList callback props, exported MainApp test boundary, and unchanged shell behavior"
    - path: "src/lib/shellSurfaceRenderProbe.ts"
      provides: "Test-controlled no-op-by-default render observer for MainApp and the three actual shell surfaces"
      exports: ["ShellSurfaceRenderTarget", "recordShellSurfaceRender", "setShellSurfaceRenderObserverForTest"]
    - path: "src/__tests__/editorSurfaceRenderIsolation.test.tsx"
      provides: "MainApp-level real-store regression for DocumentList, TerminalPanel, and activity-rail render isolation"
  key_links:
    - from: "src/lib/editorTabsStore.ts updateTabDraft"
      to: "src/App.tsx MainApp"
      via: "the existing useDocTabs subscription and a real replacement tabs snapshot"
      pattern: "updateTabDraft|useDocTabs"
    - from: "src/App.tsx MainApp"
      to: "DocumentList, TerminalPanel, and ActivityRail"
      via: "React.memo production boundaries with referentially stable props and current-snapshot callbacks"
      pattern: "memo|useCallback|useMemo"
    - from: "src/__tests__/editorSurfaceRenderIsolation.test.tsx"
      to: "the exact production components rendered by MainApp"
      via: "shellSurfaceRenderProbe counters installed before a real editorTabsStore draft publish"
      pattern: "setShellSurfaceRenderObserverForTest|updateTabDraft"
  prohibitions: []
---

<objective>
Close the single Phase 4 verification gap by protecting the real shell surfaces from editor-draft invalidation and replacing the synthetic sibling probes with a MainApp-level production-boundary regression.

Purpose: satisfy the central SHELL-03 outcome without widening Phase 4 into Phase 5 state extraction or changing any visible output, while re-proving the already-satisfied SHELL-01, SHELL-02, and SHELL-04 contracts.

Output: one no-op-by-default render-observer seam, a memoized production activity rail, stable production DocumentList callback props, and a real-store regression that directly counts the actual DocumentList, TerminalPanel, and activity rail rendered by MainApp.

**Flagged assumption for SHELL-01:** the existing four-prop OutlinePane facade is complete and may not be reopened or redesigned; this plan only proves it remains intact.

**Flagged assumption for SHELL-02:** the existing four-prop EditorPane facade and editorTabsStore draft ownership are complete; this plan must not add a second draft owner or new persisted/transient behavior.

**Flagged assumption for SHELL-03:** the acceptance boundary is the three production surfaces named by the requirement. MainApp itself may still observe a canonical tabs publish, but its unrelated memoized children must not execute when their inputs are unchanged. Dirty/tab metadata may update only the narrow subscriber whose visible state truly changed; repeated draft text changes may not invalidate the whole shell.

**Flagged assumption for SHELL-04:** the current same-node preview-mark regression is authoritative and remains unchanged except for test-suite integration.

The user explicitly selected `--skip-ui`: create no UI-SPEC, CSS, visible string, icon, geometry, interaction, or screenshot change. Preserve the unrelated dirty `docs/design-qa/*.png` files without reading, staging, or committing them.

Prohibition-probe result: zero bespoke values, privacy, fairness, transparency, or safety prohibitions remain after dropping routine render-correctness items and referring generic stale-state/access-control concerns to the threat model. Therefore no descriptor-backed prohibition is minted.
</objective>

<execution_context>
@/Users/yj.lee/Library/Application Support/orca/codex-accounts/2ae8b3be-98d2-4a96-9ebd-0dc47f78b6e5/home/gsd-core/workflows/execute-plan.md
@/Users/yj.lee/Library/Application Support/orca/codex-accounts/2ae8b3be-98d2-4a96-9ebd-0dc47f78b6e5/home/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-editor-surface-state-extraction/04-CONTEXT.md
@.planning/phases/04-editor-surface-state-extraction/04-RESEARCH.md
@.planning/phases/04-editor-surface-state-extraction/04-PATTERNS.md
@.planning/phases/04-editor-surface-state-extraction/04-VALIDATION.md
@.planning/phases/04-editor-surface-state-extraction/04-VERIFICATION.md
@.planning/phases/04-editor-surface-state-extraction/04-REVIEW.md
@.planning/phases/04-editor-surface-state-extraction/04-06-SUMMARY.md
@src/App.tsx
@src/lib/editorTabsStore.ts
@src/__tests__/editorSurfaceRenderIsolation.test.tsx
@src/components/DocumentList.tsx
@src/components/TerminalPanel.tsx
@src/components/EditorPaneFacade.tsx

<interfaces>
- `updateTabDraft(tabId, content)` synchronously replaces `editorTabsStore.tabs`; `MainApp` currently observes that array through `useDocTabs()`.
- `DocumentList` is already `memo(function DocumentList(...))`; its current MainApp call site defeats memoization with inline callback props.
- `TerminalPanel` is already `memo(forwardRef(...))`; its current memo boundary must remain intact and the regression must prove its real function does not execute on a draft-only publish.
- The activity rail currently renders inline in MainApp through ordinary `ActivityModeButton` calls; extract exactly that existing JSX into one memoized production `ActivityRail` boundary in `src/App.tsx` with stable primitive/callback props.
- `shellSurfaceRenderProbe` is instrumentation only: production behavior is a constant-time no-op, observations contain only the static target name, and tests always restore the observer.
- The test may mock IPC, startup, and unrelated heavy/lazy surfaces needed to boot MainApp, but it must not mock `DocumentList`, `TerminalPanel`, `ActivityRail`, `editorTabsStore`, or their memo boundaries.
- Existing editor/outline facades, command ports, settings keys, preview markup, lazy imports, native save/conflict code, and prop budgets are unchanged.
</interfaces>
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Protect and count the real MainApp shell surfaces on one draft-update path</name>
  <files>src/App.tsx, src/components/DocumentList.tsx, src/components/TerminalPanel.tsx, src/lib/shellSurfaceRenderProbe.ts, src/__tests__/editorSurfaceRenderIsolation.test.tsx</files>
  <read_first>
    - .planning/phases/04-editor-surface-state-extraction/04-VERIFICATION.md (the sole failed truth, exact artifacts, and missing closure)
    - .planning/phases/04-editor-surface-state-extraction/04-CONTEXT.md (D-01 through D-16, especially D-13 and the Phase 5 boundary)
    - .planning/phases/04-editor-surface-state-extraction/04-06-SUMMARY.md (implemented state, native evidence, and preserved invariants)
    - src/App.tsx (MainApp useDocTabs subscription, ActivityModeButton/activity-rail JSX, DocumentList call site, TerminalPanel call site, and existing stable-props section)
    - src/lib/editorTabsStore.ts (replaceAllDocTabs, updateTabDraft, useDocTabs, active/group hooks, and current-snapshot getters)
    - src/__tests__/editorSurfaceRenderIsolation.test.tsx (synthetic ShellProbe test to replace and the changed-domain subscriber test to retain)
    - src/components/DocumentList.tsx (existing React.memo boundary and complete props contract)
    - src/components/TerminalPanel.tsx (existing memo(forwardRef) boundary and open=false behavior)
    - src/components/EditorPaneFacade.tsx (post-commit production boundary and no-render-phase-publish rule)
  </read_first>
  <behavior>
    - RED: mount the exported production MainApp boundary with real DocumentList, TerminalPanel, ActivityRail, and editorTabsStore; initialize one real active document tab, settle boot effects, then call updateTabDraft with changed content.
    - RED proof: MainApp's counter increases from the real useDocTabs invalidation while at least DocumentList and ActivityRail also increase before the fix, demonstrating the test reaches the failing production path rather than inert siblings.
    - GREEN: after the fix, MainApp may increase, but DocumentList, TerminalPanel, and ActivityRail counters remain exactly unchanged for the draft-only publish.
    - The production DOM text, class names, icon set, order, aria labels, click behavior, terminal behavior, and editor state are byte/behavior identical.
  </behavior>
  <action>
    First replace the first synthetic ShellProbe case with a fail-first MainApp regression. Export `MainApp` for direct test mounting. Add `src/lib/shellSurfaceRenderProbe.ts` with a closed `ShellSurfaceRenderTarget` union for `MainApp`, `DocumentList`, `TerminalPanel`, and `ActivityRail`; expose `recordShellSurfaceRender` plus a test observer installer that returns/restores the prior observer. The default path must do nothing and must never retain props, paths, document content, workspace identifiers, or callbacks.

    Invoke `recordShellSurfaceRender` at the function entry of the actual MainApp, the existing memoized DocumentList implementation, the existing memoized TerminalPanel implementation, and the new production ActivityRail component. The test must use those exact implementations and reset both the observer and editor tab state during cleanup.

    Extract the current activity-rail JSX from MainApp into `ActivityRail`, defined in `src/App.tsx` and wrapped in `memo`. Move translation lookup inside the component and pass only the current mode/layout booleans plus stable command callbacks. Hoist the PKM-open and outline-toggle closures into `useCallback`; reuse the already-stable openPrimaryWorkbenchMode, openWorkbenchModeRight, openCommandPalette, and updateLayoutSettings paths. Do not change ActivityModeButton markup, iteration order, labels, icons, feature flags, or right-open behavior.

    Hoist every inline function prop at the MainApp `DocumentList` call site into a named `useCallback` in the existing stable-props section: workspace visibility, add-public-workspace, reveal-in-Files, ignore, refresh, close, reveal-handled, file-queue destination, and explorer-drag destination. Give each callback the complete dependency list so it reads the current workspace/capability/orchestration state and cannot capture a stale write target. Leave `DocumentList` memoization and every prop value unchanged. Do not change TerminalPanel behavior; its existing memoized boundary and already-stable props are the implementation under test.

    Drive RED then GREEN with a real `replaceAllDocTabs`/`updateTabDraft` transition. Mock only unrelated APIs and heavy/lazy surfaces required for deterministic MainApp boot. Do not replace the three observed surfaces with test doubles. Preserve the second existing per-domain facade subscriber test.
  </action>
  <verify>
    <automated>pnpm exec vitest run src/__tests__/editorSurfaceRenderIsolation.test.tsx &amp;&amp; pnpm typecheck &amp;&amp; pnpm exec eslint src/App.tsx src/components/DocumentList.tsx src/components/TerminalPanel.tsx src/lib/shellSurfaceRenderProbe.ts src/__tests__/editorSurfaceRenderIsolation.test.tsx --max-warnings 0</automated>
  </verify>
  <acceptance_criteria>
    - The fail-first evidence records a real MainApp draft invalidation and at least one actual named shell-surface over-render before implementation.
    - The green test calls real updateTabDraft on a real tab and observes the actual production MainApp, DocumentList, TerminalPanel, and ActivityRail functions.
    - MainApp may re-execute, but all three named surface counters remain byte-for-byte equal to their pre-update counts.
    - No `ShellProbe` or other inert replacement for the named production surfaces remains in the isolation test.
    - Activity rail markup/order/labels/icons/actions are unchanged; DocumentList callbacks remain current-snapshot safe; TerminalPanel behavior is untouched.
    - The observer records static target names only and is restored after the test.
  </acceptance_criteria>
  <done>One production-quality updateTabDraft path now crosses the real MainApp subscription while all three named shell surfaces remain isolated behind production memo boundaries, with a fail-first render-counter proof.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Expand the real-shell regression to both editor groups and re-run the Phase 4 evidence ladder</name>
  <files>src/__tests__/editorSurfaceRenderIsolation.test.tsx</files>
  <read_first>
    - src/__tests__/editorSurfaceRenderIsolation.test.tsx (Task 1 production-boundary harness and retained slice-domain test)
    - src/lib/editorTabsStore.ts (left/right active IDs, focused group, dirty transition, and synchronous draft publication)
    - src/lib/editorPaneStore.ts (canonical draft composition and keyed group/tab scope)
    - src/components/EditorPane.test.tsx (SHELL-04 same-node preview regression)
    - src/lib/outlinePaneStore.test.ts (SHELL-01 prop and facade contracts)
    - src/lib/editorSurfaceStore.test.ts (SHELL-02 prop, ownership, split, persistence, and cleanup contracts)
    - scripts/check-bundle-budget.mjs (entry CSS/JS budgets and lazy-chunk assertions)
    - .planning/phases/04-editor-surface-state-extraction/04-VERIFICATION.md (fresh verifier must close the exact blocker rather than infer from old evidence)
  </read_first>
  <behavior>
    - A clean-to-dirty left-tab update keeps tab dirty state and editor facade content fresh while the three unrelated production surface counters remain unchanged.
    - A subsequent left-tab draft update and an independent right-tab draft update each preserve the opposite group's content/scope and leave the three shell-surface counters unchanged.
    - Existing Outline prop/facade tests, Editor prop/split/persistence tests, preview exact-node identity, browser flows, and bundle/lazy-chunk constraints remain green.
  </behavior>
  <action>
    Extend the Task 1 MainApp regression to initialize two real document tabs with distinct `leftActiveTabId` and `rightActiveTabId`. Exercise a clean-to-dirty left edit, a second left edit, and an independent right edit through `updateTabDraft`. After each publish, assert the three actual production surface counters are unchanged; assert `getEditorTabsState()` and the keyed Editor facade expose the correct current draft/dirty/group values so isolation does not achieve a false pass by freezing tab badges, active documents, split identity, or persistence inputs.

    Keep the existing changed-render-domain subscriber case and convert any shared setup into explicit test helpers only when those helpers still mount the production MainApp path. Add no profile harness, benchmark framework, dependency, UI state library, Context provider, new settings key, CSS, visible copy, or Phase 5 DocumentList/TerminalPanel store extraction.

    Run the focused Phase 4 suite, `make verify`, the full Playwright suite, the bundle/lazy-chunk budget, and `git diff --check`. Confirm OutlinePaneProps and EditorPaneProps remain at four and no lazy mode import moved into the entry chunk. Preserve all unrelated `docs/design-qa/*.png` modifications. Do not hand-edit `04-VERIFICATION.md`; after execution, require a fresh gsd-verifier pass to replace `gaps_found` with `passed` based on this real production-boundary evidence.
  </action>
  <verify>
    <automated>pnpm exec vitest run src/lib/outlinePaneStore.test.ts src/lib/editorSurfaceStore.test.ts src/__tests__/editorSurfaceRenderIsolation.test.tsx src/components/EditorPane.test.tsx src/components/EditorPaneFacade.test.tsx &amp;&amp; make verify &amp;&amp; pnpm test:e2e &amp;&amp; pnpm check:bundle-budget &amp;&amp; git diff --check</automated>
  </verify>
  <acceptance_criteria>
    - Real clean-to-dirty, repeated-left, and independent-right draft updates all preserve current tab/facade state and leave DocumentList, TerminalPanel, and ActivityRail counters unchanged.
    - SHELL-01 and SHELL-02 facade/ownership/prop-budget tests pass; both target panes remain at four props.
    - SHELL-04 preview mark classes and exact marked-node identity test passes unchanged.
    - `make verify`, all Playwright E2E tests, bundle budgets/lazy-chunk checks, and `git diff --check` pass.
    - No CSS, visible string, settings schema, dependency manifest, Tauri/Rust source, preview DOM ownership, or native save/conflict path changes.
    - A fresh Phase 4 verification run cites the production MainApp-level counter and reports all four SHELL requirements satisfied.
    - None of the unrelated `docs/design-qa/*.png` files is included in this plan's diff or commit.
  </acceptance_criteria>
  <done>The real-shell regression is symmetric across left/right draft updates, all prior Phase 4 invariants remain green, and the authoritative verifier has direct evidence to close SHELL-03.</done>
</task>

</tasks>

## Artifacts this phase produces

- New `src/lib/shellSurfaceRenderProbe.ts`: `ShellSurfaceRenderTarget`, `recordShellSurfaceRender`, and `setShellSurfaceRenderObserverForTest`; no-op by default and static-target-only.
- Modified `src/App.tsx`: exported `MainApp`, memoized `ActivityRail`, stable PKM/outline/activity command props, and stable named DocumentList callbacks.
- Modified `src/components/DocumentList.tsx`: actual production render-observer call inside its existing memoized component; no prop or visual change.
- Modified `src/components/TerminalPanel.tsx`: actual production render-observer call inside its existing memoized forwardRef component; no terminal behavior change.
- Modified `src/__tests__/editorSurfaceRenderIsolation.test.tsx`: production MainApp harness using real editorTabsStore updates and the actual three named surfaces; synthetic ShellProbe path removed.
- Execution artifact `04-07-SUMMARY.md`: RED/GREEN counter evidence, focused/full/E2E/bundle/diff results, and preservation notes.
- No UI-SPEC, CSS, image, dependency, settings key, Tauri/Rust file, visible string, backend command, or external API artifact.

## Source coverage audit

| Source | ID | Requirement / constraint | Plan | Status | Notes |
|--------|----|--------------------------|------|--------|-------|
| GOAL | - | Highest-arity panes own state and editing no longer re-renders the whole shell | 04-07 | COVERED | Real MainApp publish with actual unrelated production surfaces protected. |
| REQ | SHELL-01 | OutlinePane module-store boundary | 04-07 | COVERED | Re-run existing four-prop/facade contracts; no implementation reopening. |
| REQ | SHELL-02 | EditorPane module-store boundary | 04-07 | COVERED | Re-run ownership/split/persistence/prop contracts and assert fresh draft state. |
| REQ | SHELL-03 | Editor typing does not re-render unrelated panes | 04-07 | COVERED | Sole implementation target and real production render-counter proof. |
| REQ | SHELL-04 | Preview-mark component regression | 04-07 | COVERED | Existing exact-node test is in the focused and full gates. |
| RESEARCH | stable slices | Snapshot and memo identity, no second owner | 04-07 | COVERED | Actual store publish plus memo-boundary counters; no new state owner. |
| RESEARCH | split scope | Left/right independence | 04-07 | COVERED | Task 2 exercises both real groups and current facade data. |
| RESEARCH | persistence/cleanup | Exact current settings and lifecycle | 04-07 | COVERED | Existing tests re-run; no new key or lifecycle path. |
| RESEARCH | preview/lazy output | React-owned preview and lazy bundle constraints | 04-07 | COVERED | Existing exact-node test plus explicit budget check. |
| CONTEXT | D-01 | Pane-specific facade stores | 04-07 | COVERED | Preserved and re-verified. |
| CONTEXT | D-02 | Compose canonical owners without dual write | 04-07 | COVERED | Real editorTabsStore remains the sole draft owner. |
| CONTEXT | D-03 | Workspace/group/tab keys | 04-07 | COVERED | Two-group regression asserts keyed current state. |
| CONTEXT | D-04 | Render-domain granularity and stable identity | 04-07 | COVERED | Actual surface counters and retained slice test. |
| CONTEXT | D-05 | Pure facade actions vs orchestration | 04-07 | COVERED | No command ownership change; callbacks route existing orchestration. |
| CONTEXT | D-06 | Least-authority pane ports | 04-07 | COVERED | Existing port tests re-run; activity/document callbacks stay narrow. |
| CONTEXT | D-07 | Stable current-snapshot command adapters | 04-07 | COVERED | Stable callbacks use complete dependencies/current orchestration. |
| CONTEXT | D-08 | Operation/error ownership | 04-07 | COVERED | No operation or error path changes. |
| CONTEXT | D-09 | Dedicated existing-settings adapter | 04-07 | COVERED | Existing adapter tests re-run; no App persistence props added. |
| CONTEXT | D-10 | Exact persistence membership | 04-07 | COVERED | No settings key or transient persistence change. |
| CONTEXT | D-11 | Atomic guarded hydration | 04-07 | COVERED | Existing stale-hydration test re-run. |
| CONTEXT | D-12 | Explicit cleanup and canonical draft ownership | 04-07 | COVERED | Existing cleanup tests re-run; test resets only its fixture state. |
| CONTEXT | D-13 | Left/right real render counters | 04-07 | COVERED | Synthetic probes replaced by actual MainApp boundary and surfaces. |
| CONTEXT | D-14 | Preview class and node identity | 04-07 | COVERED | Existing direct EditorPane test remains required. |
| CONTEXT | D-15 | Per-plan gates plus one native smoke | 04-07 | COVERED | Full gates re-run; previously completed native smoke is not repeated. |
| CONTEXT | D-16 | At most eight props per target pane | 04-07 | COVERED | Both existing four-prop AST contracts re-run. |

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| editorTabsStore draft publish -> MainApp memo boundaries | User-controlled document text invalidates canonical tab state; unrelated shell surfaces must not execute or observe document content. |
| MainApp stable callbacks -> workspace/file orchestration | Memoization must not freeze a stale workspace, capability, or destination inside a callback that can perform file actions. |
| test render observer -> production components | Instrumentation crosses into actual components but must be inert by default and expose only static surface identifiers. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-16 | Information Disclosure | `shellSurfaceRenderProbe` | medium | mitigate | Record only the closed static target union, install no default observer, restore test observers, and forbid props/content/path/workspace capture. |
| T-04-17 | Tampering / Elevation of Privilege | stable DocumentList callbacks in `MainApp` | high | mitigate | Use complete hook dependencies and existing capability-checked orchestration paths; typecheck, lint, unit, and E2E gates exercise the live callbacks without replacing authorization. |
| T-04-18 | Denial of Service | draft publish -> shell surfaces | medium | mitigate | Real updateTabDraft counter proves DocumentList, TerminalPanel, and ActivityRail do not execute for unchanged inputs across both editor groups. |
| T-04-19 | Tampering | editor/preview state | high | mitigate | Keep editorTabsStore as the only draft owner and re-run split/persistence/preview exact-node regressions; introduce no imperative preview DOM write. |
| T-04-SC | Tampering | package supply chain | low | accept | No package install or dependency-manifest change occurs. |
</threat_model>

<verification>
- Focused MainApp/store/component tests prove the real updateTabDraft path, both editor groups, existing prop budgets, changed-domain-only publication, and preview exact-node identity.
- `make verify` passes typecheck, lint, frontend/Rust tests, formatting, clippy, build, and bundled budget checks.
- `pnpm test:e2e` passes the full browser suite, and `pnpm check:bundle-budget` confirms entry JS/CSS budgets plus GraphView, RichMarkdownEditor, and dictionary lazy chunks.
- `git diff --check` passes and the intentional diff contains no CSS, visible copy, images, settings schema, dependencies, or Tauri/Rust source.
- The earlier D-15 native WKWebView smoke remains valid because this plan changes only React memo boundaries and test-only counters; no native/save/conflict path changes, so do not repeat it.
- A fresh gsd-verifier run re-reads `04-VERIFICATION.md`, cites the production MainApp-level regression, and changes Phase 4 from `gaps_found` to `passed` only if all four truths are satisfied.
</verification>

<success_criteria>
SHELL-03 is closed by a fail-first test of the actual production shell path: real left/right draft updates keep current tab/editor state fresh while DocumentList, TerminalPanel, and ActivityRail do not re-render. SHELL-01, SHELL-02, and SHELL-04 remain verified, the complete repository/E2E/bundle/diff ladder is green, output is pixel-identical, and the authoritative Phase 4 verifier reports all four requirements satisfied.
</success_criteria>

<output>
Create `.planning/phases/04-editor-surface-state-extraction/04-07-SUMMARY.md` when done.
</output>
