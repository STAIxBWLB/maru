{
  "plan": "08-21",
  "moduleIntegrationOwner": "08-21",
  "commands": [
    {
      "name": "export_plan",
      "module": "src-tauri/src/export/mod.rs",
      "registrationPath": "src-tauri/src/lib.rs::export::ipc::export_plan",
      "originalDisposition": "AUDIT",
      "disposition": "ISOLATED",
      "helperChain": [
        "src-tauri/src/export/mod.rs::ipc::export_plan owns the ExportPlanRequest and awaits tauri::async_runtime::spawn_blocking calling src-tauri/src/export/mod.rs::export_plan inside the worker; a JoinError becomes the display-only export_plan_task_failed String.",
        "src-tauri/src/export/mod.rs::export_plan mirrors the destination resolution with src-tauri/src/export/mod.rs::plan_bundle_paths (bundle directory + manifest.yaml, absolute-lexical) so the complete write set is admitted up front, then enters src-tauri/src/atomic_file.rs::with_path_transactions via src-tauri/src/atomic_file.rs::PathTransactionRequest with require_parent(workspace root) and with_workspace_registry, and calls src-tauri/src/export/manifest.rs::plan_bundle only inside the lease after ensure_workspace_registry, ensure_covered and before_effect; drift between the mirror and the real planner fails closed through the per-path ensure_covered re-check."
      ],
      "workerBoundary": "src-tauri/src/export/mod.rs::ipc::export_plan awaits tauri::async_runtime::spawn_blocking before any filesystem access or lock wait runs; only a JoinError maps to the display-only export_plan_task_failed String while the ExportPlanResponse payload and every domain error string pass through unchanged; owned request structs cross the await, never a borrowed State or guard.",
      "syncCallers": [
        "No Rust or CLI caller invokes export_plan synchronously today; src-tauri/src/lib.rs registers only export::ipc::export_plan, and the original synchronous src-tauri/src/export/mod.rs::export_plan keeps its name and signature for Rust callers (the phase08_21 tests exercise it directly)."
      ],
      "mutationKey": {
        "readOnly": false,
        "lexicalPaths": [
          "export bundle directory (default <source-stem>.exports/ beside the source, or the output_dir override)",
          "manifest.yaml inside the admitted bundle directory",
          "pinned workspace-root parent required before the transaction opens"
        ],
        "aliasPaths": [
          "workspace-relative source_path and output_dir resolve through absolute_lexical to the same lexical bundle paths admitted up front"
        ],
        "sharedAdmissionEntry": "src-tauri/src/export/mod.rs::export_plan admits the whole write set before any effect: src-tauri/src/atomic_file.rs::with_path_transactions opens the transaction after src-tauri/src/atomic_file.rs::PathTransactionRequest require_parent pins the workspace-root parent and with_workspace_registry attaches the registry precondition, so no manifest write escapes the admitted set.",
        "postAdmissionPreconditions": [
          "lease.ensure_workspace_registry runs before any effect",
          "lease.ensure_covered over bundle dir + manifest.yaml runs before lease.before_effect",
          "the planned path returned by src-tauri/src/export/manifest.rs::plan_bundle is re-checked with lease.ensure_covered so mirror drift fails closed"
        ],
        "domainLockOrder": "Filesystem path admission (with_path_transactions acquire) happens in src-tauri/src/export/mod.rs::export_plan before any domain lock or manifest write; the planner runs only inside the admitted transaction."
      },
      "tests": [
        {
          "command": "cargo test --manifest-path src-tauri/Cargo.toml --lib phase08_21",
          "passed": 16,
          "failed": 0,
          "ignored": 0,
          "exitCode": 0
        }
      ],
      "processingCaller": {
        "path": [
          "src/lib/export.ts",
          "src/lib/export.test.ts"
        ],
        "payload": "planExport (src/lib/export.ts:41) invokes export_plan with workspaceRoot, sourcePath, formats and the optional outputDir whenever the export dialog plans a bundle; the export dialog keeps owning that flow regardless of screen changes.",
        "terminalNoticeOwner": "The export dialog surface owns current export state display and error toasts. Navigation-safe terminal notices and fulfilled-payload classifiers remain the explicit Plan25/26 frontend handoff; this shard claims backend isolation only."
      },
      "evidence": {
        "behaviorCases": [
          "phase08_21_export_plan_validate_yield_same_poll_and_map_join_failure",
          "phase08_21_export_plan_validate_real_fixture_and_legacy_rejections",
          "phase08_21_export_plan_error_releases_admission"
        ],
        "workerProof": "export::phase08_21::phase08_21_export_plan_validate_yield_same_poll_and_map_join_failure holds the export_plan future at spawn_blocking worker entry via a #[cfg(test)] worker-stage hook keyed to the fixture workspace root, manually polls it on the same async task and proves progress while a distinct worker thread stays blocked; the panicked worker maps to the display-only export_plan_task_failed String.",
        "platformScope": "macOS unit fixtures only: atomic_file::phase08_06::Home isolation, disposable tempdir workspaces, a fake hwp/hwpx shell binary and the test-only worker-stage hook; no live workspace, credential, network endpoint or native test hook is touched.",
        "limits": "Shared admission serializes cooperating in-process writers only; external editor or converter processes are outside the in-process guarantee. PERF-01/PERF-02 remain globally outstanding for the whole phase.",
        "contract": "The ExportPlanResponse payload, the manifest.yaml plan layout and the missing-workspace / missing-source error strings are unchanged; the only new failure path is the JoinError-to-String mapping at the worker boundary plus fail-closed admission rejections for out-of-set writes."
      }
    },
    {
      "name": "export_validate",
      "module": "src-tauri/src/export/mod.rs",
      "registrationPath": "src-tauri/src/lib.rs::export::ipc::export_validate",
      "originalDisposition": "AUDIT",
      "disposition": "ISOLATED",
      "helperChain": [
        "src-tauri/src/export/mod.rs::ipc::export_validate owns the manifest_path String and awaits tauri::async_runtime::spawn_blocking calling src-tauri/src/export/mod.rs::export_validate inside the worker; a JoinError becomes the display-only export_validate_task_failed String.",
        "src-tauri/src/export/mod.rs::export_validate only reads: it delegates to src-tauri/src/export/validate.rs::validate_manifest, which performs a bounded manifest parse and per-output existence/stat checks, so the converted chain reaches no network call, process wait or unbounded walk."
      ],
      "workerBoundary": "src-tauri/src/export/mod.rs::ipc::export_validate awaits tauri::async_runtime::spawn_blocking before any filesystem access runs; only a JoinError maps to the display-only export_validate_task_failed String while the ValidationReport payload and every domain error string pass through unchanged; owned Strings cross the await, never a borrowed State or guard.",
      "syncCallers": [
        "No Rust or CLI caller invokes export_validate synchronously today; src-tauri/src/lib.rs registers only export::ipc::export_validate, and the original synchronous src-tauri/src/export/mod.rs::export_validate keeps its name and signature for Rust callers (the phase08_21 tests exercise it directly)."
      ],
      "mutationKey": {
        "readOnly": true,
        "lexicalPaths": [],
        "aliasPaths": [],
        "sharedAdmissionEntry": "export_validate performs no filesystem mutation: src-tauri/src/export/validate.rs::validate_manifest only parses manifest.yaml and stats the listed outputs, so no filesystem mutation admission is required for this read-only audit command.",
        "postAdmissionPreconditions": [
          "No mutation admission is required: the command performs no filesystem mutation, so there is no transaction key to admit and nothing to release"
        ],
        "domainLockOrder": "No path admission exists; the command takes no domain lock, so there is no admission-before-domain-lock order to record."
      },
      "tests": [
        {
          "command": "cargo test --manifest-path src-tauri/Cargo.toml --lib phase08_21",
          "passed": 16,
          "failed": 0,
          "ignored": 0,
          "exitCode": 0
        }
      ],
      "processingCaller": {
        "path": [
          "src/lib/export.ts",
          "src/lib/export.test.ts"
        ],
        "payload": "validateExport (src/lib/export.ts:73) invokes export_validate with the manifest path after planning or dispatching a bundle; the export dialog keeps owning that refresh regardless of screen changes.",
        "terminalNoticeOwner": "The export dialog surface owns current export state display and error toasts. Navigation-safe terminal notices and fulfilled-payload classifiers remain the explicit Plan25/26 frontend handoff; this shard claims backend isolation only."
      },
      "evidence": {
        "behaviorCases": [
          "phase08_21_export_plan_validate_yield_same_poll_and_map_join_failure",
          "phase08_21_export_plan_validate_real_fixture_and_legacy_rejections"
        ],
        "workerProof": "export::phase08_21::phase08_21_export_plan_validate_yield_same_poll_and_map_join_failure holds the export_validate future at spawn_blocking worker entry via a #[cfg(test)] worker-stage hook keyed to the fixture manifest path, manually polls it on the same async task and proves progress while a distinct worker thread stays blocked; the panicked worker maps to the display-only export_validate_task_failed String.",
        "platformScope": "macOS unit fixtures only: atomic_file::phase08_06::Home isolation, disposable tempdir workspaces, a fake hwp/hwpx shell binary and the test-only worker-stage hook; no live workspace, credential, network endpoint or native test hook is touched.",
        "limits": "Shared admission serializes cooperating in-process writers only; external editor or converter processes are outside the in-process guarantee. PERF-01/PERF-02 remain globally outstanding for the whole phase.",
        "contract": "The ValidationReport payload, the pass/fail per-output checks and the malformed-manifest error strings are unchanged; the only new failure path is the JoinError-to-String mapping at the worker boundary."
      }
    },
    {
      "name": "export_dispatch",
      "module": "src-tauri/src/export/dispatch.rs",
      "registrationPath": "src-tauri/src/lib.rs::export::dispatch::ipc::export_dispatch",
      "originalDisposition": "CONVERT",
      "disposition": "ISOLATED",
      "helperChain": [
        "src-tauri/src/export/dispatch.rs::ipc::export_dispatch owns the ExportDispatchRequest and awaits tauri::async_runtime::spawn_blocking calling src-tauri/src/export/dispatch.rs::export_dispatch inside the worker; a JoinError becomes the display-only export_dispatch_task_failed String.",
        "src-tauri/src/export/dispatch.rs::export_dispatch delegates to src-tauri/src/export/dispatch.rs::dispatch_bundle, whose DispatchPlan::load precomputes the admitted set (manifest.yaml plus every selected output path via src-tauri/src/export/dispatch.rs::output_path_for, absolute-lexical) before src-tauri/src/atomic_file.rs::with_path_transactions opens the transaction; src-tauri/src/export/dispatch.rs::dispatch_bundle_in_transaction then re-loads the manifest, converts each format and records per-output status, re-checking each output with the lease before writing.",
        "Converter process spawns stay inside the admitted transaction: convert_docx/convert_hwpx/convert_pdf run only after lease.before_effect, and src-tauri/src/export/validate.rs::validate_manifest re-validates the final manifest before the response is built."
      ],
      "workerBoundary": "src-tauri/src/export/dispatch.rs::ipc::export_dispatch awaits tauri::async_runtime::spawn_blocking before any filesystem access or lock wait runs; only a JoinError maps to the display-only export_dispatch_task_failed String while the ExportDispatchResponse payload and every domain error string pass through unchanged; owned request structs cross the await, never a borrowed State or guard.",
      "syncCallers": [
        "No Rust or CLI caller invokes export_dispatch synchronously today; src-tauri/src/lib.rs registers only export::dispatch::ipc::export_dispatch, and the original synchronous src-tauri/src/export/dispatch.rs::export_dispatch keeps its name and signature for Rust callers (the inline legacy tests exercise it directly)."
      ],
      "mutationKey": {
        "readOnly": false,
        "lexicalPaths": [
          "manifest.yaml of the export bundle",
          "every selected format output file under the bundle directory",
          "pinned workspace-root parent required before the transaction opens"
        ],
        "aliasPaths": [
          "manifest_path passed absolute or workspace-relative resolves to the same lexical file admitted up front",
          "requested_formats select the same output_path_for destinations admitted up front"
        ],
        "sharedAdmissionEntry": "src-tauri/src/export/dispatch.rs::dispatch_bundle admits manifest.yaml plus every selected output path (absolute-lexical via DispatchPlan::load) before any effect through src-tauri/src/atomic_file.rs::with_path_transactions over src-tauri/src/atomic_file.rs::PathTransactionRequest with require_parent(workspace root) and with_workspace_registry, so no converter or manifest write escapes the admitted set.",
        "postAdmissionPreconditions": [
          "lease.ensure_workspace_registry runs before any effect",
          "lease.ensure_covered over manifest + all selected outputs runs before lease.before_effect",
          "each output is re-checked with lease.ensure_covered immediately before its status record and converter run",
          "source-changed preflight records failures through the same admitted manifest only"
        ],
        "domainLockOrder": "Filesystem path admission (with_path_transactions acquire) happens in src-tauri/src/export/dispatch.rs::dispatch_bundle before any domain lock or converter process spawn; converter processes run only inside the admitted transaction."
      },
      "tests": [
        {
          "command": "cargo test --manifest-path src-tauri/Cargo.toml --lib phase08_21",
          "passed": 16,
          "failed": 0,
          "ignored": 0,
          "exitCode": 0
        }
      ],
      "processingCaller": {
        "path": [
          "src/lib/export.ts",
          "src/lib/export.test.ts"
        ],
        "payload": "dispatchExport (src/lib/export.ts:96) invokes export_dispatch with workspaceRoot, manifestPath and formats after the user confirms an export plan; the export dialog keeps owning that flow regardless of screen changes.",
        "terminalNoticeOwner": "The export dialog surface owns current export state display and error toasts. Navigation-safe terminal notices and fulfilled-payload classifiers remain the explicit Plan25/26 frontend handoff; this shard claims backend isolation only."
      },
      "evidence": {
        "behaviorCases": [
          "phase08_21_export_dispatch_yields_same_poll_and_maps_join_failure",
          "phase08_21_export_dispatch_real_fixture_and_legacy_rejections",
          "phase08_21_export_plan_dispatch_serialize_manifest_both_orders"
        ],
        "workerProof": "export::dispatch::phase08_21::phase08_21_export_dispatch_yields_same_poll_and_maps_join_failure holds the export_dispatch future at spawn_blocking worker entry via a #[cfg(test)] worker-stage hook keyed to the fixture workspace root, manually polls it on the same async task and proves progress while a distinct worker thread stays blocked; the panicked worker maps to the display-only export_dispatch_task_failed String.",
        "platformScope": "macOS unit fixtures only: atomic_file::phase08_06::Home isolation, disposable tempdir workspaces, a fake hwp/hwpx shell binary and the test-only worker-stage hook; no live workspace, credential, network endpoint or native test hook is touched.",
        "limits": "Shared admission serializes cooperating in-process writers only; external editor or converter processes are outside the in-process guarantee. PERF-01/PERF-02 remain globally outstanding for the whole phase.",
        "contract": "The ExportDispatchResponse payload, the per-format success/failure results and the source-changed re-plan behavior are unchanged; the only new failure paths are the JoinError-to-String mapping at the worker boundary plus fail-closed admission rejections for out-of-set writes."
      }
    },
    {
      "name": "hwped_read",
      "module": "src-tauri/src/hwped.rs",
      "registrationPath": "src-tauri/src/lib.rs::hwped::hwped_read",
      "originalDisposition": "ISOLATED",
      "disposition": "ISOLATED",
      "helperChain": [
        "src-tauri/src/hwped.rs::hwped_read is an already-isolated async entry: it owns its inputs and awaits tauri::async_runtime::spawn_blocking calling src-tauri/src/hwped.rs::read_now inside the worker; a JoinError becomes the display-only hwped_task_failed String.",
        "src-tauri/src/hwped.rs::read_now reads a document (path, base64 data or temp stage) and returns its parsed structure; temp staging goes through src-tauri/src/hwped.rs::stage_document (lexical-containment checked against the optional workspace root), then a bounded hwp-cli process wait with the fixed 60s HWP_TIMEOUT, so the chain reaches no network call or unbounded walk."
      ],
      "workerBoundary": "src-tauri/src/hwped.rs::hwped_read awaits tauri::async_runtime::spawn_blocking before any filesystem access or process wait runs; only a JoinError maps to the display-only hwped_task_failed String while the HwpedCatEnvelope payload and every domain error string pass through unchanged; owned structs cross the await, never a borrowed State or guard.",
      "syncCallers": [
        "No Rust or CLI caller invokes hwped_read synchronously today; src-tauri/src/lib.rs registers only hwped::hwped_read, and the private src-tauri/src/hwped.rs::read_now worker body stays behind the spawn_blocking boundary."
      ],
      "mutationKey": {
        "readOnly": true,
        "lexicalPaths": [],
        "aliasPaths": [],
        "sharedAdmissionEntry": "hwped_read performs no filesystem mutation: src-tauri/src/hwped.rs::read_now only reads the staged input and spawns the bounded hwp-cli probe, so no filesystem mutation admission is required.",
        "postAdmissionPreconditions": [
          "No mutation admission is required: the command performs no filesystem mutation, so there is no transaction key to admit and nothing to release"
        ],
        "domainLockOrder": "No path admission exists; the command takes no domain lock, so there is no admission-before-domain-lock order to record."
      },
      "tests": [
        {
          "command": "cargo test --manifest-path src-tauri/Cargo.toml --lib phase08_21",
          "passed": 16,
          "failed": 0,
          "ignored": 0,
          "exitCode": 0
        }
      ],
      "processingCaller": {
        "path": [
          "src/lib/hwped.ts"
        ],
        "payload": "src/lib/hwped.ts:84 invokes hwped_read from the hwped editor surface whenever it reads a document (path, base64 data or temp stage) and returns its parsed structure; the hwped surface keeps owning that call regardless of screen changes.",
        "terminalNoticeOwner": "The hwped editor surface owns current document state display and error toasts. Navigation-safe terminal notices and fulfilled-payload classifiers remain the explicit Plan25/26 frontend handoff; this shard claims backend isolation only."
      },
      "evidence": {
        "behaviorCases": [
          "phase08_21_hwped_each_wrapper_yields_same_poll_and_maps_join_failure",
          "phase08_21_hwped_real_fixture_results_and_legacy_rejections"
        ],
        "workerProof": "hwped::phase08_21::phase08_21_hwped_each_wrapper_yields_same_poll_and_maps_join_failure holds the hwped_read future at spawn_blocking worker entry via a #[cfg(test)] worker-stage hook keyed to the process tempdir, manually polls it on the same async task and proves progress while a distinct worker thread stays blocked; the panicked worker maps to the display-only hwped_task_failed String.",
        "platformScope": "macOS unit fixtures only: atomic_file::phase08_06::Home isolation, disposable tempdir workspaces, a fake hwp/hwpx shell binary and the test-only worker-stage hook; no live workspace, credential, network endpoint or native test hook is touched.",
        "limits": "Shared admission serializes cooperating in-process writers only; external editor or converter processes are outside the in-process guarantee. PERF-01/PERF-02 remain globally outstanding for the whole phase.",
        "contract": "The HwpedCatEnvelope payload and the legacy hwp-cli version/argument rejections are unchanged; the only new failure path is the shared JoinError-to-String mapping at the worker boundary, asserted with the hwped-specific prefix."
      }
    },
    {
      "name": "hwped_render",
      "module": "src-tauri/src/hwped.rs",
      "registrationPath": "src-tauri/src/lib.rs::hwped::hwped_render",
      "originalDisposition": "ISOLATED",
      "disposition": "ISOLATED",
      "helperChain": [
        "src-tauri/src/hwped.rs::hwped_render is an already-isolated async entry: it owns its inputs and awaits tauri::async_runtime::spawn_blocking calling src-tauri/src/hwped.rs::render_now inside the worker; a JoinError becomes the display-only hwped_task_failed String.",
        "src-tauri/src/hwped.rs::render_now renders a document to SVG pages plus a render report; temp staging goes through src-tauri/src/hwped.rs::stage_document (lexical-containment checked against the optional workspace root), then a bounded hwp-cli process wait with the fixed 60s HWP_TIMEOUT, so the chain reaches no network call or unbounded walk."
      ],
      "workerBoundary": "src-tauri/src/hwped.rs::hwped_render awaits tauri::async_runtime::spawn_blocking before any filesystem access or process wait runs; only a JoinError maps to the display-only hwped_task_failed String while the HwpedRenderResponse payload and every domain error string pass through unchanged; owned structs cross the await, never a borrowed State or guard.",
      "syncCallers": [
        "No Rust or CLI caller invokes hwped_render synchronously today; src-tauri/src/lib.rs registers only hwped::hwped_render, and the private src-tauri/src/hwped.rs::render_now worker body stays behind the spawn_blocking boundary."
      ],
      "mutationKey": {
        "readOnly": true,
        "lexicalPaths": [],
        "aliasPaths": [],
        "sharedAdmissionEntry": "hwped_render performs no filesystem mutation: src-tauri/src/hwped.rs::render_now only stages inputs into a tempdir outside the vault and spawns the bounded hwp-cli probe, so no filesystem mutation admission is required; the caller-supplied output directory (defaulting to a tempdir) is the only place artifacts land.",
        "postAdmissionPreconditions": [
          "No mutation admission is required: the command performs no filesystem mutation, so there is no transaction key to admit and nothing to release"
        ],
        "domainLockOrder": "No path admission exists; the command takes no domain lock, so there is no admission-before-domain-lock order to record."
      },
      "tests": [
        {
          "command": "cargo test --manifest-path src-tauri/Cargo.toml --lib phase08_21",
          "passed": 16,
          "failed": 0,
          "ignored": 0,
          "exitCode": 0
        }
      ],
      "processingCaller": {
        "path": [
          "src/lib/hwped.ts"
        ],
        "payload": "src/lib/hwped.ts:95 invokes hwped_render from the hwped editor surface whenever it renders a document to SVG pages plus a render report; the hwped surface keeps owning that call regardless of screen changes.",
        "terminalNoticeOwner": "The hwped editor surface owns current document state display and error toasts. Navigation-safe terminal notices and fulfilled-payload classifiers remain the explicit Plan25/26 frontend handoff; this shard claims backend isolation only."
      },
      "evidence": {
        "behaviorCases": [
          "phase08_21_hwped_each_wrapper_yields_same_poll_and_maps_join_failure",
          "phase08_21_hwped_real_fixture_results_and_legacy_rejections"
        ],
        "workerProof": "hwped::phase08_21::phase08_21_hwped_each_wrapper_yields_same_poll_and_maps_join_failure holds the hwped_render future at spawn_blocking worker entry via a #[cfg(test)] worker-stage hook keyed to the process tempdir, manually polls it on the same async task and proves progress while a distinct worker thread stays blocked; the panicked worker maps to the display-only hwped_task_failed String.",
        "platformScope": "macOS unit fixtures only: atomic_file::phase08_06::Home isolation, disposable tempdir workspaces, a fake hwp/hwpx shell binary and the test-only worker-stage hook; no live workspace, credential, network endpoint or native test hook is touched.",
        "limits": "Shared admission serializes cooperating in-process writers only; external editor or converter processes are outside the in-process guarantee. PERF-01/PERF-02 remain globally outstanding for the whole phase.",
        "contract": "The HwpedRenderResponse payload and the legacy hwp-cli version/argument rejections are unchanged; the only new failure path is the shared JoinError-to-String mapping at the worker boundary, asserted with the hwped-specific prefix."
      }
    },
    {
      "name": "hwped_edit",
      "module": "src-tauri/src/hwped.rs",
      "registrationPath": "src-tauri/src/lib.rs::hwped::hwped_edit",
      "originalDisposition": "ISOLATED",
      "disposition": "ISOLATED",
      "helperChain": [
        "src-tauri/src/hwped.rs::hwped_edit is an already-isolated async entry: it owns its inputs and awaits tauri::async_runtime::spawn_blocking calling src-tauri/src/hwped.rs::edit_now inside the worker; a JoinError becomes the display-only hwped_task_failed String.",
        "src-tauri/src/hwped.rs::edit_now applies argv edit operations and returns the edited bytes plus ops report; temp staging goes through src-tauri/src/hwped.rs::stage_document (lexical-containment checked against the optional workspace root), then a bounded hwp-cli process wait with the fixed 60s HWP_TIMEOUT, so the chain reaches no network call or unbounded walk."
      ],
      "workerBoundary": "src-tauri/src/hwped.rs::hwped_edit awaits tauri::async_runtime::spawn_blocking before any filesystem access or process wait runs; only a JoinError maps to the display-only hwped_task_failed String while the HwpedEditResponse payload and every domain error string pass through unchanged; owned structs cross the await, never a borrowed State or guard.",
      "syncCallers": [
        "No Rust or CLI caller invokes hwped_edit synchronously today; src-tauri/src/lib.rs registers only hwped::hwped_edit, and the private src-tauri/src/hwped.rs::edit_now worker body stays behind the spawn_blocking boundary."
      ],
      "mutationKey": {
        "readOnly": true,
        "lexicalPaths": [],
        "aliasPaths": [],
        "sharedAdmissionEntry": "hwped_edit performs no filesystem mutation: src-tauri/src/hwped.rs::edit_now only stages inputs into a tempdir outside the vault and spawns the bounded hwp-cli probe, so no filesystem mutation admission is required; the caller-supplied output directory (defaulting to a tempdir) is the only place artifacts land.",
        "postAdmissionPreconditions": [
          "No mutation admission is required: the command performs no filesystem mutation, so there is no transaction key to admit and nothing to release"
        ],
        "domainLockOrder": "No path admission exists; the command takes no domain lock, so there is no admission-before-domain-lock order to record."
      },
      "tests": [
        {
          "command": "cargo test --manifest-path src-tauri/Cargo.toml --lib phase08_21",
          "passed": 16,
          "failed": 0,
          "ignored": 0,
          "exitCode": 0
        }
      ],
      "processingCaller": {
        "path": [
          "src/lib/hwped.ts"
        ],
        "payload": "src/lib/hwped.ts:110 invokes hwped_edit from the hwped editor surface whenever it applies argv edit operations and returns the edited bytes plus ops report; the hwped surface keeps owning that call regardless of screen changes.",
        "terminalNoticeOwner": "The hwped editor surface owns current document state display and error toasts. Navigation-safe terminal notices and fulfilled-payload classifiers remain the explicit Plan25/26 frontend handoff; this shard claims backend isolation only."
      },
      "evidence": {
        "behaviorCases": [
          "phase08_21_hwped_each_wrapper_yields_same_poll_and_maps_join_failure",
          "phase08_21_hwped_real_fixture_results_and_legacy_rejections"
        ],
        "workerProof": "hwped::phase08_21::phase08_21_hwped_each_wrapper_yields_same_poll_and_maps_join_failure holds the hwped_edit future at spawn_blocking worker entry via a #[cfg(test)] worker-stage hook keyed to the process tempdir, manually polls it on the same async task and proves progress while a distinct worker thread stays blocked; the panicked worker maps to the display-only hwped_task_failed String.",
        "platformScope": "macOS unit fixtures only: atomic_file::phase08_06::Home isolation, disposable tempdir workspaces, a fake hwp/hwpx shell binary and the test-only worker-stage hook; no live workspace, credential, network endpoint or native test hook is touched.",
        "limits": "Shared admission serializes cooperating in-process writers only; external editor or converter processes are outside the in-process guarantee. PERF-01/PERF-02 remain globally outstanding for the whole phase.",
        "contract": "The HwpedEditResponse payload and the legacy hwp-cli version/argument rejections are unchanged; the only new failure path is the shared JoinError-to-String mapping at the worker boundary, asserted with the hwped-specific prefix."
      }
    },
    {
      "name": "hwped_compose",
      "module": "src-tauri/src/hwped.rs",
      "registrationPath": "src-tauri/src/lib.rs::hwped::hwped_compose",
      "originalDisposition": "ISOLATED",
      "disposition": "ISOLATED",
      "helperChain": [
        "src-tauri/src/hwped.rs::hwped_compose is an already-isolated async entry: it owns its inputs and awaits tauri::async_runtime::spawn_blocking calling src-tauri/src/hwped.rs::compose_now inside the worker; a JoinError becomes the display-only hwped_task_failed String.",
        "src-tauri/src/hwped.rs::compose_now composes a new document from a page spec and returns the bytes plus report; temp staging goes through src-tauri/src/hwped.rs::stage_document (lexical-containment checked against the optional workspace root), then a bounded hwp-cli process wait with the fixed 60s HWP_TIMEOUT, so the chain reaches no network call or unbounded walk."
      ],
      "workerBoundary": "src-tauri/src/hwped.rs::hwped_compose awaits tauri::async_runtime::spawn_blocking before any filesystem access or process wait runs; only a JoinError maps to the display-only hwped_task_failed String while the HwpedComposeResponse payload and every domain error string pass through unchanged; owned structs cross the await, never a borrowed State or guard.",
      "syncCallers": [
        "No Rust or CLI caller invokes hwped_compose synchronously today; src-tauri/src/lib.rs registers only hwped::hwped_compose, and the private src-tauri/src/hwped.rs::compose_now worker body stays behind the spawn_blocking boundary."
      ],
      "mutationKey": {
        "readOnly": true,
        "lexicalPaths": [],
        "aliasPaths": [],
        "sharedAdmissionEntry": "hwped_compose performs no filesystem mutation: src-tauri/src/hwped.rs::compose_now only stages inputs into a tempdir outside the vault and spawns the bounded hwp-cli probe, so no filesystem mutation admission is required; the caller-supplied output directory (defaulting to a tempdir) is the only place artifacts land.",
        "postAdmissionPreconditions": [
          "No mutation admission is required: the command performs no filesystem mutation, so there is no transaction key to admit and nothing to release"
        ],
        "domainLockOrder": "No path admission exists; the command takes no domain lock, so there is no admission-before-domain-lock order to record."
      },
      "tests": [
        {
          "command": "cargo test --manifest-path src-tauri/Cargo.toml --lib phase08_21",
          "passed": 16,
          "failed": 0,
          "ignored": 0,
          "exitCode": 0
        }
      ],
      "processingCaller": {
        "path": [
          "src/lib/hwped.ts"
        ],
        "payload": "src/lib/hwped.ts:123 invokes hwped_compose from the hwped editor surface whenever it composes a new document from a page spec and returns the bytes plus report; the hwped surface keeps owning that call regardless of screen changes.",
        "terminalNoticeOwner": "The hwped editor surface owns current document state display and error toasts. Navigation-safe terminal notices and fulfilled-payload classifiers remain the explicit Plan25/26 frontend handoff; this shard claims backend isolation only."
      },
      "evidence": {
        "behaviorCases": [
          "phase08_21_hwped_each_wrapper_yields_same_poll_and_maps_join_failure",
          "phase08_21_hwped_real_fixture_results_and_legacy_rejections"
        ],
        "workerProof": "hwped::phase08_21::phase08_21_hwped_each_wrapper_yields_same_poll_and_maps_join_failure holds the hwped_compose future at spawn_blocking worker entry via a #[cfg(test)] worker-stage hook keyed to the process tempdir, manually polls it on the same async task and proves progress while a distinct worker thread stays blocked; the panicked worker maps to the display-only hwped_task_failed String.",
        "platformScope": "macOS unit fixtures only: atomic_file::phase08_06::Home isolation, disposable tempdir workspaces, a fake hwp/hwpx shell binary and the test-only worker-stage hook; no live workspace, credential, network endpoint or native test hook is touched.",
        "limits": "Shared admission serializes cooperating in-process writers only; external editor or converter processes are outside the in-process guarantee. PERF-01/PERF-02 remain globally outstanding for the whole phase.",
        "contract": "The HwpedComposeResponse payload and the legacy hwp-cli version/argument rejections are unchanged; the only new failure path is the shared JoinError-to-String mapping at the worker boundary, asserted with the hwped-specific prefix."
      }
    },
    {
      "name": "hwped_validate",
      "module": "src-tauri/src/hwped.rs",
      "registrationPath": "src-tauri/src/lib.rs::hwped::hwped_validate",
      "originalDisposition": "ISOLATED",
      "disposition": "ISOLATED",
      "helperChain": [
        "src-tauri/src/hwped.rs::hwped_validate is an already-isolated async entry: it owns its inputs and awaits tauri::async_runtime::spawn_blocking calling src-tauri/src/hwped.rs::validate_now inside the worker; a JoinError becomes the display-only hwped_task_failed String.",
        "src-tauri/src/hwped.rs::validate_now validates a document and returns a per-check report; temp staging goes through src-tauri/src/hwped.rs::stage_document (lexical-containment checked against the optional workspace root), then a bounded hwp-cli process wait with the fixed 60s HWP_TIMEOUT, so the chain reaches no network call or unbounded walk."
      ],
      "workerBoundary": "src-tauri/src/hwped.rs::hwped_validate awaits tauri::async_runtime::spawn_blocking before any filesystem access or process wait runs; only a JoinError maps to the display-only hwped_task_failed String while the HwpedValidationReport payload and every domain error string pass through unchanged; owned structs cross the await, never a borrowed State or guard.",
      "syncCallers": [
        "No Rust or CLI caller invokes hwped_validate synchronously today; src-tauri/src/lib.rs registers only hwped::hwped_validate, and the private src-tauri/src/hwped.rs::validate_now worker body stays behind the spawn_blocking boundary."
      ],
      "mutationKey": {
        "readOnly": true,
        "lexicalPaths": [],
        "aliasPaths": [],
        "sharedAdmissionEntry": "hwped_validate performs no filesystem mutation: src-tauri/src/hwped.rs::validate_now only reads the staged input and spawns the bounded hwp-cli probe, so no filesystem mutation admission is required.",
        "postAdmissionPreconditions": [
          "No mutation admission is required: the command performs no filesystem mutation, so there is no transaction key to admit and nothing to release"
        ],
        "domainLockOrder": "No path admission exists; the command takes no domain lock, so there is no admission-before-domain-lock order to record."
      },
      "tests": [
        {
          "command": "cargo test --manifest-path src-tauri/Cargo.toml --lib phase08_21",
          "passed": 16,
          "failed": 0,
          "ignored": 0,
          "exitCode": 0
        }
      ],
      "processingCaller": {
        "path": [
          "src/lib/hwped.ts"
        ],
        "payload": "src/lib/hwped.ts:130 invokes hwped_validate from the hwped editor surface whenever it validates a document and returns a per-check report; the hwped surface keeps owning that call regardless of screen changes.",
        "terminalNoticeOwner": "The hwped editor surface owns current document state display and error toasts. Navigation-safe terminal notices and fulfilled-payload classifiers remain the explicit Plan25/26 frontend handoff; this shard claims backend isolation only."
      },
      "evidence": {
        "behaviorCases": [
          "phase08_21_hwped_each_wrapper_yields_same_poll_and_maps_join_failure",
          "phase08_21_hwped_real_fixture_results_and_legacy_rejections"
        ],
        "workerProof": "hwped::phase08_21::phase08_21_hwped_each_wrapper_yields_same_poll_and_maps_join_failure holds the hwped_validate future at spawn_blocking worker entry via a #[cfg(test)] worker-stage hook keyed to the process tempdir, manually polls it on the same async task and proves progress while a distinct worker thread stays blocked; the panicked worker maps to the display-only hwped_task_failed String.",
        "platformScope": "macOS unit fixtures only: atomic_file::phase08_06::Home isolation, disposable tempdir workspaces, a fake hwp/hwpx shell binary and the test-only worker-stage hook; no live workspace, credential, network endpoint or native test hook is touched.",
        "limits": "Shared admission serializes cooperating in-process writers only; external editor or converter processes are outside the in-process guarantee. PERF-01/PERF-02 remain globally outstanding for the whole phase.",
        "contract": "The HwpedValidationReport payload and the legacy hwp-cli version/argument rejections are unchanged; the only new failure path is the shared JoinError-to-String mapping at the worker boundary, asserted with the hwped-specific prefix."
      }
    },
    {
      "name": "hwped_capabilities",
      "module": "src-tauri/src/hwped.rs",
      "registrationPath": "src-tauri/src/lib.rs::hwped::hwped_capabilities",
      "originalDisposition": "ISOLATED",
      "disposition": "ISOLATED",
      "helperChain": [
        "src-tauri/src/hwped.rs::hwped_capabilities is an already-isolated async entry: it owns its inputs and awaits tauri::async_runtime::spawn_blocking calling src-tauri/src/hwped.rs::capabilities_now inside the worker; a JoinError becomes the display-only hwped_task_failed String.",
        "src-tauri/src/hwped.rs::capabilities_now probes the bundled hwp-cli version and reports editable formats; temp staging goes through src-tauri/src/hwped.rs::stage_document (lexical-containment checked against the optional workspace root), then a bounded hwp-cli process wait with the fixed 60s HWP_TIMEOUT, so the chain reaches no network call or unbounded walk."
      ],
      "workerBoundary": "src-tauri/src/hwped.rs::hwped_capabilities awaits tauri::async_runtime::spawn_blocking before any filesystem access or process wait runs; only a JoinError maps to the display-only hwped_task_failed String while the HwpedCapabilities payload and every domain error string pass through unchanged; owned structs cross the await, never a borrowed State or guard.",
      "syncCallers": [
        "No Rust or CLI caller invokes hwped_capabilities synchronously today; src-tauri/src/lib.rs registers only hwped::hwped_capabilities, and the private src-tauri/src/hwped.rs::capabilities_now worker body stays behind the spawn_blocking boundary."
      ],
      "mutationKey": {
        "readOnly": true,
        "lexicalPaths": [],
        "aliasPaths": [],
        "sharedAdmissionEntry": "hwped_capabilities performs no filesystem mutation: src-tauri/src/hwped.rs::capabilities_now only reads the staged input and spawns the bounded hwp-cli probe, so no filesystem mutation admission is required.",
        "postAdmissionPreconditions": [
          "No mutation admission is required: the command performs no filesystem mutation, so there is no transaction key to admit and nothing to release"
        ],
        "domainLockOrder": "No path admission exists; the command takes no domain lock, so there is no admission-before-domain-lock order to record."
      },
      "tests": [
        {
          "command": "cargo test --manifest-path src-tauri/Cargo.toml --lib phase08_21",
          "passed": 16,
          "failed": 0,
          "ignored": 0,
          "exitCode": 0
        }
      ],
      "processingCaller": {
        "path": [
          "src/lib/hwped.ts"
        ],
        "payload": "src/lib/hwped.ts:137 invokes hwped_capabilities from the hwped editor surface whenever it probes the bundled hwp-cli version and reports editable formats; the hwped surface keeps owning that call regardless of screen changes.",
        "terminalNoticeOwner": "The hwped editor surface owns current document state display and error toasts. Navigation-safe terminal notices and fulfilled-payload classifiers remain the explicit Plan25/26 frontend handoff; this shard claims backend isolation only."
      },
      "evidence": {
        "behaviorCases": [
          "phase08_21_hwped_each_wrapper_yields_same_poll_and_maps_join_failure",
          "phase08_21_hwped_real_fixture_results_and_legacy_rejections"
        ],
        "workerProof": "hwped::phase08_21::phase08_21_hwped_each_wrapper_yields_same_poll_and_maps_join_failure holds the hwped_capabilities future at spawn_blocking worker entry via a #[cfg(test)] worker-stage hook keyed to the process tempdir, manually polls it on the same async task and proves progress while a distinct worker thread stays blocked; the panicked worker maps to the display-only hwped_task_failed String.",
        "platformScope": "macOS unit fixtures only: atomic_file::phase08_06::Home isolation, disposable tempdir workspaces, a fake hwp/hwpx shell binary and the test-only worker-stage hook; no live workspace, credential, network endpoint or native test hook is touched.",
        "limits": "Shared admission serializes cooperating in-process writers only; external editor or converter processes are outside the in-process guarantee. PERF-01/PERF-02 remain globally outstanding for the whole phase.",
        "contract": "The HwpedCapabilities payload and the legacy hwp-cli version/argument rejections are unchanged; the only new failure path is the shared JoinError-to-String mapping at the worker boundary, asserted with the hwped-specific prefix."
      }
    },
    {
      "name": "hwp_cli_template_fields",
      "module": "src-tauri/src/hwp_cli_template.rs",
      "registrationPath": "src-tauri/src/lib.rs::hwp_cli_template::ipc::hwp_cli_template_fields",
      "originalDisposition": "CONVERT",
      "disposition": "ISOLATED",
      "helperChain": [
        "src-tauri/src/hwp_cli_template.rs::ipc::hwp_cli_template_fields owns the HwpCliTemplateFieldsRequest and awaits tauri::async_runtime::spawn_blocking calling src-tauri/src/hwp_cli_template.rs::hwp_cli_template_fields inside the worker; a JoinError becomes the display-only hwp_cli_template_fields_task_failed String.",
        "src-tauri/src/hwp_cli_template.rs::hwp_cli_template_fields resolves the canonical template alias with src-tauri/src/hwp_cli_template.rs::canonical_template, selects the released binary through src-tauri/src/hwp_cli_template.rs::select_hwp_bin and src-tauri/src/hwp_cli_template.rs::ensure_released_version, then runs the bounded slots probe via src-tauri/src/hwp_cli_template.rs::fields_with_bin, so the converted chain reaches no network call or unbounded walk."
      ],
      "workerBoundary": "src-tauri/src/hwp_cli_template.rs::ipc::hwp_cli_template_fields awaits tauri::async_runtime::spawn_blocking before any filesystem access or process wait runs; only a JoinError maps to the display-only hwp_cli_template_fields_task_failed String while the HwpCliTemplateFieldsResponse payload and every domain error string pass through unchanged; owned request structs cross the await, never a borrowed State or guard.",
      "syncCallers": [
        "No Rust or CLI caller invokes hwp_cli_template_fields synchronously today; src-tauri/src/lib.rs registers only hwp_cli_template::ipc::hwp_cli_template_fields, and the original synchronous src-tauri/src/hwp_cli_template.rs::hwp_cli_template_fields keeps its name and signature for Rust callers (the existing inline tests exercise it directly)."
      ],
      "mutationKey": {
        "readOnly": true,
        "lexicalPaths": [],
        "aliasPaths": [],
        "sharedAdmissionEntry": "hwp_cli_template_fields performs no filesystem mutation: src-tauri/src/hwp_cli_template.rs::fields_with_bin only runs the hwp-cli slots probe and parses its JSON stdout, so no filesystem mutation admission is required.",
        "postAdmissionPreconditions": [
          "No mutation admission is required: the command performs no filesystem mutation, so there is no transaction key to admit and nothing to release"
        ],
        "domainLockOrder": "No path admission exists; the command takes no domain lock, so there is no admission-before-domain-lock order to record."
      },
      "tests": [
        {
          "command": "cargo test --manifest-path src-tauri/Cargo.toml --lib phase08_21",
          "passed": 16,
          "failed": 0,
          "ignored": 0,
          "exitCode": 0
        }
      ],
      "processingCaller": {
        "path": [
          "src/lib/studio.ts",
          "src/components/studio/StudioMode.tsx"
        ],
        "payload": "fetchHwpCliTemplateFields (src/lib/studio.ts:385) invokes hwp_cli_template_fields with the fields request whenever the Studio hwp template panel inspects a template; the Studio surface keeps owning that refresh regardless of screen changes.",
        "terminalNoticeOwner": "The Studio surface (src/components/studio) owns current template-fill state display and error toasts. Navigation-safe terminal notices and fulfilled-payload classifiers remain the explicit Plan25/26 frontend handoff; this shard claims backend isolation only."
      },
      "evidence": {
        "behaviorCases": [
          "phase08_21_hwp_cli_template_each_wrapper_yields_same_poll_and_maps_join_failure",
          "phase08_21_hwp_cli_template_real_fixture_results_and_legacy_rejections"
        ],
        "workerProof": "hwp_cli_template::phase08_21::phase08_21_hwp_cli_template_each_wrapper_yields_same_poll_and_maps_join_failure holds the hwp_cli_template_fields future at spawn_blocking worker entry via a #[cfg(test)] worker-stage hook keyed to the process tempdir, manually polls it on the same async task and proves progress while a distinct worker thread stays blocked; the panicked worker maps to the display-only hwp_cli_template_fields_task_failed String.",
        "platformScope": "macOS unit fixtures only: atomic_file::phase08_06::Home isolation, disposable tempdir workspaces, a fake hwp/hwpx shell binary and the test-only worker-stage hook; no live workspace, credential, network endpoint or native test hook is touched.",
        "limits": "Shared admission serializes cooperating in-process writers only; external editor or converter processes are outside the in-process guarantee. PERF-01/PERF-02 remain globally outstanding for the whole phase.",
        "contract": "The HwpCliTemplateFieldsResponse payload and the legacy hwp-cli version/argument rejections are unchanged; the only new failure path is the JoinError-to-String mapping at the worker boundary."
      }
    },
    {
      "name": "hwp_cli_template_fill",
      "module": "src-tauri/src/hwp_cli_template.rs",
      "registrationPath": "src-tauri/src/lib.rs::hwp_cli_template::ipc::hwp_cli_template_fill",
      "originalDisposition": "CONVERT",
      "disposition": "ISOLATED",
      "helperChain": [
        "src-tauri/src/hwp_cli_template.rs::ipc::hwp_cli_template_fill owns the work_path String and HwpCliTemplateFillRequest and awaits tauri::async_runtime::spawn_blocking calling src-tauri/src/hwp_cli_template.rs::hwp_cli_template_fill inside the worker; a JoinError becomes the display-only hwp_cli_template_fill_task_failed String.",
        "src-tauri/src/hwp_cli_template.rs::hwp_cli_template_fill delegates to src-tauri/src/hwp_cli_template.rs::fill_impl, which resolves the canonical alias via src-tauri/src/hwp_cli_template.rs::canonical_template and the output via src-tauri/src/hwp_cli_template.rs::output_path, admits [output, parent] plus the vault-root parent through src-tauri/src/atomic_file.rs::PathTransactionRequest and src-tauri/src/vault.rs::resolve_inside_vault, then inside src-tauri/src/atomic_file.rs::with_path_transactions selects the released binary, checks src-tauri/src/vault_list.rs::assert_maru_can_write and runs src-tauri/src/hwp_cli_template.rs::fill_with_bin with the lease so the final publish re-resolves and re-covers the output."
      ],
      "workerBoundary": "src-tauri/src/hwp_cli_template.rs::ipc::hwp_cli_template_fill awaits tauri::async_runtime::spawn_blocking before any filesystem access or lock wait runs; only a JoinError maps to the display-only hwp_cli_template_fill_task_failed String while the HwpCliTemplateFillResponse payload and every domain error string pass through unchanged; owned inputs cross the await, never a borrowed State or guard.",
      "syncCallers": [
        "No Rust or CLI caller invokes hwp_cli_template_fill synchronously today; src-tauri/src/lib.rs registers only hwp_cli_template::ipc::hwp_cli_template_fill, and the original synchronous src-tauri/src/hwp_cli_template.rs::hwp_cli_template_fill keeps its name and signature for Rust callers (the inline legacy tests exercise it through a bin-overriding helper)."
      ],
      "mutationKey": {
        "readOnly": false,
        "lexicalPaths": [
          "resolved .hwpx output file inside the vault",
          "output parent directory (covers the .maru-hwp-cli-* stage dirs created beside the output)",
          "pinned vault-root parent required before the transaction opens"
        ],
        "aliasPaths": [
          "request.output_path given relative resolves through src-tauri/src/vault.rs::resolve_inside_vault to the same lexical output admitted up front"
        ],
        "sharedAdmissionEntry": "src-tauri/src/hwp_cli_template.rs::fill_impl admits the output file and its parent directory before any effect through src-tauri/src/atomic_file.rs::with_path_transactions over src-tauri/src/atomic_file.rs::PathTransactionRequest with require_parent(vault root) and with_workspace_registry, so the hwp-cli stage dirs and the final publish stay inside the admitted set.",
        "postAdmissionPreconditions": [
          "lease.ensure_workspace_registry runs before any effect",
          "lease.ensure_covered over [output, parent] runs before the binary select and src-tauri/src/vault_list.rs::assert_maru_can_write",
          "lease.before_effect runs after the write-policy check and before the hwp-cli fill spawn",
          "fill_with_bin re-resolves the output and re-checks lease.ensure_covered immediately before the atomic publish"
        ],
        "domainLockOrder": "Filesystem path admission (with_path_transactions acquire) happens in src-tauri/src/hwp_cli_template.rs::fill_impl before the write-policy check and before the hwp-cli process spawn; the converter process runs only inside the admitted transaction."
      },
      "tests": [
        {
          "command": "cargo test --manifest-path src-tauri/Cargo.toml --lib phase08_21",
          "passed": 16,
          "failed": 0,
          "ignored": 0,
          "exitCode": 0
        }
      ],
      "processingCaller": {
        "path": [
          "src/lib/studio.ts",
          "src/components/studio/StudioMode.tsx"
        ],
        "payload": "fillHwpCliTemplate (src/lib/studio.ts:393) invokes hwp_cli_template_fill with workPath and the fill request whenever the Studio hwp template panel fills a template; the Studio surface keeps owning that flow regardless of screen changes.",
        "terminalNoticeOwner": "The Studio surface (src/components/studio) owns current template-fill state display and error toasts. Navigation-safe terminal notices and fulfilled-payload classifiers remain the explicit Plan25/26 frontend handoff; this shard claims backend isolation only."
      },
      "evidence": {
        "behaviorCases": [
          "phase08_21_hwp_cli_template_each_wrapper_yields_same_poll_and_maps_join_failure",
          "phase08_21_hwp_cli_template_real_fixture_results_and_legacy_rejections",
          "phase08_21_hwp_cli_template_fill_serializes_same_target_both_orders",
          "phase08_21_hwp_cli_template_denied_and_error_release_admission"
        ],
        "workerProof": "hwp_cli_template::phase08_21::phase08_21_hwp_cli_template_each_wrapper_yields_same_poll_and_maps_join_failure holds the hwp_cli_template_fill future at spawn_blocking worker entry via a #[cfg(test)] worker-stage hook keyed to the fixture workspace root, manually polls it on the same async task and proves progress while a distinct worker thread stays blocked; the panicked worker maps to the display-only hwp_cli_template_fill_task_failed String.",
        "platformScope": "macOS unit fixtures only: atomic_file::phase08_06::Home isolation, disposable tempdir workspaces, a fake hwp/hwpx shell binary and the test-only worker-stage hook; no live workspace, credential, network endpoint or native test hook is touched.",
        "limits": "Shared admission serializes cooperating in-process writers only; external editor or converter processes are outside the in-process guarantee. PERF-01/PERF-02 remain globally outstanding for the whole phase.",
        "contract": "The HwpCliTemplateFillResponse payload and the legacy values/alias/extension/cli_missing rejections are unchanged (values, alias and extension rejections now precede cli_missing, matching wire behavior); the only new failure paths are the JoinError-to-String mapping at the worker boundary plus fail-closed admission rejections for out-of-set writes and blocked-workspace retries."
      }
    },
    {
      "name": "template_get_fields",
      "module": "src-tauri/src/template_fill.rs",
      "registrationPath": "src-tauri/src/lib.rs::template_fill::ipc::template_get_fields",
      "originalDisposition": "CONVERT",
      "disposition": "ISOLATED",
      "helperChain": [
        "src-tauri/src/template_fill.rs::ipc::template_get_fields owns the work_path String and TemplateFieldRequest and awaits tauri::async_runtime::spawn_blocking calling src-tauri/src/template_fill.rs::template_get_fields inside the worker; a JoinError becomes the display-only template_get_fields_task_failed String.",
        "src-tauri/src/template_fill.rs::template_get_fields resolves the template through src-tauri/src/template_fill.rs::resolve_template_path (src-tauri/src/vault.rs::resolve_inside_vault lexical containment), probes the bounded hwpx tool slots output, merges it with the kordoc_lite scan and returns the merged field list, so the converted chain reaches no network call or unbounded walk."
      ],
      "workerBoundary": "src-tauri/src/template_fill.rs::ipc::template_get_fields awaits tauri::async_runtime::spawn_blocking before any filesystem access or process wait runs; only a JoinError maps to the display-only template_get_fields_task_failed String while the TemplateFieldResponse payload and every domain error string pass through unchanged; owned inputs cross the await, never a borrowed State or guard.",
      "syncCallers": [
        "No Rust or CLI caller invokes template_get_fields synchronously today; src-tauri/src/lib.rs registers only template_fill::ipc::template_get_fields, and the original synchronous src-tauri/src/template_fill.rs::template_get_fields keeps its name and signature for Rust callers (the phase08_21 tests exercise it directly)."
      ],
      "mutationKey": {
        "readOnly": true,
        "lexicalPaths": [],
        "aliasPaths": [],
        "sharedAdmissionEntry": "template_get_fields performs no filesystem mutation: src-tauri/src/template_fill.rs::template_get_fields only resolves, probes and scans the template, so no filesystem mutation admission is required.",
        "postAdmissionPreconditions": [
          "No mutation admission is required: the command performs no filesystem mutation, so there is no transaction key to admit and nothing to release"
        ],
        "domainLockOrder": "No path admission exists; the command takes no domain lock, so there is no admission-before-domain-lock order to record."
      },
      "tests": [
        {
          "command": "cargo test --manifest-path src-tauri/Cargo.toml --lib phase08_21",
          "passed": 16,
          "failed": 0,
          "ignored": 0,
          "exitCode": 0
        }
      ],
      "processingCaller": {
        "path": [
          "src/lib/studio.ts",
          "src/components/studio/StudioMode.tsx"
        ],
        "payload": "getTemplateFields (src/lib/studio.ts:359) invokes template_get_fields with workPath and the field request whenever the Studio template panel inspects a hwpx template; the Studio surface keeps owning that refresh regardless of screen changes.",
        "terminalNoticeOwner": "The Studio surface (src/components/studio) owns current template-fill state display and error toasts. Navigation-safe terminal notices and fulfilled-payload classifiers remain the explicit Plan25/26 frontend handoff; this shard claims backend isolation only."
      },
      "evidence": {
        "behaviorCases": [
          "phase08_21_template_fill_each_wrapper_yields_same_poll_and_maps_join_failure",
          "phase08_21_template_real_fixture_results_and_legacy_rejections"
        ],
        "workerProof": "template_fill::phase08_21::phase08_21_template_fill_each_wrapper_yields_same_poll_and_map_join_failure holds the template_get_fields future at spawn_blocking worker entry via a #[cfg(test)] worker-stage hook keyed to the fixture workspace root, manually polls it on the same async task and proves progress while a distinct worker thread stays blocked; the panicked worker maps to the display-only template_get_fields_task_failed String.",
        "platformScope": "macOS unit fixtures only: atomic_file::phase08_06::Home isolation, disposable tempdir workspaces, a fake hwp/hwpx shell binary and the test-only worker-stage hook; no live workspace, credential, network endpoint or native test hook is touched.",
        "limits": "Shared admission serializes cooperating in-process writers only; external editor or converter processes are outside the in-process guarantee. PERF-01/PERF-02 remain globally outstanding for the whole phase.",
        "contract": "The TemplateFieldResponse payload, the workspace/bundled source attribution and the requires-a-.hwpx-template rejection are unchanged; the only new failure path is the JoinError-to-String mapping at the worker boundary."
      }
    },
    {
      "name": "template_prepare_hwpx_template",
      "module": "src-tauri/src/template_fill.rs",
      "registrationPath": "src-tauri/src/lib.rs::template_fill::ipc::template_prepare_hwpx_template",
      "originalDisposition": "AUDIT",
      "disposition": "ISOLATED",
      "helperChain": [
        "src-tauri/src/template_fill.rs::ipc::template_prepare_hwpx_template owns the work_path and source_path Strings and awaits tauri::async_runtime::spawn_blocking calling src-tauri/src/template_fill.rs::template_prepare_hwpx_template inside the worker; a JoinError becomes the display-only template_prepare_hwpx_template_task_failed String.",
        "src-tauri/src/template_fill.rs::template_prepare_hwpx_template only inspects the template (existence, extension and a bounded kordoc_lite scan) to report a ready status, so the converted chain reaches no network call, process wait or unbounded walk."
      ],
      "workerBoundary": "src-tauri/src/template_fill.rs::ipc::template_prepare_hwpx_template awaits tauri::async_runtime::spawn_blocking before any filesystem access runs; only a JoinError maps to the display-only template_prepare_hwpx_template_task_failed String while the TemplatePrepareResponse payload and every domain error string pass through unchanged; owned Strings cross the await, never a borrowed State or guard.",
      "syncCallers": [
        "No Rust or CLI caller invokes template_prepare_hwpx_template synchronously today; src-tauri/src/lib.rs registers only template_fill::ipc::template_prepare_hwpx_template, and the original synchronous src-tauri/src/template_fill.rs::template_prepare_hwpx_template keeps its name and signature for Rust callers (the phase08_21 tests exercise it directly)."
      ],
      "mutationKey": {
        "readOnly": true,
        "lexicalPaths": [],
        "aliasPaths": [],
        "sharedAdmissionEntry": "template_prepare_hwpx_template performs no filesystem mutation: it only stats and scans the template before reporting readiness, so no filesystem mutation admission is required.",
        "postAdmissionPreconditions": [
          "No mutation admission is required: the command performs no filesystem mutation, so there is no transaction key to admit and nothing to release"
        ],
        "domainLockOrder": "No path admission exists; the command takes no domain lock, so there is no admission-before-domain-lock order to record."
      },
      "tests": [
        {
          "command": "cargo test --manifest-path src-tauri/Cargo.toml --lib phase08_21",
          "passed": 16,
          "failed": 0,
          "ignored": 0,
          "exitCode": 0
        }
      ],
      "processingCaller": {
        "path": [
          "src/lib/studio.ts",
          "src/components/studio/StudioMode.tsx"
        ],
        "payload": "prepareTemplate (src/lib/studio.ts:367) invokes template_prepare_hwpx_template with workPath and the template path before a fill; the Studio surface keeps owning that check regardless of screen changes.",
        "terminalNoticeOwner": "The Studio surface (src/components/studio) owns current template-fill state display and error toasts. Navigation-safe terminal notices and fulfilled-payload classifiers remain the explicit Plan25/26 frontend handoff; this shard claims backend isolation only."
      },
      "evidence": {
        "behaviorCases": [
          "phase08_21_template_fill_each_wrapper_yields_same_poll_and_maps_join_failure",
          "phase08_21_template_real_fixture_results_and_legacy_rejections"
        ],
        "workerProof": "template_fill::phase08_21::phase08_21_template_fill_each_wrapper_yields_same_poll_and_map_join_failure holds the template_prepare_hwpx_template future at spawn_blocking worker entry via a #[cfg(test)] worker-stage hook keyed to the fixture workspace root, manually polls it on the same async task and proves progress while a distinct worker thread stays blocked; the panicked worker maps to the display-only template_prepare_hwpx_template_task_failed String.",
        "platformScope": "macOS unit fixtures only: atomic_file::phase08_06::Home isolation, disposable tempdir workspaces, a fake hwp/hwpx shell binary and the test-only worker-stage hook; no live workspace, credential, network endpoint or native test hook is touched.",
        "limits": "Shared admission serializes cooperating in-process writers only; external editor or converter processes are outside the in-process guarantee. PERF-01/PERF-02 remain globally outstanding for the whole phase.",
        "contract": "The TemplatePrepareResponse payload, the ready status semantics and the missing-template / unsupported-extension rejections are unchanged; the only new failure path is the JoinError-to-String mapping at the worker boundary."
      }
    },
    {
      "name": "template_fill_hwpx",
      "module": "src-tauri/src/template_fill.rs",
      "registrationPath": "src-tauri/src/lib.rs::template_fill::ipc::template_fill_hwpx",
      "originalDisposition": "CONVERT",
      "disposition": "ISOLATED",
      "helperChain": [
        "src-tauri/src/template_fill.rs::ipc::template_fill_hwpx owns the work_path String and TemplateFillRequest and awaits tauri::async_runtime::spawn_blocking calling src-tauri/src/template_fill.rs::template_fill_hwpx inside the worker; a JoinError becomes the display-only template_fill_hwpx_task_failed String.",
        "src-tauri/src/template_fill.rs::template_fill_hwpx resolves template and output through src-tauri/src/template_fill.rs::resolve_template_path and src-tauri/src/template_fill.rs::resolve_output_path (src-tauri/src/vault.rs::resolve_inside_vault lexical containment), admits [output, parent] plus the vault-root parent through src-tauri/src/atomic_file.rs::PathTransactionRequest, and inside src-tauri/src/atomic_file.rs::with_path_transactions checks src-tauri/src/vault_list.rs::assert_maru_can_write before lease.before_effect.",
        "src-tauri/src/template_fill.rs::template_fill_hwpx_in_transaction writes the values JSON through src-tauri/src/template_fill.rs::write_temp_values, runs the bounded hwpx fill and validation probes via src-tauri/src/template_fill.rs::run_command, then rewrites form fields in place through kordoc_lite so the last admitted writer owns the final bytes."
      ],
      "workerBoundary": "src-tauri/src/template_fill.rs::ipc::template_fill_hwpx awaits tauri::async_runtime::spawn_blocking before any filesystem access or lock wait runs; only a JoinError maps to the display-only template_fill_hwpx_task_failed String while the TemplateFillResponse payload and every domain error string pass through unchanged; owned inputs cross the await, never a borrowed State or guard.",
      "syncCallers": [
        "No Rust or CLI caller invokes template_fill_hwpx synchronously today; src-tauri/src/lib.rs registers only template_fill::ipc::template_fill_hwpx, and the original synchronous src-tauri/src/template_fill.rs::template_fill_hwpx keeps its name and signature for Rust callers (the phase08_21 tests exercise it directly)."
      ],
      "mutationKey": {
        "readOnly": false,
        "lexicalPaths": [
          "resolved .hwpx output file inside the vault",
          "output parent directory (create_dir_all runs inside the admitted transaction)",
          "pinned vault-root parent required before the transaction opens"
        ],
        "aliasPaths": [
          "request.output_path given relative resolves through src-tauri/src/vault.rs::resolve_inside_vault to the same lexical output admitted up front",
          "omitted output_path defaults to .maru/studio/filled/<stem>-filled.hwpx under the same vault root"
        ],
        "sharedAdmissionEntry": "src-tauri/src/template_fill.rs::template_fill_hwpx admits the output file and its parent directory before any effect through src-tauri/src/atomic_file.rs::with_path_transactions over src-tauri/src/atomic_file.rs::PathTransactionRequest with require_parent(vault root) and with_workspace_registry, so the hwpx probe rewrite and the kordoc_lite in-place fill stay inside the admitted set.",
        "postAdmissionPreconditions": [
          "lease.ensure_workspace_registry runs before any effect",
          "lease.ensure_covered over the output runs before src-tauri/src/vault_list.rs::assert_maru_can_write",
          "lease.before_effect runs after the write-policy check and before the first process spawn",
          "fs::create_dir_all(output parent) runs inside the transaction after the lease covers the parent"
        ],
        "domainLockOrder": "Filesystem path admission (with_path_transactions acquire) happens in src-tauri/src/template_fill.rs::template_fill_hwpx before the write-policy check and before any hwpx process spawn; converter and validation processes run only inside the admitted transaction."
      },
      "tests": [
        {
          "command": "cargo test --manifest-path src-tauri/Cargo.toml --lib phase08_21",
          "passed": 16,
          "failed": 0,
          "ignored": 0,
          "exitCode": 0
        }
      ],
      "processingCaller": {
        "path": [
          "src/lib/studio.ts",
          "src/components/studio/StudioMode.tsx"
        ],
        "payload": "fillTemplate (src/lib/studio.ts:378) invokes template_fill_hwpx with workPath and the fill request whenever the Studio template panel fills a hwpx template; the Studio surface keeps owning that flow regardless of screen changes.",
        "terminalNoticeOwner": "The Studio surface (src/components/studio) owns current template-fill state display and error toasts. Navigation-safe terminal notices and fulfilled-payload classifiers remain the explicit Plan25/26 frontend handoff; this shard claims backend isolation only."
      },
      "evidence": {
        "behaviorCases": [
          "phase08_21_template_fill_each_wrapper_yields_same_poll_and_maps_join_failure",
          "phase08_21_template_real_fixture_results_and_legacy_rejections",
          "phase08_21_template_fill_serializes_same_target_both_orders",
          "phase08_21_template_denied_and_error_release_admission"
        ],
        "workerProof": "template_fill::phase08_21::phase08_21_template_fill_each_wrapper_yields_same_poll_and_map_join_failure holds the template_fill_hwpx future at spawn_blocking worker entry via a #[cfg(test)] worker-stage hook keyed to the fixture workspace root, manually polls it on the same async task and proves progress while a distinct worker thread stays blocked; the panicked worker maps to the display-only template_fill_hwpx_task_failed String.",
        "platformScope": "macOS unit fixtures only: atomic_file::phase08_06::Home isolation, disposable tempdir workspaces, a fake hwp/hwpx shell binary and the test-only worker-stage hook; no live workspace, credential, network endpoint or native test hook is touched.",
        "limits": "Shared admission serializes cooperating in-process writers only; external editor or converter processes are outside the in-process guarantee. PERF-01/PERF-02 remain globally outstanding for the whole phase.",
        "contract": "The TemplateFillResponse payload, the replaced_count/validation_ok semantics and the no-values / requires-a-.hwpx-template rejections are unchanged; the only new failure paths are the JoinError-to-String mapping at the worker boundary plus fail-closed admission rejections for out-of-set writes and blocked-workspace retries."
      }
    }
  ]
}
