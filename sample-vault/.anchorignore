# Anchor sample vault — gitignore-style segment patterns. The defaults
# baked into vault::DEFAULT_ANCHOR_IGNORE already cover .DS_Store,
# .gitkeep, .keep, Thumbs.db, and Icon\r — list them here only if you
# want them documented per-vault.
node_modules
target
.venv
